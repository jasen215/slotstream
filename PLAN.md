<!-- Generated by Tools/projections.py from db/records/design/, db/records/plan/ Edit the records, then run the script; this file is a projection. -->

# slotstream — Qwen3.8-Flash-Next on every Apple Silicon Mac

**One binary. MLX + Swift. SSD-streamed experts + n-gram/PLE store, RAM cache slots as the
speed↔memory knob, Ollama-compatible API.**

Created 2026-08-28. This file is the whole plan: design, math, build phases, test matrix,
and the living status tracker. Update it in place as milestones land (checkboxes + the
Measured columns). Everything marked **est.** is a first-principles estimate to be replaced
by a measurement; everything marked **verified** came from the model config or a live check.

---

**Positioning note (2026-09-16).** "Every Apple Silicon Mac" describes compatibility, not the target. The engine is optimized for Macs that cannot hold the model, 16 to 64 GB; larger Macs run it and are not the optimization target ([[records/decisions/target-range-macs-that-cannot-hold-the-model]]).

## Sevra integration: maintained model choices for hardware
Owner-approved engineering direction, September 8, 2026. Implementation of
the maintained-selection lifecycle is planned; the current Slotstream engine
continues to support its documented checkpoint and independent interfaces.

Sevra will select and maintain a small set of models for supported hardware,
optimizing inference, context, tools, memory and the interface as one experience.
The appropriate model may differ by hardware profile and change as better
choices qualify. The permanent single-flagship constraint is superseded at the
product level. This does not change existing engine defaults, weights or goldens.

Engineering requirements for that integration:

- Bind a versioned hardware profile to exact model/quantization, tokenizer,
  template, engine/runtime and resource defaults. The product owns model
  selection; the engine validates the selected pack and execution plan.
- Qualify each candidate on the same hardware against the current choice and
  credible alternatives. Score correct complete tasks and total latency,
  context, memory, storage and sustained behavior, not decode speed alone.
- Integrate and test only a few selected targets deeply. Current sparse-MoE
  optimizations and numerical gates remain exact. New graphs need explicit
  implementation and qualification; there is no general runner fallback.
- Keep model-dependent cache identities exact. A model/template/runtime change
  invalidates incompatible derived state; durable user data and permissions
  survive. Do not change a running request's model or silently route it remotely.
- Preserve documented Slotstream CLI/API/Swift behavior. New product selection
  does not silently retarget pinned or existing engine clients.
- Plan transactional model updates with clear notices, deferral, resource
  accounting, interrupted-update recovery and supported rollback. Offline use
  retains the installed qualified setup. Normal users need no model catalog or
  backend expertise; supported resource/context controls remain available.
- Before advertising a new model/profile, record its actual native support
  evidence and full application conformance. A narrow initial Mac release can
  use the current model; no full future matrix is a prerequisite.

Documentation is part of each implementation change. Update the relevant
canonical design/plan/measurement/claim records, README, hardware/API docs and
changelog alongside the behavior; regenerate PLAN.md with Tools/projections.py
and llms-full.txt with Tools/llms_full.sh. Distinguish engine evidence from app
support, estimates from measurements and planned features from released ones.
Keep old installation and embedding links working. Current README wording can
describe the product direction only with the app's in-development status.

This section specifies engine/product integration constraints. It is not an
implementation receipt, a model selection, a benchmark win or a binary release.

## Compatibility-tier support, September 13, 2026

Carlos requested that the public memory table state that Compatibility-tier
support is coming soon. README.md, the hardware guide and llms.txt now carry
that roadmap status while retaining the current model limitation. The present
8 GB plan still does not fit; no runtime support, selected future model,
release date or performance result is established by this wording change.

## 0. Status tracker (living)
Measured data lives in **[MEASUREMENTS.md](MEASUREMENTS.md)**; this file keeps the
design and the estimates it replaces.

| Milestone | State | Exit proof |
|---|---|---|
| M0 Ground truth & feasibility | ✅ **done 2026-08-28** | byte-exact model inventory, Metal limits, cold SSD curve, MLX+Swift slot-pool gate, Swift prior-art survey — all in MEASUREMENTS.md |
| M1 Expert-locality study | ✅ **done 2026-09-03** | traced from the Swift engine (`SLOTSTREAM_ROUTER_TRACE`), simulated, curves in `bench/locality/summary.json`: CLOCK 0.557 against LRU 0.568 and LFU 0.480 at 30 experts/layer, so the shipped policy stays; the trace also fixes the compulsory-miss ceiling at 0.906 and shows 10% of records serving 71% of accesses |
| M2 `.ssmodel` container + repack | **skipped, by measurement** | engine streams from original shards (9 preads/expert); repack is now a measured-optimization backlog item |
| M3 Swift engine, resident correctness (incl. QSA indexer) | ✅ **done 2026-08-28** | layers 0–1 **bit-exact** vs mlx-0.31.1 reference; template/ngram/dequant goldens exact; deeper layers ≤2.4% RMS (vendored-kernel ulp skew, documented) |
| M4 Slot streaming decode (first full-model run) | ✅ **done 2026-08-28** | **golden equivalence passed** (30 experts/layer cached ≡ 181/layer, identical greedy text); full model generates coherently on the 48 GB dev Mac |
| M5 Prefill/prefetch perf | ✅ **done 2026-09-02** (as N2) | prefill target met: 8k prompt 184 tok/s at a 16 GB target (was 91), 222 at a 4096-token pass on a matched pool; the cross-token prefetcher stays deprioritized (a decode lever). The 20.0 tok/s warm-decode figure has not reproduced on 0.1.6 — re-anchored to 11.2 at 120/layer |
| M6 Ollama-compatible server | ✅ **done 2026-08-28** | /api/version·tags·show·ps·chat·generate + /v1/chat/completions·models, NDJSON + SSE streaming, all passing `Tools/api_test.sh`; GUI-client validation pending (sandbox blocks local HTTP clients) |
| M7 CLI, install, packaging | ✅ **done 2026-08-28** (LaunchAgent deferred) | full CLI + Makefile; resumable sha256-verified parallel `pull`; one-line installer to a CI-built, provenance-signed release |
| M7.5 Serving hardening | ✅ **done 2026-08-29** (v0.1.5) | adversarial pass over the whole system: three process-killing inputs, streaming corruption, and a sampler 0/0 fixed. `Tools/api_robustness.sh` is the standing gate |
| M7.6 Deferred gaps closed | ✅ **done 2026-08-29** | prefill pass sized from the memory plan (40 → 92 tok/s); sampler golden vs numpy; governor policy driven through all 19 branches |
| M8 Matrix bench + tier validation | ◐ first data — **deprioritized 2026-08-29** | the bench rig and tier table are credibility artifacts, not adoption gates; moved behind N1–N5 |
| v0.1 Definition of Done (§11) | ◐ | see updated checklist |
| N1 Conversation prefix cache | ✅ **done 2026-08-29** (0.1.6) | TTFT flat in conversation length — 6.0 s at turn 8 against 25.8 s uncached. Gated by `prefix-check` against a prefill-rechunk control |
| N2 Prefill sweep | ✅ **done 2026-09-02** | passes of 256+ tokens sweep each layer's experts through staging groups and MLX's grouped GEMM (sorted indices), read consecutive experts as one `pread` per piece, never write the pool, and admit the prompt's hot experts on the last pass; n-gram rows read in parallel. 8k prompt 91 → 184 tok/s at 16 GB, prose 66 → 140, floor 51 → 93 at a 1.5 GB lower peak, `context-check` 64 → 152. Gated by `sweep-check` (3.3% of logit spread vs a 5.1% rechunk control; bit-identical cold and warm pool) |
| N5 Real GUI client | ✅ **done 2026-08-30** | Open WebUI driven through its own UI. It found a real bug: its interleaved title request defeated the single-slot prefix cache, which now holds four |
| N6 Prefill: bound the pass, then read each expert once | ◐ **unified implementation in progress** | Detailed prefill chapter of the 37-item whole-engine program: one baseline/status map, explicit output/fold/weight workspace alternatives, bounded indexer/attention/GDN, shared/resident read overlap, terminal work pruning and specialized top-K. The first ownership and final-forward candidates have native parity, serving and bounded A/B evidence; remaining acceptance and every OPT status live in the unified program. |
| N3 Download size · N4 Quality vs FP8 | **removed from the queue 2026-08-30** | hosting is not the download's bottleneck and partial-start is worse than a progress bar; the FP8 gate needs a credential that is not provisioned. Findings kept in MEASUREMENTS.md |
| M9 MTP self-speculative decode | ✅ **done 2026-09-01** | the head's 31 tensors converted from the official release (the pinned conversion drops them); Swift port **bit-exact** vs the Python reference; measured accept 85.8% at depth 1, 41.3% for a 4-chain; auto enables only ≥120 experts/layer after its 1.6 GB. A/B on 0.2.0 (four drafts): ×0.55 / 0.69 / 0.88 / 0.96 at 20 / 29 / 42 / 57 experts/layer, all below break-even; at 122/layer (auto's size) depth 4 reads ×0.88, depth 2 ×1.13, depth 1 ×1.17, which selected the historical default of 1; the current adopted default is 2 ([[records/decisions/draft-depth-defaults-to-two]]) and auto's floor stands; with the rebuild eliminated (per-position recorded state) depth 1 reads ×1.24 there and ×1.18 sampled. Gates: `mtp-parity`, `mtp-check` |

**What is actually next: [§8.1](#81-next--the-ordered-queue-post-015).** M0–M8 are the
build-out phases; §8.1 is the live queue, ordered by what decides whether a person keeps
using slotstream after their first session rather than by what completes the milestone map.

---

Optimization execution follows [[records/plan/whole-engine-optimization-2026-09-04]]; [[records/plan/n6-prefill-bound-the-pass-then-read-each-expert-once]] supplies its detailed prefill contract. These are one program, with OPT status tracked once.

## 1. Goal

Run **Qwen3.8-Flash-Next** (released 2026-08-26; 125B total / 6B active, + 51B n-gram
embedding store, + 4B MTP) **in 4-bit on this Mac (MacBook Pro, M5 Pro, 48 GB) and on Macs
with much less memory (down to 16 GB officially, 8 GB experimentally)**, by:

1. Keeping the small dense trunk resident.
2. Streaming the **routed experts** from SSD into a fixed pool of **cache slots** — sized
   in **experts per layer** (of each layer's 512; 0.133 GB per expert/layer), *the*
   RAM↔speed tradeoff knob (`--experts-per-layer`).
3. Streaming the **n-gram / per-layer-embedding (PLE) store** from SSD with a small row
   cache — near-free because lookups are known from token ids alone (perfect prefetch).
4. Shipping it as a single Swift binary (`slotstream`) with a one-line install and an
   **Ollama-compatible API**, so every existing Ollama/OpenAI client just works.

Non-goal framing: this is not a research vehicle; it's an engineering product with a
pre-registered test matrix and measurable done-criteria. The mechanism (slots + streaming)
is model-agnostic; this model is the first and only v0 target.

---

**Target range, stated 2026-09-16.** The goal above is the target: Macs that cannot hold the model, 16 to 64 GB. Macs of 96 GB and more, where the model fits in memory, run it without being the optimization target. Decision: [[records/decisions/target-range-macs-that-cannot-hold-the-model]].

## 2. Ground truth: the model (verified from `config.json`, 2026-08-28)

`Qwen/Qwen3.8-Flash-Next`, `model_type: qwen4_exp` ("Qwen4 architecture preview"),
multimodal (vision encoder + text), license `qwen-community-1.0`.

| Fact | Value |
|---|---|
| Layers | 48, pattern `(GDN GDN GDN QSA) × 12` (`full_attention_interval: 4`) |
| Hidden size | 2560 |
| MoE (every layer) | 512 experts, top-10 routed + 1 shared, expert ffn dim 640 |
| Gated DeltaNet (36 layers) | 48 v-heads × 128, 16 qk-heads × 128, conv kernel 4, state in fp32 |
| Qwen Sparse Attention (12 layers) | 24 q-heads × 256, 2 kv-heads (GQA), partial RoPE 0.25 (dim 64); lightning-style indexer (4 q-heads, 1 kv-head, dim 128), budget 2048 tokens / 512 blocks (compress ratio 4) |
| Hyper-connections | `hc_count 4`, low-rank 320 (residual-stream variant, part of trunk) |
| N-gram / PLE store | 20,000,000 entries × 2560 dim = **51.2B params**; `ngram_size 3` (bi+tri-grams), `heads_per_ngram 8`, `split_ngram_parts 128`, conv kernel 4, injected at `ple_layer_ids [2]` |
| MTP | 1 hybrid layer, ~4B params (self-speculative decoding) |
| Vision encoder | 27 layers × 1152 (~0.4B), patch 16 |
| Vocab | 248,320, untied embeddings |
| Context | 262,144 native, 1M via RoPE scaling |
| Total on disk | ~180B params ≈ 355 GB BF16; GGUF Q4 ≈ 111 GB |

Why this model is *unusually* streamable — the three properties the whole design leans on:

- **96.6% of main-model params are routed experts** (120.8B of 125B), touched sparsely
  (10/512 per layer per token).
- **The 51B n-gram store is addressed by token n-grams alone** — no hidden state needed to
  know which rows you'll need. Qwen's own card says it's "more amenable to offloading than
  MoE"; Unsloth's guide explicitly recommends SSD offload for it. We can prefetch it with
  zero speculation.
- **Only 12 of 48 layers keep a KV cache** (GDN layers carry a fixed ~3.2 MB fp32 state
  each), and QSA reads at most 2048 tokens per step — so long context is cheap in both
  memory and decode compute. KV ≈ **27.6 KB/token** total (2.3 KB × 12 layers, incl.
  indexer keys).

Ecosystem state (2026-08-28): mlx-lm support is an **open PR (#1788)**; community MLX
conversions exist and ship their own `qwen4_exp.py` (pipenetwork 4bit / 6bit / mixed-4-8,
Vontra oQ4, …) — that Python file is our porting reference and test oracle. llama.cpp
support landed via PRs #27739/#27742 (unsloth GGUFs live). Official FP8 release exists for
cloud verification.

**M0 must re-verify all of the above against the actual downloaded artifacts** — the model
is 2 days old and conversions will churn. Every constant below is parameterized in code
from `config.json`, never hard-coded.

---

## 3. The byte math (first principles)

Quantization overhead (MLX affine, group size 64): bits + 0.5 bpp for fp16 scale+bias
→ 4-bit = 0.5625 B/param, 6-bit = 0.8125, 8-bit = 1.0625, 3-bit = 0.4375.

### 3.1 Component sizes at 4-bit

| Component | Params | 4-bit size | Placement |
|---|---|---|---|
| Routed experts (48×512) | 120.8B | **67.948 GB** ✅measured | SSD, slot-cached |
| — one expert record (3 × 2560×640) | 4.92M | **2,764,800 B** ✅measured | unit of streaming |
| — one layer's experts (512) | 2.52B | 1.4156 GB ✅measured | unit of prefill sweep |
| Shared experts (48) | 0.24B | 133 MB | resident, always |
| Routers (48 × 2560×512) | 63M | 126 MB (bf16) | resident (routing precedes fetch) |
| Dense trunk (GDN+QSA+HC+norms) | ~2.7B | 1.5 GB (2.2 GB @6-bit) | resident |
| Embeddings + lm_head (untied) | 1.27B | 0.72 GB | resident |
| N-gram/PLE store | 51.2B | **32.0 GB** ✅measured (320,001,536 rows × 100 B) | SSD, page-cached, exact-prefetched |
| MTP block | ~4B | 2.25 GB (est.; absent from the pinned conversion) | optional, off in v0 |
| Vision encoder | ~0.4B | 0.43 GB @8-bit | optional, off in v0 |
| **Fully resident total** | ~180B | **~102 GB** | doesn't fit even in 48 GB → streaming is mandatory on the dev Mac too; exceeds the default wired limit even on 128 GB (see §5) |

Resident floor (v0 text-only, trunk @6-bit): **≈ 3.3 GB** + KV + GDN state (113 MB fp32)
+ MLX graph/activations (~0.4 GB) + slot pool + ngram row cache (128–256 MB) + IO staging
(~128 MB).

KV by context: 8k → 0.23 GB · 32k → 0.90 · 64k → 1.8 · 128k → 3.6 · 262k → 7.4 (bf16;
fp8-KV halves this, later knob).

### 3.2 Decode model

Per token: 48 layers × 10 routed experts = 480 expert-uses. With hit rate *h* on the slot
cache:

```
miss_bytes/token = 480 × (1 − h) × 2.7648 MB     (+ 16 ngram rows ≈ 1.6 KB, prefetched)
t_token ≈ max(t_compute, miss_bytes / BW_ssd_eff) + stalls
```

- `t_compute` floor: unified-memory bandwidth measured at **235 GB/s**, so a purely
  bandwidth-bound token (3.375 GB active) would be 14 ms → ~70 tok/s. But a batch-1
  4-bit matmul was measured at only **47 GB/s** (launch/occupancy-bound, not
  bandwidth-bound), so decode is dominated by **kernel-launch count**, not bytes.
  The real figure must come from running the model; do not extrapolate from either
  number alone.
- `BW_ssd_eff` (this Mac, measured never-repeat cold): **17.3 GB/s** at expert-record
  size, saturating by QD8; 9.46 GB/s even at QD1. Small-page IO is ~100× worse at
  QD1 (4 KiB: 0.08 GB/s, 53.6 µs latency) — the quantitative case for record-sized
  reads over page-granular mmap. Base-capacity SSDs on small Macs will be much
  slower; Stage C must re-measure per machine.
- Worst case h=0 (cold): 1.33 GB/token → 77 ms = **13 tok/s**, from IO alone. The
  8 GB tier is memory-constrained, not bandwidth-constrained.
- Per-layer fetch parallelism is bounded (only that layer's ≤10 misses are known before
  its MoE runs), so latency hiding comes from (a) QD within the layer, (b) overlapping the
  fetch with the shared-expert branch (independent of routed experts), (c) **cross-token
  prefetch** (issue predicted per-layer expert sets for token t+1 right after sampling
  token t — prediction = union of each layer's experts over the last W tokens).

IO-side decode bounds at the **measured 17.3 GB/s** (see MEASUREMENTS.md §M0.5;
the 5–7 GB/s originally assumed here was ~3× too pessimistic):

| h | miss MB/tok | IO ms/tok | IO-bound cap |
|---|---|---|---|
| 0.98 | 27 | 1.5 | 650 tok/s |
| 0.90 | 133 | 7.7 | 130 tok/s |
| 0.50 | 663 | 38 | 26 tok/s |
| **0.00** | 1,327 | **77** | **13 tok/s** |

**The headline consequence: even a zero-hit cache sustains ~13 tok/s from IO.**
Streaming bandwidth is therefore *not* the binding constraint on any tier; memory
(what fits) and compute (kernel-launch-bound decode) are. h still governs how far
above that floor a tier lands, so M1 still matters — but it is no longer the
difference between "viable" and "not".

The single biggest unknown in this whole plan is **h(cache_size, workload)** for this
model's router. Everything else is deterministic byte-pushing. Hence M1 exists to measure
exactly that, cheaply, before the expensive Swift work depends on it.

### 3.3 Prefill model — dense sweep

At batch ≥ ~512 tokens, ~all 512 experts per layer get routed to (10 draws/token ×
C tokens over 512 experts → coverage ≈ 1−e^(−10C/512)). So on-demand caching is the wrong
mode for prefill. Instead, per layer: run the router for the whole chunk → **stream that
layer's used experts sequentially in groups** (staging G×2.76 MB, e.g. G=64 → 177 MB),
apply each group to its gathered tokens, discard, next group. Prefill cost per chunk ≈ one
~68 GB sweep (less router-skipped experts) regardless of chunk size → **make chunks big**:

| chunk C | sweep IO / token | prefill IO cap @**17.3 GB/s** (measured) |
|---|---|---|
| 1,024 | 66 MB | ~260 tok/s |
| 4,096 | 16.6 MB | ~1,040 tok/s |
| 8,192 | 8.3 MB | ~2,100 tok/s |

A full 68 GB sweep costs 3.9 s at the measured rate, so prefill is compute-bound at
every useful chunk size and the sweep design has ample headroom.

Effective prefill = min(IO cap, compute cap ~300–800 tok/s est.). Short prompts below a
threshold (~512 tok) use the normal cached path. **The sweep must be scan-resistant**: it
runs through staging only, bypassing the slot cache for placement while still updating
frequency stats, and admits experts into slots only by frequency/hot-set — otherwise
every long prefill flushes the warm cache and the decode that follows starts cold.
N-gram rows for the entire prompt are known upfront → batch-fetch before layer 2.

Cold start: resident load (**3.822 GB** ✅measured) + optional hot-set preload, at the
measured 17.3 GB/s → **first token in seconds, not minutes** — no full-model load ever
happens. ✅Confirmed on the real checkpoint: lazy `load()` of all 97 GB returns in
**0.4 s with 0 GB resident**.

**Built 2026-09-02, as N2.** The sweep above is what ships, with three
departures from this note, each decided by measurement (MEASUREMENTS.md,
"N2 — the prefill sweep").

- **The threshold is 256 tokens, not ~512, and it is the only input to the
  decision.** A pass of 256 tokens or more takes the sweep; anything shorter
  (decode, speculative verify passes, a short follow-up turn) gathers over the
  slot pool exactly as before. The choice is a function of the token count
  alone so that the pool's size and contents cannot change the math — the
  golden-equivalence invariant of §6 — and `sweep-check` proves the sweep
  bit-identical on a cold and a warm pool.
- **Groups are 32 experts, not 64, and each group is one grouped GEMM per
  projection.** Sorting a pass's rows by expert is what reaches MLX's
  `gather_qmm_rhs` kernel, which reads an expert's weights once per tile of
  tokens instead of once per token; the old per-(token, expert) gather never
  could. MLX takes that kernel only for a call with at least 16 rows and four
  per expert of the weight array it is handed, so a group that is short of
  that is padded up to it: the kernel a row meets depends on the routing
  alone. 16 and 32 tied at the top and used the least memory; 64 and 128 were
  slower and peaked higher.
- **Reads are contiguous runs.** Experts are read in ascending id order, and
  consecutive ids are one `pread` per piece rather than nine ~307 KB pieces
  per record, which is what the 2026-08-30 measurement identified as the cap
  on prefill IO. Resident experts are copied out of the pool instead, so a
  warm cache still saves reads. The GPU works on one group while the CPU
  reads the next; at most two groups of staging exist at once, so the peak
  is bounded the way the 32-record slices bounded it.
- **Scan resistance is the absence of writes.** The sweep never writes the
  pool, so a long prompt cannot flush what decode was using. Frequency
  admission runs on the final pass only: each layer's most-used experts, its
  fair share of the pool, are written in so decode starts warm. The batch
  n-gram fetch this note asked for landed in the same change, as a parallel
  read of every row a pass needs before its embedding is assembled.

### 3.4 Why explicit slots instead of mmap-and-pray

llama.cpp/LM Studio can already mmap this model and let the page cache thrash. We don't,
because on macOS that path has: unbounded RSS → memory pressure → compressor/swap death
(writes!, the thing that actually wears SSDs and freezes Macs), no QD control, no
prefetch, no layout control, and double-caching. Explicit slots give: deterministic
footprint chosen per machine, F_NOCACHE reads (no page-cache duplication), record-aligned
layout, measured QD, exact PLE prefetch, and a dense-sweep prefill mode none of the mmap
runtimes have. (A `--io-mode=pagecache` flag is kept for A/B honesty — the OS cache as a
free-but-nondeterministic L2 is itself one of the combinations to test on big-RAM tiers.)
MLX gets us Metal-first compute on unified memory: SSD→RAM is the only copy; RAM→GPU is
free.

Also: reads don't meaningfully wear SSDs; sustained decode at ~20 tok/s × 100 MB/tok ≈
2 GB/s read is thermally and endurance-wise fine. What kills machines is swap — which this
design's fixed budget exists to prevent.

---

## 4. Architecture

```
┌────────────────────────── slotstream (single Swift binary) ──────────────────────────┐
│  CLI (swift-argument-parser): pull · run · serve · install · bench · doctor · repack │
│  Server (Hummingbird/NIO): Ollama API (/api/*) + OpenAI (/v1/*) · NDJSON streaming   │
│  Engine (SlotstreamCore, mlx-swift):                                                 │
│    Qwen4Exp graph: GDN · QSA(+indexer) · MoE via gatherQMM over slot pool ·          │
│                    n-gram/PLE module · hyper-connections · sampler                   │
│    SlotPool: preallocated quantized pool [S × expert], (layer,expert)→slot map,      │
│              CLOCK/LFU eviction, per-step pinning, hot-set pinning, chunked segments │
│    IO: ExpertStore + NgramStore — F_NOCACHE aligned preads, thread-pool QD 8–32,     │
│        staging ring → pool write; DiskBench                                          │
│    Prefetcher: exact n-gram prefetch · cross-token expert prediction (window W)      │
│    Governor: static budget/preset + DISPATCH memory-pressure source → shrink/degrade │
│    Runtime: KV (12 QSA layers) · GDN fp32 states · sessions/keep-alive · cancel      │
│  Format (.ssmodel): manifest.json + resident.safetensors + experts.bin + ngram.bin   │
│                     (+ mtp/vision.safetensors) + tokenizer/                          │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

### 4.1 `.ssmodel` container (a directory)

- `manifest.json` — format version, source repo+revision, quant recipe per component,
  tensor inventory with shapes/dtypes, record geometry, per-file sha256, default preset
  hints, hot-set list (expert ids by global frequency, from calibration; refreshed from
  runtime stats).
- `resident.safetensors` — trunk, shared experts, routers, embeddings, norms. Loaded whole.
- `experts.bin` — 24,576 fixed-size records, `idx = layer×512 + expert`,
  record = packed(gate|up|down: weights+scales+biases) padded to 16 KiB multiple
  (**2,764,800 → 2,768,896 B**, ✅measured); pure `pread(fd, idx × record_size)`.
  **67.948 GB**.
  Note: in the source checkpoint each layer's 512 experts are *already contiguous*
  per projection (`[512, 640, 320]` etc.), verified by matching a direct `pread` of
  expert *i* against `mx.load(...)[i]`. So a working engine can stream straight from
  the original shards with **9 preads per expert**; `experts.bin` collapses that to
  **1**. The repack is a measured optimisation, not a prerequisite — which makes it
  safe to build the engine first and A/B the repack's real benefit.
- `ngram.bin` — ✅measured geometry: **320,001,536 rows** (128 shards × 2,500,012)
  of **160 dims**, quant group size **32**, packed **100 B/row** (80 weight + 10
  scales + 10 biases), **32.0 GB**. The repack's job here is *interleaving*: in the
  source checkpoint a row's three parts live in three different tensors ~3.25 GB
  apart, so an unrepacked reader needs **3 preads per row = 48 per token**; packing
  each row contiguously makes it **16 per token**. Rows stay packed (not padded):
  at 100 B a row straddles a 16 KiB page only ~0.6% of the time, and padding to
  128 B would cost +9 GB of disk for nothing. Read in 16 KiB aligned windows
  (~163 rows each); the cache is **page-granular, not row-granular**.
- `mtp.safetensors`, `vision.safetensors` — optional, absent in v0 default build.
- `tokenizer/` — tokenizer.json, tokenizer_config.json (chat template), generation_config.

Repack is **bit-exact** (no requantization): source = a vetted community MLX 4-bit
conversion (pipenetwork first candidate; spot-checked, checksummed, revision-pinned), so
our quality baseline equals mlx-lm's by construction. Own conversion from official
FP8/BF16 on a rented box is the fallback if community quants prove broken (billed, so it
needs an explicit decision).

### 4.2 Slot pool mechanics

> **Measured 2026-08-28 — the slot pool is mandatory, not an optimisation.**
> MLX has no sparse-materialisation path out of a memory-mapped tensor:
> `mx.gather_qmm` with top-10 indices materialises **all 512 experts of the layer**
> (471.9 MB for one projection → ~68 GB across the model), and a 16-row `mx.take`
> on an n-gram shard materialises **the whole 200 MB shard** to read 1.3 KB. Stock
> lazy loading therefore pulls in ~100 GB and dies on any prompt length. A
> pre-allocated, fully-resident, bounded pool that we fill by explicit `pread` is
> the only construction that avoids this under MLX. See MEASUREMENTS.md §M0.8.


- One **global pool across layers** (all experts share the shape 3×2560×640): quantized
  weight/scales/biases tensors sized `[S, …]`; per layer, router output ids are mapped
  through the slot table → `gatherQMM(x, pool…, rhs_indices: slots)`. Misses trigger IO;
  the token's active slots are pinned until the step completes.
- Eviction: start with CLOCK + frequency byte (approximates LFU-with-decay); simulator
  (M1) also scores LRU and W-TinyLFU on real traces — cheapest adequate policy wins.
- Slot fill v0: staging buffer → sliced write into the quantized pool arrays (they're
  plain uint32/fp16 tensors; a 2.76 MB device copy is ~µs against memcpy bandwidth).
  v1 option if profiling demands: pread directly into the pool's MTLBuffer contents
  (shared storage mode on Apple Silicon needs no sync) — measure before adopting.
- Resizing: default is a **single pool** — `gatherQMM` gathers within one tensor, and
  sharding the pool would put per-segment dispatch on the hot path. Governor shrink/grow
  = rebuild the pool at the new size (rare event, costs seconds, keeps the hot path
  clean). A segmented variant is adopted only if microbenching shows per-segment
  dispatch overhead is negligible.
- **M3-entry gate — the slot-write microbench (before any streaming work):** prove that
  scattered writes into the quantized pool tensors execute in place (MLX buffer donation
  — no full-pool copy per update) and sustain > 5 GB/s of slot fills. If MLX's
  functional-update semantics force copies at this size, switch to preads directly into
  the pool's MTLBuffer contents (shared storage mode needs no sync on Apple Silicon).

### 4.3 N-gram/PLE path

> **Measured**: this path is the single worst offender under stock MLX — a 16-row
> lookup materialises a 250 MB shard (~150,000× amplification), and touching most of
> the 128 shards costs the full 32 GB. Explicit row-level `pread` into a small page
> cache is required. Note the contrast with llama.cpp, which mmaps the PLE and lets
> the OS work at 4–16 KiB page granularity — that is survivable; MLX's whole-tensor
> evaluation semantics are not.


Keys for token t's rows are a pure function of input token ids (bi/tri-grams × 8 heads,
✅measured **16 rows × 100 B = 1.6 KB**, touching ≤16 of the 128 shards).
Decode: after sampling token t, issue reads for step t+1
immediately — a handful of 4–16 KiB reads (~0.1–0.3 ms) mostly hidden behind layers 0–1
of that step plus the row cache. Prefill: batch-read the whole
prompt's (deduplicated) rows before layer 2. Small LRU row cache (128–256 MB) absorbs
n-gram repetition in natural text. The exact hashing scheme is read out of the reference
`qwen4_exp.py`/PR #1788 at M0 (`heads_per_ngram 8`, `split_ngram_parts 128` — verify
lookup count per token and per-part addressing; if row addressing turns out non-uniform,
adjust stride/layout accordingly). Quality note: Unsloth warns n-gram tables tolerate ≤4-bit
poorly → ngram bits is an independent knob (4 default, 6/8-bit variants for quality runs —
IO stays trivial; only disk grows).

### 4.4 Server: Ollama-compatible surface (v0)

- `/api/version`, `/api/tags`, `/api/show`, `/api/ps`, `/api/chat` (streaming NDJSON,
  `think` toggle → chat-template thinking switch, `tools` passthrough into template),
  `/api/generate`, `/api/pull` (streamed progress), `/api/delete`; `/api/embed` → clean
  400 (not an embedder); `/api/create` → 501 with message.
- OpenAI: `/v1/chat/completions` (incl. SSE streaming), `/v1/models`, `/v1/completions`.

**What actually shipped is narrower than this section**, which is the v0 design and
not a description of the server. `/v1/completions`, `/api/delete`, `/api/pull` progress,
and `tools` passthrough do not exist; `keep_alive` is accepted and ignored because the
model never unloads; and of the Ollama options only `temperature`, `top_p`, `top_k`,
`min_p`, `presence_penalty`, `seed`, `num_predict`, and `stop` are honoured — `num_ctx`
and the rest are refused rather than silently dropped. `docs/API.md` is the description
of the shipped surface; read it, not this list.
- Semantics matched to Ollama: `keep_alive` (default 5m; model unload = free slots+KV,
  keep fd/manifest), options mapping (`num_ctx`, `temperature`, `top_p`, `top_k`,
  `min_p`, `presence_penalty`, `seed`, `num_predict`, `stop`), request cancellation on
  client disconnect, single-flight generation with FIFO queue (continuous batching is a
  non-goal in v0).
- Chat template: real Jinja rendering of the shipped `chat_template` via
  swift-transformers' Jinja engine. Recommended defaults per model card: instruct
  t=0.7/top_p=0.8/top_k=20/presence=1.5; thinking t=1.0/top_p=0.95.
- Port: 11434 if free, else 11435 (warn), `--port`/`SLOTSTREAM_HOST` override. Model name
  exposed as `qwen3.8-flash-next:4bit` (+`:mixed-4-8`, `:compact-3bit` variants later).

### 4.5 CLI & install UX

```
slotstream pull                            # ✅ parallel (8 conns), resumable, sha256-verified download of the pinned revision (`--verify` re-checks)
slotstream doctor [--memory-gb G]          # ✅ device report + the plan any flags produce + target table
slotstream run qwen3.8-flash-next:4bit     # REPL chat
slotstream serve                           # ✅ zero-config: auto-tunes, announces the plan, resizes elastically
slotstream serve --memory-gb 16            # ✅ total-process memory target (cache gets G − 4.4 GB; pinned)
slotstream serve --no-elastic              # ✅ pin an auto size too
slotstream serve --mtp on|off|auto         # ✅ MTP speculative decode (auto: only ≥120 experts/layer after its 1.6 GB)
slotstream elastic-check                   # ✅ byte-identical output across live pool grow/shrink
slotstream mtp-parity                      # ✅ draft head bit-exact vs the Python reference fixture
slotstream mtp-accept                      # ✅ measure the draft accept curve on real greedy continuations
slotstream mtp-check                       # ✅ speculative gates: determinism, state integrity, accept sanity
slotstream install                         # LaunchAgent (com.slotstream.server), starts at login
slotstream bench [--suite full|quick] [--sim-ram 16]
```

Config: `~/.slotstream/config.json` + env overrides; models in `~/.slotstream/models/`;
logs via os_log + `~/.slotstream/logs/`. ✅ Distribution (2026-08-28): public curl
installer (`install.sh` at the repo root → latest `gh release` tarball of binary +
metallib, sha256-checked, installed to `~/.slotstream/bin`, PATH wired, optional
handoff to `serve`); `serve`/`run` offer the model download interactively when weights
are missing. Later: Homebrew tap + notarized binaries + CI-built releases. Disk
requirement enforced by `pull`: ~105 GB for the 4-bit build (+ transient download
staging; `--purge-source` streams the repack to stay under ~130 GB peak), ~84 GB for
the later compact build.

---

## Measured operating policies and revision criteria
Maintaining good operating choices is part of the product: model selection, inference, context, memory, responsiveness and resource use must work together. A useful default saves users from repeating the engineering investigation. It may deliberately leave capacity unused when further allocation has no demonstrated benefit. Best means the best-supported tradeoff for the stated objective and evidence, not proof of a universal optimum.

## Classify the number before changing it

| Kind | Meaning and revision rule |
|---|---|
| Model or format fact | An exact dimension, byte count or format requirement. Verify against the pinned artifact; do not tune it independently. |
| Safety or correctness bound | Protects physical feasibility, numerical validity or ownership. A performance preference never overrides it. |
| Qualification limit | Marks the configuration actually validated. Expanding it requires the corresponding acceptance gates, not merely spare capacity. |
| Operating default | Chooses a practical resource/performance tradeoff. Retain a justified default until evidence or an explicit change of objective warrants revision. Document any supported override and its consequences. |
| Estimator bound | Limits what a prediction may claim. Holding an estimate flat outside its verified range does not establish a flat physical response. |

A value can serve several roles; state each one and separate their controls. A manual operating override does not waive a safety or qualification limit. Some legacy explicit paths warn rather than refuse, so describe their actual behavior instead of inventing protection.

## Record the reasoning where it is used

For each important new or materially revised tuning value, keep a named code constant and a nearby explanation linked to a canonical record. Record:

- Purpose and units, including whether a budget covers the whole process or one component.
- Kind of limit and evidence basis: measured observation, derived arithmetic, bounded estimate or provisional engineering choice.
- Tested model/revision, engine/backend, hardware and workload scope, with links to evidence and failed or excluded runs.
- The objective and tradeoff, including costs displaced elsewhere in the stack.
- Automatic behavior, allowed override and consequences for resizing, safety checks and user control.
- The condition that would justify revision and the gate that checks implementation behavior.

Keep this proportional to the decision; this does not require a separate record for every loop literal. Existing records can own a coherent family of constants. Examples include expert-cache targets, prefill candidates, draft activation thresholds, I/O concurrency, context qualification and allocator allowances. Preserve their distinct evidence rather than treating every number as one type of cap.

## Make the best supported choice with incomplete evidence

Use first principles to identify the bottleneck and feasible alternatives, then the available measurements to choose a conservative default. More RAM, larger batches or greater concurrency can displace useful resources or hit another bottleneck. Unavailable target hardware does not itself invalidate the existing choice or require automatic benchmarking on every user's machine.

When a revision is justified, compare configurations with matched work, controlled warm/cold state and relevant context/draft settings. Measure user-visible time and the full resource cost; fewer cache misses or a faster estimate alone is insufficient. Freeze meaningful comparison criteria before scored runs and retain failures. Change the default only within its proven safety and correctness envelope. A simpler measured profile may suffice; a live tuner needs evidence that its benefit repays its complexity and transition cost.

## Memory ceiling example and maintenance
The base automatic total-process ceiling remains 33 GB. The clean development-Mac cache ladder showed diminishing returns near 120 to 150 experts/layer, and the target accommodates the chosen prefill workspace. An enabled draft head adds its separately charged cost. RAM share, Metal limits and live availability may lower the target. The fixed-size CLI flags bypass the operating ceiling and pin the cache. The adaptive `--memory-limit-gb` override and the Mac app’s Custom limit instead preserve resizing within the selected ceiling and supported hardware budget. This also makes the first Custom selection inherit the current budget. See [[records/decisions/adaptive-memory-limits]].

The original larger-target sweep evaluated an already-bounded prediction curve. It cannot prove that all larger allocations have no benefit. The existing ceiling is still a defensible default; no allocation change is justified merely by that limitation in the evidence. See [[records/measurements/automatic-memory-default-evidence-scope-2026-09-09]], [[records/decisions/auto-target-is-the-33-gb-knee-not-70-percent-of-ram]] and [[records/claims/auto-memory-target-ceiling-33-gb]].

Keep code comments, CLI help/diagnostics, policy checks, canonical decisions/claims and relevant README/guides aligned. Regenerate PLAN.md, MEASUREMENTS.md and llms-full.txt from their declared sources. Preserve historical source bytes and annotate interpretations through records. This policy documents ongoing engineering responsibility; it does not claim a completed audit of every existing constant or authorize new benchmarks, spending, telemetry or background tuning.
## Functional memory acceptance and benchmark eligibility

Global macOS paging is diagnostic for ordinary correctness, context-capacity and process-budget acceptance. It cannot attribute system activity to Slotstream. Keep process ceilings, real headroom, OS pressure handling, allocation safeguards and complete numerical/work checks; report paging separately. Performance comparisons retain declared clean-interval rules, and historical frozen results stay unchanged. The controlling decision is [[records/decisions/global-paging-is-diagnostic]].

## Public claim review

Claim-text gates detect stale phrases, not unsupported implications. Review
supporting sources and supersession notes before changing a public claim's
scope. Keep hardware, release, workload, total-process target, measured
quantity and comparison baseline together. An estimate is labeled where it
appears, including a table cell; capped extrapolation does not establish a
physical plateau. Simulated capacity is not hardware qualification, a version
bump is not publication, and a cache-only equality test does not cover changed
prefill grouping or speculative decoding. Preserve legitimate historical
results with their dates and limits instead of discarding an entire record
when only one of its measurements was withdrawn.

The documented correction and its verification are in
[[records/measurements/public-documentation-evidence-audit-2026-09-13]].

Best-effort public estimates may combine incomplete evidence when useful to
users. Give their construction and assumptions, separate actual measurements,
and state when a configuration is transferred to unmeasured hardware. Do not
present an editorial range as a calibrated confidence interval or speed bound.
[[records/measurements/hardware-planning-ranges-2026-09-13]] records the memory
range example and its revision conditions.

## Persistent prefix cache defaults
`serve --prefix-cache-dir` is off unless a directory is named, so none of these values changes a default server. They are provisional engineering choices for an opt-in tier, not measured optima.

| Value | Kind | Purpose and tradeoff | Override and revision |
|---|---|---|---|
| 20 GB quota (`PersistentPrefixConfiguration.defaultMaxBytes`) | Operating default | Bounds every head and row segment in the directory, applied when the directory opens and before each write. When it is full, states nobody continued go first, then previous-turn states kept for regenerating, then continued conversations, then prefixes several conversations start from, least recently used first within each; files from other builds do not survive opening. A conversation costs one recurrent-state head per kept turn plus its rows once, so a larger quota keeps more long conversations at more disk. | `--prefix-cache-disk-gb`. Revise from measured restart hit rates and real conversation sizes. |
| 2,048-token minimum (`defaultMinimumTokens`) | Operating default | Every write stores the fixed recurrent state; a conversation's first write also stores every cached row, and later turns add only their new rows. Shorter conversations save less prefill for the same fixed write, and each write adds disk wear. | `--prefix-cache-min-tokens`. Revise from measured save cost against prefill time across hardware. |
| 30-day maximum age (`defaultMaxAgeDays`) | Operating default | Conversation contents should not stay on disk indefinitely just because the quota has room. A state neither written nor restored for this long is removed when the directory opens and before writes; a longer age keeps older conversations resumable. | `--prefix-cache-max-age-days`; `0` disables. Revise from how long real users return to conversations. |
| Previous turn kept, older turns removed | Operating default | The kept state restores a regenerated or edited last reply after a restart without re-reading the conversation, for one more recurrent-state head; earlier turns are rarely resumed. | None. Revise if measured use shows edits further back. |
| Shared prefixes kept once, never replaced (`PersistentPrefixEntry.shared`, `PersistentPrefixValue.shared`) | Operating default | A state written inside a prompt at a boundary other conversations start with, its system message or the head it shares with a kept state, is exempt from the ancestor removal a later save of an extending conversation performs, so it outlives every conversation that started from it, and goes after conversations when the quota needs room. A shared prefix nobody started from is one-off, and a reply two conversations continue counts as shared too: lineages decide, not the flag. | None. Revise if measured directories fill with prefixes nobody returns to; the maximum age already removes those. |
| 512-token minimum shared prefix (`Generator.sharedPrefixMinimumTokens`) | Operating default | A shared save costs a GPU synchronize, a fork into the memory tier and, with the disk tier, one head plus rows; below this length the prefill it saves a later conversation is small next to that cost. Memory keeps shared prefixes from 512 tokens; the disk tier applies its own minimum length. | Library: `Generator.sharedPrefixMinimumTokens`. Revise from measured save cost against prefill time. |
| Shared save point: the last existing pass end at or before the boundary (`PrefillSchedule.lastPassEnd`) | Correctness bound | A shared prefix is written where a prefill pass already ends, the 256-token grid by default, never by splitting or reshaping a pass, so every arithmetic shape and every output stays identical to a run without the save; the next conversation processes up to one pass of the system prompt again. | None. A save at the exact boundary would need the rechunking numerical contract re-qualified. |
| 32 segments per head (`PersistentPrefixCache.maximumSegments`) | Operating default | Bounds the files one restore reads and how long replaced rows stay referenced; past it a write stores every row again. | None. Revise from measured restore cost and disk use on long conversations. |
| 2 GB free-volume margin (`PersistentPrefixCache.minimumFreeBytes`) | Safety bound | A save is skipped rather than filling the volume. | None. Not a performance value. |
| Identity: executable image digest, config digest, first and last 4 MiB of every weight file, cache geometry, optimization settings | Correctness bound | A state is restored only by the computation that wrote it; sampled weight content survives a copied model but rejects a different checkpoint with the same file sizes. | None. A cheaper identity needs evidence that it still rejects every changed computation. |
| CRC-32 per payload and header, rename without fsync | Correctness bound | A torn or corrupted head or segment fails its checksum and is removed with every state that uses it; it is never restored. Durability after a crash is best effort. | None. |
| Rows reused only through lineage (`State.persistedLineage`) | Correctness bound | A write references earlier rows only when its state descends from that persisted head without a rewind below it. Equal token ids never qualify: a re-prefill of the same ids produces other bits. | None. |

## Coding agent defaults

These values connect coding agents to a server. The launch values apply only to `slotstream launch`; the serving values apply to every client of the endpoints named. The live runs behind them are in [[records/measurements/coding-agents-launch-2026-09-17]].

| Value | Kind | Purpose and tradeoff | Override and revision |
|---|---|---|---|
| Chat reply budget without a request limit: a quarter of the window, 256 to 8,192 tokens (`GatewayDialect.outputBudget`) | Operating default | `/v1/responses` and the fx endpoints already used it. Chat completions used 512 tokens, which cut agents' edits and summaries short. A longer reply holds its slot for more decode time. The Ollama endpoints keep 512. | Per request: `max_tokens` or `max_completion_tokens`. Revise if measured agent replies stop at the ceiling, or clients time out waiting for them. |
| Smallest served window the launch accepts: 32,768 tokens for Claude Code and Codex, 16,384 for Pi and opencode, 65,536 for Hermes (`CodingToolLaunch.Tool.minimumContext`) | Qualification limit | The opening prompt plus a full 8,192-token reply must fit, with room for a conversation. Opening prompts measured on 2026-09-17: about 15,300 to 15,500 tokens for Claude Code, 10,400 for Codex, 1,600 for Pi, 7,300 for opencode and 12,300 for Hermes. Hermes 0.21.1 itself refuses a window below 64,000 tokens. Below the limit the launch restarts a server it started itself and nothing uses, with that window; any other server is left running and the launch refuses, naming the `--max-context` to serve. | None in the launch; the guides still describe manual setup. Revise when a supported agent version changes its opening prompt. |
| 30-minute agent timeouts: Claude Code `API_TIMEOUT_MS` and `CLAUDE_STREAM_IDLE_TIMEOUT_MS` (`CodingToolLaunch.claudeDefaults`), Codex `stream_idle_timeout_ms`, Hermes's stream and side-task timeouts | Operating default | A cold prompt near the full window is read for minutes before its first token; the server estimated 8.8 minutes for 65,536 tokens at `--memory-gb 12`. Shorter agent limits abandon the request and send it again, which starts the read over. The cost is that a server that really stopped is noticed later. | Claude Code: an exported value or its own settings win. Codex and Hermes: their own configuration. Revise if measured cold reads on a supported plan approach 30 minutes. |
| Window of a server the launch starts: the automatic window, raised to the agent's minimum when that is larger (`CodingToolLaunch.BackgroundServer.window(for:automatic:)`); in practice 65,536 tokens only for Hermes below 36 GB | Operating default within the qualification limit above | `docs/CODING-AGENTS.md` recommended `--max-context 65536` for every agent. `slotstream doctor` on the development Mac showed what a fixed 65,536 costs on small targets: at `--memory-gb 12` it keeps whole conversations but pins the expert cache at about 13 experts per layer (1.8 GB) against about 31 (4.1 GB) at the automatic 32,768, and the plan's decode estimate falls from about 6 to about 3 tok/s; at 10 GB it keeps only 6,828 tokens of a conversation, so every turn is read again; at 9 GB the plan is refused (38,912 tokens at most). The automatic window is the one the planner qualifies for each Mac, with speculative decoding kept. Claude Code and Codex fit their opening prompt and a reply in 32,768 tokens; long sessions compact sooner. | Run the server yourself with `--max-context`; the launch then uses it as it is. Revise if the automatic policy changes, or an agent's opening prompt no longer fits the automatic window of the smallest supported target. |
| Lifetime of a server the launch starts: it runs while any process the launch registered runs, and stops 30 minutes after the last request ends and the last registered process exits (`CodingToolLaunch.BackgroundServer.defaultIdleMinutes`, `serve --idle-exit`); a sleeping Mac's time counts | Operating default | Starting the server takes seconds, but a session that finds no server in memory reads its agent's instructions from disk or, without a saved state, again for minutes (15,487 tokens in 158.41 s for Claude Code). A server that outlives each session keeps the model's expert cache warm between sessions and between a person's pauses. Ollama keeps a model loaded 5 minutes after the last request; a coding session pauses longer while a person reads a diff, and the registered agent keeps the server anyway while it runs, so the timer only matters after the last agent exits. The cost is the server's memory, up to its plan's target, for half an hour after work ends. | `slotstream launch --idle-exit <minutes>` (0 keeps it until `slotstream stop`, at most 10,080); `slotstream stop` ends it at once. Revise if people report servers that stop between sessions they consider one piece of work, or memory held after they finish. |
| Prompt caches on disk for a server the launch starts: `~/.slotstream/prefix-cache` with the `serve` quota (20 GB) and age limit (30 days) | Operating default | `serve` keeps the disk tier opt-in; a launched server exists to run agents, whose opening prompts repeat across sessions and restarts. With the directory, a new session after a restart restored Claude Code's 13,312-token shared prefix in 0.08 s instead of reading it. The cost is disk writes, about 470 MB for a 12,800-token shared prefix at this model, bounded by the quota. | Run the server yourself without `--prefix-cache-dir`; `slotstream prefix-cache --clear` empties the directory while no server holds it. Revise if the writes measurably slow sessions or wear matters more than the saved reads. |
| Which server the launch may stop: only the one it started, recognized by the process id and start time it recorded matching `/slotstream/status` and the running process, and only while it has no request running and no registered process; launches that may start a server take turns through a lock per port | Safety or correctness bound | A server someone started in a Terminal, or one another agent is using, belongs to that person or agent; restarting it would lose their work or their choice of window. A process id alone can be reused by a later process. Without the lock, two launches at once would each start a server; the second would fail on the port or the model lock, after overwriting the first one's record. `slotstream stop` is the explicit way to stop any server. | None. |
| A request that declares tools keeps its shared prefix like a conversation (`SharedPrefixRetention.conversation`, set by the server) | Operating default | An agent's instructions and tools open every later session, subagent and compaction request. Held as an optional snapshot, they were the first state other conversations displaced: in the first live pass, second sessions read their whole opening prompt again (Claude Code: 15,487 tokens, 158.41 s). Kept like a conversation, Claude Code's second session reused 13,312 tokens and took 48.89 s. The checkpoint now competes with conversations, least recently used first. Plain chat keeps `.optional`. | Library: `RequestControl.sharedPrefixRetention`. Revise if measured tool-free workloads lose conversations to agent checkpoints. |
| Retention kept once the vision tower loads: the largest that leaves the pool's slots and prefill pass as the ordinary re-plan sizes them (`Planner.loadingVision`) | Operating default | Applies only when the text plan retained a whole window. The ordinary re-plan fell back to the budget share, 10,807 tokens at a 12 GB target with a 65,536-token window, so every coding agent conversation after the first picture was read in full on each turn. For that case on a 48 GB Mac, `vision-check` sizes the graded retention at 43,882 tokens with the peak still within the target. In the last live pass the cache held 29,645 tokens after the picture turn, more than the old ceiling; that turn reused 15,587 of 16,332 tokens, and a later Codex turn 10,416 of 10,526. | `--memory-gb` and `--max-context` size the plan. Revise if measured image turns need more transient memory than the plan charges. |

## 5. The configuration space (the tradeoffs to test)

Axes — every named preset is a point in this space:

| Axis | Values | Effect |
|---|---|---|
| Cache size | **experts per layer** (of 512; 0.133 GB per expert/layer; CLI `--experts-per-layer`, raw-pool alias `--pool-gb`, total-process form `--memory-gb`, default **auto** from RAM + working set) | THE memory↔speed knob |
| Resident policy | experts-streamed · +ngram-streamed (default) · +ngram-resident (big RAM) | footprint vs decode variance |
| Quant recipe | all-4bit (default) · mixed-4-8 (trunk/attn 8-bit) · compact (3-bit experts) · ngram bits 4/6/8 | quality vs disk/RAM |
| KV | bf16 (default) · fp8 (later) | context memory |
| Context cap | 8k…262k | KV budget per tier |
| Prefetch | off · cross-token(W=1,4,16) · +hot-set pinning | latency hiding |
| Prefill mode | cached · dense-sweep (auto threshold) | prefill throughput |
| IO mode | nocache (default) · pagecache-L2 | determinism vs free OS cache |
| QD / staging | 8–32 / 64–256 MB | SSD utilization |
| MTP self-spec decode | off (v0) · on (M9, big-memory tiers only — see the M9 design note) | decode multiplier where launch-bound; costs ~17 experts/layer |

### Presets v1 (est. columns to be replaced by M8 measurements)

Budget rule: total footprint ≤ ~65–70% of RAM **and** under
`max_recommended_working_set_size`, leaving OS+apps alive. ✅measured on this Mac:
working set = **37.4 GiB of 48 GB** (78%), so the real ceiling is tighter than "RAM"
everywhere — `doctor` must read this value, not infer it from `hw.memsize`. Also
✅measured: `max_buffer_length` = **28.1 GiB**, which caps any *single* MLXArray;
harmless in the 9-tensor pool layout (a 204-experts/layer = 27 GB pool's largest tensor is 8.0 GiB) but
it would have been binding for a single-tensor pool.

Resident floor ✅measured at **3.822 GB** (everything except experts and n-gram),
up from the 3.3 GB estimated — footprints below include this.

**✅ Auto-tuning planner implemented (2026-08-28), replacing named presets as the
default UX.** `SlotstreamCore/Plan.swift` is the single policy used by run/serve/
doctor and echoed in `/api/show` `details.memory_plan`. Policy: ceiling =
min(70% of RAM, working set − 2 GB), then **clamped to currently reclaimable
memory** (free + purgeable + file-cache pages via `host_statistics64` — what can
be taken without compressing/swapping other apps; `kern.memorystatus_level` was
rejected as too optimistic) minus max(1.5 GB, 5% RAM) slack, with an explanatory
note whenever the clamp binds; quiet machine ⇒ clamp never binds ⇒ deterministic.
Pool = target − 3.9 GB fixed footprint (resident weights + n-gram row cache)
− 0.5 GB margin; floor 640 global slots (~14/layer); min honest target 6.2 GB.
Precedence `--experts-per-layer` > `--pool-gb` > `--memory-gb` > auto, losing
knobs noted, never silently dropped; explicit knobs are never resized by the
clamp (informational note only). `doctor --sim-ram/--sim-working-set/
--sim-available` previews any machine; `Tools/planner_gates.sh` pins seven setups
(48 GB pristine/busy, 16 GB pristine/busy, 8 GB, 128 GB, explicit-on-busy).
✅Measured live under a 21.5 GB incompressible hog: auto sized 36.1 → 10.7 GB
(47/layer), generated at a 9.4 GB actual peak with no thrash, and recovered to
34.4 GB when the hog exited.

**✅ Elastic pool implemented (2026-08-28): `serve` resizes the cache while
running.** `SlotstreamCore/Governor.swift` re-runs the feasibility replan every
15 s (crediting the pool + fixed footprint a restart would release; absolute
dead-bands −1 GB shrink / +2 GB grow, one-step convergence) and subscribes to
OS memory-pressure events as the overcommit backstop (warning ≥2 GB/15%,
critical ≥4 GB/50%, repeated events keep shedding; growth waits 60 s of calm).
Resizes run strictly between requests under the generation lock: grow copies
contents piece-by-piece (transient ≤ one piece), shrink frees before
reallocating (transient = max(old,new), restarts cold — under pressure,
holding two pools to keep warmth would spike memory at the wrong moment).
Auto-sized pools only; explicit knobs and `--no-elastic` pin. Byte-exactness
across live resizes is a standing gate (`slotstream elastic-check`), and live
hog experiments (shed cascade, identical outputs under pressure, contents-kept
recovery, macOS-swaps-idle equilibrium) are in MEASUREMENTS.
The startup announce prints device, target, experts/layer cached, expected peak,
and est. warm tok/s (log-linear between the measured anchors 30/layer = 5.6 and
181/layer = 20.0, flat above — decode is launch-bound past ~181/layer). Promise
✅measured: `--memory-gb 8` → 27/layer, **7.0 GB actual peak** (predicted 7.5),
5.2 tok/s, byte-identical greedy output; auto on this Mac → 36.1 GB target,
239/layer, **35.0 GB actual peak** (predicted 35.6). The preset table below
remains the cross-machine *map*; the planner is the *mechanism* that lands each
machine on its row without anyone reading the table.

| Preset | RAM | **Experts/layer cached** (of 512) | Cache mem | Footprint | Ctx default | h est. | Decode est. | Measured |
|---|---|---|---|---|---|---|---|---|
| `max192` | ≥192 GB | **512** + ngram resident, MTP on | 67.9 GB | ~110 GB | 262k | 1.0 | 40–80 | — |
| `big128` | 128 GB | **512**; ngram streamed | 67.9 GB | ~79 GB | 262k | 1.0 | 35–70 | — |
| `big96` | 96 GB | **~414**; ngram streamed | ~55 GB | ~63 GB | 128k | ~0.99 | 30–60 | — |
| `big64` | 64 GB | **~271** | ~36 GB | ~42 GB | 128k | .93–.98 | 25–45 | — |
| `pro48` ← this Mac | 48 GB | **~204** | ~27 GB | ~32 GB | 64k | .88–.96 | 18–35 | 20.0 warm @181/layer |
| `mid32` | 32 GB | **~121** | ~16 GB | ~21 GB | 32k | .80–.92 | 12–25 | — |
| `mid24` | 24 GB | **~79** | ~10.5 GB | ~15.5 GB | 32k | .70–.87 | 8–16 | — |
| `lite16` | 16 GB | **~41** | ~5.5 GB | ~10 GB | 16k | .55–.80 | 4–9 | 5.6 @30/layer (emulated) |
| `edge8` (experimental) | 8 GB | **~14** (the floor) | ~1.8 GB | ~5 GB | 8k | .30–.60 | 1–4 | — |

**The decode-est. column is now known to be too pessimistic at the low end.** With
17.3 GB/s measured, IO alone floors decode at ~13 tok/s even at h=0, so `lite16`
(4–9) and `edge8` (1–4) are wrong *as IO estimates*; their real limit is memory
pressure and compute. Those bands are left unrevised deliberately — M8 replaces them
with measurements rather than a second round of guessing.

Notes: fully-resident-incl-ngram on a 128 GB Mac (~110 GB with KV) exceeds the default
wired limit (~96 GB) — possible only with an explicit `iogpu.wired_limit_mb` bump, which
is why `big128` streams the n-gram store instead: it needs no sysctl and loses almost
nothing (n-gram IO is ✅measured at 16 rows ≈ 1.6 KB/token, exact-prefetched). Smaller Macs also have slower SSDs (1.5–3.5 GB/s) — folded into the est. bands;
`lite16`/`edge8` use trunk @4-bit to shave the floor; `edge8` may additionally need the
compact 3-bit expert build and a raised `iogpu.wired_limit_mb` (doctor detects + explains,
never auto-sudos). External USB4 NVMe (~3 GB/s) is a supported weights location — worth a
row in the matrix for 256 GB-internal-disk Macs.

---

**Rows above the target range (2026-09-16).** `big96`, `big128` and `max192` describe Macs where the model fits in memory. They are outside the optimization target ([[records/decisions/target-range-macs-that-cannot-hold-the-model]]), their decode estimates were never measured, and the one community report on a 128 GB M5 Max reads 31.5 tok/s at a 73 GB target on 0.2.3 ([[records/measurements/c2-macbook-pro-m5-max-128gb-community]]). Keep the rows as the historical map; do not plan work against their estimates.

## 6. Correctness strategy (non-negotiable invariants)

1. **Streaming must be math-invisible.** Same engine, tiny cache vs infinite cache, greedy
   sampling → **identical token sequences**, always. This is a claim about the *expert
   pool*, where a hit and a miss deliver the same bytes to the same kernel. It does not
   extend to anything that changes how tokens are *batched* — prefill chunk size or
   conversation prefix reuse — where MLX picks reduction orders by shape and floating
   point is not associative (measured: MEASUREMENTS.md). Those are gated against a
   control instead. This is the golden test run in CI on
   a synthetic small qwen4_exp config and on truncated real weights.
2. **Parity with the Python reference** (the `qwen4_exp.py` shipped in the MLX conversions
   / PR #1788, revision-pinned and vendored into `Tools/reference/`): layer-by-layer
   hidden-state comparison on real weights for each of the four block types (GDN, QSA,
   MoE, PLE) — the full model never needs to fit in RAM to prove the port is right.
   Target ≤1e-2 relative; investigate anything above.
3. **GDN state stays fp32** (`mamba_ssm_dtype: float32`) — numerics drift here is a known
   architecture foot-gun.
4. **QSA: the indexer is core, not optional.** Dense attention is exactly equivalent only
   while context ≤ the indexer budget (2048 tokens) — beyond that the trained behavior is
   top-2048-token sparse attention, and a dense path silently diverges from the model as
   trained (and from the Python reference, so parity would fail anyway). Implement the
   indexer in M3 proper; keep the dense path as a *test oracle* for ≤2048-token parity
   runs and as a debug flag, never as the shipped path.
5. Repack is checksummed both directions (source tensor sha → record sha), and `pull`
   verifies before first run. Model is 2 days old; expect re-releases — pin revisions.

---

## 7. Test matrix & iteration protocol ("test them all", tractably)

Three stages so the combinatorics stay sane:

- **Stage A — simulator sweep (cheap, thousands of configs).** The M1 cache simulator
  replays recorded router traces against {policy × slot-count × prefetch-window} in
  minutes, no model needed. Output: h-curves per workload per tier → prune to top-3
  configs per tier.
- **Stage B — live sweeps on the dev Mac.** Real engine, real SSD: run Stage-A winners per
  tier using `--sim-ram` (hard-capped budgets emulating 8–32 GB on this 48 GB machine),
  plus the axes the simulator can't see (QD, staging, io-mode, dense-sweep threshold,
  quant recipe). `slotstream bench` emits JSON + markdown into `bench/results/<host>/`,
  committed.
- **Stage C — real machines.** The binary + `bench --suite full` is a 10-minute favor on
  any Mac. Validate ≥1 real ≤16 GB machine before claiming the tier (sim-RAM can't emulate
  the real wired-limit and slower SSD). Borrow/family machines; MacStadium rental is the
  fallback.

**Bench workloads** (fixed, versioned in `bench/workloads/`): W1 cold-start→first-token ·
W2 warm short chat (256/256) · W3 8k prefill+512 · W4 64k prefill+512 (≥48 GB tiers) ·
W5 15-min sustained generation (thermal/soak) · W6 agentic trace replay (many short
alternating turns — realistic expert locality) · W7 two queued clients.
**Metrics per run**: ttft, prefill tok/s, decode tok/s p50/p95, slot hit rate, GB read,
read amplification, peak phys_footprint, wired GPU MB, **swap delta (must be 0)**, SSD
MB/s, thermal pressure + energy (powermetrics).

**Iteration protocol** (repeat until §11 done): pick worst tier vs targets → profile
(Instruments Metal System Trace + fs_usage) → change one knob → bench → commit results +
update preset table → next. Locality traces from real usage (opt-in local stats) feed back
into hot-set lists.

---

## 8. Milestones

Effort in focused solo days (agent-assisted); order is dependency-driven. M1 gates nothing
except tuning — Swift work starts in parallel after M0.

### M0 — Ground truth & feasibility (0.5–1 d)
Download the pinned community MLX 4-bit conversion; script that recomputes §3 from the
actual `config.json` + safetensors index (fail loudly on drift). Read the reference
`qwen4_exp.py` / PR #1788; document the exact n-gram hashing + lookup count, MTP wiring,
QSA indexer math into §2. Write `DiskBench` (pread QD×size sweep incl. 2.76 MB and
4–16 KiB patterns, F_NOCACHE on/off) and run on this Mac; record Metal
recommendedMaxWorkingSetSize and default wired limit; check `mlx-swift-lm` for an existing
Qwen3-Next/GDN Swift implementation (if present, M3 shrinks by days). Verify
swift-transformers Jinja handles this chat template.
**Exit:** §2/§3 tables re-verified; SSD curve measured; weights source pinned (or fallback
conversion plan triggered); `gatherQMM` + quantized in-place updates confirmed available
in mlx-swift (slot-write microbench specced); M3 effort re-estimated.

### M1 — Expert-locality study (1–2 d, the cheap de-risk)
**Current local expert-prediction experiment (September 10):** [[records/plan/expert-lookahead-local-experiment-2026-09-10]] defines the causal native capture, training, bounded prefetch and speed qualification now requested. Its first run uses the actual checkpoint on the existing Mac. The proxy/cloud collection route below belongs to the historical M1 proposal and is not a prerequisite or spending instruction for this local experiment.

Traces: instrument the Python reference (mlx-lm branch) to dump per-layer top-k ids.
(a) **Proxy, free, local**: Qwen3-Next-80B-A3B (same 512-expert/top-10 routing family)
fits this Mac at 3–4-bit — collect traces across chat/code/long-doc/agentic corpora.
(b) **Real model, small paid run (approval-gated, ~US$30–60)**: rented 2×H200 (or 4×A100)
running the official FP8 via vLLM day-0 recipe with a 20-line router-logging patch; 1–2M
tokens ≈ ~1–2 GB of traces. Build the **cache simulator** (replays traces vs policy ×
size × prefetch-window; also computes prefill per-chunk expert coverage and hot-set
concentration).
**Exit:** h(size) curves per workload committed; eviction policy + prefetch window chosen;
preset table §5 updated; explicit go/no-go note for `lite16` (needs h ≥ ~0.6 @ 5.5 GB) and
`edge8`.

### M2 — Container + repack (1–2 d)
`Tools/repack.py` (Python, tooling-only): MLX safetensors → `.ssmodel` per §4.1, bit-exact,
checksummed, config-driven geometry. Swift `Format/` reader + verifier.
**Exit:** full 4-bit repack on this Mac; random-sampled records byte-equal to source
tensors; `slotstream verify` green.

### M3 — Swift engine, resident-path correctness (**3–5 d**, revised down after M0)

Revised because M0 found far more Swift prior art than assumed: `gatedDeltaUpdate` +
`Qwen3NextGatedDeltaNet`, `SwitchGLU`/`QuantizedSwitchLinear`, and
`gatherQuantizedMM` all already exist and are verified working (MEASUREMENTS.md
§M0.3–4). The slot-write gate is already passed, so the microbench that opened this
milestone is done. Novel work is now only: QSA indexer, hyper-connections, PLE path.
Add the metallib/Xcode decision (open question 9) before anything ships.

Entry task: the §4.2 slot-write microbench. Then port qwen4_exp to mlx-swift in
`SlotstreamCore/Model/`: GDN (chunked gated delta rule; fp32 state; custom Metal kernel
only if profiling demands — start with pure MLX ops), QSA **including the indexer**
(dense path kept only as the ≤2048-token test oracle, per §6.4), MoE via `gatherQMM`
(resident pool = all experts of the layers under test), hyper-connections, n-gram/PLE
module, sampler (temp/top-p/top-k/min-p/presence). No vision, no MTP. Test rig: truncated
real-weight prefixes (first 4–8 layers) vs Python reference, layer-by-layer; synthetic
tiny config for full-graph unit tests + CI.
**Exit:** parity per §6.2 on all four block types (QSA checked both ≤2048 dense-oracle
and >2048 indexer-vs-reference); tiny-config end-to-end generation matches Python greedy
output; tokenizer round-trip parity vs Python on a mixed corpus (code/multilingual/emoji).

### M4 — Slot streaming decode → first full-model run (3–5 d) — **the gating milestone**

Reordered emphasis after M0.8: because MLX materialises whole tensors out of mmap,
**no full-model forward pass is possible at all until the slot pool exists**. M3's
layerwise parity work is unaffected (its 4–8-layer truncated rig needs only
5.6–11 GB of experts, which does fit), but "run the model" and "stream the model"
are the same milestone, not consecutive ones. Plan M3 and M4 as one continuous push
with a single integration point.

SlotPool + eviction + pinning; ExpertStore/NgramStore with F_NOCACHE read pool; staging →
pool writes; n-gram exact prefetch; Governor v0 (static budgets); golden equivalence
(§6.1). Then the milestone moment: **the full 125B+51B model generating on this 48 GB Mac
under `pro48`.** Sanity-compare outputs against the official FP8 cloud endpoint on a fixed
prompt set (distribution-level, not bit-level).
**Exit:** golden test green; warm-chat decode ≥ 10 tok/s on this Mac (pre-tuning);
footprint within budget ±5%; 30-min soak with zero swap growth.

### M5 — Dense-sweep prefill + prefetch + perf (2–4 d)
Chunked sweep prefill with grouped staging + auto threshold + scan-resistant admission
(§3.3); cross-token prefetcher; QD autotune from doctor data; overlap shared-expert
branch with miss fetches.
**Exit:** dev-Mac targets — prefill ≥ 150 tok/s @8k, decode ≥ 20 tok/s warm chat,
`--sim-ram 16` decode ≥ 4 tok/s.

### M6 — Server (2–3 d)
Hummingbird app per §4.4; NDJSON/SSE streaming; template rendering incl. `think` and
`tools`; keep_alive lifecycle; cancellation; queueing.
**Exit:** Open WebUI, one editor client (Zed/Continue via OpenAI path), and raw `curl`
scripts (incl. `ollama` CLI pointed via `OLLAMA_HOST`) all work: list, chat, stream,
cancel, unload.

### M7 — CLI, install, packaging (1–2 d)
`pull` (resumable HF download + repack + verify + disk preflight), `doctor`, `install`
(LaunchAgent), config/env/logs, `make install`, `gh release` arm64 binary.
**Exit:** clean-machine bring-up in ≤ 3 commands ≤ 30 min (download-dominated).

### M8 — Matrix bench + tier validation (2–4 d first pass, then the §7 loop)
Run Stages A→C; fill every Measured column in §5; tune; freeze presets v1; write honest
per-tier expectations into README.
**Exit:** §5 table complete for ≥4 tiers incl. one real ≤16 GB Mac; §11 checklist review.

---

### 8.1 Next — the ordered queue (post-0.1.5)

Ordering only, deliberately no day estimates. The ordering principle is **what decides
whether someone keeps using this after their first session**, which is not the same as
what completes the milestone map — see the deprioritized list at the end.


**Inference optimization:** [[records/plan/whole-engine-optimization-2026-09-04]] preserves the completed OPT00–OPT36 program. [[records/plan/n6-prefill-bound-the-pass-then-read-each-expert-once]] is its detailed prefill chapter. The new local learned-prediction experiment is [[records/plan/expert-lookahead-local-experiment-2026-09-10]]; it owns its separate implementation packages, resource bounds and acceptance criteria. Historical milestones and failed experiments remain available, and overlapping mechanisms are implemented once.

### N1 — Conversation prefix cache (KV + GDN state reuse across requests)

**The single largest win, and it was not in this plan until now.**
`Generator.generate` calls `model.makeState()` on every request, so a conversation
re-prefills its entire history every turn. At the measured 92 tok/s that is ~9 s of dead
air at turn 2 (~800 tokens), ~33 s at turn 5 (~3k), ~65 s at turn 10 (~6k) — by which
point prefill is ~72% of the wait for a 500-token reply, and every second of it is
recomputing tokens the previous turn already processed. Agentic and tool-loop use, many
short turns over a long identical prefix, is the worst case and is also the use that most
justifies a local model.

**Mechanism.** Hold the live state keyed by the token prefix that produced it. If a new
request's `promptIds` start with the cached prefix, prefill only the delta; otherwise
rebuild. Generation already leaves the state covering prompt + completion, which is
exactly the next turn's prefix.

**Architectural constraint.** The GDN recurrent state cannot be rewound to an arbitrary
prefix, only continued forward — so this is exact-prefix-extend or full rebuild, never
partial rewind. That covers the dominant chat/agentic case and falls back correctly when
the user edits earlier history or switches conversation. This is a real difference from a
pure-attention runner, where any prefix is a slice.

**Cost.** A held state is ~27 KiB/token (KV + indexer), so a 32k conversation pins
~0.9 GB. It must be charged in the memory plan (§5) and be the first thing the governor
sheds under pressure — ahead of shrinking the pool, on the same reasoning as the M9
ordering note.

**Exit (revised — the original was unachievable).** ✅ Turn-N latency independent of
conversation length for an extending prefix; ✅ state dropped under memory pressure.

**The byte-identity criterion written here first was wrong and had to be replaced.**
Reusing a state pushes the same tokens through the model in a different batching, MLX
selects kernels and reduction orders by shape, and floating point is not associative —
swept over a 64-token sequence, *all 63* split points give different logits from a single
pass. §6.1 is about the expert pool, where the bytes fetched are identical and the claim
holds; it does not extend to re-batching. The replacement gate is a **control**: prefix
reuse must not move logits more than re-chunking a plain prefill already does (measured
4.37% vs 5.90% of logit spread), must stay flat with depth, must be deterministic run to
run, and must actually be reusing something. Full data in MEASUREMENTS.md, including the
by-product finding that the existing "byte-identical at every prefill chunk size" result
is luckier than it reads.

### N2 — Dense-sweep prefill (the remainder of M5)

N1 cannot help the first turn, a pasted document, or a fresh RAG context; only a faster
pass can. Sizing the pass from the memory plan already took prefill 40 → 92 tok/s
(M7.6); the remaining work is the dense sweep proper per §3.3 — grouped staging, auto
threshold, scan-resistant admission — plus the cross-token prefetcher. This multiplies
with N1 rather than overlapping it: N1 removes repeated work, N2 speeds up the pass that
is genuinely unavoidable.

**Exit:** ≥150 tok/s @8k on the dev Mac; byte-identical output at every pass size (the
existing standing gate).

**Done 2026-09-02.** The sweep shipped as designed in §3.3 with the departures the
design note's addendum records, and the exit is met: the 8k acceptance prompt reads
**184 tok/s at a 16 GB target** against 91 on the same code base the day before
(three interleaved rounds), 222 at a 4096-token pass on a matched 60-per-layer pool,
247 at 4096 by override at 16 GB, and 152 by `context-check --tokens 8192` at 16 GB
against 64. The second half of the exit was written wrong and is replaced by the gate
that N1 already uses: output is inside the prefill-rechunk band at every pass size
(`sweep-check`: 3.3% of logit spread against a 5.1% control), bit-identical on a cold
and a warm pool, not byte-identical across pass sizes, which re-batching never was.
The cross-token prefetcher was not built; it is a decode lever, not a prefill one, and
stays with the deprioritized list. What N2 leaves open is in MEASUREMENTS.md, "N2 — the
prefill sweep": the auto plan on this Mac, the prose-versus-acceptance-prompt gap, the
serial router-and-attention time between groups, and the read rate against the SSD.

### N3 and N4 — removed from the queue (2026-08-30)

Both were dropped as work items. They are recorded here rather than deleted so
that neither gets re-proposed from first principles later; the measurements are
in MEASUREMENTS.md.

- **N3 (the 104 GB download).** Three levers, all closed. Hosting is not the
  bottleneck — Cloudflare R2 measured no faster than Hugging Face (42 to 51
  against 36 to 50 MB/s) on a link that does 134, and Hugging Face is free.
  Serving before the download completes fails on arithmetic, not engineering: a
  missing expert is a wrong answer rather than a slow one, so the fetch must
  block, and one token needs ~1.3 GB of experts — about 26 s per token until the
  download catches up, which is worse than the progress bar it would replace.
  That left a smaller build, which needs a billed re-quantization run and a
  quality gate to prove it cost nothing.
- **N4 (quality against the FP8 reference).** That gate needs an inference
  credential for Qwen3.8-Flash-Next FP8 — Qwen's own DashScope or an aggregator
  carrying it. None is provisioned and it is paid.
  `Tools/quality_probe.sh` remains in the battery: 15 checkable items covering
  recall, arithmetic, sorting, instruction obedience, translation and code. It
  catches gross quantization damage and gates any future re-quantization. It is
  **not** a comparison against FP8 and must never be described as one.

### N5 — Real GUI and editor clients

Open WebUI and one editor client (Zed or Continue via the OpenAI path) — the remaining
M6 exit criteria.

The claim that used to stand here — that the Ollama CLI and the OpenAI Python SDK were
both "proven end to end as of 0.1.5, so the wire protocol is not in doubt" — was wrong,
and it is exactly the mistake the claims discipline in `CLAUDE.md` is about: it was
asserted from this document rather than checked against the system. 0.1.8's strict
validator broke `ollama run` and `ollama show`, and no gate noticed for three releases,
because nothing here ran the real CLI. Both clients are now genuinely exercised —
`ollama list`, `ollama ps`, `ollama show`, one-shot and interactive `ollama run`, and
the OpenAI SDK's streamed and non-streamed paths — and the request shapes they send are
gated in `Tools/api_robustness.sh`. An editor client and Open WebUI remain untested.

### Unified whole-engine optimization program — 2026-09-04
**Draft-depth follow-up, September 11:** [[records/decisions/draft-depth-defaults-to-two]] adopts two drafts for new work. The default-change implementation is separate from the frozen OPT00–OPT36 program below: keep its explicit one-draft controls and historical identities unchanged. New default-path qualification must bind a fresh source identity and declare depth two rather than inheriting an old protocol.

**Public release follow-up, September 11:** v0.2.14 publishes the completed optimization work and is installed and serving locally. The exact successful CI archive, public provenance and installation are verified. All 25 original model gates and all 31 installed-release checks now pass. The final unchanged governor and long-prompt tests completed with zero swap activity after Carlos authorized temporarily closing Chrome and Wispr Flow. Every excluded earlier attempt is preserved and no assertion was relaxed. Normal serving and both apps were restored. Current release identity and complete evidence are in [[records/measurements/release-qualification-0-2-14]]. This closes public v0.2.14 acceptance without changing the completed OPT00–OPT36 dispositions or their original evidence. Later depth-two default changes and prospective Expert Lookahead work remain separate from this exact published artifact.

**Preserved optimization closure and source-qualified activation:**

**Final status, September 10: the approved OPT00–OPT36 optimization program is complete and the exact qualified candidate is active locally.** Every required execution gate is closed. All thirty-seven outcomes are accounted for; selected changes are implemented and tested, rejected candidates stay disabled, and explicitly prerequisite-dependent research remains deferred. This is completion of the approved program, not a claim that every possible future optimization has been implemented.

**Session record and later live-server benchmark.** The completed session's exact final closure is [[sources/runs/2026/09/2026-09-10-optimization-final-program-complete]]. The original work, all-item outcomes, accepted and rejected experiments, failed runs, resource/thermal scheduling corrections and activation history remain preserved below. The later three-prompt benchmark, every clean timing, explicit discarded attempts and observed-versus-estimated tokens/sec are now documented separately in [[records/measurements/user-server-throughput-2026-09-10]]. It does not change the original qualification or the conservative calibration decision.

The actual working source matches all 150 qualified source inputs. The installed executable SHA-256 is `9268e4b2a3371e78a71d493d7788559a06a22498e8061a89278c4918c6764673`; the installed Metal library also matches the qualified asset. Installed defaults match the final qualification, and a bounded generation check returns exactly `OK`. The previous installation remains available for rollback. Resource admission, cleanup, free model lock and unchanged Git index checks pass, with no owned model/compiler left running. Nothing is staged, committed, pushed or released by this activation: [[sources/runs/2026/09/2026-09-10-optimization-final-local-activation-and-all37-outcomes]].

Final reconciliation checks the complete candidate matrix, source and artifact identities, all thirty-seven historical dispositions, empirical calibration, public claims and generated documentation. Database validation has zero errors and one unchanged historical `LOG_UNKNOWN_KIND` warning at `log.md:124`; it is not reported as warning-free: [[sources/runs/2026/09/2026-09-10-optimization-final-preactivation-closure]]. The installed activation adds a final delivery check to those already-passed gates.

**All thirty-seven final outcomes.** The captured inventory retains the detailed source/evidence links and original historical dispositions. Deferred rows retain their explicit prerequisites; no required execution remains open.

| Item | Area | Final outcome |
| --- | --- | --- |
| OPT00 | measurement foundation | implemented and integrated |
| OPT01 | attribution and baselines | implemented |
| OPT02 | compact retained convolution/PLE windows | implemented |
| OPT03 | compact retained MTP row | implemented |
| OPT04 | tail-aware prefill scheduling | rejected accelerator, bounded fallback retained |
| OPT05 | skip unused final forward | implemented |
| OPT06 | sampler threshold and device draw | implemented |
| OPT07 | compact ngram rows and FIFO | implemented with rejected alternatives |
| OPT08 | allocation accounting and planner | implemented; conservative calibration retained |
| OPT09 | completed indexer blocks and compact raw history | rejected independent accelerator |
| OPT10 | bounded indexer masks/scoring | implemented bounds with rejected fusion |
| OPT11 | shared and fused partial RoPE | implemented with platform dispatch |
| OPT12 | expert placement/reduction workspace | implemented bounded scope workspace |
| OPT13 | bounded routed rows | implemented bounded scope workspace |
| OPT14 | bounded PLE and ngram lookahead | implemented bounds with rejected lookahead |
| OPT15 | selected D256 attention | rejected |
| OPT16 | layer-major read scope | implemented |
| OPT17 | router weight projection cache | rejected |
| OPT18 | compute fusion | implemented RoPE, other candidates rejected |
| OPT19 | shared/resident expert overlap | rejected |
| OPT20 | expert indexing and pin bookkeeping | implemented correctness, accelerators rejected |
| OPT21 | admission and floor residency | rejected |
| OPT22 | checked reads/layout/direct writes | implemented checked recovery, layout accelerators rejected |
| OPT23 | fused GDN recording | rejected |
| OPT24 | first MTP entry reuse and bounded draft tail | rejected accelerators, validity preserved |
| OPT25 | adaptive/speculative mode validity | implemented validity; adaptive policy rejected |
| OPT26 | parallel recurrence | deferred by measured bottleneck prerequisite |
| OPT27 | vision attention | implemented |
| OPT28 | image preparation/reuse/startup | implemented preparation accounting, reuse rejected |
| OPT29 | partial and complete prompt checkpoints | implemented |
| OPT30 | bounded output and responsive governor | implemented |
| OPT31 | continuous batching | deferred by demand prerequisite |
| OPT32 | integrated matrix | complete |
| OPT33 | planner/artifacts/delivery | complete; qualified candidate locally activated |
| OPT34 | broader alternatives | complete with explicit conditional/deferred outcomes |
| OPT35 | terminal prefill demand | implemented demand contract, pruning accelerators rejected |
| OPT36 | router/indexer selection | rejected, limited successor conditional |

Performance details and scope are in [[records/measurements/optimization-final-composition-2026-09-09]]. Reference controls remain through the specified post-adoption release window. No public release is performed here.

**Preserved preactivation and earlier execution history follows. Pending, live and unqualified wording below belongs to the stated checkpoint and is superseded by the completed status above. Prior failed attempts remain failed.**

**Current status, September 10: every required final candidate workload now qualifies; empirical calibration is closed. Exact local activation is next.** Both complete sustained cohorts pass the original acceptance rules, alongside all eight short paired studies, both sixty-request lifetimes, the full original verification battery, public consumer checks and isolated installed upgrade/rollback qualification. All 37 OPT00–OPT36 dispositions remain in scope, including rejected and prerequisite-dependent alternatives. Qualification does not mean every optimization improved every metric or that deferred hardware/aggregate-demand research has been implemented.

The final MTP-on cohort completes all thirty-two first responses and thirty-two measured responses, each with 512 outputs, for 32,768 outputs. Every paired prompt ID, output ID and text comparison is exact, including excluded rows. Twelve clean joint pairs remain; rounds 1, 2, 3 and 9 retain their original thermal/VM exclusions. No partial result is reused, replacement response added or acceptance threshold changed. Maximum sampled request process peak is 9,958,183,832 bytes under the fixed 12 GB target and 1,362 slots in both arms. Source/artifact identity, deadline and owned-process cleanup all pass: [[sources/runs/2026/09/2026-09-10-optimization-final-long-on-complete-pass]]. The actual bounded launch evidence is [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-thermal-recovery-guarded-launch]].

**The final sustained result is a speed/memory tradeoff.** Plain decoding is effectively flat: median paired active TPS improves 0.4560390212% with 6.5436095540% lower sampled process peak. Fixed-MTP active TPS is 3.6550887835% slower and request duration 3.5592440567% longer, with 3.9434511045% lower sampled process peak. This passes the original 5% request nonregression allowance; it is not an MTP speedup. No universal performance, energy, unbounded lifetime or new hardware claim follows.

The completed empirical decision retains all seventeen parameters of `m5-pro-reference-envelope-v1`, with zero new permanent expert-capacity credit and no prefill/decode speed multiplier. It reconciles both sustained cohorts, both lifetimes, the actual context allocation ledger, short resource envelopes and the separately qualified 192-response prefill prerequisites: [[sources/runs/2026/09/2026-09-10-optimization-final-empirical-calibration-decision]]. Source/application activation and the final installed smoke and record closure remain to be performed.

**Preserved earlier execution history follows. Any pending, live or unqualified status below describes its original checkpoint and is superseded by the current status above.**

**Current execution, September 10: continue the complete MTP-on qualification with prospectively tested recovery from excluded completed fair responses.** The user instructed continuation based on actual capacity; the prior quiet-window hold is superseded. V667 changes the benchmark control flow only. It explicitly adds `recover_completed_fair_thermal_requests=true`: a complete nominal-to-fair response remains excluded from timing, then may continue only through the unchanged bounded nominal-readiness check. Full work, exact parity, original twelve-GB cap, thirty-two cells, sixty-four 512-output responses, eight-clean-joint-pair minimum and nonregression criteria remain. Invalid or missing observations, low power mode, serious/critical thermal state, footprint violations, swap-outs and bounded readiness failures still stop.

All eighty-six model-free checks pass, including the full simulated driver, both recovery points, cleanup and the original joint assessor. A full cohort whose first responses are all thermally excluded still fails qualification. The initial test representation error and corrected exact expectations are retained. V668 performs actual bounded prelaunch readiness before the single frozen run; V669 and V670 preserve the original terminal reporting and calibration criteria with their new evidence locations rebound. No passed cohort is repeated or partial observation reused. No inference source or user installation has changed. Full MTP-on qualification, final empirical calibration, final all-item/source/artifact/claim/projection closure and exact local activation remain open: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-thermal-recovery-preparation]].

**Closed prior V653 attempt:** eight first responses and seven measured responses complete all 512 outputs and 511 intervals. The four available first-response pairs and three complete measured pairs retain exact prompt IDs, output IDs and text. The final reference first response completes in 111.1651487500003 seconds and reports nominal-to-fair thermal state; its measured response is never started. Maximum sampled request peak is 9,960,494,024 bytes, minimum sampled reclaimable memory is 14,449,229,824 bytes and no new swap-outs occur. Source/artifact proofs, reservation and cleanup pass with no remaining owned jobs. This is incomplete and unqualified, with no partial performance comparison: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-user-directed-thermal-stop]].

The prior driver treated this completed fair response as an immediate whole-study abort even though its existing idle readiness helper could already wait through fair. V667 corrects that control flow prospectively without changing the inference candidate or timing eligibility. Nominal observations still do not certify an unloaded machine, and host-load limits remain reportable.

**Preserved execution history follows; its former live statements are superseded by the current status above.**

**Current execution, September 10: the complete MTP-on repetition is running under the user's instruction to continue now.** The user-directed continuation supersedes the prior quiet-window availability hold. Actual V656 startup readiness observes 121.419873333 sampled nominal seconds across sixty observations, normal pressure and no competing model/compiler, then starts frozen V653 under deadline 2026-09-10T18:41:33.844074+00:00. The unchanged full workload, cooling schedule, 18 GB startup check, 12 GB sampled request cap, live resource/ownership guards, timing exclusions and original acceptance remain in force. No applications are closed and no prior partial results are reused: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-user-directed-guarded-launch]].

Only the closed readiness and launch evidence is captured at this point. The full cohort is live and has not qualified. Host load and power observations remain part of the existing serving evidence; nominal thermal state does not certify an unloaded machine. Final reporting must retain those environmental limits. V654 is the prepared terminal reporter and V655 the prepared empirical calibration closure for this exact cohort. Both remain unexecuted. Full sustained MTP-on qualification, final all-item/evidence closure and actual local source/binary activation remain required.

**Earlier preparation before user-directed continuation:** V653 is a separate, unlaunched repetition of the complete MTP-on protocol after V637's thermal stop. Its protocol, driver, helper, inference candidate, cooling schedule, workload and acceptance rules remain byte-identical. The runner's sole change is its output directory; reversing that change reconstructs V637 exactly. All seventy model-free checks pass for the fresh identity, and the original native/eight-paired prerequisite evidence is revalidated during freezing. The failed partial cohort stays separate and contributes no replacement observations: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-quiet-interval-preparation]].

V654/V655 preserve the original terminal-report and calibration criteria with only their new executor/report locations rebound. V656 preserves bounded prelaunch readiness and checks the immediately preceding failed cohort's cleanup. All three reverse-source and syntax checks pass; none of these scripts has executed. A user-confirmed quiet interval and actual bounded memory/thermal readiness are required before a fresh 12,600-second work plus sixty-second cleanup reservation. No deadline is granted and no model is launched. Full MTP-on qualification, empirical calibration, final all-item/evidence closure and actual local activation remain open; the other passed gates and all thirty-seven dispositions stay intact.

**Preserved prior execution, September 10: the distributed-cooling MTP-on cohort stopped and remains unqualified.** V637 terminates after 13 recorded cells: twelve measured responses and thirteen first responses, each with all 512 outputs. V649 independently verifies all 12,800 completed outputs, full prompt work, all 511 emission intervals per response, sampled request caps and exact prompt/output IDs and text across all six complete A/B pairs. Maximum sampled request peak is 9,910,768,536 bytes against the original 12 GB cap. Minimum sampled reclaimable memory is 15,214,362,624 bytes, maximum owned RSS is 4,945,199,104 bytes, and no new swap-outs occur. Exact source/artifact proofs, the reservation and owned-process cleanup pass, with no remaining owned jobs: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-distributed-thermal-stop]].

The stop occurs during round 7 reference's first response, which finishes its complete workload in 83.77691487499942 client seconds. The generator reports nominal before and fair after, with low power mode disabled and no generator swap-ins or swap-outs. The measured request in that cell is never started. The outer incomplete-cohort rejection preserves this actual resource failure; it does not qualify a partial comparison. Distributed cooling delivered twelve between-request pauses in the complete cells, but cannot guarantee a nominal thermal state throughout every continuous request. The reference-arm failure does not establish a candidate regression.

The next required condition is a quiet external machine interval before any fresh complete comparison. No automatic retry, replacement observations, partial-data pooling, shortened workload or relaxed acceptance is authorized by this failed result. V642/V643 remain unused preparations bound to the failed cohort and cannot close empirical calibration or authorize activation. All previously passed exact-build gates and all thirty-seven accepted/rejected/conditional/deferred dispositions remain intact. Full MTP-on sustained qualification, final empirical calibration, final disposition/artifact/claim/projection closure and actual local source/binary activation remain open. No model is left running and no source activation, installation, stage, commit, push or release has occurred.

**Earlier V637 preparation and launch, September 10:** V637 retains the same minimum 180 seconds idle per cell: sixty reserved seconds plus thirty sampled nominal seconds before the first request, then ninety sampled nominal seconds before the measured request. Both 512-output requests remain continuous. All original sixteen pairs, work and numerical checks, timing exclusions, hard resource/thermal stops, 12,600-second work allowance and sixty-second cleanup remain. This changes benchmark scheduling only; inference code and the candidate binary are unchanged.

All seventy model-free checks pass. The between-request pause keeps the already ready child alive with its native model reservation, checks child and lock status before and after observations, preserves the live reclaimable floor and outer memory guard, and stops before another cell if readiness fails. Incomplete raw assessment now reports the original stop before reading nonexistent later responses. Explicit reverse transformations reconstruct the prior runner, serving driver and helper exactly. The failed V627 evidence stays failed and supplies no replacement rows. V638 observes 121.28519829199999 sampled nominal seconds across sixty actual observations with normal pressure and no competing model/compiler, then starts the frozen full cohort under deadline 2026-09-10T16:58:48.859112+00:00. That cohort subsequently stopped and did not qualify, as recorded above. Source and frozen preparation: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-distributed-cooling-preparation]].

**Completed delivery evidence reconciled:** V641 rechecks 194 required artifacts against their exact earlier captures, along with current bound drivers, all 150 external-consumer source inputs, candidate binary, Metal library and installer asset. The full build, metadata, static, consumer, resource, four real client, 25/0 verification and 31/0 installed upgrade/rollback results remain valid. The packaged candidate uses the exact activation key exercised by the isolated installer. V642/V643 preserve the original terminal-report and calibration criteria, with only their current MTP-on executor/report paths rebound; both remain unexecuted. These read-only checks do not close MTP-on qualification or activate the user installation. Exact evidence and the two corrected audit path-resolution attempts: [[sources/runs/2026/09/2026-09-10-optimization-final-distributed-launch-and-delivery-reconciliation]].


**Current all-37 disposition reconciliation:** V646 preserves all thirty-seven original dispositions and their complete history, records completed MTP-off sustained generation and the exact delivery audit, and keeps full MTP-on, empirical calibration, final artifact/claim/projection closure and actual local activation open. All 149 existing shared preimages still match; the exact patch passes git apply --check, and the installed binary remains the prior version. OPT26's phase-share evidence is explicitly limited to its original fixed-forward probes; it is not asserted as a current automatic-scope critical-path breakdown or scaled by unmatched prefill gains. The existing conditional attribution/numerical/quality/serving prerequisites remain. No rejected or conditional accelerator is promoted: [[sources/runs/2026/09/2026-09-10-optimization-final-all37-delivery-and-activation-reconciliation]].

**Previous failed cohort, preserved:** V627 finishes two first and two measured responses, all with 512 outputs. The completed pair retains exact prompt IDs, output IDs and text in both phases. Maximum sampled request peak is 9,899,021,496 bytes, below the original 12 GB cap. The combined measured request changes from nominal to fair and records eight global swap-ins; the reference measured request records sixteen swap-ins. No new swap-outs occur. Source proofs, deadline and cleanup pass, with no remaining owned jobs. The original thermal guard stops the cohort; the outer missing next warmup is secondary. No partial performance comparison or replacement evidence is accepted: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-cooled-thermal-stop]].

The required 60-second cooldown and at least 120 sampled nominal seconds were delivered before each cell. The first and measured 512-output requests nevertheless run back to back, totaling 187.00727912500008 client seconds for the combined cell. Before-cell cooling was insufficient on this attempt. This does not isolate a code regression or prove a remedy. Next qualify bounded cooling between the two complete requests prospectively, preserving every output, original inference behavior, paired criterion and hard resource rejection. At that checkpoint no successor cohort had been frozen or launched. V630/V631 remain unexecuted report preparations attached to a failed cohort and cannot close calibration or authorize activation. Passed gates remain passed; final MTP-on sustained qualification, calibration, complete disposition reconciliation and actual local activation remain open.

**Earlier preparation, preserved after the later V627 stop:** V616 completes seven first and seven measured 512-output responses before round four combined changes from nominal to fair. All 7,168 completed outputs pass full-work checks, all three completed pairs retain exact first/measured IDs and text, and all sampled peaks remain below 12 GB. Maximum sampled peak is 9,912,832,968 bytes, minimum outer reclaimable memory is 20,621,852,672 bytes, and no new swap-outs occur. The thermal exclusion is the actual stop; the outer missing later warmup is secondary. Source proofs and cleanup pass. This partial identity remains failed and provides no sustained comparison: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-long-thermal-stop]].

V627 preserves every original request, paired exclusion, numerical requirement, memory/pressure/swap-out/thermal stop and full 12,600-second work allowance plus 60-second cleanup. The fresh protocol uses a 60-second reserved cooldown followed by 120 sampled nominal seconds before each model launch, with the original 600-second readiness bound. V624's unattempted freeze is preserved after prelaunch review catches an evidence-count error: a real 121.36-second stable interval has 60 samples because polling takes time. The corrected minimum follows the unchanged maximum four-second observation gap, and all 60 model-free checks pass, including that real observation. Driver and inference behavior are unchanged; nominal state still does not guarantee physical equilibrium. V628 passes 121.41 sampled nominal seconds with normal pressure and no competing model/compiler, then starts the fresh full cohort. V630/V631 bind the unexecuted terminal and calibration reports to this exact new identity: [[sources/runs/2026/09/2026-09-10-optimization-final-cooled-launch-and-report-binding]]. Passed MTP-off is not repeated; no failed row is pooled or replaced: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-cooled-cohort-preparation]].

**Prefill and final-report reconciliation:** All 192 responses in the three already qualified automatic-prefill profiles are independently rechecked for full work, exact paired tokens/text, numerical geometry and sampled caps. Original descriptive percentages reproduce exactly: preview reductions are 25.65%, 20.91% and 17.82% at the 10/12/16 GB profiles. The two smaller profiles use more temporary memory. These remain prerequisite mechanism results, separate from the exact final short-request and sustained cohorts. V621's final calibration-report preparation and exact 150-input activation preimages are preserved, but its unused V616 terminal dependency must follow the new cohort before execution: [[sources/runs/2026/09/2026-09-10-optimization-final-prefill-reconciliation-and-calibration-preparation]].

**Current execution, September 10: the complete final MTP-off sustained cohort passes.** All 32 first and 32 measured responses complete 512 outputs, with exact paired prompt/output IDs and text. Thirteen clean pairs remain after swap-in exclusions in rounds 4, 12 and 15. Median paired active TPS improves 0.46%, later-output TPS 0.34%, and sampled process peak falls 6.54%; timing is effectively flat, not a material sustained-speed gain. The original assessor, source proofs, deadline and cleanup pass. V607's overly strict descriptive extractor incorrectly required child exit 0; V608 follows the original driver's excluded-row exit semantics and independently reproduces the qualified result. Full evidence: [[sources/runs/2026/09/2026-09-10-optimization-final-long-off-complete-pass]].

**Current MTP-on method: full work continues, while startup swap-ins exclude the entire pair.** V581's initial MTP-on attempt refused its incompatible large-memory delivery profile before any model launch: [[sources/runs/2026/09/2026-09-10-optimization-final-long-on-delivery-refusal]]. V609 corrected delivery with 18 GB startup headroom and the unchanged 12 GB process cap, then stopped after eight global swap-in pages during the combined first response. Both first responses completed all 512 outputs with exact paired IDs/text; there were no swap-outs, minimum guarded reclaimable memory was 22.44 GB, and first-response sampled peak stayed below 9.88 GB. This partial identity remains failed and supplies no performance comparison: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-long-startup-swapin-stop]].

V616 addresses that concrete method failure prospectively for only the exact original frozen MTP-on protocol. Startup swap-ins leave startup eligibility false and exclude the whole A/B pair through the unchanged original assessor, while the fixed workload continues. Swap-outs, invalid/reset counters, memory pressure, footprint violations and output/parity failures retain their stops; no replacement or partial-data reuse is allowed. The 18 GB startup requirement, 12 GB process cap, all 64 full 512-output responses, original minimum-pair/nonregression checks and full time allowance remain. All 55 model-free checks pass, including a complete synthetic cohort proving pair exclusion and mandatory numerical correctness even inside an excluded pair. That V616 cohort later stops on a fair thermal observation and remains failed; MTP-off is not rerun. Prepared evidence: [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-long-delivery-correction]], [[sources/runs/2026/09/2026-09-10-optimization-final-mtp-long-startup-exclusion-preparation]].

**Remaining completion work:** qualify the entire corrected MTP-on sustained cohort, make the final empirical estimator/envelope decision, close all 37 dispositions and final measurement/claim/projection/source/dependency/weight checks, then activate only the exact qualified source and local binary. All previously passed exact-build gates remain available. No source activation, installation, stage, commit, push or release has occurred.

**Previous execution checkpoint, September 9: both original final lifetimes pass, alongside all eight paired studies, complete verification and installed compatibility.** Exact final V519 retains passing build, 25,360 pure assertions, 116 CLI checks, seven native cases (5,556 assertions), static/public-consumer delivery, seven resource checks, four client cases, all eight paired studies, all 25 original full-verification checks and all 31 installed checks plus five install/upgrade/E2E/rollback phases. Both original 60-request lifetimes now pass. V583 has frozen the MTP-off sustained study and is proceeding through its original readiness and serial long-generation queue. Both full sustained cohorts, empirical calibration and final all-37 measurement/claim/projection/artifact closure still precede exact local activation. The real user installation and shared source remain unchanged.

**V604–V606 measurement and all-item reconciliation:** [[records/measurements/optimization-final-composition-2026-09-09]] now consolidates the exact final short-request, bounded-lifetime and full/installed verification results, with actual target/pool labels, separate preview/memory scope and explicit sustained/calibration/activation limits. V605 independently recomputes each original eligible paired request ratio and reports observed extrema and faster-pair counts. Complete-repeat and distinct-tail improve every one of their sixteen pairs; fixed short MTP is slower in fifteen of sixteen despite its qualified memory saving. The original three-pair serving A/A pilot has a 0.86% median apparent reduction and an 8.93% maximum, illustrating variability without defining this final build's noise distribution. No p95, significance, rescoring or changed threshold is claimed. Evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-observed-spreads-and-original-aa-context]]. The compact V606 inventory contains all thirty-seven original IDs exactly once, replaces stale current remaining-lifetime statements, and retains the complete earlier inventory and item history through its captured source/digest. It distinguishes selected implementation, unconditional fixes, rejected accelerators and prerequisite-based deferral. Actual OPT35 source preserves final last-logit/last-row demand without promoting rejected intermediate/terminal pruning; speculative head work still retains full multis. Both original sustained cohorts, the empirical family decision, final artifact/claim checks and exact activation remain open: [[sources/runs/2026/09/2026-09-09-optimization-current-all37-source-dispositions]].

**V603 calibration input audit and profile-label correction:** Exact final PlannerCostModel constants are byte-identical to the previously verified 441-case family. Removing only the three device-observation functions and imports reconstructs the final pure Plan.swift policy exactly; their bodies remain unchanged in PlannerDevice.swift, and all files match the qualified final public consumer. This is source continuity, not a new 441-case V519 execution. Across all eight final paired cohorts, all 512 first/measured responses, including timing exclusions, meet their original output counts and sampled physical caps. Actual commands confirm 8.1 GB and 827 fixed slots for short-one, unique-prose and sampled-short; the two prose/sampled paragraphs below incorrectly said 10 GB and are corrected here. MTP-resource uses 10 GB/838 slots; the four retention/default cohorts use 8.1 GB/640 slots. Original protocols, outputs, acceptance and percentage calculations were correct and remain unchanged, and old source records plus the pre-correction body are preserved. The actual final 2,048-token context rung samples 8,536,952,072 bytes against an 8,997,561,600-byte expected ledger at a 10 GB target, leaving 460,609,528 bytes below that estimate. This one rung supplies no larger-context or sustained-throughput proof. Scoped prefill gains cannot uniformly multiply a coupled prompt/decode estimator, and transient vision savings cannot become permanent expert residency without coexistence accounting. Both full sustained cohorts and an explicit empirical family decision remain required. Exact source/row continuity, command profiles, memory-envelope calculations and correction: [[sources/runs/2026/09/2026-09-09-optimization-final-calibration-inputs-and-profile-label-correction]].

**V599/V600 final lifetime results:** MTP off completes all sixty original requests in 378.573092583 seconds at 10 GB; maximum sampled request peak is 9,037,173,152 bytes. Fifty-nine requests are resource-clean. Measured cycle 2 prose has eight swap-ins and zero swap-outs in both client and generator intervals, so that observation remains excluded from clean growth calculations without replacement. All output/work and absolute physical-cap checks still pass. Prose retains seven clean measured cycles and every other position eight, satisfying the original minimum-five and early/late requirements. MTP on completes all sixty requests in 415.03290825000005 seconds at 12 GB; all sixty are resource-clean and maximum sampled peak is 10,828,060,144 bytes. In both modes, all six positions pass exact replay, 64 MiB active-growth and 256 MiB physical-end-growth bounds, bounded embedding rows and charged prefix capacity. The original executor and independently recomputed unchanged assessor agree. Source/protocol/driver identities, full reservations and cleanup pass with no remaining owned jobs. These are bounded repeated-request lifetime results, with no unbounded stability or sustained TPS claim. Exact raw requests, resource samples, assessments and receipts reconstruct from [[sources/runs/2026/09/2026-09-09-optimization-final-lifetime-off-pass]] and [[sources/runs/2026/09/2026-09-09-optimization-final-lifetime-on-pass]].

**V601/V602 control and disposition review:** Exact source review confirms the fourteen selected integration assignments, explicit public reference initializer and fused-rotation default dispatch restricted to native ARM64, Mac17,9, Apple M5 Pro and OS build 25G83. Other platforms retain the original shared-angle/reference fallback; explicit diagnostic controls remain available without a new hardware claim. Rejected bounded MTP tail, adaptive speculation, image reuse and other prototypes are absent from the selected family. Automatic read grouping still selects its bounded prerequisites per eligible request; base false fields do not mean their code is unreachable. The original plan retains reference switches through qualification and one release after adoption. No such release has occurred, so their retention now is intentional and preserves public Codable/diagnostic compatibility. No candidate source or numerical golden changes. The refreshed inventory preserves OPT00 through OPT36 exactly once, all rejected/conditional/deferred outcomes and prior history, and names the remaining sustained/calibration/artifact/activation gates. Evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-control-and-fallback-review]] and [[sources/runs/2026/09/2026-09-09-optimization-final-lifetime-disposition-checkpoint]].

**V570/V583 actual lifetime handoff:** Actual preparation succeeds and binds both unchanged 60-request lifetimes to all eight completed paired studies, all seven native proofs, exact V519 and the existing public-consumer planner proof. The original off/on 10/12 GB profiles, empty-environment defaults, all requests, source/driver/acceptance and full allowances remain intact. V583 passes its initial readiness check and starts the original MTP-off lifetime. The queue then runs MTP on and both full sustained studies serially. Only closed preparation and the completed preparation-stage receipt are captured here; active measurement files remain live until each stage closes: [[sources/runs/2026/09/2026-09-09-optimization-final-lifetime-actual-binding]]. No lifetime result, sustained-TPS gain or activation is claimed at this checkpoint.

**V585/V597 exact installed qualification:** The actual public installer installs the preserved prior release into an isolated root, upgrades it to exact V519, passes all 31 original installed CLI/Ollama/OpenAI/stream/unicode/long/prefix/concurrency/disconnect checks, restores the prior release through the same installer, and proves rollback with actual generation. All five phases pass in 83.706180625 seconds. Exact candidate, driver, original prior-release and native/eight-paired prerequisite identities remain unchanged; the original 3600-second work allowance, 30-second cleanup, 13 GB startup/owned guard and 3 GB live minimum are retained. Cleanup frees the model lock with no remaining jobs and stays inside the reservation. The real user-installed binary still has its original SHA-256, so this is isolated installation qualification rather than final activation. All raw records, scripts, resource samples and archive checksums reconstruct exactly; large installed binary/Metal/archive copies are represented by their verified content identities. The refreshed V596 inventory keeps all 37 IDs, makes current paired/full/installed status coherent and preserves prior wording as history. Exact evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-installed-qualification-pass]]. The earlier installed V304 result stays scoped to V304; this new result proves V519. Both original lifetimes, both sustained studies, empirical calibration and exact activation remain.

**V587/V595 complete final verification:** The actual unchanged Tools/verify.sh battery finishes on exact V519 with 25 top-level passes, zero failures and zero skips in 1030.115082792 seconds. Every pinned weight file verifies. Independent n-gram, template, layer, MTP-head and float32 vision parity pass, as do planner and sampler gates, cross-cache output equality, elastic resizing and the full governor drill, prefix/sweep invariants, full MTP/image state checks, short/long/context memory without swapping, long-context recall, all 15 behavioral quality items, symlink loading, all 74 API robustness probes and the original full photographs through serving APIs. The original full work/capacity and 3600-second guard remain intact. Candidate, drivers and all native/eight-paired prerequisite identities are unchanged; cleanup leaves no remaining jobs and frees the model lock inside the reservation. The raw pixel and embedding arrays, original full stdout, legacy outputs, resource samples and exact source/provenance are captured and reconstruct byte-for-byte: [[sources/runs/2026/09/2026-09-09-optimization-final-complete-verification-pass]]. This closes the previously missing full final-build battery obligation; it supplies no new performance estimate or installed/lifetime/sustained qualification. The old failed V307 full run remains failed on its old build.

**V594 terminal final paired matrix:** Independent recomputation equals V573's terminal state: all eight original paired cohorts pass. The original passed short-one execution is reused exactly once; the failed prior unique-prose identity stays separate and contributes no replacement pairs. Actual-default one-token completes 32 measured and 32 first responses, all original one-output counts and paired prompt/output IDs/text, with 16 clean measured and 16 clean startup pairs. At the original 8.1 GB target and fixed 640-slot pool, MTP off, 440 measured prompt tokens and zero reused prefix tokens, median paired request duration is 2.98259360% lower and sampled process peak is 7.08682647% lower. Median paired sampled saving is 483,508,500 bytes and active saving 235,085,872 bytes. First-job duration is 9.07832337% lower. All original work/resource/source/reservation checks and cleanup pass in 2405.166757584 seconds. The combined arm has no experimental enable flags. Every measured one-token output is whitespace, so visible-preview latency and active TPS are not applicable. The V591 reporting attempt incorrectly required positive visible-text latency; its failure is preserved. V593 changes only descriptive handling of missing visibility, retains the original raw assessment and full output checks, and reconstructs V582 exactly when its recorded edits are reversed. No model is rerun and no missing metric is replaced with zero or first-token time. These comparisons use reference paths and selected optimized defaults on the same exact build. Exact terminal state, raw cohort, corrected report and original reporting failure: [[sources/runs/2026/09/2026-09-09-optimization-final-actual-default-and-eight-paired-complete-pass]].

**Historical execution order before V587/V585:** Run the already prepared complete original V587 verification battery and V585 isolated installed upgrade/E2E/rollback checks before the more expensive V583 lifetime/long queue. This checks final correctness and compatibility before sustained performance measurement without changing any frozen workload, acceptance or resource limit. Both full suites, both lifetimes, both long studies, empirical calibration and exact activation remain unrun at this checkpoint.

**V588 final retention-only result:** All 32 measured and 32 first responses complete the original 16-output workload with exact paired prompt/output IDs and wire text. All 16 measured and 16 startup pairs qualify without exclusions. Both arms use the original 8.1 GB target, 640 pool slots, MTP off and 440 measured prompt tokens with zero reused tokens. Median paired request duration is 0.83512491% lower, first visible text is 0.20651314% later, and sampled process peak is 4.99989718% lower. Median paired sampled saving is 341,008,600 bytes and active saving 93,871,040 bytes. First-job duration is 2.71202939% lower. The original memory/nonregression criteria, source/build identities, full reservation and cleanup pass in 2564.057827667 seconds. This is scoped retention-cost qualification, not a sustained-TPS or positive-preview claim. Independent original reassessment and exact persisted artifact recovery pass: [[sources/runs/2026/09/2026-09-09-optimization-final-retention-complete-pass]]. Lightweight source inspection and preparation occurred during this cohort; no second model or compiler launched. Original eligibility rules remain unchanged and do not prove absence of all host background activity.

**V585/V587/V589 final coverage audit:** Review of the actual verification script and executed receipts confirms that the seven final resource checks are a subset, not the whole battery. The older full V307 attempt failed on V304 without a final summary; the passed 31-check installed qualification also belongs to V304. Neither proves complete V519 qualification. V587 now preserves the actual unchanged complete Tools/verify.sh command on V519, requiring all 25 top-level checks and no skips, including independent parity, quality, API and full vision checks. V585 preserves all 31 installed checks and five isolated install/upgrade/E2E/rollback phases on V519. Both retain original 3600-second work ceilings plus 30-second cleanup, capacities, resource checks and cleanup, and require all eight final paired proofs and all seven native proofs before and after execution. Syntax and exact reversible-source binding pass; both remain unrun. These close existing OPT32/33 requirements rather than substituting targeted proofs. The refreshed all-37 inventory retains old dispositions and all remaining work. Exact preparation and inventory: [[sources/runs/2026/09/2026-09-09-optimization-final-complete-verification-and-installed-preparation]].

**V584 Complete repeated prompt:** All 32 measured and 32 first responses retain the original 16-output count, exact paired prompt/output IDs and text, original work and resource/capacity checks. All 16 measured and 16 startup pairs qualify without exclusions. Median paired request duration is 70.96% lower, first visible text 96.48% earlier, and sampled process peak 5.73% lower. The original workload uses 440 measured prompt tokens, a 8.1 GB target, MTP off, and 440 reused prompt tokens in the combined arm. Original source/provenance, full reservation and cleanup pass; independent raw reassessment matches the outer receipt and saved summary. These short workload results do not establish sustained TPS. Exact closed evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-complete-repeat-complete-pass]].

**V584 Edited suffix with shared prefix:** All 32 measured and 32 first responses retain the original 16-output count, exact paired prompt/output IDs and text, original work and resource/capacity checks. All 16 measured and 16 startup pairs qualify without exclusions. Median paired request duration is 31.52% lower, first visible text 40.61% earlier, and sampled process peak 1.59% lower. The original workload uses 445 measured prompt tokens, a 8.1 GB target, MTP off, and 256 reused prompt tokens in the combined arm. Original source/provenance, full reservation and cleanup pass; independent raw reassessment matches the outer receipt and saved summary. These short workload results do not establish sustained TPS. Exact closed evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-distinct-tail-complete-pass]].

**V584 Fixed-MTP resource cohort:** All 32 measured and 32 first responses retain the original 16-output count, exact paired prompt/output IDs and text, original work and resource/capacity checks. All 16 measured and 16 startup pairs qualify without exclusions. Median paired request duration is 2.00% higher, first visible text 0.35% later, and sampled process peak 4.82% lower. The original workload uses 17 measured prompt tokens, a 10 GB target, MTP on, and 0 reused prompt tokens in the combined arm. Original source/provenance, full reservation and cleanup pass; independent raw reassessment matches the outer receipt and saved summary. The 2.00% request regression stays inside the original nonregression limit; this is a memory-benefit result, not a measured speed gain. These short workload results do not establish sustained TPS. Exact closed evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-mtp-resource-complete-pass]].

**V581/V583 prospective long-study scheduling:** The user requested faster validation and a first-principles safety review. The final choice preserves all eight already-frozen paired protocols and both original 60-request lifetimes. Only the two unfrozen long studies replace fixed 60-second cell sleeps with 30 seconds of sampled nominal thermal state, a 600-second maximum and 2-second polls. All original requests, 16 pairs, 32 measured plus 32 first 512-output responses per mode, exact output/work checks, startup swap-in rule, resource guards, eight-clean-pair minimum, nonregression criteria and 12,600+60-second bounds remain. The isolated driver adds actual helper hashes to raw evidence. All 43 checks pass: 20 unchanged runner, four final-prerequisite, 13 thermal and six schedule checks. An initial synthetic refusal-precedence failure is preserved and corrected without changing the original tests. The ideal idle saving is 32 minutes across both long studies; no actual saving or inference gain is measured yet, and nominal state does not prove constant clocks or thermal equilibrium. V583 prepares the serial lifetime/long handoff using original executors, cleanup and full allowances, with actual 13/15 GB startup requirements. Neither lifetime, long mode nor this queue has started. V571 remains an unrun prior preparation; V581 is the chosen prospective long executor. Exact preparation: [[sources/runs/2026/09/2026-09-09-optimization-prospective-long-adaptive-scheduling]].

During complete-repeat, lightweight source preparation and model-free scheduler checks took place in observed between-cell cooldown windows. No second model or compiler launched. The frozen workload, rest, thermal/VM exclusions and acceptance remain unchanged; qualification proves the declared observations rather than an absence of all host background activity.

**V580 complete final sampled pass:** All 32 measured and 32 first responses complete in 2,363.2679885840002 seconds. Every response retains the original 16-output count, paired prompt/output IDs, wire text and resource/capacity checks. Fifteen measured pairs qualify; round 9 retains its swap-in exclusion. Thirteen startup pairs qualify; rounds 3, 8 and 9 remain excluded. The original request, memory and startup criteria pass with exact source/provenance, full allowance and complete cleanup. Independent original reassessment and saved-summary equality pass. Median paired request duration is 8.063280820932661% lower, first visible text 0.2367983750575453% earlier, and sampled process peak 6.150647968349587% lower. Median paired sampled saving is 368,754,976 bytes and retained active saving 357,657,600 bytes. Median first-job reduction is 5.202113876513248%. These results apply to the original sampled fixture with 17 measured prompt tokens, 16 outputs at an 8.1 GB target and fixed 827-slot pool. They do not isolate the sampler contribution or establish sustained TPS. Exact closed evidence and independent reporter: [[sources/runs/2026/09/2026-09-09-optimization-final-sampled-complete-pass]]. V573 is checking readiness before its original full MTP cohort.

**V579 complete final prose pass:** All 32 measured and 32 first responses complete in 2,680.1101424579997 seconds. Every response retains the original 16-output count, paired prompt/output IDs, wire text and resource/capacity checks. Fourteen measured pairs qualify; rounds 5 and 6 retain swap-in exclusions. Thirteen startup pairs qualify; rounds 4, 5 and 6 remain excluded. All original request, memory and startup criteria pass with exact source/provenance, full allowance and complete cleanup. Independent original reassessment and saved-summary equality pass. Median paired request duration is 2.3935864616893285% lower, first visible text 0.379463549097514% earlier, and sampled process peak 7.465551371665518% lower. Median paired sampled saving is 544,497,952 bytes and retained active saving 357,641,216 bytes. Median first-job reduction is 2.2507005578685457%. These results apply to the original 440-token prose prompt, 16 outputs at an 8.1 GB target and fixed 827-slot pool. They do not establish sustained TPS or the larger-prompt automatic scope benefit. Exact closed evidence and independent reporter: [[sources/runs/2026/09/2026-09-09-optimization-final-prose-complete-pass]].

**V578 full resumption:** The unchanged V573 coordinator starts the V569 continuation, preserving all original full reservations and workload/acceptance controls. Its prior passed short-one proof remains byte-bound to the single original execution; the failed V525 unique-prose identity stays separate. Independent evidence processing pauses during each timed cohort. Exact initial binding and actual readiness observation: [[sources/runs/2026/09/2026-09-09-optimization-final-paired-resumption]]. All remaining final stages are still required; this running checkpoint is not a result or activation claim.

**V576 third-turn thermal impasse:** At 21:02:39 UTC, actual Foundation state remains fair with low power mode off, 27,337,031,680 reclaimable bytes, normal memory pressure, unchanged swap-outs and no competing model/build work. Model ownership is free and V573 has never launched. The previous goal turn preserved evidence but made no benchmark execution progress; there is no live process to await. Independent continuation, lifetime, sustained-throughput and exact activation preparation is complete. The remaining empirical gates cannot be replaced by more model-free preparation. The same blocker has persisted for three consecutive goal turns, so the full objective is now marked blocked. Resume after a verified material thermal change, retaining all workloads, thresholds, failed identities, exact source proofs and all 37 dispositions. No app is closed and no source, binary or default is activated. Exact audit: [[sources/runs/2026/09/2026-09-09-optimization-final-thermal-impasse-audit]].

**V575 readiness result:** The unchanged observer terminates after 600.03108375 seconds without launching a model. All 295 samples report fair thermal state, normal memory pressure and no competing model/build work. Minimum reclaimable memory is 29,068,656,640 bytes; swap-outs remain unchanged and four swap-ins occur. The fresh 21:01 UTC observation remains fair, with the model lock free. Candidate, installed binary, shared release and unapplied activation patch hashes remain exact. V573 is still unrun. The next dependent action is the prepared seven-stage continuation after a valid quiet thermal window; no passed study is repeated and no excluded observation is rescored. Exact closed observer and independent audit: [[sources/runs/2026/09/2026-09-09-optimization-final-readiness-thermal-timeout]].

**V568 stopped cohort:** V525 unique-prose completes 26 measured plus 26 first requests. All 52 completed responses emit the original 16 tokens, preserve paired prompt/output IDs and wire text, and satisfy the original sampled physical/RSS caps; the largest observed sampled peak is 7,397,314,640 bytes. Only three measured cells are timing-eligible. Nineteen rows have thermal-only exclusions, one has swap-only exclusion and three have both. Contaminated rounds 2–13 leave at most four clean pairs even if all future cells pass, below the required five. The owned coordinator is stopped at this mathematical impossibility; original cleanup drains all model processes, preserves the failed 2,275.273204333-second outer receipt and stays inside the full reservation. No missing completion file or unfinished request is invented. Exact failed evidence and independent completed-response audit: [[sources/runs/2026/09/2026-09-09-optimization-final-unique-prose-thermal-stop]].

**V569–V573 remaining work is concrete:** A fresh continuation preserves the one passed short-one execution byte-for-byte, with raw assessment, original outer receipt and source/provenance verification. Eight reuse/tamper/refusal checks pass and every original paired workload, control, acceptance, rest and time allowance is unchanged; only seven unrun stages remain. V570's original two 60-request lifetimes retain all twelve checks. V571's original two 512-output, 16-pair sustained-TPS studies retain all twenty prior checks plus four full-final-prerequisite checks, the original 10/12 GB caps and 12,600+60-second bounds. No V545 startup-method change is silently adopted. Neither lifetime nor long mode is frozen or executed. V573's serial continuation is prepared and unrun. Source/check/continuation evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-continuation-and-remaining-delivery-preparation]].

**V572 exact activation review:** All 150 archived source inputs match final V519; 134 already match shared source and exactly 16 deployment paths differ. The new patch passes the real workspace `git apply --check --whitespace=error-all` and has SHA-256 `45ec2fa8ac234f047d22d67f70c04e69c467cdc9ed60feaa76bde17ac8a8af30`. Shared source is unchanged. This supersedes the stale V376 patch without applying code, installing a binary or bypassing remaining gates: [[sources/runs/2026/09/2026-09-09-optimization-exact-final-activation-preparation]].

**V574 inventory/readiness:** All 37 IDs remain, with old item-level status retained as history and current evidence refreshed. The unchanged bounded observer is prepared to check 120 sampled nominal seconds within a 600-second ceiling, with original memory/VM/pressure/job checks and no model launch: [[sources/runs/2026/09/2026-09-09-optimization-final-dispositions-and-readiness-checkpoint]]. A nominal snapshot does not establish future timing conditions. Earlier execution checkpoints below preserve their timestamped wording; the current entry controls their status, while the original OPT00–OPT36 requirements and acceptance gates remain in force.

**V567 first final paired result:** The exact final composition completes original paired/short-one in 2,230.091796209 seconds. All 32 measured requests are valid, yielding all 16 measured pairs with exact output IDs/text and no measured exclusions. Median paired client duration is 11.276750885290265% lower. The separate startup gate passes with 15 clean pairs; round 3 stays excluded under its original rule, with 8.442020957959684% median first-job reduction. Exact source/native/build bindings, original full work, cleanup and reservation pass with no remaining owned jobs. This one-token workload does not establish active or sustained TPS. Closed artifacts are captured between cohorts before the next model launch: [[sources/runs/2026/09/2026-09-09-optimization-final-short-one-complete-pass]]. The serial queue next runs unique-prose; seven final paired studies remain, plus both lifetimes, both sustained-TPS studies, calibration and exact activation.

**V565–V566 final client and paired execution:** All four original V564 client cases independently pass in 623.1600090000001 seconds: CLI OK with no tool use, forced local-model compression plus retained-marker follow-up, Hermes vision discovery/tool/follow-up and actual Ollama image response. All Hermes cases report zero blocked nonlocal connections at the original 65,536 context setting. Compression completes with stop and reduces 99,684 to 87,014 characters. Exact final candidate, pinned dependencies and root drivers, original full reservation, normal pressure and owned cleanup pass. The explicit absent scanner path is isolated to this fixture; scanner coverage is not claimed. V562 remains failed. Evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-offline-client-full-pass]].

All eight original final paired studies are now queued through unchanged V525 execution. Sixteen pairs retain 32 measured and 32 first responses per workload, original output/work/ID/geometry/resource rules, fixed timing criteria, full original reservations and exact source/native proofs. A source-only serial coordinator adds no acceptance rule and stops on any failure; existing bounded thermal settling runs before each cohort, and independent evidence processing pauses during inference. No paired stage had run at freeze: [[sources/runs/2026/09/2026-09-09-optimization-final-eight-paired-serial-preparation]]. Both original final lifetimes, both sustained-TPS studies, empirical calibration and exact activation remain after the paired matrix. All 37 items remain retained; no final-composition speedup or program completion is claimed yet.

**V564 continuation:** The complete V562 resource suite passes all seven original assertions in 351.287725208 seconds, with zero skips/failures, exact source/candidate, normal pressure, full reservation and no remaining owned jobs. Full original image/MTP memory validates: sampled peak 10,439,923,112 bytes and lifetime RSS 5,904,629,760 bytes under the 12 GB target, with both swap counters unchanged during that request. The earlier V557 resource failure remains failed; no assertion was weakened or partial results pooled. Exact original raw artifacts and independently checked fresh legacy files: [[sources/runs/2026/09/2026-09-09-optimization-final-resource-pass-and-client-installer-stop]].

The following V562 client attempt fails after 34.876236917 seconds despite CLI exit 0 and the exact OK answer: its unchanged zero-outbound-attempt assertion observes one blocked nonlocal connection. The remaining compression/image/Ollama cases never run in that identity. The pinned Hermes Tirith installer independently reproduces a GitHub release request with no model and rejection before DNS. Its supported explicit scanner path suppresses auto-install when that scanner is absent, preserving its already-unavailable scanner fallback; scanner security coverage is not claimed. V564 binds that setting only into the isolated full client test subprocess. All original model prompts, local tool allowlist, zero-outbound checks, exact binary/dependency bindings, deadlines and physical guards remain. No Hermes source, root helper, production setting, installed binary or final default changed. Original wrappers reverse exactly after only output-path changes. The passed full resource suite will not be repeated. Source/configuration qualification and still-unrun full client preparation: [[sources/runs/2026/09/2026-09-09-optimization-offline-client-fixture-preparation]].

Remaining required work is the original full client family, all eight final paired studies, both 60-request final lifetimes, both 512-output sustained-TPS studies, empirical envelope/calibration, all 37 dispositions and exact qualified local activation. Earlier isolated gains are not final-composition or sustained-TPS results. Independent evidence processing pauses during model execution.

**Full scope qualification (V484/V499–V501):** The original 32 measured and 32 first responses complete in 6,480.035078625 seconds. All 64 preserve exact prompt/output IDs, wire text, equal work and chronological numerical compute schedules, and pass the original 10 GB sampled process cap. Maximum sampled peak is 9,454,424,024 bytes. Eleven clean measured pairs (3,4,5,6,7,9,11,13,14,15,16) pass the original request gate; eight clean first-job pairs (3,4,5,6,7,11,14,16) pass the separate startup gate. Seven measured rows remain excluded for VM activity, excluding five paired rounds. No rows are reused, replaced or pooled. All source/native/binary proofs, original cleanup and full reservation pass, with no remaining jobs and no new swap-outs. The generic child exit 1 remains preserved because it requires every row individually valid; the unchanged dedicated cohort assessor qualifies the declared paired gates.

Median paired request reduction is **51.21128759790277%**, first visible text arrives **53.83086628773317%** earlier, and prefill takes **53.84613744390523%** less time. Median first-job reduction is **50.659089935630846%**. Request medians are 49.504474584000036 and 24.05436937500008 seconds; first-visible-text medians are 47.21178429199995 and 21.69943020899973 seconds. Short active emission TPS is **2.604644087427177% lower**, with medians 6.516350154188401 and 6.391667085104254. Sampled peak is **14.971647516642218% higher**, with medians 8,196,247,584 and 9,422,475,176 bytes. These are measured tradeoffs of the isolated explicit scope, not automatic sizing, sustained TPS or final-composition performance. Exact full source, independent audit and auditor self-check: [[sources/runs/2026/09/2026-09-09-optimization-quiet-scope-complete-pass]]. Preserve V467 and every earlier failed/stopped identity. Do not repeat the unchanged qualified component.

**New exact candidate (V491–V496/V503):** Bounded smaller-group sizing compiles from all 150 source inputs in 150.531319292 seconds. All 16 shared source entries, original release artifacts and mutable SwiftPM state restore exactly. Candidate SHA-256 is `d3701afdb0540850f376ca9a696a2a0ffa31a67121362e341f87ebc9e27fd7dc`. Pure tests pass 25,337 assertions across all 33 groups, preserving every earlier 22,840 name/multiplicity and adding 2,497 assertions. All 116 CLI checks pass; complete pure/CLI time is 11.287843667 seconds. V496 freezes all seven original native cases with their original executor, workloads, guards and full intervals, preserving every V436 assertion and requiring both new actual intermediate budget choices. Exact build/source/test/preparation evidence: [[sources/runs/2026/09/2026-09-09-optimization-adaptive-scope-build-pure-and-native-freeze]]. Automatic and vision defaults remain off in this candidate.

**Complete native qualification (V496/V507/V509):** All seven cases pass, totaling **5,556 assertions** and preserving every one of the earlier 5,300 assertion names and multiplicities. Lifecycle passes 1,914, MTP/vision 873, both geometry cases 817 each, ordinary integration 286, MTP integration 327 and read-failure recovery 522. Both new actual intermediate process/device choices preserve exact output, routes, compute shapes, state and continuation. Every original guard, complete interval, build/source proof and cleanup passes. No case is rerun. Exact terminal reports, memory samples and independent receipt audit: [[sources/runs/2026/09/2026-09-09-optimization-adaptive-scope-seven-native-pass]]. This proves native correctness, not automatic serving benefit or default activation.

**Public-planner freeze (V505/V509):** Seven model-free checks pass. All three original unrun V475 workloads are rebound only by binary path and evidence identity, with their original input, controls, dimensions, acceptance, exclusions, actual planner observations, process caps, quiet periods and complete intervals unchanged. An initial model-free preparation refused because the native import used `/private/tmp` instead of its literal frozen `/tmp` name, despite identical bytes. The adapter now imports the exact original name, and a regression check revalidates all seven actual native proofs. No model launched during the refusal and no prior cohort is retried or pooled. Exact preparation: [[sources/runs/2026/09/2026-09-09-optimization-adaptive-public-planner-cohort-freeze]].

**Pre-model delivery correction (V511/V512):** V505 planner-256 stops after 1.880104959 seconds before model launch or any response because the raw protocol inherited the incompatible template-only `think=false` field. Exact stderr preserves this root cause; the outer receipt reports the consequent missing completion file. Cleanup, source proof and the original complete allowance pass. V511 removes only that incompatible field from the preserved raw request declaration, retaining the original prompts/IDs, controls, geometry, outputs, dimensions, criteria, guards and intervals. Eight model-free checks pass, including complete request-body equality through the actual factory for both inputs in all three profiles. No prior sample exists, no study is pooled, and both old unrun profiles remain frozen. The complete fresh source-bound three-profile preparation and failed execution are preserved at [[sources/runs/2026/09/2026-09-09-optimization-public-planner-raw-delivery-correction]].

**Final composition preparation (V506/V508/V509):** Six model-free protocol checks preserve all eight original final workloads while making the reference AUTO=0 and explicit combined automatic selection explicit, since the original manual scope controls suppress inherited automatic defaults. Compact n-gram rows remain selected; selected vision query tile is 256; rejected MTP tail remains zero. The actual-default combined environment stays empty. No final binary or automatic disposition is bound. A two-file source-only draft preserves explicit alternative vision-padding settings when query tiling later becomes the default, and the symmetric explicit query override; two explicitly enabled alternatives still refuse. Twelve added diagnostic assertion sites cover inheritance, zeros, alternatives, conflicts and malformed values. Existing assertion sites and multiplicities remain. This draft is uncompiled, untested in Swift and has not changed the current candidate or shared source. Exact scripts, source and preserved initial fixture failures: [[sources/runs/2026/09/2026-09-09-optimization-final-component-control-and-vision-override-preparation]]. Integrate and qualify this compatibility behavior with the eventual final defaults; no final gate is waived.

**Actual planner-observation correction (V514–V516):** V511 completes one original first reference response, 1,027 prompt tokens and 16 outputs, with compute passes `[256,256,256,256,3]` and sampled process peak 6,591,991,168 bytes. It stops at the observation validator because generation metrics do not contain the expected plan object. The server publishes that object as `memory_plan` inside `/api/tags` model details. No measured response or paired result exists, and the 320.650236583-second failed attempt stays consumed with original cleanup and source/guard proof. No engine arithmetic or capacity failure is shown.

V514 reads that actual endpoint, preserves its bounded response bytes, and compares the real plan with the existing effective chunk/pool metrics. It retains every original work, geometry, capacity, timing, exclusion and guard check. The identical metadata request remains inside each arm's original first-job clock. All twelve original driver test names and four additional metadata tests pass, including real loopback HTTP and the saved generation report with explicitly synthetic endpoint data for model-free delivery testing. V515 adds exact endpoint-byte verification to the unchanged complete assessor and passes five adapter checks. All original raw fixtures/IDs, dimensions, controls, criteria and full allowances remain. Complete failure and fresh three-profile freeze: [[sources/runs/2026/09/2026-09-09-optimization-public-planner-metadata-delivery-correction]]. No prior response is reused or pooled. The V513 terminal auditor has been prepared against V515 and will run only after a complete terminal cohort.

**Final source preparation checks (V518–V520):** Eight model-free checks pass against the exact prior source and vision override delta. The prospective in-memory final composition changes two files while retaining fourteen other overlays exactly and preserving the public reference initializer. Negative admission tests require all three original public-planner cohorts, every terminal gate, exact receipt/audit binding, the original assessor and no competing model/compiler. Synthetic test envelopes are explicitly labeled and never recorded as execution proof. The source-only preparation and original build lease retain every guard, full allowance and restoration operation. One initial test-fixture omission and its correction are preserved. No final source output, build protocol, compilation or activation has occurred. Exact scripts and tests: [[sources/runs/2026/09/2026-09-09-optimization-final-source-preparation-checks]]. The full final qualification program remains required.

**Final qualification driver preparation (V522–V526):** The prospective pure/CLI driver retains all 33 original groups and 116 CLI checks, requires every prior 25,337 assertion name/multiplicity and adds the explicit vision override cases. Five native-adapter tests preserve all seven original native workloads, all 5,556 prior assertions, original guards and full intervals. Five final-control tests reconstruct all eight paired workloads and both original 512-output long studies after reversing only final identity and independent selected switches. Seventeen campaign-controller tests preserve original raw evidence and reject altered work, failed prerequisites, insufficient resources/time, interruption and retry. Actual prior native evidence is used only in temporary model-free test packets, never to qualify the future final source. No final build, native freeze, paired measurement or activation exists. Exact scripts and tests: [[sources/runs/2026/09/2026-09-09-optimization-final-qualification-drivers-preparation]].

**Complete 10 GB public-planner qualification (V515/V513/V530):** All 32 measured and 32 first responses complete in 4,948.323305458 seconds. Fourteen clean measured pairs pass the original request gate; thirteen clean first-job pairs pass the separate startup gate. All 64 preserve exact prompt/output IDs, text, equal work and chronological compute schedules, all original 10 GB sampled process caps, and exact saved public metadata responses. Maximum sampled process peak is 8,012,975,272 bytes. Two measured rows have swap-ins, excluding rounds 6 and 7; round 5 additionally has startup swap-ins. No new swap-outs occur. Every raw row and exclusion remains, with no replacement, retry or pooling. The generic child exit 1 is retained; the unchanged complete-cohort assessor qualifies the declared paired gates. All source/build/native proofs, original guards, complete allowance and cleanup pass, with no remaining jobs. Exact raw evidence and independent audit: [[sources/runs/2026/09/2026-09-09-optimization-public-planner-256-complete-pass]].

Median paired request reduction is **21.892122229338796%**, first visible text arrives **25.65483411080369%** earlier, prefill takes **25.66058850957055%** less time, and first-job reduction is **20.30221884435235%**. Request medians are 14.217511062499966 and 11.032386999999972 seconds; first-visible medians are 12.11103258349982 and 8.975965812500135 seconds. Short 16-output active emission TPS is **1.0330498561391233% higher**, with medians 7.147024940719544 and 7.330356247052334. Sampled process peak is **16.028924800976874% higher**, with medians 6,864,416,128 and 7,975,283,916 bytes. This is one automatic-sizing profile and its measured memory tradeoff, not sustained TPS, final composition, a larger budget or whole-machine savings.

**Final source delivery preparation (V528–V530):** Six intercepted static-lease tests preserve the original complete suite, guard and interval, including exact source/mode/mtime/environment restoration, interruption, partial copies and foreign-edit preservation. Four external-consumer preparation tests reconstruct all 150 actual prior archive inputs in memory, reject changed or incomplete archives and retain the original consumer/executor/policy. No final source is extracted, no final protocol is prepared and no compiler/model/static suite is launched. Exact scripts and tests: [[sources/runs/2026/09/2026-09-09-optimization-final-static-consumer-preparation]]. Actual final-source delivery remains required.

**12 GB public-planner stop and fixture defect (V515/V532):** The consumed cohort stops after 1,561.355770709 seconds during round 5 reference startup, where macOS records four swap-ins and no swap-outs. Eight measured and nine first responses complete. All seventeen pass the original 12 GB sampled process cap, with maximum observed peak 9,577,565,520 bytes; minimum outer sampled reclaimable memory is 28,644,130,816 bytes. Eight measured responses pass original work constraints and form four eligible pairs. All nine first responses naturally stop at five output tokens, below the original sixteen-output workload, so no startup pair qualifies. All four completed pairs preserve exact prompt/output IDs, text and equal work within each phase, and all observed automatic/read/compute boundaries and public endpoint bytes verify. This is observed-only evidence, not a performance qualification. The complete-cohort assessor correctly rejects the incomplete 32-cell study. Source/build/native proofs, whole reservation and cleanup remain exact, with no remaining jobs or new swap-outs. The driver currently defers first-job logical-work validation until terminal assessment; enforce that existing requirement before measurement and qualify the distinct corrected first fixture before a fresh full cohort. Exact failed evidence is captured with discarded:true: [[sources/runs/2026/09/2026-09-09-optimization-public-planner-512-resource-stop-and-first-work-failure]].

**First-job delivery correction (V533–V535):** Nineteen driver checks pass, preserving all sixteen earlier tests and adding actual five-token rejection, check placement and intercepted early-stop/cleanup coverage. The driver now invokes the unchanged original first-work validator immediately after saving the first response, before any measured request. This tightens execution to the existing terminal requirement; no work or resource guard is relaxed. V534 changes only the 512 first instruction, retaining exactly 2,051 tokens, distinct IDs and the same code body. The original measured 512 and both 1,024-pass fixtures remain byte-for-byte unchanged. Five adapter checks preserve the original executor and spatial guards and reject short diagnostic reservations. V535 freezes two separate four-response functional checks with 1,800-second work and 60-second cleanup allowances. Their one pair cannot satisfy the retained minimum-five performance criteria, and pilot timings cannot be pooled or used for selection. This functional preparation does not shorten or replace either original full 15,000+60-second cohort. Exact preparation, initial mock-only failure and correction: [[sources/runs/2026/09/2026-09-09-optimization-first-work-guard-and-fixture-delivery-preparation]].

**Corrected 12 GB fixture delivery passes (V535/V536):** All four first/measured responses produce sixteen outputs, preserve paired exact IDs/text and work, actual public-plan and compute/read geometry, and the original 12 GB sampled cap. Maximum sampled peak is 9,517,452,552 bytes. The complete 497.380690583-second functional check passes source/build/native proof preservation, complete allowance and cleanup with no remaining jobs. This is explicitly `fixture_delivery_qualified:true` and `performance_qualified:false`; original minimum-five timing criteria remain unmet and generic child exit 1 is preserved. Its timings are not pooled. The inherited progress display uses a fixed 32-cell denominator, but the frozen diagnostic protocol, result and assessor correctly require two cells/four responses. Exact proof: [[sources/runs/2026/09/2026-09-09-optimization-public-planner-512-fixture-delivery-pass]].

**Original 16 GB first fixture fails early (V535/V538):** The original 4,099-token input returns one period and `finishReason:stop`, below the original sixteen-output requirement. V533 now catches that existing work violation immediately after preserving the first response, before any measured request or automatic arm starts. This consumed 330.875314-second diagnostic is incomplete/unqualified. Its original numerical passes and 16 GB sampled cap hold, with first-response peak 12,880,973,496 bytes. Source/build/native proofs, allowance and cleanup pass; no model remains and no swap-ins or swap-outs occur. Exact discarded evidence: [[sources/runs/2026/09/2026-09-09-optimization-public-planner-1024-first-fixture-short-stop]].

**Prospective continuation correction and full cohorts (V539–V541):** Both 1,024-pass fixtures retain their first 4,085 original tokens and add a fixed numbered-analysis continuation cue at exactly 4,099 total tokens. Both already-qualified 512 fixtures remain byte-identical. Five V540 adapter checks pass, and one original-spatial-guard four-response diagnostic is frozen for 1024; it grants no performance qualification. Six V537 full-cohort adapter checks preserve sixteen pairs, 32 measured plus 32 first responses, sixteen outputs, all original work/geometry/control/resource/thermal/swap/acceptance requirements and the original complete 15,000+60-second allowances. Only declared fixture and provenance fields differ; no pilot timing is pooled. Its two full cohorts cannot prepare until both independent functional deliveries pass. Original V5151024 remains frozen/unrun; the new full driver enforces first work early. Final-source preparation must bind the qualified V515256 and eventual V537512/1024 proofs explicitly; the older unexecuted V518 all-V515 prerequisite draft is not applicable as-is. Exact preparation and earlier unprepared source: [[sources/runs/2026/09/2026-09-09-optimization-continuation-fixture-and-full-cohort-preparation]].

**Corrected 16 GB fixture delivery passes (V540/V543):** All four first/measured responses produce sixteen outputs and preserve paired exact IDs/text/work, actual public-plan geometry, chronological 1,024-token compute passes, zero reuse and the original 16 GB sampled cap. Maximum sampled peak is 13,173,690,040 bytes. The complete 523.07406625-second diagnostic passes source/build/native proofs, the spatial guards, full allowance and cleanup, with no remaining jobs. This is functional delivery only, `performance_qualified:false`; the minimum-five timing criterion remains unmet and generic child exit 1 is retained. Outer VM counts remain in evidence and no pilot interval is pooled into performance. Exact source: [[sources/runs/2026/09/2026-09-09-optimization-public-planner-1024-continuation-delivery-pass]].

**Two full cohorts frozen (V537/V542/V543):** Actual prerequisite assessment verifies the qualified V535512 and V5401024 functional deliveries independently, their exact receipts and every original work/geometry/cap condition. V537 now freezes both full original 16-pair, 32 measured plus 32 first, sixteen-output cohorts, with all original numerical/control/resource/thermal/swap/acceptance/exclusion requirements and full 15,000+60-second allowances. No pilot data enters their results. The V542 terminal auditor reconstructs the earlier full 64-response auditor exactly after reversing only its source binding and descriptive classification. All old consumed failures remain preserved; qualified V515256 is not repeated and old V5151024 stays frozen/unrun. Exact freeze and auditor: [[sources/runs/2026/09/2026-09-09-optimization-qualified-fixture-full-planner-cohorts-freeze]]. Neither new full cohort is yet qualified.

**Second full 12 GB startup stop (V537/V544):** The consumed cohort stops after 2,667.275097708 seconds: fifteen measured and sixteen first responses complete, with sixteen recorded cells. All thirty-one satisfy the original sixteen-output work, public-plan and chronological compute requirements and the 12 GB sampled physical cap; maximum sampled peak is 9,600,388,408 bytes. Fifteen completed phase pairs preserve exact output/work. Round 7 reference has eight measured swap-ins, excluding that pair. Round 8 reference has twenty-eight startup swap-ins and triggers the former startup abort before measurement. No new swap-outs occur; minimum outer reclaimable memory is 26,595,131,392 bytes and all physical/thermal/power checks pass. Source/build/native proofs, full allowance and cleanup pass. The original full assessor correctly rejects the incomplete study. This is observed-only failed evidence, preserved with discarded:true: [[sources/runs/2026/09/2026-09-09-optimization-public-planner-repeat-startup-swapin-stop]]. V5371024 stays frozen and unrun.

**Explicit prospective startup-noise correction (V545–V548):** The former driver excluded measured swap-ins but aborted a greater-than-10 GB full cohort on startup swap-ins even with unchanged swap-outs and passing physical limits. A new optional declaration, `startup_swapin_handling: exclude-pair`, makes monotonically increasing startup swap-ins with unchanged swap-outs exclude the entire pair from both measured and startup timing. This deliberately changes that one benchmark abort rule; it is not retrospective rescoring or a claim that every original guard is unchanged. Undeclared protocols preserve the former behavior. New swap-outs, counter resets/invalid counters, physical/RSS/headroom limits, thermal/power failures, contention and failed work still stop execution. All sixty-four responses, including excluded pairs, must satisfy exact output/work/geometry/caps. Sixteen pairs, sixteen outputs, minimum five clean pairs, at least 5% median request improvement, at least 80% positive pairs, the separate startup gate and the full 15,000+60-second allowance remain. All nineteen earlier driver checks plus seven new tests pass. Controlling decision: [[records/decisions/benchmark-startup-swapin-exclusion-2026-09-09]]. This decision controls over earlier generic statements that all original swap rules remain unchanged.

**Corrected-method full freeze (V546/V547/V548):** Eight adapter checks preserve the original execution and assessment bodies, full workloads, spatial policies and independently qualified V539 fixtures. Prerequisites revalidate V535512 and V5401024 functional proofs, all seven native proofs and the failed V537512 evidence. Both full cohorts are frozen before launch. The assessor independently recomputes startup exclusions from saved counters and requires contaminated rows to remain invalid in both timing phases; V547 retains the original independent sixty-four-response audit. No pilot or prior failed row is pooled and qualified V515256 is not repeated. Exact driver, checks, both frozen protocols and terminal auditor: [[sources/runs/2026/09/2026-09-09-optimization-explicit-startup-swapin-method-and-full-freeze]].

**Complete 12 GB public-planner qualification (V546/V547/V550):** All thirty-two measured and thirty-two first responses complete in 5,274.917663458 seconds. Eleven clean pairs pass both original request and startup acceptance gates. All sixty-four preserve exact prompt/output IDs, wire text, original sixteen-output work, chronological numerical geometry and actual public endpoint bytes, and satisfy the original 12 GB sampled physical cap. Maximum sampled peak is 9,580,268,856 bytes. Four measured swap-in rows and one declared startup swap-in row exclude entire rounds 1, 3, 5, 8 and 12 from timing; all exclusions independently recompute. No new swap-outs occur; minimum outer reclaimable memory is 23,199,006,720 bytes and maximum owned RSS is 3,237,380,096 bytes. Full reservation, hard limits, source/build/native proofs and cleanup pass, with no remaining jobs. The generic child exit 1 and the outer guard's resulting passed:false remain visible; the unchanged dedicated complete-cohort assessor and independent sixty-four-response audit qualify the declared gates. No earlier failed row or pilot is pooled. Exact terminal evidence: [[sources/runs/2026/09/2026-09-09-optimization-public-planner-512-complete-pass]].

Median paired request reduction is **18.649931460099246%**; first visible text arrives **20.905586537067133%** earlier; prefill takes **20.913078529703412%** less time; and first-job reduction is **21.01184366732166%**. Request medians are 19.728837249999742 and 15.42967304100057 seconds; first-visible medians are 17.64438541599975 and 13.284941084000366 seconds. Short sixteen-output active emission TPS is **0.4618070889098935% lower**, with medians 7.1102820290522715 and 7.1221053931520615. These paired percentage summaries are not ratios of the marginal medians. Sampled process peak is **12.686041741463772% higher**, with medians 8,464,764,264 and 9,527,053,672 bytes. This is one public automatic profile and a measured memory tradeoff; it is not sustained TPS, final composition or whole-machine memory savings. The qualified 10 GB cohort and the isolated scope/vision components are not repeated.

**Current final-source prerequisite preparation (V549/V550):** Eleven model-free checks retain all eight original V518 test names, the exact source composition function and its original main function apart from the required check count. Prerequisites explicitly bind qualified V515256 and qualified V546512/1024, require matching exact candidate builds, their actual independent sixty-four-response audits and independently recomputed declared startup exclusions. Provenance paths and hashes are retained per profile. Synthetic negative/admission envelopes are never recorded as model qualification. An initial mock installed its evidence-read patch before constructing the second provider fixture; that setup-only failure and its correction remain preserved. No final source, protocol, build, model result or activation is created. Exact draft and tests: [[sources/runs/2026/09/2026-09-09-optimization-final-source-current-profile-binding-preparation]]. V551 now binds the unused downstream preparer/controllers to this exact source preparer; see the subsequent current check evidence.

**Complete 16 GB public-planner qualification (V546/V547/V552):** All thirty-two measured and thirty-two first responses complete in 5,672.578586792 seconds. Six clean pairs (5,6,9,10,12,13) pass both original timing gates; ten entire paired rounds remain excluded for declared startup or measured swap-ins. All sixty-four responses preserve exact prompt/output IDs, wire text, sixteen-output work, chronological numerical geometry and actual public-plan bytes, and pass the original 16 GB sampled cap. Maximum sampled peak is 13,257,035,496 bytes. Exclusions independently recompute. No new swap-outs occur; hard limits, original source/build/native proofs, complete allowance and cleanup pass. Generic child exit 1 and consequential outer passed:false remain preserved, while the declared complete-cohort assessor and independent sixty-four-response audit qualify this identity. No failed row or pilot is pooled. Exact evidence: [[sources/runs/2026/09/2026-09-09-optimization-public-planner-1024-complete-pass]].

Median paired request reduction is **16.70375455058452%**, first visible text arrives **17.824754200492016%** earlier, prefill takes **17.94799964900455%** less time and first-job duration falls **16.26686964140962%**. Short sixteen-output active TPS is **3.1069224546734775% higher**; sampled peak is **4.164014223809054% lower**. Request medians are 24.63215843749981 and 20.219215833499902 seconds; preview medians are 22.835593229000096 and 18.51196285449987 seconds. These are paired-profile results, not final composition or sustained throughput.

**Final binding and source review (V551/V552):** Exactly four path literals in three unused final preparation/controller files now bind V549 instead of V518. Every other byte reconstructs exactly. All twenty-seven affected current model-free checks pass (five native, five controls, seventeen campaign); old bytes and old check results remain preserved. Actual candidate source confirms bounded group sizing and memory fallback, existing checkpoint boundaries, and final-compute-range hot-expert counts. Resident-first installation may change subsequent LRU history; no sustained decode gain is inferred. Prefix-enabled small prompts can suppress grouping at an existing checkpoint boundary, so component ratios cannot calibrate every combined workload. Original planner anchors remain nominal costs; temporary vision savings are not allocated permanently to expert residency. Timing counters follow cooldown correctly. No additional algorithm defect or new policy study was established. Exact amendment/check/source evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-preparation-binding-amendment-and-source-review]].

**Exact final build and pure/CLI qualification (V549/V519/V522/V553):** All three actual public-planner proofs and the vision qualification pass before selecting final integration defaults: automatic read grouping, compact n-gram rows and 256-query vision tiling, with bounded MTP tail off. The final sixteen-overlay composition changes two files from V492, preserving the other fourteen overlays and all 150 source inputs. The public reference initializer and explicit user overrides remain compatible. Binary SHA-256 is `9268e4b2a3371e78a71d493d7788559a06a22498e8061a89278c4918c6764673`. Build completes in 147.7809725 seconds with exact shared source, release and mutable SwiftPM restoration and all original resource/full-interval checks. All **25,360 pure assertions** across 33 groups pass, retaining every earlier 25,337 name/multiplicity and adding 23 vision-override assertions. All **116 CLI checks** pass. Complete pure/CLI time is 11.681030499999999 seconds. Seven original native cases are frozen with the same workloads, guards and complete allowances, requiring all 5,556 prior assertion names. Exact closed source/build/pure/native-freeze evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-composition-build-pure-and-native-freeze]].

**All final native cases and serving freeze (V523–V525/V554/V556):** All seven cases pass on the exact final binary, preserving every earlier name/multiplicity: 817 assertions for each geometry, 1,914 lifecycle, 873 MTP/vision, 286 ordinary integration, 327 fixed-MTP integration and 522 serving read-failure recovery, totaling **5,556 assertions**. Every original workload, physical guard, complete interval, source/build identity and cleanup passes. The first MTP/vision preflight refused nonnominal thermal conditions before any attempt or model; its original stderr is retained. The unchanged V464 bounded cooling observer then passes before each of the four remaining cases. No claimed native attempt is repeated. V525 independently recomputes all seven source-bound raw proofs and imports the original three integration executions into its campaign without further inference. All eight original paired studies and both original 512-output long protocols are bound to the exact final build, preserving workloads and acceptance; the actual-default environment remains empty. Exact terminal, cooling, control and campaign-freeze evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-composition-seven-native-pass-and-serving-freeze]].

**Final lifetime binding checks (V555/V556):** Twelve model-free checks pass. The adapter preserves the original V321 execution, assessment and protocol-binding function bodies, interruption handling and original guards/allowances. Both corrected V308 sixty-request lifetimes retain their exact 10/12 GB budgets, empty default environments, sequences, output counts and acceptance. Only final source/native/eight-paired/public-consumer identities are rebound; actual old V451 public-consumer proof exercises the verifier in model-free tests but cannot qualify the final binary. Preparation still requires all eight final paired gates and the final external consumer. No final corrected contract, lifetime attempt or lifetime result exists. Source and checks: [[sources/runs/2026/09/2026-09-09-optimization-final-lifetime-binding-preparation]].

**Final static and public-consumer delivery passes (V528/V529/V559):** The full original static suite passes in **317.442506125 seconds** against all 150 exact final source inputs and the final executable. All sixteen leased source files restore exactly; release, candidate and driver identities remain unchanged, no jobs remain, and the original 600-second work/620-second whole interval and 7/3/3 GB policy pass. This original static policy permits VM activity and proves delivery/correctness, not speed or capacity. The independent public Swift consumer passes in **252.413431083 seconds** from all 150 exact final inputs and the unchanged reviewed consumer script. Public API, 22 diagnostic assertions and four automatic-control/Codable assertions pass. Source remains exact, no model loads, no jobs remain, and the original 9.5 GB startup/6 GB live/3 GB owned/600+20-second bounds pass. V555 independently verifies this actual final public-planner receipt, including the original 10 GB combined refusal and 12 GB admissibility. This alone does not qualify a lifetime or empirical inference capacity. Exact source, full outputs and receipts: [[sources/runs/2026/09/2026-09-09-optimization-final-composition-static-and-external-pass]].

**Original resource/client binding (V387/V557/V560):** The unchanged seven-check-qualified preparer binds the original four resource families, seven assertions and four Hermes/Ollama client cases to the exact final binary and all seven native proofs. Reversing only the declared source/output/candidate/prerequisite replacements reconstructs every original executor byte. Workloads, images, model, 65,536 client window, physical bounds, actual dependencies, assessors and full allowances remain. These original outer guards permit new swap-outs; their original per-request resource exclusions and acceptance are unchanged. No planner swap-in method is applied to these suites. Closed source/preparation evidence: [[sources/runs/2026/09/2026-09-09-optimization-final-resource-client-exact-binding]].

**Final resource interruption (V557/V561):** The original full suite finishes in **204.35684874999998 seconds** with six passes, one failure and no skips. Governor shrink/cooldown/growth, MTP-head bit parity, long-prompt memory/recall and 2K context checks pass. The MTP diagnostic stops after its first 48-token determinism/speculation checks when global swap-ins rise by four, from 44,360,397 to 44,360,401, while swap-outs remain 77,895,019. Its sampled peak is **8,382,911,048 bytes** and lifetime RSS peak **4,778,557,440 bytes**, below the 12 GB target, but `memory_validated:false` remains and the remaining MTP/image work did not complete. This is a failed/incomplete resource suite, with no decode correctness failure established and no responsible process attributed. Source/binary/driver proofs, original full interval and cleanup pass, with no remaining jobs. Independent evidence processing was active during the run; causation is unproven. Exact failed evidence, discarded:true: [[sources/runs/2026/09/2026-09-09-optimization-final-resource-global-swapin-stop]]. V557 client remains unrun.

**Quiet full repetition preparation (V562/V563):** The original source reverses byte-for-byte to V557 after changing only output identity, and the same final build and seven-native proof recompute. One fresh full resource repetition is prepared, with no reused observations, shortened work, changed request controls or acceptance relaxation. The existing bounded prelaunch observer uses 120 nominal seconds and a 600-second maximum instead of 30/300, within its existing validated range. This is settling only and does not promise a future swap-free interval. Independent evidence processing is paused during execution; the controller forwards existing lightweight progress without starting additional shells. Every original physical/VM/thermal check stays mandatory. Only a complete resource pass permits the fresh unrun client family. Exact closed preparation: [[sources/runs/2026/09/2026-09-09-optimization-final-quiet-resource-repetition-preparation]].

**Current execution:** V563 quiet coordinator is queued to settle, then run the full original V562 resource family and, only after success and another settle, the client family. Resource work retains the complete 1,200-second guard; client work receives its original full 3,600-second maximum, with cleanup and admission slack. All eight final paired studies, both sixty-request lifetimes, both sustained-throughput studies, empirical envelope and exact activation remain open. Installed and shared release artifacts remain unchanged.

**Short-decode followup (V502/V503):** All eleven scope pairs have identical decode records, read bytes, expert hit rate, n-gram cache rows/payload and embedding cache rows/payload. Scope has lower end MLX active/cached bytes and lower end physical footprint in every pair. Median paired end-footprint reduction is 2.0757279160788733%, distinct from its higher transient peak. Reported I/O time rises in nine of eleven pairs, with median paired increase 4.129632759282642%. This does not establish a causal mechanism or sustained throughput. Exact observations: [[sources/runs/2026/09/2026-09-09-optimization-qualified-scope-decode-observations]]. The original long decode tests remain required.

**Remaining work:** Complete original resource/client families, all eight original paired studies, both lifetimes, both sustained-TPS studies, empirical envelope/calibration and exact qualified activation. Preserve rejected and conditional dispositions. Qualified isolated mechanisms, all three public-planner cohorts and functional diagnostics are not repeated or pooled. No final-source or installed-default qualification exists.

## Previous execution state through V498

The snapshots below are historical. Their former current, running, pending, uncompiled or blocked statements do not override the status above.

**Current status, September 9: the vision mechanism is qualified and the fresh scope cohort is running.** The full optimization program remains incomplete. All 37 OPT00–OPT36 items and original gates remain. V436 retains its seven passing native cases (5,300 assertions), 22,840 pure assertions, 116 CLI checks, and exact-source external/static passes. Installed binaries and public defaults remain unchanged.

**Vision result and selection (V462/V488–V490):** The full original 24 measured and 24 first responses complete in 4,591.124162042 seconds. All 48 preserve exact prompt/output IDs, wire text, original work and the sampled 12 GB process cap. The largest sampled peak is 10,281,472,512 bytes. Eight clean pairs (1, 3, 4, 5, 7, 9, 10 and 12) pass every original mechanism criterion. Four pairs remain excluded: two for measured swap activity and two for startup/warmup swap-ins. No rows are replaced, pooled or continued. Original source/native/photo/binary proof and cleanup checks pass, with no remaining jobs and no new swap-outs.

The median paired reduction in sampled process peak is **30.24868124370113%**, with median peaks of 10,102,747,564 and 7,043,158,336 bytes. Every clean pair saves at least 2,945,908,736 bytes. Median paired request reduction is **4.397867048466331%**, and visible text arrives 4.3979240220168805% earlier. Image encoding itself takes 40.45309918790301% less time; text prefill is 1.699535144118669% slower in these observations. First-job time is descriptively 5.597929548134689% lower on the eight clean pairs. One-output requests provide no TPS estimate. These numbers describe the isolated V304 vision component, not the final V436 composition. Select query tile 256 for the final combined candidate, subject to the unchanged final-composition gates. Exact complete evidence and audit: [[sources/runs/2026/09/2026-09-09-optimization-vision-mechanism-complete-pass]].

The original vision mechanism has startup resource, capacity, exactness and contamination guards, but **no separate comparative first-job acceptance contract**. Earlier references to its startup criteria mean those existing guards; no comparative startup gate is claimed or waived. Final-composition startup gates remain required. The frozen generic driver returns exit 1 when any row is excluded. Its nonzero status is preserved; the unchanged dedicated cohort assessor recomputes the declared paired criteria and passes.

**Current execution (V484):** The fresh complete original scope cohort starts at 2026-09-09T07:55:23.956324 UTC, on session 90494, with 40,091,975,680 reclaimable bytes and nominal thermal admission. Chrome and Wispr Flow pass the declared absence precondition. The full 15,000-second work plus 60-second cleanup allowance fits before 12:30 UTC. Protocol `9e36268ea1bcf7b4b3aacda9e2a8aca53e0d272825b061ef5785d22c31f36db2` preserves all original 32 measured plus 32 first requests, explicit scope controls, 10 GB sampled cap, work checks, exclusions, acceptance and bounded cooling. V467 remains complete but startup-unqualified; its rows are never reused. This new run has no result yet. One model runs at a time, with no concurrent compiler.

**Remaining work:** Complete V484. The smaller-group refinement below requires a new exact build, preserving all original native cases and all three V475 public-planner threshold workloads; finalize scope selection and the exact combined defaults; then complete all original eight paired studies, both lifetimes, original resource/client families, both sustained-TPS studies, planner calibration and delivery/activation. The vision component decision is resolved, but final combined vision validation remains. Rejected and conditional dispositions remain explicit. The five-pair V467 short-decode followup records identical read work with higher reported I/O time, without a causal or sustained-throughput claim: [[sources/runs/2026/09/2026-09-09-optimization-scope-short-decode-observations]].

**Smaller automatic prefill groups (V491/V493):** Actual Generator code tries the largest eligible group and then ordinary prefill, leaving feasible smaller groups unused. The isolated refinement enumerates prefixes from the original largest group down to four full passes, prices them under the same process target and atomic shared reservation, and retains ordinary fallback. Every compute shape, checkpoint, partial tail, cancellation and pressure guard remains. New diagnostics cover exact boundaries, overflow, preferences, queued preparations, concurrency, empty choices and sticky failures. Native extensions constrain actual process/device room to exercise an intermediate four-pass group and compare exact output, routes, state, continuation and actual expert reads. All old named assertion sites and multiplicities remain. The five-file change is source-audited only, uncompiled and unrun. Its existing defaults and shared checkout are unchanged. V484 continues on frozen V436. Exact draft evidence: [[sources/runs/2026/09/2026-09-09-optimization-adaptive-scope-sizing-source]]. V475's three source-bound unrun identities remain preserved; after this source refinement, create explicit exact-source successors retaining their whole workloads and gates rather than rewriting those frozen protocols. The new adaptive choice is part of OPT08/OPT16, not a replacement for any original acceptance gate.

**Qualification executor preparation (V492/V495–V497):** Execution scripts are ready for the smaller-group source change, but no new build or native preparation has run. V492 preserves the original cached lease executor except for fresh identity/output/restoration names, including the original 1,250-second reservation, 1,220-second complete lease bound and exact shared source/release/SwiftPM restoration. V495 retains all prior V438 pure assertion names/multiplicities and 116 CLI cases. V496 reuses the original scope/integration executor and guard bodies, all seven original workloads and their unchanged resource/interval policies, requires every V436 native assertion, and adds both actual intermediate budget-choice checks. Five model-free adapter checks pass; initial test-only exception/source-inspection errors remain preserved. Exact source and outputs: [[sources/runs/2026/09/2026-09-09-optimization-adaptive-scope-qualification-executors]]. These are preparation checks, not compiled/native/performance qualification. V484 remains unchanged and running.

## Previous execution state through V486

The snapshots below are historical. Their former current, running, pending or blocked statements do not override the status above.

**Current status, September 9: execution continues; the full program remains incomplete.** All 37 OPT00–OPT36 items and original acceptance gates remain. Exact V436 passes all seven native cases (5,300 assertions), 22,840 pure assertions, 116 CLI checks, and the exact-source external/static suites. The current installed binary and default activation remain unchanged.

**Complete explicit-scope result (V467/V480/V482):** All 32 measured and 32 first responses complete. Exact prompt/output IDs, wire text and original equal-work checks pass for all 64 responses. Every sampled process peak stays under the original 10 GB cap; the maximum is 9,508,589,552 bytes. Original cleanup and source/native/binary proof checks pass, with no remaining jobs and no new swap-outs. The request gate passes on five clean pairs (1,9,14,15,16). The independent startup gate fails because only three pairs (9,14,15) qualify, below five. The full cohort remains unqualified. No rows are replaced, continued or pooled.

On those five clean measured pairs, median paired request reduction is 51.49086719481968%, prefill reduction 54.07310301803512%, and first visible text arrives 54.05546069140925% earlier. This isolated mechanism trades a 15.0371815092738% higher sampled process peak for reduced prefill time; short active emission TPS is 2.838204833544866% lower. These sixteen-token observations do not establish sustained TPS or final-composition performance. Startup values are withheld from the descriptive report because its minimum cohort is absent. Exact complete raw evidence, original recomputation, halfway snapshot and descriptive reporting: [[sources/runs/2026/09/2026-09-09-optimization-explicit-scope-complete-startup-inconclusive]]. A fresh full scope cohort under the materially improved conditions is required for startup qualification, preserving this failure and every original rule.

**Current execution:** V462's original full vision mechanism starts at 2026-09-09T06:35:03.909829 UTC after 39,411,400,704 reclaimable bytes, normal pressure and nominal thermal admission. Session 73954 reserves all 12,600 work plus 60 cleanup seconds before 10:30 UTC. Protocol a9cce28c7dfa95a81a1286290a884184d76d51c211f51307c7364dd19f6a1b84 retains 24 measured plus 24 first responses, original 12 GB cap, exact work/output, at least 200 MB saving in every clean pair and original timing/startup criteria. This is the isolated V304 vision component, not final V436 composition. No compiler or second model runs beside it. Chrome and Wispr Flow are observed fully absent; no quit was needed after V467 and this task did not quit them during that cohort. Global VM events are not attributed to those apps.

**Remaining execution:** Complete V462, a fresh original scope cohort, and the separately frozen V475 public planner 256/512/1024 threshold studies. Then settle optional default selection, qualify the exact final composition through all original eight paired studies, both lifetimes, original resource/client families, both sustained-TPS studies, planner calibration and delivery/activation. Rejected and conditional dispositions stay explicit. V475 remains three frozen/unrun studies and cannot replace original scope/vision/final gates. All source/default activation remains pending these proofs.

**Fresh original scope cohort frozen (V484–V486):** V484 preserves the complete V467 startup failure and binds the observed 8,324,923,392-byte increase in reclaimable memory. Protocol `9e36268ea1bcf7b4b3aacda9e2a8aca53e0d272825b061ef5785d22c31f36db2` differs from V467 only in its study label and provenance classification. All original 32 measured and 32 first responses, 10 GB sampled cap, read-work controls, exclusions, acceptance, thermal settling and full 15,000-second work plus 60-second cleanup allowance remain. The actual original executor and assessor bodies are reused unchanged. Five model-free checks pass, including actual dispatch interception and prelaunch refusal when an app reopens or the complete interval does not fit. All original source/native proofs and the completed prior evidence are revalidated. Exact preparation: [[sources/runs/2026/09/2026-09-09-optimization-quiet-resource-scope-cohort-freeze]]. This cohort is frozen and unrun while V462 vision continues; it cannot reuse prior rows or replace final-composition qualification.

## Earlier execution snapshots

The entries below preserve historical state and evidence. Their former running, pending, blocked and current-composition statements do not override the September9 status above.

**Status: execution resumed; the full implementation program is incomplete.** All 37 OPT00–OPT36 items and all original acceptance gates remain in scope. All seven current-source native cases pass on V436, totaling 5,300 assertions. Build, pure/CLI and exact-source external/static delivery passes remain valid. The full original explicit-scope serving cohort V467 is running; vision V462 is frozen and unrun. Automatic serving, final composition and every final paired/lifetime/resource/client/TPS/calibration/activation gate remain open. No final installed/default activation or final speed/memory claim is qualified.

**Real public planner threshold cohorts frozen (V475–V477):** Three separate full cohorts now bind the exact V436 candidate and all seven passing native proofs. Actual public targets of 10/12/16 GB must select compute passes of 256/512/1,024. First and measured prompts are distinct and frozen at 1,027/2,051/4,099 tokens: four full chronological passes plus an odd three-token tail. Both actual prompt IDs and actual read/compute boundaries must match; the automatic arm must group the four full passes without a forced planner override, disabled prefix allocation or altered pool. Each profile retains sixteen alternating pairs, 32 measured plus 32 first responses, sixteen outputs, minimum five clean pairs, at least 5% median client improvement, at least 80% positive pairs and the separate first-job gate. Actual sampled process footprint may not exceed its target. Each complete 15,000-second work plus 60-second cleanup interval must fit before launch. Ten model-free checks pass, including intercepted actual dispatch and tamper/time/consumed-identity refusal. These new policy measurements cannot replace any original explicit-scope, vision or final short/prefix/MTP guard. Exact source: [[sources/runs/2026/09/2026-09-08-optimization-public-planner-threshold-cohort-freeze]]. All three remain unrun while V467 completes; V462 vision remains next after original scope drains. The initial missing abort-on-resource declaration failed preparation, is preserved, and was corrected before freezing the new protocols; V467 was unchanged.

**All current native cases pass; original full scope study starts (V473–V474):** Expanded lifecycle passes 1,658 assertions in 395.23099354199996 seconds and expanded MTP/vision passes 873 in 201.232272542 seconds. Actual automatic execution covers all three eligible planner geometries, two memory fallbacks and 4,096 MTP prompt tokens. Combined with the earlier two geometry families and three integrations, all seven current cases pass 5,300 assertions with exact source/binary/driver proofs and complete cleanup. These durations are test execution time, not inference speedups. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-all-current-native-pass-and-scope-freeze]].

V467 binds all seven actual proofs and freezes the complete original 32 measured plus 32 first explicit-scope cohort at protocol `9597d8ba4fc007bc4ee0c700746ce34a7f9de8789d6873be84aab012e57964b0`. It starts on session 92583 after fresh nominal/resource admission with the entire 15,000-second work plus 60-second cleanup reservation fitting before 2026-09-09T09:15:00Z. Original 10 GB absolute first/measured cap, workload, read-work bounds, exclusions and acceptance remain. Earlier V377/V409 rows are never reused. Automatic selection remains disabled within this isolated explicit mechanism study. No other model or build may run alongside it.

The old serving driver forces a prefill chunk and therefore cannot prove actual public planner selection. V472 adds an isolated declared automatic family that omits that override, retains prefix allocation and verifies actual planned/effective geometry, pool, options and chronological read/compute shapes on both first and measured responses. Twelve model-free checks pass, including the actual entrypoint with all model launch intercepted; the original path-canonicalization test failure is preserved. Read-only actual doctor observations establish available planner configurations, not performance or capacity proof. No automatic protocol is frozen and no automatic cohort is run. Exact source: [[sources/runs/2026/09/2026-09-08-optimization-public-planner-serving-driver-preparation]]. Full original final workloads remain required.

The dated execution snapshots below are historical; their prior pending/running statements do not override the current status above.

**All three original integrations pass after memory recovery (V462–V471):** Ordinary integration passes 286 assertions in 249.793905417 seconds and fixed-MTP integration passes 327 in 295.050282459 seconds. Recovery passes 522 in 164.713488 seconds, including checked read faults, queued preparation, retained checkpoints, streaming framing and retries. Exact source/binary/driver proofs and complete cleanup pass for all three, without new swap-outs. Native correctness policy records swap-ins separately; these durations are test execution time, not inference speedups. Sources: [[sources/runs/2026/09/2026-09-08-optimization-resumed-ordinary-mtp-native-pass]] and [[sources/runs/2026/09/2026-09-08-optimization-resumed-recovery-and-scope-preparation]]. Recovery’s first preflight refused non-nominal conditions without creating an attempt; the V464 bounded cooling observer then passed. A separate bounded cooling observer precedes the expanded V459 lifecycle case.

The full vision mechanism successor V462 is frozen at protocol `a9cce28c7dfa95a81a1286290a884184d76d51c211f51307c7364dd19f6a1b84`. Its original 24 measured plus 24 first responses, 12 GB mechanism cap, every-clean-pair 200 MB saving and full timing/resource/quality requirements remain. All nine unchanged runner checks pass after correcting preparation order; the original missing-protocol failure is preserved. This still qualifies the isolated V304 vision component and cannot substitute for final V436 composition proof. Source: [[sources/runs/2026/09/2026-09-08-optimization-vision-resource-resumption-preparation]].

V467’s complete explicit-scope serving successor passes twelve model-free checks but is not frozen or run. It requires all seven current native proofs, with the existing V439 geometry cases, fresh V459 expanded cases and V444 integrations. All original 32 measured/32 first cells, 10 GB cap, read-work bounds and acceptance remain; V377 and V409 stay preserved without pooling. Current automatic-policy serving at real planner geometries remains a separate required gate. No required item has been removed.

**Resource recovery and fresh native successors (V458–V461):** V444 ordinary integration starts after its unchanged startup check with 29,569,335,296 reclaimable bytes and nominal thermal state. V459 preserves both V439 pressure stops and prepares fresh full lifecycle and MTP/vision cases with the same source, workloads, numerical assertions, policy and complete 1800-second work plus 30-second cleanup allowance. Eleven model-free checks pass, including AST identity of the original execution/assessment functions. The two already-passing V439 geometry cases retain their original proof and are not scheduled again. Exact preparation: [[sources/runs/2026/09/2026-09-08-optimization-resource-resumption-native-preparation]]. Each next model still requires an independent original resource preflight. The previous blocked audit remains historical below; this user-requested resumed run starts a fresh blocked audit.

**Third-turn resource impasse verified (V456–V457):** The previous goal turn made concrete progress through V451 and V452. The same hardware constraint has now recurred for three consecutive goal turns. Read-only checks confirm no competing model/build job to await, Colima stopped, and the original V444 executor still ready with all three integration cases unrun. Current reclaimable memory is 17,534,091,264 bytes, below its unchanged 22.2 GB startup requirement. Actual earlier lifecycle, MTP and vision pressure stops remain preserved. The remaining model-bound acceptance and final composition decisions cannot be replaced by static checks or inherited earlier-source evidence. No independent unresolved source defect was established; repeating passed tests or preparing unexecutable final bindings would not resolve these missing proofs. The backend goal is now blocked, with the full objective retained. Resume after a verified material resource change; never weaken workloads/guards or reuse consumed study identities. Exact audit: [[sources/runs/2026/09/2026-09-08-optimization-resource-impasse-audit]].

**Current-source package and static gates pass (V450–V455):** Memory rechecks preserved the blocker, but independent required work remained. V451 reconstructed all 150 exact V436 inputs and compiled/ran a separate Swift package in 223.591635416 seconds. It preserved the original public API and planner-lifetime checks, passed 22 schedule diagnostics and added four public automaticReadScope/Codable assertions. Exact sources and cleanup pass, without new swap-outs or any model load. The existing 12 GB planner preflight remains planning evidence only. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-automatic-scope-external-consumer-pass]].

The earlier static wrapper selected the candidate executable while source-reading gates still saw the shared checkout. V452 corrects that scope with a reversible lease of all 16 changed source entries: the complete original suite now sees both the V436 executable and all 150 exact candidate source inputs. Six tiny-fixture tests cover success, suite failure, exceptions, interruption, partial copying and preservation of foreign edits. The full static suite passes in 297.585322083 seconds, including the 64 planner checks, installer checks and package/download fixtures. All original source bytes/modes/mtimes, release artifacts, driver hashes and candidate identities are restored or unchanged. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-automatic-scope-exact-source-static-pass]]. Sixteen global swap-outs occurred under the unchanged static policy that permits them; this is a correctness/delivery pass, not an inference-capacity, thermal or performance result.

At 2026-09-09T02:04:27Z, post-check headroom is 18,624,741,376 bytes and OS pressure is normal while idle. Chrome has 61 observed processes and Wispr 11; no owned model/build jobs remain. Further model launches stay held for sufficient real room. This turn completed required independent gates; the goal remains active and no full-plan completion is claimed.

**Expanded MTP pressure stop and clean handback (V448–V449):** V439 scope-mtp-vision stops after 110.244883 seconds at OS pressure level 2, before emitting a report. Its instantaneous diagnostic footprint at failure is 9,059,094,848 bytes, not a sampled peak. Source/binary proofs remain exact, no new swap-outs occurred, all processes drained, and cleanup fits the original reservation. This consumed case cannot resume or retry. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-automatic-mtp-pressure-stop]]. The original V444 three integration cases are prepared and unrun; both expanded scope cases require fresh full successors after a verified resource change, preserving these failures and every original test/guard. This turn made build, pure/CLI and two native-family progress; the backend goal remains active rather than claiming completion.

**Two scope families pass; lifecycle pressure stop remains explicit (V442–V447):** V439passes817assertions at2,051tokens in54.774623seconds and817at4,096tokens in88.66650125seconds, preserving original assertion names/multiplicities and exact source/binary/Metal proofs. Both cleanups pass. V444's three original integration executors preserve byte-identical workload/policy/command/assessor/run functions and original900-second work/30-second cleanup with22.2GBouter preflight. Those cases remain unrun. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-automatic-scope-family-pass-and-integration-preparation]].

The actual expanded lifecycle stops after159.558152459seconds on memory pressure level2, with no JSON report and therefore no qualified assertions. Its instantaneous failure footprint is7,913,999,672bytes; this is not a sampled peak. Proof preservation and full cleanup pass; no retry or continuation of that consumed case. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-automatic-scope-lifecycle-pressure-stop]]. A separate original MTP/vision case with its expanded automatic checks is now running under the complete1800+30reservation before03:00UTC. Colima remains stopped; observed Chrome/Wispr background processes have not been terminated by this task.

Installation identity clarification: preserved shared `.build/release/slotstream` is SHA`9002c67e0094ebb14b22efed27168e513b37639a43b2a28bb8fb5a28e1c74cf9`; actual PATH-installed `/Users/carlos/.slotstream/bin/slotstream` is SHA`7f540b73b5ff4cf48975ff122a3d17f57e53103e616ad84a76cfc71d551be5b8`. They are separate preexisting binaries. Earlier summaries calling the shared release artifact installed were imprecise; neither has been replaced by this optimization task.

**Automatic scope compiles and pure checks pass; vision pressure stop preserved (V430–V441):** V413 stops after926.582115166seconds when macOS reports memory pressure level2. Three of24 measured responses and four first responses completed; all seven retain exact output IDs/text/work and the original12GBsampled physical cap. The fourth measured response was interrupted, no complete cohort assessment exists, and model cleanup/proof preservation pass with no new swap-outs. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-vision-mechanism-pressure-stop]]. This consumed identity cannot resume or borrow rows.

Source review corrects a real UInt64-to-Int Mach footprint mismatch, using clamping plus fail-closed saturation even without a process cap. The first guarded buildV431 then catches a diagnostic crossing MTPHead's internal attention boundary. V435 adds a package read-only observation; the failed compiler output and exact restoration stay preserved. V436 compiles all150 source inputs in139.382588458seconds, restoring16 shared source entries and original release/SwiftPM state. Binary SHA-256: `d626d992404099949e57114c3520dc3bf6ca4aef9d6b5860e0b99f4113256b1f`. V438 passes22,840 pure assertions in33groups and116CLI checks in12.016048417seconds, preserving every original22,363 assertion name/multiplicity and adding477. Exact source/build/test evidence: [[sources/runs/2026/09/2026-09-08-optimization-automatic-scope-build-pure-and-native-preparation]].

V439 freezes all four original scope native cases and their1800-second work/30-second cleanup,20.112GBstartup/17.112GBowned/3GBlive envelope. Its assessor retains original assertion multiplicities, requires named actual-Generator grouping/fallback/refusal/prefix/error/MTP checks and all new geometry metrics; eight model-free tests pass. Scope-lifecycle is running with its complete reservation before03:00UTC. The four-pass policy and real process-cap/serving benefit still need measurement. All held V402 behavior, V416refusal and V419atomic fallback code are in the compiled candidate; no public option is temporarily mutated during automatic execution.

Earlier dated execution notes below are historical snapshots. Their live-process statements do not override this current status; the original required optimization gates remain binding.

**Scope headroom stop is preserved; independent vision study is running (V425–V427):** V409 stops after4908.252713333seconds before round11scope model launch. Its22rows contain21first and21measuredcompleted responses plus one prelaunch refusal. Settling observes19,804,995,584bytes reclaimable against the unchanged20,112,000,000-byte minimum. There are zero new swap-outs, unchanged exact build/native/source proofs and complete cleanup with no remaining jobs. The original full-cohort assessor returns incomplete/unqualified. Only three startup pairs (2,3,8) meet the original gate, below five.

An observed-only audit applies the original per-response and per-arm work validators to all42completed responses: exact IDs/text and work, all under10GB, maximum9,548,894,120bytes. This is not full serving capacity or benefit qualification. Preserve the consumed identity without retry, continuation, replacement or pooling. Exact source: [[sources/runs/2026/09/2026-09-08-optimization-corrected-scope-serving-headroom-stop]]. All original scope and final gates remain required.

Colima remains stopped; no other app is closed. V413 then passes its independent original18GBpreflight and starts at2026-09-09T01:00:33.801759UTC under the full12600-second work plus60-second cleanup allowance before06:00UTC. Protocol1ca5b6deaa7ea02c63ea6cafa78a09736e6456979de80de01e889836fd14a801, live session18468. Its original24measured+24first,12GBcap, every-clean-pair200MBsaving, startup/thermal exclusions and acceptance remain unchanged. No vision result is qualified yet. No compiler or other model runs alongside it. V422 source integration remains uncompiled/unrun and automatic defaults stay unchanged.

**Automatic scope scheduling and safe execution controls are wired in an isolated draft (V422–V424):** V422 joins V416/V419 with the actual Generator and Engine. The explicit-only policy groups at least four identical full256/512/1024compute passes within4096tokens, using the actual scheduled size under the planner ceiling. It preserves partial/short tails, scheduled common-prefix checkpoints and explicit experimental controls. A request controller atomically selects the fully priced scope or ordinary fallback, and observed footprint plus new allocations must fit the live Engine process target and device working set. No possible future cache eviction or empirical reserve credit is subtracted.

Source review caught and removed a concurrency problem in the initial draft: public model controls must not change while another request prepares. The final draft passes local execution options through forward, attention, kernels and mixing; public configuration stays stable. The private MTP indexer flag restores before direct speculative drafting. New pure checks cover Codable/override compatibility and schedule/budget edges. New native checks cover all three compute geometries, exact output/routes/state/teacher continuation, read reduction, prefix reuse, short requests, both memory fallbacks, cancellation, read-error/retry and public controls observed during execution. Aligned4096-token MTP cases exercise the first draft after a final scope.

All ten changed sources bind to exact V402; applying/reversing reconstructs both versions exactly. Exact source: [[sources/runs/2026/09/2026-09-08-optimization-automatic-scope-integration-preparation]]. This is uncompiled and unrun, and automatic defaults remain unchanged. The four-pass policy threshold, actual automatic serving at all eligible geometries, short/prefix nonregression, memory targets and full final composition remain unqualified. Use the complete V402 candidate plus these final source versions for the next build; do not lose V391/V401 or held default/planner changes by copying shared reference preimages.

At 2026-09-09T00:56:20.984857+00:00, V409 remains live with 22/32 measured cells and 17 eligible cells. Full cohort, first-request and capacity/benefit assessment are still pending. V413 remains frozen/unrun for after this study drains. No compiler or other model runs concurrently, and no default or installed binary is activated. All37items and every original final gate remain.

**Atomic optional-workspace fallback implemented in isolated sources (V419–V421):** The existing request API makes allocation failure sticky, so automatic selection cannot safely probe a larger workspace then retry an ordinary pass. V419 adds ordered preferred/fallback selection under the existing shared reservation lock; only the chosen dispatch becomes owned, queued and retained preparation stays charged, and only fallback infeasibility fails the request. Pure tests cover actual concurrent contention, replacement and release of ownership, overflow, invalid arguments, unreadable observations, cancellation, pressure and deadlines. The implementation and tests are apply-checked but uncompiled and unrun. Generator wiring, actual planner accounting and automatic activation remain pending. Exact source: [[sources/runs/2026/09/2026-09-08-optimization-atomic-workspace-fallback-preparation]].

At 2026-09-09T00:26:57.870159+00:00, V409 is confirmed live on session98925 with 15/32 measured cells and 12 timing-eligible cells. Completed measured requests so far remain under the original10GBcap, but first-request and full-cohort assessment are pending. Swap-contaminated pairs remain excluded without replacement. Do not compile or run another model alongside this study. V413 is still the next frozen independent vision cohort after scope drains and fresh admission passes; the entire required interval must fit. V416/V419 must be joined and tested in the next actual composition build. All 37 items and original final gates remain in scope.

**Native allocation-refusal wiring prepared and inventory refreshed (V416–V418):** V416 adds 56 prospective native assertions covering real Generator refusal after admission, cold and cached prefixes, and direct/shared reservations. It checks no expert reads, forward callbacks, state growth or token output before refusal, exact retained-prefix tensor fingerprints, and released cache/pin/reservation ownership. It is source-reviewed and apply-checked, but uncompiled and unrun; it earns no native pass. Exact source: [[sources/runs/2026/09/2026-09-08-optimization-scope-allocation-refusal-native-preparation]]. Merge this diagnostic into the next composition build; do not compile alongside the live study.

At 2026-09-09T00:12:06.372751+00:00, V409 has 10 of 32 measured cells, 9 timing-eligible so far. The excluded measured reference in round 4 observed swap activity; its entire pair remains excluded without replacement. The complete original study and first-request gates remain mandatory. No final speed or memory percentage is qualified. Colima is still stopped and global swap-outs remain 77,586,649. V413 remains frozen and unrun. The per-item inventory now reflects actual V402 build/native state instead of stale V360/V392 statuses; all 37 items and every remaining final gate are retained.

**Fresh full vision identity is ready while scope runs, September 8 (V413–V415):** V413 preserves both stopped vision cohorts and freezes a new complete 24-cell identity at protocol `1ca5b6deaa7ea02c63ea6cafa78a09736e6456979de80de01e889836fd14a801`. Its driver bytes remain exactly V395’s, including startup-swap-in pair exclusions, original work, 12 GB mechanism cap and acceptance. Nine original model-free checks pass in 29.873 seconds; the actual driver entrypoint intercepts all model launches. It retains the entire 12,600-second work plus 60-second cleanup allowance and original 18/14/3 GB resource envelope. Exact source: [[sources/runs/2026/09/2026-09-08-optimization-vision-headroom-resumption-preparation]]. V413 is frozen and unrun; it must wait for V409 to drain and pass fresh original admission. Its V304 vision mechanism evidence cannot be relabeled as final V402 composition qualification.

V409 is still running its unchanged full fixed cohort. Its first reference cell records valid timing, but no completed cohort or paired benefit is claimed. No native/compiler runs alongside it. Colima remains stopped, and no other app is closed by this task. No plan item or original final gate is removed.

**All seven native cases pass; full corrected scope study starts, September 8 (V411–V412):** All original V405 integrations pass on exact V402: 286 ordinary, 327 MTP and 522 read-recovery assertions. With the four scope cases, all seven native workloads total 4,084 passing assertions. Exact source/build proofs and complete cleanup pass, with no new global swap-outs. V409 passes ten model-free checks in 0.603 seconds, including actual dispatch with model launch intercepted and rejection of missing or altered evidence and insufficient full execution time. Its actual seven native prerequisites then validate and the full 32-cell/32-first-request study freezes at protocol `7e2f4e9a69cb418a663d8863baccce71dd5462e788a9471f7981b0b278d32991`. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-explicit-scope-integration-pass-and-serving-freeze]].

V409 starts at 23:31:37 UTC after original admission, with the entire 15,000-second work and 60-second cleanup allowance fitting before 06:00 UTC. It retains the original workload, controls, 10 GB absolute first/measured footprint cap and acceptance; each cell keeps its original cooldown and requires 60 sampled nominal seconds before launch, at most 300 added seconds. No old V377 rows enter this cohort. The current run is completing startup quiet/cooling checks and has no qualified speed or savings result. No compiler or native test runs alongside it. Colima remains stopped; other apps remain open. All default-policy, vision and final campaign/delivery/activation requirements remain open.

**All scope natives pass and ordinary integration clears, September 8 (V408–V410):** V402 passes every original scope family under its original resource envelope: 817 assertions at each of 2,051 and 4,096 tokens, 832 lifecycle assertions and 483 MTP/vision assertions, totaling 2,949. Direct checked read faults, state rollback, exact retry and cache-limit restoration pass. Ordinary full integration passes all 286 assertions in 253.492637 seconds. Every completed case has exact build/source proofs and successful cleanup. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-explicit-scope-native-family-pass]].

The next V405 MTP launch refuses before creating an attempt because thermal state is fair; current reclaimable memory is 25.32 GB, above its original 22.2 GB outer preflight. No model or compiler remains running while the Mac cools. Recovery remains unattempted. This is not a native correctness failure or permission to weaken the thermal check.

V409 is a draft corrected full-scope serving executor, not a materialized protocol or model run. It requires all seven exact V402 native cases. Its binding review reconstructs every original V377 workload, control, 32 first requests, 32 measured cells, work bound, 10 GB absolute cap and acceptance field after reversing only candidate/label/settling bindings; the original assessor is unchanged. It retains the full original 5,400-second work allowance plus at most 300 additional settling seconds for each of 32 cells, totaling 15,000 seconds work and 60 cleanup. Each cell retains its original cooldown and requires 60 sampled nominal seconds before launch. Original failed V377 capacity evidence remains intact; no previous row enters the new cohort. Additional execution/negative binding checks, full native prerequisites and a fresh complete interval remain required before freeze/launch.

**Explicit workspace allocation correction and vision headroom stop, September 8 (V401–V407):** The Generator now prices full expert workspace weights, possible workspace and decode-pool replacements, bounded managed staging, padded routed intermediates and retained outputs before dispatch in an isolated candidate. It retains ordinary compute/frontier allowances and assumes no allocator donation. Applying the three-file delta to exact V392, then reversing it, reconstructs that candidate byte-for-byte. V402 builds in 143.688209 seconds and restores all 12 shared source preimages and release/SwiftPM state exactly. Binary `b4c357ad94ae232c8a15fcca1cc39e1e833563326a0f0dddf6b7bfb2f9c50e35`. All 22,363 pure assertions, including 14 new geometry/refusal cases, and 116 CLI checks pass in 12.350820625 seconds. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-explicit-scope-allocation-build-and-pure-pass]]. These are build/pure results, not complete native or serving qualification.

V395 stops after 257.96958541699996 seconds before any model launch: its settling sample reads 17,962,188,800 bytes against the unchanged 18 GB minimum. One invalid reference cell contains no server PID or first request; there are zero completed requests, no new swap-outs, exact source/native/photo proofs and complete cleanup. Preserve this consumed attempt without retrying or pooling it. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-vision-settling-headroom-stop]]. Colima remains stopped.

After available memory recovers to 20.56 GB with nominal thermal conditions, the exact V402 native executor V404 starts under the original scope family envelope. Both scope-family cases now pass all 817 assertions: 4,096 tokens in 99.196854125 seconds and 2,051 in 53.954850167 seconds, with exact source proofs and completed cleanup. Lifecycle is running; direct MTP/vision is pending. V405 separately binds the original three ordinary/MTP/recovery integrations with unchanged workloads, 900-second work plus 30 cleanup, original live guard and conservative 22.2 GB outer admission. Those cases remain unrun. The earlier V392 native passes remain historical evidence, not V402 qualification. Direct runtime refusal before expert reads/state advance and actual default policy still require qualification before adoption.

**Latest correctness and resource status, September 8 (V399–V400):** The original 2,051-token scope family passes all 817 assertions in 54.23988945799999 seconds on exact V392, directly observing zero disposable MLX buffer cache and restoration of the caller’s limit. The same build passes all 22,349 pure assertions and 116 CLI checks in 12.096510291 seconds. Exact outputs, build binding and cleanup are preserved in [[sources/runs/2026/09/2026-09-08-optimization-scope-cache-correctness-and-memory-status]]. These are correctness results, not speed or memory-saving measurements. Family 4,096, lifecycle and direct MTP/vision scope remain unattempted; a fresh launch refuses at 17.86 GB against the unchanged 20.112 GB startup minimum.

After its prior container finishes, Colima has no containers, host Docker jobs or non-base guest workloads. It is stopped under the user’s conditional authorization at 22:52:42 UTC after a final empty-container check. Other apps remain open. Available memory subsequently settles at 18.105 GB with normal pressure, sufficient for V395’s independent original 18 GB preflight. V395 starts at 22:55:04 UTC with its full 12,600-second work and 60-second cleanup allowance fitting before 03:00 UTC. It is currently waiting through the original quiet-period and prospective thermal checks; no timing or memory benefit has qualified. No native or compiler runs alongside it. The previous request about stopping an active container no longer applies because that container finished on its own.

First-principles allocation review remains open: full-layer expert weights, possible scatter replacement storage, bounded staging and padded routed tiles need explicit dynamic pricing before automatic scope adoption. Actual staging uses managed aligned MLX buffers; do not charge the obsolete Swift-array-copy path. Any conservative bound must also account for decode-pool admission writes and must not assume allocation donation without verifying the actual pinned implementation. No untested accounting formula or automatic scope policy has been activated.

**Original recovery native passes, September 8 (V398):** All522read-recovery assertions pass in194.930963417seconds on exact V392, with unchanged proofs, completed cleanup and no remaining jobs. Combined with286ordinary and327MTP assertions, all1,135original integration assertions now pass on the cache-corrected build. Exact source: [[sources/runs/2026/09/2026-09-08-optimization-scope-cache-recovery-native-pass]]. V396scope-family-2051 is running under the original20.112GBstartup/17.112GBowned native family guard. Family4096, scope lifecycle and direct MTP/vision scope remain unrun. No10GBserving or performance result exists for this correction yet.

Colima is repeatedly restarted between independent container jobs; earlier empty-instance stops succeeded, and active containers were left alone. A brief gap between those jobs is no longer treated as an unused workflow. Existing resource/model-lock/cleanup guards still control every local run; no other app is closed. V395 remains frozen/unrun and every final gate remains open.

**Current native progression and corrected scope guard, September 8 (V395–V397):** Both complete integrated cases now pass on V392:286ordinary assertions in275.552433667seconds and327MTP assertions in334.229990291seconds, with unchanged source proofs and successful cleanup. Recovery initially refuses before any attempt at20.32GBreclaimable against its22.2GBadmission screen. After the Colima container finishes, the authorized empty-VM stop completes at22:19:58UTC; recovery then passes fresh admission and is running.

Before any scope case runs, review catches the generic ordinary10GBnative-owned envelope in V394's scope entries. Those four entries must remain unrun. V396 restores the actual original V364scope-native guards—20.112GBstartup,17.112GBowned tree,3GBlive,1800seconds work and30seconds cleanup—while preserving all four workloads and the new cache assertions. Five model-free checks pass, including direct comparison against the original executor's constants and environment. This does not change the independent10GBserving capacity gate. Existing V394ordinary/MTP results remain valid and are not repeated. Current V396scope cases are prepared but unrun. Exact native reports, binding correction and planner observations: [[sources/runs/2026/09/2026-09-08-optimization-scope-cache-native-progress-and-policy-binding]].

Actual read-only Doctor checks show a10GBdefault plan uses256-token compute and961slots,12GBuses512/1491, and16GBuses1024/2576.26/35GBrequests refuse under observed live availability. Original V377explicit256/no-prefix-cache serving uses1217slots. These are metadata observations, not inference measurements or a default-policy speed claim. The final supported geometry, prefix-retention/short-prompt guard and dynamic scope allocation accounting remain open.

V395 is now frozen and unrun. Nine model-free checks pass in36.035seconds, including actual vision-driver startup with model launches intercepted, all original acceptance/work/resource assessors, dependency drift and prelaunch refusal. Its first test invocation preceded protocol freeze and one dispatch test correctly failed on the absent protocol; that output is preserved, then unchanged tests pass against the actual frozen protocol. The new complete24-cell study reserves12600seconds work plus60cleanup, adds60sampled nominal seconds (at most300extra seconds) after each original60-second cooldown, and preserves the original12GBmechanism cap,200MBper-clean-pair saving,5percent median regression limit and startup-swap-in pair exclusions. Protocol `cdc2e9ccd822c2060266e0a1a158551d60808f1543f7342bcb29710cbf3a68c4`. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-vision-thermal-settling-preparation]].

**Scope cache correction build and first native pass, September 8 (V394):** V392 builds successfully in 145.825641916 seconds under the original serial build guard. All shared source, release artifacts and SwiftPM state restore exactly. Its binary is `dddd07d153d41160c40cb0cedf119e4a4ef372d5df69c95d7f45ae89311e7ac5`, source archive `07f8b4a0edf53ef39f08cff314f85ca5c6fe3846303df19dd37d3e82e5dc70fd`, with unchanged Metal. The three changed sources implement the cache disposal/restoration policy and added direct native lifecycle assertions. Exact source/build/preparation evidence: [[sources/runs/2026/09/2026-09-08-optimization-scope-buffer-cache-correction-build]].

V394 binds all seven original native cases on this exact build; six model-free binding tests pass. The first ordinary integration passes286assertions in275.552433667seconds, with exact source proofs and successful cleanup. Speculative integration is running; recovery and all four scope native cases remain unrun. These are correctness observations only. Scope performance, original10GBcapacity, automatic-policy qualification, vision mechanism, final composition and every original final gate remain required. A prospective24-cell vision successor with60sampled nominal seconds before each model launch and all added wait time reserved is being checked; it is not a model run or an acceptance waiver.

**Scope allocator correction and vision stop, September 8 (V391–V393):** V385 stops after 853.207719292 seconds and five of 24 original cells, with no clean complete pair. Startup swap-in-only contamination excludes round 1; round 2 has measured swap activity; round 3 reference reaches non-nominal generator thermal/power conditions and the original immediate resource stop fires. No new global swap-outs occur. Original source/native/photo proofs and cleanup pass. No speed or savings claim qualifies. Exact preserved attempt: [[sources/runs/2026/09/2026-09-08-optimization-vision-mechanism-thermal-stop]].

The actual pinned MLX allocator checks whether currently cached bytes are below its threshold before caching an entire freed buffer; setting a positive threshold does not evict existing cached allocations and can overshoot by one whole buffer. The isolated V391 correction sets the cache threshold to zero and explicitly clears disposable buffers only during expert-workspace forwards, restoring the prior limit on every exit. It preserves expert residency and all numerical shapes/operations. Added native assertions observe zero retained disposable buffers and test direct success, cancellation, checked expert-read error, state rollback and exact retry. The patch applies cleanly but is not activated. V392 compiles those three files together with the existing seven held V360 default/planner changes under the original guarded, exact-restoration build lease. It is running and has no compiled/native/capacity result yet. Actual dynamic scope allocation accounting, automatic planner policy, corrected serving and all final gates remain open.

Colima restarts after its successful empty-instance stops and has an active container by 21:59 UTC. It is left running under the user's conditional authorization. No active container or other app is stopped.

**Complete scope capacity failure and vision execution, September 8 (V389–V390):** V377 completes its full fixed cohort in 4,805.299075167 seconds. An independent read-only audit checks every original first/measured response: all 64 have identical prompt/output IDs and text, original per-arm work bounds, equal logical work, equal pool slots, 256-token compute and MTP off. Round 16 scope reaches 10,007,138,360 bytes, exceeding the unchanged 10,000,000,000-byte cap by 7,138,360 bytes. Its timing exclusion cannot excuse an absolute resource violation. Only measured pairs 1, 14 and 15 and startup pairs 1 and 15 qualify for timing, below the required five. No performance percentage is qualified. Cleanup and source/native proofs remain exact, with no remaining jobs and no new global swap-outs. Exact original artifacts and independent diagnosis: [[sources/runs/2026/09/2026-09-08-optimization-scope-serving-capacity-failure]].

V389's seven model-free preparation checks pass and the exact source is preserved in [[sources/runs/2026/09/2026-09-08-optimization-thermally-settled-scope-executor-preparation]]. It deliberately refuses a predecessor with a non-thermal failure. Consequently it remains unmaterialized, unfrozen and unrun after V377's capacity failure. Cooling alone is not a correction. Actual source inspection identifies a full 512-record layer workspace plus bounded staging, separately tiled MoE intermediates, retained scope outputs and the MLX buffer cache as allocations to reconcile. The public engine takes its chunk from MemoryPlan, which may exceed 256; the measured 256-token mechanism does not establish automatic-policy benefit. No empirical allowance is reduced or guessed.

V385 starts at 21:43:51 UTC after V377 drains and fresh original resource admission passes. The frozen original 12 pairs / 24 cells retain their native/photo prerequisites, 12 GB measured mechanism cap, 200 MB per-clean-pair saving and at most 5 percent median latency regression. Startup swap-in-only contamination excludes the entire pair prospectively. It is running, with no performance conclusion. The generic progress wrapper prints a hardcoded total of 32; the frozen protocol and actual driver still schedule 24. No in-flight source is edited. At 21:49 UTC Colima is observed restarted with zero running containers; the user-authorized stop completes at 21:49:51 UTC. Other apps are untouched. All final-composition gates remain open.

**Prospective thermal-settling preparation, September 8 (V388):** The original V377 comparison continues unchanged. Its fixed 60-second pauses sometimes end while Foundation still reports fair thermal state. An isolated future driver now supports a bounded sampled nominal-condition wait before each model launch, while holding the normal model lock and rechecking original startup headroom, normal pressure and competing work. Low-power mode, severe or unknown thermal state, malformed VM observations, changed swap-out counters and timeout stop before launch. Thirteen model-free methods pass under the actual repository Python 3.9.6 and Python 3.12.9, including real driver-entrypoint launch interception and lock cleanup. Three declared source changes reconstruct the shared driver exactly. The initial test failure and its correction preserve the original scope protocol's post-request exclusion handling; the new prelaunch wait independently stops its own failures. This is unbound preparation, not a new performance run or relaxation of any current acceptance gate. Future use needs a separately frozen complete cohort, exact source closure and enough declared time for the added waits. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-prelaunch-thermal-settling-preparation]].

**Final resource/client binding preparation, September 8 (V387):** A model-free preparer now binds the original four resource families and real Hermes/Ollama client extensions to a candidate campaign's exact binary, source and three recomputed native proofs. Execution revalidates those proofs and generated source before any model launch. Reversing only candidate, output and prerequisite bindings reconstructs every original wrapper/worker byte; resource sections, seven original assertions, client behaviors and all workload/acceptance/resource/time limits remain unchanged. Seven model-free methods pass, including binary/source drift refusals and an actual client-worker startup whose model launch is intercepted. The initial synthetic path-alias test failure remains preserved. The real final packet is not yet materialized or executed; final optional disposition and exact-composition proof still precede it. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-final-resource-client-binding-preparation]].

**Benchmark delivery correction and current scope execution, September 8 (V385–V386):** V383 stops after 4.411021209 seconds before loading any model: the isolated benchmark driver cannot copy a missing sibling helper during manifest preparation. The failed attempt and cleanup receipts remain unchanged. The corrected directory now contains the exact helper bytes that the driver imports. An actual-entrypoint test verifies manifest creation and source fingerprints and intercepts both model-launch calls, with zero real model launches. All six corrected binding checks pass. V385 binds the complete helper/policy/provenance closure and is frozen but unrun, protocol `4c1d5616f6ba4a9f1b7b3f73a5d1e089e370379bdb16b0b6697ec375769e322c`. Original acceptance and the prospective startup-contamination exclusion remain unchanged. Exact failure, correction and preparation: [[sources/runs/2026/09/2026-09-08-optimization-vision-driver-delivery-correction]].

V377 starts the complete original prefill/read-scope cohort at 20:18:47 UTC. It retains all 32 fixed measured cells and 32 first requests, the original workload, original guards and full time allowance. It is still running and has no qualified performance result. No other model runs concurrently. The user's explicit resource change permits continuing this work; the earlier elapsed peer window is historical. V385 follows after the current model process drains and fresh admission passes. All final-composition, lifetime, resource/client, sustained-TPS, calibration, delivery and activation gates remain open.

**Original 10 GB image capacity passes, September 8 (V384):** The original seven-cell study completes in 1,137.247891209 seconds with five qualifying cells (rounds 1, 2, 4, 6 and 7). Rounds 3 and 5 remain excluded for measured swap activity. The actual native 10 GB target processes the original 1,536-square image and 2,329-token prompt with query tile 256, producing the exact predeclared Red token and original mechanism counts. Qualifying sampled request peaks range from 7,970,754,712 to 8,058,425,664 bytes, below the original 10,000,000,000-byte ceiling. Full first/measured work and absolute cap checks remain intact; cleanup completes with no remaining jobs. The generic one-arm child exit 1 is retained while the dedicated original capacity assessor and outer qualification pass. This is capacity evidence only, not comparative speed or memory savings. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-vision-query-capacity-pass]].

V383's new complete paired mechanism study starts at 20:14:19 UTC with nominal thermal state and the additional live swap-out stop explicitly true. It retains the original fixed 24 cells and acceptance with the documented prospective startup-contamination exclusion. It is running and has no qualified result yet. V377's complete original scope comparison remains frozen and unrun. All final-composition, lifetime, resource/client, sustained-TPS, calibration, delivery and activation gates remain open.

**Large-image comparison stop and prospective startup handling, September 8 (V380–V383):** V369 stops after 531.303302416 seconds with three recorded cell entries: round 1 has two complete eligible measurements, and round 2 query has a completed first request but no measured response. The original large-pool warmup guard stops the study after global swap-ins; the global swap-out counter remains unchanged. The responsible process is unknown. One pair cannot qualify the original 24-cell study, and no speed or memory-saving percentage is promoted. Cleanup completes with no remaining jobs. Original evidence: [[sources/runs/2026/09/2026-09-08-optimization-vision-mechanism-startup-swap-stop]].

A prospective successor now classifies startup swap-in-only contamination separately: it completes both requests but excludes the entire affected pair, then continues the fixed cohort without replacement cells. It does not count contaminated requests as clean. Host and generator swap-out changes, missing or malformed counters, non-nominal thermal/power state, incomplete output and physical-limit failures cannot take this exception. All clean measured client and generator intervals still require unchanged swap counters. The original workload, 24 fixed cells, minimum five clean pairs, exact output/work, 200 MB peak saving in every clean pair and 5 percent maximum median response regression remain unchanged. Four bounded driver edits reconstruct the original shared source exactly when reversed. Eleven model-free policy checks pass. V382 then exposes the inherited outer live swap-out stop as disabled; its failed binding test and frozen, unrun preparation remain preserved. V383 explicitly enables that stronger immediate stop and passes all six binding checks. The distinct V383 cohort is frozen but unrun, with no old timing reuse or rescoring. This is an explicit prospective protocol amendment, not an original-study pass. Exact source, preparation and tests: [[sources/runs/2026/09/2026-09-08-optimization-vision-startup-swapin-policy]].

The separate original seven-cell, 10 GB capacity study is running on V369 under its unchanged protocol. Full native and full-photo correctness remain passed. Optional vision adoption, all final-composition gates and activation remain open.

**Original full-photo serving passes, September 8 (V379):** All 25 original photo-serving assertions pass in 88.666419625 seconds on exact V304, using the unchanged photographs, 14.5 GB target, 3,072-token prefill reservation and query tile 256. The executed encoding responses expose the expected query-tile mechanism. Original source, fixture, command, memory and cleanup guards remain intact; the qualified receipt records successful cleanup and no remaining jobs. This is correctness evidence only, not a speed or capacity result. The original 12-pair maximum-image mechanism protocol is now frozen and running; its original capacity study remains unrun. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-resumed-vision-photo-pass]].

**Current resource resumption and complete vision native results, September 8 (V378):** The user closed Android, Wispr Flow and Chrome and explicitly authorized stopping Colima if unused. The live Docker check found no running containers; Colima stopped successfully. This current user direction supersedes the earlier elapsed peer window. Both complete V368 integrations now pass their original workloads and guards on exact V304: ordinary 270 assertions in 242.807003 seconds and MTP 284 assertions in 329.519084833 seconds. Both executions record successful cleanup and no remaining jobs. The three earlier component executions retain 157 assertions and eight exact whole-tower geometries; they are reused evidence, not reruns. These are correctness results, not serving speed, memory-saving or final V360 composition evidence. The full-photo 25-assertion original profile is now running, one model process at a time. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-resumed-vision-native-pass]].

**Fresh full scope serving preparation, September 8 (V377):** The substantial resource change justifies a new complete original 32-cell V170 study, preserving the exact V304 build, all 2,698 native assertions, original workload/controls, live guards and 5,400-second work plus 60-second cleanup allowance. Restoring only its two provenance fields reconstructs the earlier protocol exactly. The original execution and assessment bodies are reused. Seven model-free checks pass, including changed-proof/dependency refusal and no attempted output on prelaunch resource refusal. The new protocol is frozen but unrun. No old timing row is reused, replaced or continued; the earlier 29-cell pressure stop remains preserved and unqualified. Scope stays out of defaults pending valid benefit, its conditional short guard and final composition qualification. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-scope-serving-resource-resume-preparation]].

**Exact current activation preparation, September 8 (V376):** The stale V355 review artifact is superseded by a new unapplied patch for the existing corrected V360 build. All 150 archived source inputs match the recorded build identity; 143 already match the shared workspace and the same seven deployment changes remain. The real workspace `git apply --check` passes. Compact BF16 cache rows are selected, fixed MTP verification is retained, and no scope/image/vision experiment is added. Patch SHA-256: `ca495bdb672e26633d022411fd69659bfc4a2561ff27ee008a6799a42b011e7d`. All shared preimages remain unchanged. This preparation launches no model, compiler or server and performs no activation. It retains every original final-native/eight-paired/lifetime/resource/client/long-decode/calibration/delivery gate and all 37 explicit dispositions. Exact source, patch and checks: [[sources/runs/2026/09/2026-09-08-optimization-fixed-mtp-activation-preparation]].

Cleanup review preserves both observed PermissionError diagnostics and the later independent drain proofs. The underlying cause of those signal errors is not established by the records; no permission denial is bypassed, no failed run is rescored and no frozen guard is edited. Before any later model work, the original ownership, model-lock, full-interval and resource checks still apply.

**Original unique-image guard pressure stop and early handback, September 8 (V371/V375):** The unchanged original guard stops at OS memory pressure level 2 after 248.649103291 seconds, before any complete first request or measured cell. Fresh original admission had passed; all original controls, 3,300-second work allowance and 30-second cleanup allowance remain unchanged. There is no performance estimate, completed cohort, replacement or continuation. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-unique-image-serving-pressure-stop]]. This does not alter the separate completed duplicate-image speed-gate rejection; image reuse stays out of default selection.

The original PermissionError cleanup diagnostics remain preserved. Independent verification at 17:14:27 UTC finds groups 85060 and 89379 empty, the model lock free, no competing jobs, normal pressure, nominal thermal state and 18.357551104 GB reclaimable. All owned model sessions are terminal. The Mac reservation was returned early and the other task acknowledged taking its emulator validation window. There is no later model/build interval granted. Only lightweight source/evidence work continues until a new resource window is coordinated. The two interrupted studies neither qualify a speed/memory claim nor change the original native passes or failed-feature dispositions. Chrome, Docker Desktop and user apps remain open.

**Original scope serving pressure stop, September 8 (V364/V374):** The unchanged original V170 cohort stops at OS memory pressure level 2 after 4,552.139056334 seconds, recording 30 complete first requests and 29/32 measured cells. All 14 complete measured pairs have exact token IDs and text; all recorded measured requests emit 16 outputs and pass the original work checks. Only round 1 is request-timing eligible; thermal and VM exclusions remain preserved. The 15th scope measurement is interrupted and both 16th-round measured cells are unrun. There is no complete cohort, no replacement or continuation, and no qualified preview, active-TPS or memory-saving percentage. Scope stays out of default selection pending valid serving/resource qualification; the original short guard and policy remain conditional on a qualified benefit. Exact evidence: [[sources/runs/2026/09/2026-09-08-optimization-scope-serving-pressure-stop]].

The guard’s PermissionError cleanup diagnostics remain recorded. A later independent observation at 17:01:26 UTC finds owned groups 70341 and 73045 empty, the model lock free, no competing jobs, nominal thermal state, normal memory pressure and 20.088291328 GB reclaimable. This later drain proof does not erase the original cleanup errors or pressure failure. The existing Mac reservation ends at 18:15 UTC and covers only the original scope and unique-image studies with their full original allowances; the requested later vision interval was not granted because another task needs Mac emulator validation. The unique-image guard may launch only with fresh original admission and its complete 3,330-second allowance. User apps remain open.

**Compiled default identity check:** Inspection of V360’s existing archived build source confirms that its compiled `environment()` resolves `deploymentCandidate()`, with compact cache rows selected and bounded MTP tail disabled. Its temporary build lease already included the intended deployment settings before restoring the shared source. If optional decisions do not change this composition, the final matrix can qualify that exact existing binary; a second untested default flip is not required. The installed release and shared defaults remain unchanged. All eight final paired workloads are still unrun on V360; older V304 serving percentages are historical workload-specific observations.

**Completed duplicate-image comparison, September 8 (V371/V373):** The original fixed cohort completes all 32 measured cells and 32 first requests in 2,802.5069 seconds. Exact output and work checks pass. Round 5 reference has measured swap activity and its pair is excluded without replacement. On 15 eligible request pairs, median request reduction is 1.3207396304 percent and preview is 1.3222807137 percent earlier; only 8 of 15 pairs improve. Both the original 5 percent median and 80 percent positive-pair criteria fail. Startup non-regression passes on its original 13-pair cohort, with 0.4737875090 percent median first-job reduction. Sampled process peak is 0.0709608814 percent higher on the 15 request pairs, while active tensor allocation is 0.0205646205 percent lower. One-output requests do not measure TPS. These are descriptive measurements of a rejected optional optimization, not final V360 performance. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-duplicate-image-serving-rejected]].

The original assessor recomputes exactly from raw records; no error or resource stop truncated the cohort. Child exit 1 records the failed speed gate. Independent 15:31:11 UTC checks find both owned groups empty, the model lock free, no competing jobs, nominal thermal state and normal pressure. Reclaimable memory has recovered to 26.080 GB. Image reuse stays out of default selection; the original unique-image guard remains queued and unattempted.

The coordinated window now extends through 18:15 UTC for the original scope and unique-image studies, one at a time. Their entire 5,460- and 3,330-second allowances must fit independently; scope must start by 16:44 UTC. No build or activation is included. With larger headroom now observed, original scope serving is next after fresh unchanged admission. All user apps remain open.

**Original image comparisons ready, September 8 (V370–V372):** V371 now freezes both original image-reuse studies against the unchanged V304 76-assertion native proof. Each retains 16 fixed pairs, 32 measured cells and 32 first requests, one output, original photographs and exact work/output checks, 10 GB request/warmup caps, 13 GB admission, 12 GB owned and 3 GB live guards, and 3,300 seconds work plus 30 seconds cleanup. The original V330 interrupted duplicate study retains zero measured cells; no result is relabeled or pooled. Nine model-free methods pass. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-resumed-image-serving-preparation]].

V370's initial model-free imports refused the different /tmp and /private/tmp spellings of the same source dependency. The unrun preparation, frozen protocols and eight setup errors remain preserved. V371 normalizes dependency keys and binds distinct output/label identities; original execution and acceptance code remain unchanged. The same one-line correction is applied to the still-unprepared V367 campaign adapter, with all 17 model-free methods passing. Original V367 source is preserved in its earlier capture. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-campaign-path-normalization]].

The scope study remains frozen and unattempted: nominal thermal state and normal pressure do not replace its unchanged 20.112 GB admission requirement. Observed reclaimable memory has fallen to 17.734 GB after other heavy work drained. The shared interval through 16:45 UTC now also explicitly covers the two original image comparisons while scope admission remains unavailable. Each complete 3,330-second interval must fit; no build or activation is included. The duplicate-image comparison is next after fresh original admission. All user apps remain open.

**Resumed image qualification preparation, September 8 (V368–V369):** V368 preserves the three original passed vision components with 157 assertions and eight byte-exact whole-tower geometry comparisons, the earlier ordinary integration pressure failure, and the original unattempted MTP identity. Its two new prospective full integration cases retain the complete original commands, 900-second work plus 30-second cleanup, and 13/10/3 GB live policy. The additional 22.2 GB prelaunch screen is conservative; no live threshold is relaxed. Eight model-free methods pass. Both model runs remain unattempted and need a separate native interval after scope. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-resumed-vision-native-preparation]].

V369 redirects only the original V334 output path, native helper, original/resumed output lookup and label. Reversing those four changes reconstructs all original source bytes. The full-photo profile retains 25 assertions over the unchanged photograph inventory, 14.5 GB, 3,072-token prefill and query tile 256; the original V159 mechanism and V160 capacity protocols retain all work, limits and acceptance. Here, 25 counts serving assertions. Seven original protocol tests and two original full-photo assessor tests pass. No real native integration, full-photo request, maximum-image study or serving protocol freeze has occurred under these new bindings. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-resumed-vision-serving-preparation]].

The competing task has now explicitly reported all heavy compilation and qualification processes drained. Scope serving is next, still subject to the original nominal thermal/normal pressure checks and full 5,460-second interval fitting before 16:45 UTC. No model process is currently owned by this task.

**Latest final-campaign harness work, September 8 (V367):** The model-free adapter now binds V360's exact 1,135 native assertions to the original eight paired workload contracts with compact rows selected and the rejected tail option disabled. It preserves original native bytes, original command/environment/build and guard proofs, and explicitly identifies derived campaign manifests as imports of one prior execution. The untouched serving prerequisite checker accepts the imported proof. A private module instance substitutes only the exact V363 control validator; the original campaign assessor and execution function remain unchanged. All 16 model-free tests pass, covering altered or failing evidence, stale builds, control changes, missing new assertions, incomplete cleanup, insufficient reservation, prohibited retries, legacy lifetimes and signal/environment restoration. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-fixed-mtp-campaign-adapter]]. No real campaign is prepared or launched; the final composition and all eight measurements remain open.

The shared Mac benchmark interval is now explicitly coordinated through 16:45 UTC, contingent on the other heavy local checks draining and the original admission checks passing. The full original scope allowance of 5,400 seconds plus 60 seconds cleanup must fit, so no scope serving launch may occur after 15:14 UTC under this grant. All four scope native proofs already pass and have drained. No build or activation is included; user apps remain open.

**Latest original scope native qualification, September 8 (V364/V366):** All four original cases pass on exact frozen V304: 812 assertions at 2,051 tokens, 812 at 4,096 tokens, 832 lifecycle assertions and 242 MTP/image rollback assertions. The final case includes exact next-target logits after rollback. The full 2,698 assertions, original work allowances, memory guards, source family and earlier failed identities are preserved. Independent cleanup at 14:05:44 UTC confirms all four process groups empty and the model lock free. These results qualify the original scope mechanism for its next serving test; they do not qualify a later final composition or real-photo quality. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-resumed-scope-native-pass]].

The original 32-cell V170 long-prefill serving study is frozen with protocol SHA-256 84020528a4746632a745cd6768dca82c3826455663e44570322861da89522fe5. The 3,730-token code fixture, 16 outputs, 16 fixed pairs, 32 first requests, 10 GB target, 256-token compute schedule, 4,096-token read scope, 1,024-token routed tile, original resource/work checks and acceptance thresholds remain unchanged. Its full allowance is 5,400 seconds plus 60 seconds cleanup. At this record, serving is unrun and awaits an explicitly coordinated benchmark interval after other heavy local checks drain. The scope-native grant alone does not authorize benchmark use of the shared window. User apps remain open; no candidate default is activated.

**Latest completed native proof and corrected measurement preparation, September 8 (V361–V365):** V361 passes every original ordinary, MTP and recovery case on exact V360, totaling 1,135 assertions. Restoring fixed verification shapes resolves all six prior output failures, including the short-output/longer-prefix equality checks. All source proofs, full allowances and cleanup checks pass, with no new swap-outs. Independent checks find all owned groups empty and the model lock free before the next stage; shared source and release artifacts still match the restoration record. Native run duration and sampled resource observations are not paired speed or memory-saving claims. Full raw evidence: [[sources/runs/2026/09/2026-09-08-optimization-fixed-mtp-native-pass]]. The default disposition is recorded in [[records/decisions/bounded-mtp-tail-excluded-from-combined]].

V362 refreshes all 33 pure checks with 22,349 assertions and all 116 CLI checks on exact V360 in 11.3426 seconds. The one-assertion net reduction reflects removing two selected-tail override checks and adding the fixed-shape guard; no output-equality case is dropped. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-fixed-mtp-metadata-pass]].

V363 prepares the complete original eight paired and two 512-output drafts with NG1/TAIL0 in explicit combined arms, both0 in reference, and the actual-default arm unforced. Seven model-free tests pass, including rejection of re-enabling the failed tail option. The new V365 long runner binds these controls and the V361 native assessor while preserving all original workloads, memory/ownership limits, full work/cleanup allowances, the V340 reporter and V330 execute body. All 20 model-free methods and an actual old-campaign CLI refusal pass. No final campaign, long binding, paired or long execution is created; remaining optional disposition and all eight exact paired proofs remain mandatory. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-fixed-mtp-validation-controls]] and [[sources/runs/2026/09/2026-09-08-optimization-fixed-mtp-long-decode-executor]].

The corrected-build native batch drained at 13:37:38 UTC. A separate explicit scope-native interval through 16:45 UTC covers only the original four prefill numerical/lifecycle/MTP-image cases on frozen V304, each with the original 1,800-second work and 30-second cleanup allowance and 20.112/17.112/3 GB startup/owned/live guards. This successor preserves the earlier interrupted V331 identity. Thermal admission refusals before any attempt remain separate from executed results; neither changes the original readiness gate. No benchmark, build or activation is included in that interval. User apps remain open.

**Earlier native counterexample and correction, September 8 (V358–V361):** V358 is a new prospective ordinary-native attempt after the original V350 pressure interruption and the later resource change. It passes all 286 assertions in 236.6891 seconds on exact V349. The originally unrun V350 MTP command completes all 327 assertions in 295.8704 seconds with six failures: four final tokens differ across complete eight-output reference/candidate requests, and a two-output candidate returns [33, 461] instead of both the reference and its longer prefix [33, 428]. The separate original recovery case passes all 522 assertions in 172.0091 seconds. No new swap-outs occur; ownership, complete allowance, build proofs and cleanup checks remain intact. These are native correctness results, not speed or memory-saving measurements. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-compact-tail-native-counterexample]].

The failed bounded-tail composition is excluded from activation. V343's earlier isolated two-output fixture remains a qualified measurement of that fixture only; its percentages are neither erased nor generalized to these failing prompts. Greedy selection does not make a changed verification batch numerically invisible. V359 removes boundedDraftTail from integrationCandidate, preserves compact BF16 rows and every exact-output case, and requires both arms to retain the original terminal one-draft/two-target schedule. The experimental flag remains explicit-only. Generate.swift changes only its explanation; no arithmetic, sampling or context rule is replaced. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-mtp-verification-shape-correction]].

V360 compiles all 150 exact source inputs in 137.5207 seconds. Only those four correction files differ from V349; the seven held default/planner changes remain confined to the temporary lease. Shared source, six release artifacts, release alias and mutable first-party SwiftPM state restore exactly. The owned compiler group is independently empty and the model lock free. Binary SHA-256 is 7a132a2fc2270d8f3695e7d740abbebd22ac0d79b80562c7c47aa73a143e24ea; source archive 31f5718f308621dbbac08fe2994e9deb8691de28a1e61fd551857a00c5af5be2. Metal is unchanged. V355's activation patch and V352/V357 tail-on drafts/runner remain preserved historical preparations and require corrected final binding. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-compact-cache-fixed-mtp-build]].

V361 binds all three original native commands to V360. Its original 900-second work and 30-second cleanup allowance and live guard are unchanged, with the additional conservative 22.2 GB admission screen. Complete assertion minima are 286 ordinary, 327 MTP and 522 recovery. Failed original MTP evidence and shortened coverage cannot qualify. MTP was checked first because it directly tests the correction; all three suites subsequently passed as recorded above. That earlier explicit Mac grant ran through 14:35 UTC for this one serial build and these complete native suites; no benchmark or activation is authorized by that interval. User apps remain open.

**Earlier executor work and resource boundary, September 8 (V357):** The guarded runner for both original 512-output measurements is implemented and passes 20 model-free methods. An actual CLI check refuses the older V304 campaign. The runner reconstructs both V352 long drafts exactly and preserves all original workload, memory, timing, cooldown, replacement and ownership bounds. It recomputes all three native and all eight paired prerequisites for the exact candidate, including the new cache and tail assertions, before freezing. The existing V330 execution function and explicitly bound V341 failure observer preserve signal/environment restoration, owned-process cleanup and failed-attempt evidence. The original V340 reporter remains byte-identical. No study binding or model execution exists. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-long-decoding-guarded-executor]].

The tests cover changed builds/controls/budgets, stale or failing raw proofs, missing new assertions, insufficient reservation, memory refusal before claiming an attempt, prohibited retries, early EOS, incomplete intervals/first jobs, wrong output in excluded cells, insufficient clean pairs, altered acceptance and cleanup. A further conservative check applies the existing five-percent request bound to the common eligible first-job/measured cohort; excluded first jobs cannot mask its regression. Synthetic test percentages are harness fixtures and never inference evidence. The executor SHA-256 is 210e01cfbc9530a9f3ec8fbe277be51adf9dac6ca382e99ab2e8da1ac6cabf14.

Historical 05:21 UTC boundary before the later user resume: Current reclaimable memory is 15,475,294,208 bytes, below the additional 22.2 GB screen for the next full-native attempt. OS pressure is normal and no competing model/build job is observed, but the peer retains the Mac reservation and has granted no further heavy interval. No owned test process is live. The preceding goal turn made progress through V353–V356; this turn closes the independent long-runner implementation gap. Neither clears the same live-native blocker. Remaining option decisions and final-composition qualification depend on actual model evidence; more normal-pressure polling or a new plan cannot supply it. Preserve the whole queue and resume only after a meaningful resource change, a fresh explicit grant and a new prospective identity for interrupted ordinary native. All user apps stay open; no threshold is relaxed, prior run replaced, default enabled or binary installed.

**Earlier acceptance, September 8 (V353–V356):** V353 completes all 33 T0 checks with 22,350 assertions and all 116 CLI/context metadata and pre-allocation checks in 11.6695 seconds on exact V349. The four additional assertions cover zero-disable and one-restore for the two newly selected flags. This command set creates no Engine and runs no model or GPU tier. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-compact-tail-metadata-pass]].

V354 compiles and runs the unchanged external public-library consumer from all 150 exact archived V349 source inputs. It passes public API/constructor/function-value checks and 22 diagnostic assertions in 253.4813 seconds. The unchanged public planner preflight rejects the forced MTP/vision lifetime profile at 10 GB and accepts the original corrected 12 GB profile; nominal minimum is 10.6 GB. These are planning and interface checks, with no model loaded and no empirical capacity or throughput claim. The consumer's printed 48-second estimate is not a measured inference result. All archived source bytes remain exact. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-compact-tail-external-consumer]].

V356 passes the complete static acceptance suite in 294.0524 seconds, including the original small C/Swift transport-fixture compiles. Candidate and frozen drivers remain unchanged. Maximum owned RSS is 1,239,728,128 bytes; minimum observed reclaimable memory is 15,609,233,408 bytes, with no new swap-outs. No model or GPU test runs. Independent checks confirm an empty owned process group, a free model lock and no competing jobs; the static interval is explicitly returned early at 04:52:20 UTC. The full raw output is preserved exactly. An initial capture verifier normalized three carriage returns while reading stderr; verification through exact byte decoding subsequently reconstructs all nine original artifacts. The immutable source needed no change. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-compact-tail-complete-static]].

V355 prepares an exact, unapplied seven-file activation patch for this compiled candidate. It accounts for all 150 source inputs, with 143 already matching, and passes git apply --check without modifying shared source. Patch SHA-256 is 6ec23041fac16bf10de27e8eec122f79a1f7e964ee5215aa2c7176455ee29c91. It replaces stale V324 preimages for review only; every remaining native, paired, lifetime, resource, client, calibration and delivery gate still precedes activation, and the 512-output measurements remain explicit. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-compact-tail-activation-preparation]].

At the 04:59:55 UTC read-only admission check, reclaimable memory is 17,274,437,632 bytes, below the additional 22.2 GB screen for a new full-native attempt. OS pressure is normal and no competing job is observed, but no new heavy interval is granted; the peer explicitly retains the Mac reservation. No model, build or test process is currently running. Keep all user apps open. The same native resource blocker persists despite this turn's useful non-model qualification progress; it is not reset by unrelated passing checks.



**Earlier implementation and measurements, September 8 (V343/V347–V352):** The fresh, prospectively frozen V343 greedy cache-carryover study completes32 measured cells plus32 first requests and qualifies all16 pairs. Median paired repeated-request time falls11.8760percent and first-job time5.1830percent. Initial prefill is3383records in every fresh process; subsequent3380reference versus3359candidate records are the declared treatment outcome. All outputs, original neural-work bounds, pool and resource/startup criteria pass. No V338 timing is reused or rescored; its original greedy primary failure and sampled wrapper error remain unchanged. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-fixed-tail-cache-carryover-qualified]].

The unchanged reporter applied to the exact16-pair cohort finds visible text11.6685percent earlier and sampled process footprint1.6171percent lower. The active-emission rate rises58.7401percent over the single interval in this two-output request. That is terminal short-request work, not sustained TPS, and none of these optional-study percentages is added to the earlier combined-engine results. Active tensor allocation is0.0056percent higher. The512-output ordinary and fixed-depth-one MTP measurements remain required.

V347 selects boundedDraftTail in the explicit integrationCandidate alongside V342 compact rows. Runtime diagnostics retain override/reference restoration, and integrated diagnostics cover actual selection, cache format/payload, exact two-output terminal work and sampled work/replay. V344 narrows the cancelled-prefill state's diagnostic lifetime without removing cases or changing geometry. The new three-file composition compiles as V349 in138.8631seconds; binary7fb2cc0c6cccf93e8a589d02ce5aa839eea38140a1cb8f44cefeb10ec3f472f6, source archiveff2a4c967bd785261e792d88c63a2d2f71d875e28429c136c258be7e4d58193e. All150source inputs are bound. Shared source, release artifacts/symlink and compiler state restore exactly; no new swap-outs occur. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-bounded-tail-candidate-integration]] and [[sources/runs/2026/09/2026-09-08-optimization-compact-tail-combined-build]].

The single V350 ordinary native attempt stops after41.6512seconds at OS pressure level2 in the reference long1025 case, before images or the candidate arm. Standard output is empty; no complete native assertion report exists. V341's explicitly bound observer captures the owned process at7,202,034,152bytes physical footprint versus1,674,706,944bytes resident size. The guard's later pressure error is preserved; the independent04:25:17UTC check confirms group55473empty, the model lock free and no competing jobs. The unused interval is explicitly returned. A successful build is not full-model correctness, and the diagnostic lifetime change has not resolved the resource stop. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-compact-tail-native-pressure-stop]].

The earlier V304 ordinary native pass began with22.1897GB reclaimable, compared with15.5589GB here. This is descriptive context across different builds, not causal proof or a guaranteed threshold. A future full native launch will additionally require at least22.2GBreclaimable, a meaningful resource change and a fresh explicit Mac grant, while preserving every original live guard. The interrupted V350 identity cannot retry. No grant is currently held; keep all user apps open. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-native-headroom-comparison]].

V352 prepares all eight paired drafts for V349: only the two selected controls change to1 in explicit combined arms, reference stays0, and the actual-default arm stays empty. Reverting exactly those controls reconstructs the original frozen templates. Both512-output long drafts preserve this family; MTP explicitly fixes depth1, matching the actual Generator default, without altering the matrix. Six model-free tests pass. The initial preparation failure/partial drafts remain preserved. These are unrun drafts; exact native proofs, remaining optional dispositions, guarded executor and resource availability are still required. Both60-request lifetimes retain their original corrected capacity profile and all-eight-paired prerequisite. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-compact-tail-validation-controls]].

**Earlier original studies, September 8 (V345):** A fresh explicit peer handback at01:35UTC and an extension through03:30 enabled both original V338 MTP serving studies. Together they completed64 measured cells and64 first requests, all resource-eligible with exact outputs. The sampled guard passes its original request and first-job criteria on16 pairs. The original wrapper still records a postprocessing KeyError: it supplied an empty dictionary for absent optional work constraints. A separate V345 assessor substitutes the original benchmark parser for that one expression; all other checks, data and thresholds remain unchanged. Five reporting tests pass, including reproduction of the error and preservation of the two-output rejection. No inference reran for the correction. Full original artifacts, including all source archives, are embedded once per hash and independently reconstructed byte-for-byte. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-original-fixed-mtp-serving]].

The greedy two-output study still supplies no qualified primary speed claim: its16 original pairs are excluded because following-request prefillRecords is3380 in reference and3359 in candidate. Initial prefill work is3383 in both arms, while the declared optimization removes one draft and one unused target position. Outputs and required neural work pass. Its separate first-job gate passes16 pairs with6.3371percent less median paired first-job time; that does not replace primary acceptance or establish steady active TPS. Sampled fallback work is identical between arms; its0.1051percent later client time and0.3745percent earlier first-job time pass the original5percent non-regression bounds and are not an added speed claim.

V343 initially froze a distinct, unrun cache-carryover study, subsequently executed and qualified above. It keeps the original inputs, outputs, pool, controls, work bounds,5percent benefit/80percent positive-pair threshold,32 cells and resource/time guards. Subsequent read counts are treatment outcomes; every first-job prefill must instead have equal positive record counts, and original neural-work/adaptive exclusions apply to both first and measured requests. No original two-output timing is reused. Seven model-free tests pass after binding the separately corrected sampled guard. Protocol55597fca87c8c73d4f3bb3e922bfc5ed3d17c2af36c6e2e8b49e3acc022abfd4. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-mtp-cache-carryover-preparation]].

V344 narrows the integrated diagnostic's cancelled-prefill phase into a helper that returns only state/logit hashes before independent full generation. Original cases and assertions remain; the progress callback also clears on early exit. This change was source-only at capture and is now compiled in V349. It has no measured memory benefit; V350 still stopped on OS pressure. V304 and all its earlier results remain unchanged. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-native-state-lifetime]].

After the03:01:54UTC return, the initial04:15grant refused V343 admission before an attempt because its complete3330second allowance did not fit. An explicit extension to04:25enabled that single study, which passed and returned early at04:01:07. Separate build and ordinary-native grants then enabled V349 and V350, with independent handbacks at04:19:24and04:25:17respectively. No grant remains. User apps stayed open, and no default, installation, Git commit or release activation occurred.

**Earlier resource boundary, September 8 (V339):** At that checkpoint heavy qualification was paused after three consecutive OS pressure-warning stops. The unused interval through 02:30 UTC was explicitly returned early around 00:35 UTC; no Mac-heavy grant was then held. All owned process groups are now confirmed empty and the model lock is free. Keep user apps open. A resource-availability question is pending; a later normal pressure reading by itself does not justify repeating the interrupted studies. No runtime/default/installation activation has occurred.

Completed on exact V304: the three original vision components pass 157 assertions and eight byte-exact full-tower comparisons; fixed MTP work passes 284 assertions; image reuse passes 76; the complete current static suite passes after all recent harness fixes. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-current-vision-mtp-static-gates]]. Adaptive policy remains rejected: one of its 259 assertions fails and its full report stays failed. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-current-adaptive-policy-counterexample]].

V338 resolves the incorrect fixed-tail dependency without changing any report result or original serving criterion. Its two original MTP studies now bind the separately passing 284 fixed-work and 258 lifecycle observations, while retaining the adaptive-policy rejection and requiring that policy to be disabled both in controls and actual warmup/measured traces. Five model-free methods pass; both original studies subsequently execute once, with their distinct outcomes recorded above under V345. The sole excluded assertion concerns the rejected adaptive policy's target-only output agreement, not a fixed-tail lifecycle invariant. Any lifecycle failure still blocks the studies. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-fixed-mtp-lifecycle-dependency]].

The full-model vision integration and first 2051-token scope native attempts were interrupted before an assertion report completed. The original duplicate-image study then stopped after its first reference warmup, with zero measured cells, four swap-ins and no eligible comparison. All three remain unqualified and cannot retry under their existing frozen identities. Original cleanup signal errors are retained alongside the later independent empty-group/model-lock verification. Source inspection confirms uncached expert reads are already implemented and does not establish a new allocation bug from these aggregate observations. Remaining scope, image, lifetime, resource, client, calibration and final-composition gates remain open. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-native-memory-pressure-stops]] and [[sources/runs/2026/09/2026-09-08-optimization-image-serving-pressure-stop]].

V340 prepares the missing longer active-decoding measurement: a 512-output prose fixture, final-control-preserving protocol materializer, and reporter with 13 passing model-free methods. The two future modes keep explicit 10 GB plain and 12 GB fixed-depth-one MTP targets. The reporter measures all 511 emission intervals and the later 384 intervals separately, requires eight clean pairs from 16 fixed pairs, and cannot use token windows as independent replication or turn a terminal-forward saving into active TPS. Both studies are unrun and still need the final candidate/proofs/executor binding and a safe interval. This closes a measurement-tool gap, not inference qualification. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-long-decode-measurement-preparation]].

V341 adds a bounded failure-time observation of the owned root process and descendants using Darwin physical-footprint counters separately from RSS. Thirteen model-free methods and an actual small-child cleanup fixture pass. The separate guarded-run copy preserves the original guard byte-for-byte except for identity binding and failure capture; original policy, stop and cleanup remain intact. It performs no model run, cannot qualify earlier pressure failures, and must be explicitly bound before a future execution. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-failure-footprint-observer]].

V342 implements the already selected compact BF16 row cache in the next explicit integrationCandidate and includes its flag in existing override/reference-restoration checks. The integrated native cases now verify actual cache format, populated rows and exact two-byte-versus-four-byte payload accounting alongside the existing state/logit/output/MTP/lifecycle assertions. Those three files initially had source/diff checks only; V349 now compiles their final V347 form, while V350 native qualification remains incomplete after OS pressure. Shared/reference automatic defaults and installed binaries are unchanged. V304 remains the evidence-bearing old binary for its frozen optional studies. The historical V324 activation patch is retained with stale preimages. V355 now supplies an exact unapplied replacement for V349; any later candidate change requires another exact-source preparation before qualified activation. Evidence: [[sources/runs/2026/09/2026-09-08-optimization-compact-cache-candidate-integration]].

**Current execution boundary, 2026-09-08:** Implementation and qualification remain
in progress. Shared automatic optimization and embedding defaults remain off;
shared release remains V215. The current source includes the context task's
unchanged PlannerDevice extraction, context-test target and V279's strengthened
read-failure diagnostic. All prior failed runs and the full 37-item inventory
remain preserved.

The historical V304 build and all its existing results remain immutable; V349 above is the current compiled candidate. Historical V304 binary
`af5ff036d03e85210ed40fd3ba02207ecc935d44dc67264cd44a05eb70d2632d`, source archive
`c823ca060a2ad4dae68616c74a7c20b109b912a2533577f94c1900f206477935`, Metal
`198488eb61359e953580a9c4530400feee1a06dd2f28a930a6ffa58aec66a597`.
Native Swift measurements justify grouping the two retained-window evaluations
in LinearCache.compactWindows. The independent contiguous copies and synchronous
materialization remain; one eval now completes both present windows. This is the
only runtime change from V280, with no arithmetic, kernel, planner or public API
change. The 137.456063250-second build passes and restores the temporary seven-file
lease, shared release/symlink and first-party build state exactly. The intentional
Layers.swift implementation remains in shared source; shared defaults remain off.

V307 completes the original V305 recovery prerequisite: all 522 assertions pass
in 194.306982958 seconds. The current build now has all three native prerequisites
passed, including the earlier 242 ordinary and 256 MTP assertions. Current metadata
passes 22,346 T0 and 116 CLI checks; the complete static gate and all 363 additional
component assertions pass. Five real Ollama/SDK/Hermes client paths pass, as do
74 API and four typed context-limit error checks. Both actual portable fallback
suites pass (242 ordinary, 256 MTP), with shared RoPE and zero fused dispatch.
These are exact-build correctness results; the portable run does not qualify
another chip or provide inference timing. API VM activity is preserved separately.

The first current-build paired study passes all 32 fixed cells and 16 clean request
pairs. At the original 8.1 GB / 17-input / one-output-token profile, median paired
completion time falls 15.3310%, sampled process peak falls 6.2496%, and active
allocation falls 6.6791%. First protocol delta is 0.5246% later and prefill is
0.5803% slower. Fifteen eligible startup pairs show 8.7218% less first-job time;
round 13 remains startup-excluded. Whitespace token 271 supplies no visible-preview,
active-emission or decode-phase TPS estimate. Percentages are medians of per-pair
changes and are not ratios of arm medians. No V280 percentage is relabeled V304.

The original campaign has three native and one paired stage qualified. Its prose
stage completes all 32 cells and 32 warmups in 2,897.8931 seconds. The original
raw-execution validator passes every identity, configuration, output, work and
warmup/measured physical-ceiling check. All 16 pairs have identical 16-token IDs
and wire text. Measured peak is at most 7,416,254,640 bytes and warmup peak at most
7,381,569,568 bytes against 8.1 GB. Eighteen measured cells report Foundation
thermalState fair; five have VM activity, overlapping three thermal exclusions.
Only four request pairs (11,12,15,16) and three startup pairs (11,15,16) qualify,
below the required five each. That original prose cohort qualifies no preview,
active-TPS or memory-saving percentage. Aggregate counters identify no responsible process.
The failed attempt remains immutable. Original run-one refuses to retry it; six
paired studies remain unrun in that original packet. V320 is one distinct fixed confirmation.
Its qualification contract is byte-identical to V305. Only the three qualified
native stages and short-one proof are copied byte-for-byte with explicit original
path/hash provenance and recorded times. These are imported original executions.
The failed prose cohort is never pooled into new acceptance. Seven unchanged
32-cell workloads each receive one attempt. Nine model-free test methods pass;
non-nominal/low-power/unavailable state, changed proof, insufficient reservations,
failed attempts and legacy lifetimes are refused before model execution. No new
lifetime is claimed as run or reserved. The fixed prose confirmation subsequently
executes once under the separate granted 19:10–20:10 UTC interval below.

V320 completes its fixed prose confirmation in 2840.5432 seconds: all 32 cells and
32 warmups execute, with 15 clean request and first-job pairs. Round 2/reference
has four swap-ins and remains excluded; all measured thermal endpoints are nominal.
All 16 recorded pairs have exact IDs/text and first-job effective configuration.
All original request/startup acceptance checks pass. Maximum measured footprint is
7,372,476,544 bytes and warmup maximum is 7,371,116,528 bytes against 8.1 GB.
Qualified median paired reductions are request 3.3499%, first visible text 1.8215%,
prefill 1.7051%, fresh first-job 2.0996%, sampled process memory 7.4314% and active
tensor allocation 6.6332%. Active emission rate rises 0.4359%; descriptive arm
medians are 6.20245 and 6.25914 tok/s. This short workload does not establish a
substantial or long-run steady throughput improvement. Decode-phase derived TPS
rises 7.1864%, including terminal work, and is not active emission TPS.

V326 corrects the derived startup report to reuse the original acceptance cohort,
including both measured/warmup eligibility and exact first-job outputs/configuration.
The original derived 16-pair/1.7259% first-job report is preserved as superseded;
the corrected 15-pair/2.0996% result matches original acceptance exactly. Eleven
reporting test methods pass, including all relevant exclusion and numerical edges.
All nonstartup metrics remain identical; frozen drivers, original rows and criteria
are unchanged. The independent audit verifies original/imported provenance, all
32 raw cells, exact outputs and current short/prose reporting parity.

V325 refreshes current native cache prerequisites: the original 256-token ngram
and cache-bookkeeping commands pass 1,160 and 1,163 assertions, in 18.4817 seconds
overall. Both workers drain and current build/driver identities remain unchanged.
These are row/state/EOS/eviction/FIFO correctness proofs, not full-cache speed or
serving claims. The stopped two-cell V244 study remains unchanged and cannot resume.
V327 subsequently completes all 28 original fixed full-cache cells, with 25
eligible cells and every output hash exact. Compact BF16 storage qualifies on six
clean pairs: payload is exactly 50% smaller (116,449,280 bytes saved), median paired
physical-filled savings are 222,093,444 bytes, and warm assembly is 49.2309% faster.
Those are component measurements. Three four-swap-in cells stay excluded. Both
ring variants fail the original five-percent eviction-benefit criterion and remain
rejected; their exactness does not justify activation.

V328 completes the original 32-cell ordinary-serving guard on the same V304 build.
All 16 measured output pairs are exact; 14 request and first-job pairs qualify.
Candidate rounds 13 and 16 remain excluded for four and 104 swap-ins respectively.
Median paired request time is 0.2475% lower, startup 0.0729% higher, visible text
0.2187% later and active emission TPS 0.7169% lower. Original request/startup
nonregression criteria pass. This qualifies the cache memory optimization for final
integration; it establishes no additional serving speed or short-workload process
memory gain. No independent percentage is added to the combined-engine result.

V329 recomputes the raw component and serving assessments and reporter cohorts.
Source proof confirms compact rows retain exact BF16 bits from already rounded
Float32 values, with no new quantization or Float16 conversion. Pinned MLX
Contiguous copies oversized backing allocations; already-contiguous allocations
within output size plus 16,384 bytes may share. Both studies drain, and a fresh
21:58:03 UTC check confirms the model lock is free and no owned worker remains.
Compact storage stays explicitly selected only in its measured candidate arm until
the remaining optional dispositions and final combined acceptance are complete.


V311 compiles and executes the public consumer from all 150 exact V304 source
inputs: original API checks and 22 diagnostic assertions pass in 231.9284 seconds.
It fixes only the V310 fixture's missing required optional arguments. The actual
public planner confirms the 10.6 GB nominal MTP-plus-vision minimum, rejects 10 GB
for that combination and admits the existing 12 GB profile with 10,998,846,976
expected bytes, 798 slots and 11,574 retained prefix tokens. This is admissibility,
not empirical memory qualification. V308's corrected lifetime contract changes
only the two on-mode capacity fields; off stays 10 GB and every original wire
request, cycle, growth, exactness and no-replacement requirement remains. V312's
executor and actual consumer proof are validated. V321 is the explicit unrun
successor that binds the V320 confirmation and imported original provenance.
Its actual planner proof and 15 negative checks pass. Both corrected lifetimes
remain unrun and require all eight qualified paired proofs. Every original
60-request workload, growth/VM/exactness criterion and V308 capacity correction
is unchanged. Both old and new legacy lifetimes remain unrun permanently; the
original failed cohort is preserved and excluded from confirmation acceptance.

The original full battery verifies all 25 pinned files (105.3 GB), goldens,
planner/sampler checks, pool equality/resizing, prefix/sweep, short resource/output
and long recall. Elastic and MTP resource intervals each have four swap-ins;
long has 16 and the 2K context interval 44, with no swap-outs in those intervals.
Those four resource families remain unqualified. The independent earlier long
failure with four swap-ins remains separate. V313 fixes context-check's unguarded
set-e exit while retaining process status, stderr and every original assertion;
all 16 shell tests, including nine context edge cases, pass.

V323 now prepares those four unresolved resource families as an exact unchanged
subset of the current verification source, including the original MTP parity
prerequisite: seven required assertions total. Shell/source checks and an actual
expired-deadline refusal pass; neither protocol nor execution directory is created.
It needs a new 20-minute interval plus cleanup, original resource caps, normal
pressure/headroom and nominal Foundation state. No resource rerun has occurred.
The unrun client extensions retain all 13,308 pinned dependency files byte-exact.

Every previously unexecuted verification-tail assertion is now covered on V304.
After preserving the 21 GB headroom refusal and a later full-tail time-admission
refusal, the unchanged source is split at its vision section into bounded subsets.
V315 passes all 15 quality probes, real symlink loading and all 74 API robustness
checks in 226.1130 seconds. V316 passes both independent tower checks and all 25
original vision-serving checks in 125.0590 seconds. Original photographs, 14.5 GB
vision target, 3072 prefill and 32768 context remain unchanged. No skipped gate
passes, no request is shortened and no tail success replaces the earlier resource
failures. The original full no-VM acceptance is still open.

Actual isolated public installation also passes all five phases: prior install,
V304 upgrade, all 31 installed end-to-end checks, exact prior rollback and a real
OK generation from the restored release. It takes 86.4582 seconds. User installation
and shell configuration are untouched; no public release or default activation
occurs. Bounded source reads happen only after prose timing has ended, during
separate correctness/compatibility work.

V317 adds a model-free Foundation thermal/power pre-launch observer matching the
runtime API. Eight tests and a live 0.0318-second observation pass. V319 makes its
test suite mandatory in static_gates.sh; the first missing fixture stub is preserved,
then corrected with all 19 dispatcher tests passing. These affected harness checks
do not relabel the earlier full native/static battery or change frozen benchmark
drivers, data or acceptance thresholds. A nominal snapshot is not continuous
stability, temperature or energy evidence.

V322 verifies all 150 archived source inputs and reconciles all 48 Boolean and
five numeric optimization controls plus three construction choices. Each reference
arm explicitly sets all 54 environment keys. The first four combined arms disable
retention, the next three include it, and the final combined arm uses the real
empty-environment default resolver. All use the original chunk256. The benchmark
removes inherited SLOTSTREAM_ and SS_DEBUG variables before adding frozen controls,
so legacy scheduling/storage overrides cannot leak from the shell. Shared defaults
remain off; the exact candidate alone selects ten Boolean defaults, checkpoint256
and embedding rows, with the same platform-specific fused rotation guard. Public
ResidentWeights.arrays can materialize the matrix; no unconditional resident-reserve
credit is taken. This closes source/control inventory reconciliation, not empirical
calibration, performance or activation acceptance.

V324 prepares the current exact seven-file activation patch directly from the
V304 archive. The other 143 source inputs already match. Existing preimages and
archive-exact postimages are preserved, and git apply --check passes without any
workspace mutation. This supersedes the stale four-file V199 draft for review;
no source/default, release, installation or unrelated change is applied. All final
eight paired proofs, corrected lifetimes and remaining acceptance still gate it.

After the preserved 17:54:55 UTC handback, the peer grants 19:10–20:10 UTC.
The first prose invocation refuses before claiming a stage because Foundation is
fair; it starts only after nominal/non-low-power observation at 19:12:43 UTC.
Prose and cache prerequisites fully drain. Fresh preflight at 20:07:43 UTC confirms
a free model lock, no competing model jobs and 21.996 GB reclaimable. Root returns
the Mac early; the peer acknowledges and resumes its native Android batch.
No future interval is granted yet. Source/evidence and small model-free work
continue, with Chrome and all other apps open. Remaining work includes four resource
families, six unrun paired workloads, both corrected lifetimes, actual client
extensions, fixed optional studies, empirical calibration, final control/artifact
reconciliation and activation. The goal remains active.

**Preserved V280 qualification and measurements (prior build):**

The preserved predecessor is V280: binary
`eba9ee894aba4722d082fda7d39192339d0299fe7af264a624dd4209d1e648cf`, source archive
`56c60764434a75913ddb46d91ed7b38c66a6969ea46d5c1a6159ea55dd8143b4`, Metal
`198488eb61359e953580a9c4530400feee1a06dd2f28a930a6ffa58aec66a597`.
Only the read-failure diagnostic differs from V273's 150 build inputs; the
selected default/planner/runtime family is unchanged. Its 138.398395292-second
serial build passes and restores the seven-file lease, shared release, symlink,
metadata and first-party SwiftPM build state exactly.

All three V280 native prerequisites pass. V281 records 242 ordinary
and 256 MTP integrated assertions. Its initial interval leaves recovery unstarted
when too little reserved time remains; no failed native stage is retried. V284
uses the granted extension and passes all 522 read-failure assertions in
180.901931583 seconds. Every one of 28 actual HTTP faults fires and every same
HTTP retry reproduces independent successful output. All 14 enabled-checkpoint
state/logit byte comparisons match; disabled checkpoints leave an empty cache.
Both output queues and all seven wire formats pass original framing, delivered
text, pin cleanup, health and unrelated-retry checks. No generation, cache or
HTTP-runtime rule changed merely to satisfy V278's old empty-cache assertion.
The original 14 failures remain preserved as the test-contract counterexample.

V282 passes all 22,346 T0 assertions across 33 groups and 116 CLI checks on V280.
The full static gate passes in 299.335126667 seconds, including runtime defaults,
explicit opt-out, planner, transport, installer, claims and evidence validators.
It records 81 swap-ins and zero swap-outs. Its initial “no compilation” label
is corrected in a preserved companion: small C/Swift transport fixtures compile,
but Slotstream is not rebuilt and no model is loaded. All 363 additional exact
read, UNIX/TCP output, prefix-capacity and RoPE assertions pass in 3.229794167
seconds on the same candidate. Identities and cleanup checks pass. These are
correctness results, not inference performance or physical-capacity claims.

V283 executes the complete original verification on this exact binary: all 25
pinned files (105.3 GB) hash correctly, with 22 top-level passes, three failures
and no skips in 955.806118333 seconds. Parity, cache/elastic/prefix/sweep math,
short memory, long recall, 2K context, behavior 15, API 74, independent vision
reference and all 25 original-photo serving checks pass. Full acceptance fails
on a macOS governor-status parser and swap activity in the MTP and long-memory
intervals. The complete failed run and original tool/output closure are preserved.

V286 repairs real BSD-sed status parsing and the old false-positive PASS substring
case. All 15 real-shell regression tests pass; missing, duplicate, conflicting,
malformed and failed-process statuses fail closed. Only verification Tools change,
not the inference binary, defaults, limits or workloads. V287's actual governor
now passes. V287's subsequent MTP attempt stays swap-excluded; its extracted long
wrapper omitted BIG_MEMORY and failed argument parsing before model load. V289
preserves the original constants explicitly and enables nounset.

V289's 7972-token long request returns correct SEVENTEEN recall, but four swap-ins
exclude its 7.185 GB peak from the 10 GB no-VM resource gate. After a full clean
60-second preflight, its complete 12 GB MTP check passes all text, original-image,
recording, rollback and cross-request prefix checks. Whole-interval peak is
10.333721952 GB with zero swap-ins or swap-outs and memory_validated true. The
actual governor and MTP gates are recovered on V280; V283 is not relabeled a full
pass. V290 refuses before model load when its quiet preflight sees VM activity.
The earlier consumer preflight refusal is preserved; V288 subsequently completes
its actual independent Swift Package build against all 150 exact V280 source
files in 223.308387708 seconds. Public API use and 22 diagnostic assertions pass.
No model is loaded and the shared source/release remain unchanged. V295 refuses
at an inherited 21 GB full-suite floor; V297 restores the original long-workload
13 GB preflight, then refuses before model load when its 60-second quiet check
sees VM activity. The original 10 GB model and zero-VM criteria remain unchanged.
No threshold or image is reduced.

V273's prior build, V274's metadata/runtime/planner, V275's actual API 74 plus
four typed 400 responses, V268's source consumer and V270's isolated 964,167
planner-policy assertions remain separately scoped historical evidence. V271's
build-floor stop, V276's native pressure stop and V278's test mismatch are not
rescored. V277 retains the actual pressure level in failures without changing
eligibility. No older-build result substitutes for a current required gate.

The V281 campaign is frozen around V280's complete contract. V293 completes the
first paired study: all 32 fixed cells, 15 clean request pairs and exact whitespace
output [271]. At 17 input tokens / one output token / 8.1 GB, median paired
completion latency falls 12.9725%, sampled process peak falls 6.2374%, and active
tensor allocation falls 6.6791% (358,305,280 bytes). All 15 clean pairs improve
completion. Round 2 remains excluded for measured VM activity. The official
first-job non-regression gate passes on 14 eligible pairs with 8.5749% reduction;
round 5 additionally has invalid startup/warmup VM evidence.

This small workload has a real tradeoff: first protocol output is 3.6128% later
and prefill is 3.6545% slower. Visible preview and active tokens/second are
unavailable because the single output is whitespace. V296 repairs the misleading
secondary report that divided one token by terminal bookkeeping time; both
throughput metrics now require at least two emitted tokens. All nine reporting
tests pass and every original report/result remains preserved. Latency/memory
results and benchmark/acceptance contracts do not change.

V298 completes the second fixed combined study:440-token nonrepeating prose,
16 output tokens and the original8.1GB target. All32 cells complete with exact
outputs. Fifteen clean request pairs yield median paired reductions of3.7195%
in completion latency,1.8134% in visible-text latency,1.8015% in prefill and7.3960%
in sampled process peak; active tensor allocation falls6.6338%. Active token
emission is1.4569% higher on this fixed16-token workload. The separate8.5929%
decode-phase increase includes removed terminal work and is not the active-rate
gain. These observations do not establish long-output equilibrium throughput.
Round7's reference has four measured swap-ins and remains excluded; round9's
combined warmup has four swap-ins but its measured interval is clean. The official
first-job gate passes on14 pairs with3.1359% lower latency. The original fixed
pair count, cooldowns, no-replacement, equality, memory and non-regression gates
all pass; no inference or planner source is changed to obtain this result.

V292's actual current-build Ollama and published AI SDK checks pass at both32768
and65536; pinned Hermes tool execution, follow-up, discovery and title generation
pass at65536. The complete guarded run takes115.019631667 seconds. Hermes's actual
64000-token minimum stays explicit. V291 separately passes the complete ordinary
242/MTP256 suites through the real portable selector with shared RoPE enabled and
zero fused dispatch. Its542.286777625-second run remains qualification on this Mac,
not an untested-chip performance claim. All identities, ownership and deadline
checks pass. CLI/compression/client-image extensions and installed E2E/rollback
remain prepared but unrun; V300's fresh long-resource copy also remains unrun
because its original four-minute scheduling minimum did not fit the remaining
reserved time. Its inherited historical output paths were corrected before any
launch, preserving all earlier artifacts.

V301's Python MLX component is followed by V303's actual native Swift probe.
V302 preserves its two type-annotation compile errors; V303 corrects only those
annotations, compiles against the pinned MLX/Cmlx/Numerics objects and passes exact
Data bytes, shape/dtype and all 12 both/one/absent-window cases. All 48 fixed native
pairs are clean. At parent lengths 17, 256 and 4096, paired component-time reductions
are 47.3151%, 48.0968% and 46.2384%. Native Swift wrapper version is 0.31.6 and the
separate Python distribution is 0.31.1. Native allocator active peak is 336,183,296
bytes. The compile-and-run wrapper takes 31.467029375 seconds. These are isolated
operation measurements, not serving gains. V304 implements the proven scheduling
change and V305 supplies its first whole-model correctness results above.

V280 retains five of thirteen passed stages; its six remaining paired studies and
two soaks were not run before the new runtime change. V305 preserves the entire
same study shape against V304 with its own exact identity. Active emission rate is
(N-1)/sum(interTokenSeconds); decode-phase rate separately includes terminal work
and is unavailable for one token. Percentages are medians of eligible paired
ratios, never sums of isolated gains. No full-matrix preview, active-token-rate
or memory-saving conclusion exists yet. Optional scope, full-cache, tail, image
and selector controls remain off unless their own frozen evidence qualifies them.

**Context scope:** separate P5 expansion through 262144 is not a prerequisite
for optimizing the unchanged 32768 default/65536 public limit. Its portable
and 93-command window matrix evidence does not waive applicable existing-cap
C01–C17/C20–C22, full governor/MTP/vision resource tests, actual 32K/65K client,
install/rollback coverage, N6 phases or final performance/lifetime criteria.

Next: complete the resumed prefill scope and remaining image/vision dispositions, preserving all interrupted and failed original identities. The corrected V360 native composition now passes. Once the exact final optional family is fixed, bind and execute all eight original paired workloads, both original lifetimes and both 512-output studies, then close resource/client, empirical calibration, final delivery and exact activation gates. No original workload, criterion or guard is shortened, and historical results never substitute for current-build evidence.

Both V338 cohorts and the fresh V343 study retain their recorded results. The later complete V349 native counterexample excludes boundedDraftTail from the combined candidate, alongside already rejected rings, first-entry reuse and adaptive policy. Compact BF16 cache remains selected in corrected V360. The original unique-image guard, remaining scope/vision cases and serving/capacity gates remain open. V352's tail-on drafts are preserved but stale; V363 now prepares corrected controls with MTP_TAIL=0 and NGRAM_ROWS=1 for explicit combined arms, reference both0, actual-default unforced. All original final/native/lifetime/resource/client criteria still apply.

After optional dispositions, qualify the exact final composition through the six
remaining fixed paired workloads and any affected existing proofs, then both
original-length V321 corrected lifetimes after all eight paired proofs qualify.
Bind and execute the V340 longer-throughput measurements on the final composition;
keep their results separate from the existing short-output estimates. Complete the
four original resource families, actual client extensions, empirical calibration
and final static/artifact/activation acceptance. Preserve prior output,
source, control and workload identities. Installed upgrade, 31 end-to-end checks,
rollback and the original text/vision tail already pass for V304. Current static
and final activation still require their appropriate final-composition gates.

Initial execution evidence (preserved history): [[sources/runs/2026/09/2026-09-05-optimization-initial-implementation]] and [[sources/runs/2026/09/2026-09-05-optimization-second-implementation]]. The final-forward confirmation has five valid positive pairs after one swap-excluded pair was replaced; the compact-state resource confirmation has three valid pairs. State, MTP, n-gram and indexer diagnostics pass with the exact provenance limits recorded in those runs. Later bounded-work and lifecycle evidence is captured in [[sources/runs/2026/09/2026-09-05-optimization-bounded-prefill-and-lifecycle]]. Sweep placement plus 256-row tiles passed 1,127 exact assertions; its exploratory request/memory comparison contains too many swap-excluded pairs for a speed claim. The cancellation/mode-transition diagnostic passed 135 checks on its frozen executable. The first indexer tiling failed exact parity; preserving the full masked key domain and merging small tails restored all 1,127 checks. GDN recording passed 522 component and 1,127 real-model assertions; PLE tiling passed 1,127 assertions. Shared-angle performance and all new kernels' integration/compatibility gates remain open. Workspace C passed 1,127 exact current-pass assertions. The first 8,192-token scope failed three state gates; preserving the reference nonexpert compute shapes made V2 exactly match every checked state, logit and route, with 15,633 versus 28,598 expert reads. This is diagnostic I/O evidence, not a speed or peak-memory claim; see [[sources/runs/2026/09/2026-09-05-optimization-read-scope-and-capacity]]. Allocated-capacity lifecycle passed 138 assertions. The generator now has an experimental bounded read-scope transaction that preserves the existing compute schedule and restores a coherent checkpoint on cancellation, with tiled MTP consumption and final-pass admission. Its corrected native checks passed 593 scope lifecycle, 240 synthetic image-span/MTP recovery and 112 MTP first-entry/tail mechanics assertions; see [[sources/runs/2026/09/2026-09-05-optimization-scope-generation-and-mtp]]. The first lifecycle helper was invalid (zero output means maximum allowance in this API), was terminated during seed decode, and is preserved with the correction. The first real 7,961-token pilot reduced expert reads from 45,018 to 18,753 but its only clean pair was 22.5% slower; two reference cells had swap activity and both pairs were excluded. The 256-token tile is not adopted. Both 2,048-token routed tiles and the later 4,096-token tile plus compact frontier passed 117 numerical, 593 lifecycle and 240 synthetic image/MTP assertions on their own frozen executables. Neither pilot produced a clean performance pair because of swap activity. See [[sources/runs/2026/09/2026-09-05-optimization-larger-routed-tile]] and [[sources/runs/2026/09/2026-09-05-optimization-large-tile-and-frontier]]. Workspace GPU-wait/CPU-sort counters are now populated; all timing claims from older zero counters remain explicitly unavailable. The bounded chronological-prefill calibration fitted the existing expected peak at 8,192 tokens but could not advance to longer contexts because every attempt had swap activity. V6 adds per-generator VM interval snapshots and an opt-in real-server observation path. Three short server A/A pairs passed without swap; every long chronological-prefill pair was excluded. Exact evidence and limits are in [[sources/runs/2026/09/2026-09-05-optimization-prefill-family-and-serving-intervals]]. No planner constants changed. Future timing blocks avoid concurrent workspace shell commands as an additional contamination control; swap exclusion is unchanged. All read-scope controls remain off by default. The bounded socket-output queue passed 38 real socket-pair assertions and all 74 live API robustness checks after fixing a blocked-send counterexample; [[sources/runs/2026/09/2026-09-05-optimization-bounded-output]] preserves both revisions. Live TCP stall/Gateway integration and latency nonregression remain open. The router selector passed 1,128 main state/route and 367 MTP state/route assertions, but all four valid short-request pairs were slower. The output queue narrowly missed its client nonregression threshold. Neither is adopted; [[sources/runs/2026/09/2026-09-05-optimization-router-selection]] preserves the exact gate results and 702 refreshed ordinary-generation assertions. No whole OPT item is marked complete from its first substep, and no combined speedup or release readiness is claimed.


**Correctness correction discovered during OPT10, 2026-09-05.** The first dense-visibility component gate exposed an existing sparse-mask error: tensor `/` promotes Int32 positions to float32, so the old current-block-tail expression becomes `qp + 1` and drops every partial current block. [[sources/runs/2026/09/2026-09-05-indexer-partial-tail-counterexample]] preserves the 32 failing assertions, source binding and independent pinned-MLX reproduction. The unconditional `floorDivide` correction passed 262 component assertions, including an independent scalar retained-key oracle; the dense-only and bounded-plus-dense full-model continuation gates each passed 1,127 assertions. The corrected 8,192-token read-scope comparison passed all 117 assertions. Both new catalogues passed all 24 T0/T1 groups. Exact evidence is in [[sources/runs/2026/09/2026-09-05-optimization-corrected-mask-and-shared-overlap]]. The optional dense shortcut also conservatively checks operands against NaN/infinity/overflow before skipping selection. Every new long-context optimization comparison must bind a corrected reference; historical exact equivalence to a frozen buggy reference is not intended-model parity. Earlier below-threshold ownership and terminal-forward measurements are not invalidated by this mask change.

**OPT19 shared substep:** the projection-only prelaunch passed 1,128 exact state/continuation assertions. Four clean short-request pairs showed a 1.66% median client reduction, but the declared gate required five clean pairs and at least 5%; the swap-contaminated first pair stays excluded. No adoption follows. V129 now implements bounded one-token resident-routed overlap and passes native state/lifetime tests; its frozen serving comparison and GPU-event proof remain pending. The linked corrected-mask/shared-overlap run preserves the failed qualification and the new host-condition/harness observations.

**Chronological and read-scope qualification, V15–V21:** the larger chronological pass failed 41 of 806 expanded numerical assertions; no larger-pass policy is adopted from its read savings ([[sources/runs/2026/09/2026-09-05-chronological-prefill-numerical-counterexample]]). Sharing reads while retaining the original 256-token compute schedule passed 808 exact state/route/continuation assertions at each of 1,024, 2,051 and 4,096 tokens. Its code-review pilot has one swap-clean exact-output pair (50.43 to 20.84 seconds) but all candidate peaks exceed the 10 GB target, so it fails qualification; two other pairs remain swap-excluded ([[sources/runs/2026/09/2026-09-05-optimization-exact-256-compute-scopes]]). Compact frontier and then piecewise workspace writes preserve exactness but still fail the memory limit. The phase trace puts the peak in routed computation/reduction, so smaller routed tiles are the next bounded candidate. The block selector's denormal counterexample, corrected 163/43 component gates and 1,128 integrated block-selector state assertions are preserved in [[sources/runs/2026/09/2026-09-05-optimization-denormals-and-workspace-peak]]. Isolated selection savings do not qualify inference; MTP/long-context/real-request gates remain open. Request-entry kernel preparation and no-op phase attribution are being hardened. No memory target, numerical gate or production default was relaxed.

**V22–V25 continuation:** reducing routed tiles to 1,024 retained all 808 exact 4,096-token scope assertions and passed the one-cell 10 GB resource gate at 9,612,759,144 sampled bytes. Its new paired development run produced no clean pair: three reference intervals had swap activity, the third also became thermally fair, and the run was stopped during round 4 after five valid pairs were no longer possible. This is a failed/incomplete qualification, not a speed claim; [[sources/runs/2026/09/2026-09-05-optimization-smaller-routed-tile]] preserves it. The cached-prefix MTP selector test passed 514 assertions with actual main/head dispatch and exact continued state. Request-local image reuse passed 64 real-tower assertions before the successor added processor/tower/padding identities for prefix safety; [[sources/runs/2026/09/2026-09-05-optimization-mtp-selector-and-image-reuse]] preserves those exact versions and ten host-only harness tests. Cross-request image caching/startup and image serving qualification remain open. The 72-wide attention prototype passes 60 pinned Python/Float64 component assertions: width 128 is the more promising measured large-shape candidate, while width 80 saves memory but is slower. These exploratory component timings do not certify inference; Swift/full-tower/quality/request gates remain open ([[sources/runs/2026/09/2026-09-05-optimization-vision-padding-probe]]). The memory battery now uses structured sampled footprint rather than a rounded CLI value that can miss a transient peak. No production optimization or planner family has been activated.

Objective: improve time to first token, steady decode latency, peak memory and memory efficiency across the pinned model's text, tool-use and image paths. Preserve model quality, cache correctness, numerical contracts, bounded memory, cancellation, serving APIs and independent CLI use. Optimize these jointly at explicit memory budgets; there is no universal fastest configuration for every workload.

Evidence: [[records/analyses/2026-09-04-first-principles-inference-audit]], including its three captured raw runs. Source starting point: `b88fdb2cf1f4070c838bccac59911ad2625d25bb`; recheck current source and dependency identities before implementation. The native state-compaction probes establish allocator savings and final-logit equality on two synthetic prompts. Their timings are excluded from performance claims.

### One program, with a detailed prefill chapter

This is the **single optimization program**, now 37 OPT items across seven stages. [[records/plan/n6-prefill-bound-the-pass-then-read-each-expert-once]] is its detailed prefill chapter. Its phase and P1–P5 ownership map points to the OPT items here; it is not a second implementation queue. This record owns execution status, dependency order and experiment lifecycle. N6 owns the detailed prefill algorithms, workspace alternatives and per-phase gates. Keep one raw-run/measurement ledger and link results from both views.

The former independent execution sequences are superseded by this program. Consolidation cancels no optimization work: overlapping mechanisms have one OPT owner, while N6 retains their detailed design and evidence. The historical audit remains supporting evidence. Resolve any future disagreement between the program and its detail chapter in both records before implementing the affected item; do not silently choose the weaker acceptance criterion.

The combined source review is captured in [[sources/runs/2026/09/2026-09-04-prefill-unification-first-principles]]. Current source identity is `6e66a367f93e636c3729179e24007aa99c97b9a6`; the inference tree and package pins still match the original audit. This review adds source-derived opportunities, not production changes or measured speedups.

**The preferred strategy is to combine read reuse with bounded compute and remove work before tuning it.** Start with compact state ownership and remaining-aware scheduling. Then make indexer/attention, PLE, expert rows and reduction storage explicitly bounded. Compare read-scope workspace alternatives at the actual memory target. Integrate independently winning selection, shared/resident overlap, attention and GDN kernels; remeasure their combination because they can compete for the same resources or remove the same wait. Prefer measured end-to-end latency and capacity over the largest scope or most concurrency.

The merge resolves five real planning gaps:

- Disjoint MoE output placement still costs N×K×H. N6 U1 now compares canonical full output, deterministic numerical fold and one-layer weight workspace explicitly; a compact fold is not assumed to preserve the existing reduction tree.
- OPT36 targets the pinned backend's full sorts behind `argPartition` in router and indexer, with ordered IDs and tie behavior gated separately from scoring changes.
- OPT35 removes unused final mixing and investigates final-layer query/MoE pruning when full multis are not required. It preserves every state-producing operation and disables the terminal pruning for MTP/full-output diagnostics.
- OPT09/10 include compact raw-tail ownership, absolute-visibility dense shortcuts and fused indexer score preparation, with explicit rollback and numerical gates.
- OPT19/26 probes no longer wait unnecessarily for the full read-scope redesign. Prefill lifecycle, tail warming and progress fixes have explicit owners; capacity improvements use a memory-benefit plus latency-nonregression gate.

Read N6 U1–U5 for the derivations, alternatives and test cases. This overview deliberately does not maintain another copy of their byte tables or performance numbers. Both chapters are projected into PLAN.md, with this program overview first.

### Execution rules and completion states

Items without an explicit current status remain **unstarted**. On work, mark an item **in progress**, then **verified**, **rejected with evidence**, or **blocked with a concrete dependency**. A completed prototype alone is not verified. An optimization is kept only after its correctness contract and intended resource/latency benefit pass. A candidate that fails is a completed investigation, not a reason to leave an unhelpful feature in the default path.

Ship work in small independent changes, with a known reference path while an experiment is active. Do not stack unverified changes or regenerate numerical goldens simply to accept a discrepancy. For performance candidates, keep typed controls default off during experiments and retain the verified reference arm through qualification and one release after adoption; then remove temporary switches and redundant implementations unless measured hardware/shape differences require dispatch. Correctness/reporting fixes need no switch restoring incorrect behavior. Preserve public diagnostic APIs independently of experimental flag cleanup. Repeat checks after further changes only where those changes affect the previous result.

A verified memory/capacity optimization can be adopted on explicit resource benefit with latency nonregression; it need not wait for a later scope redesign to deliver a speedup. A speed claim still requires measured request latency improvement. No blanket speed multiplier is promised. Freeze each experiment's primary metric, minimum useful benefit, allowed regressions and repetition count **before** running it. Choose thresholds from an A/A noise characterization and the intended workload. Report failed and inconclusive results, including memory gains that did not translate to physical-footprint or latency gains.

### Dependency and fallback semantics

The stages express priority; they are not a requirement to finish every item in one stage before starting an independent item in the next. Dependency IDs refer to required **capabilities and evidence**, not an assumption that every preceding optimization succeeds. A rejected accelerator may be replaced by the existing validated path when that path still satisfies the dependent item's memory, state and correctness contract; name that fallback in the result. Do not waive a necessary bound because a candidate was rejected. This is particularly relevant to output tiling before larger read scopes and to pool lifetime proofs before asynchronous writes.

OPT08 means initial accounting for accepted early changes. Its later recalibrations are acceptance checks attached to those changes, not a dependency cycle that waits for the entire program. OPT03/23/24 are conditional on MTP availability; OPT25 can first improve output-budget handling and mode policy using the existing verified recording/reconciliation implementation. It does not depend on both recording and reconciliation accelerators winning. Missing vision fixtures likewise do not block unrelated text work.

The starting baseline must be reconstructible, not merely hashed: retain an exact buildable source/dependency snapshot or a commit plus the exact relevant patch, and the baseline executable/metallib identities. Preserve unrelated uncommitted work. Changes to a shared prerequisite require rerunning the dependent checks they affect. Failed candidates retain their experiment evidence and can be removed or disabled cleanly without requiring users to downgrade model artifacts.

V25/V26 correctness and the unresolved vision memory failure are captured in [[sources/runs/2026/09/2026-09-05-optimization-vision-identity-and-router-weights]]. Padded attention passes 60 Swift component assertions and the unchanged full-tower reference bands. Image identity/reuse passes 68 native checks; image serving passes 22 correctness checks but samples peaks of 10.336 and 10.043 GB above its 10 GB target. The corrected same-geometry image-content test passes four checks after preserving an earlier ineligible-prefix test failure. Router-weight promotion passes 30 component, 1,130 main-state and 370 MTP assertions; the frozen V26 catalogue passes 27 groups. The extra 251.66 MB main-router / 256.90 MB main-plus-head storage is reported, not yet qualified at fixed total memory. OPT17 is in progress. A first-image resident reservation and governor policy correction are under test; no new default or performance claim is earned.

V27/V28 evidence is in [[sources/runs/2026/09/2026-09-05-optimization-vision-budget-and-cache-bookkeeping]]. First-image resident memory is now reserved inside a target-driven plan, allocator cache is bounded before vision encoding, and governor replans preserve resident features/context under one generation lock. The 26-check image sequence passes correctness; all recorded peaks are below 9 GB but four generator intervals have swap and remain excluded from strict resource qualification. A new separate first-image preparation observer passes a two-request bounded check with clean swap counters and maxima below 7.55 GB. The low-target refusal preserves the existing pool/prefix plan and subsequent text generation. These do not qualify maximum image resolution or sustained resource bounds. The direct expert index, touched-slot pin list and FIFO ring pass 1,163 main-state and 367 MTP checks plus the 28-group catalogue. All seven short-serving pairs are valid and exact, but median latency regresses 0.08248% and only three improve; the frozen speed criterion fails and controls stay off. Large-pool and full n-gram eviction effects, asynchronous read recovery and memory epochs remain open.

The separately frozen 701-token code-prefill workload completed seven paired rounds; five clean exact-output pairs all slowed down (median 9.26105%) despite the first pair reading 25.704 GB fewer expert bytes. Rounds 3 and 7 are swap-excluded. The scope remains off for this workload and the crossover point is still unqualified; see [[sources/runs/2026/09/2026-09-05-optimization-shorter-prefill-workspace-counterexample]]. OPT22 now has a checked-read foundation, bounds/overflow checks and interrupted/short-read fault seams. V29 passes the 29-group catalogue and 702 real-model generation assertions. Current synchronous callers still fail closed on nonrecoverable read errors; request-level recovery, async publication and stale completions remain open, as explicitly recorded in [[sources/runs/2026/09/2026-09-05-optimization-checked-read-foundation]]. The governor cannot silently remove the memory charge for a head it has not unloaded. OPT18 pointwise normalization fusion passes component/full-state/MTP gates, but six valid serving pairs show a 0.04850% median latency regression and only three improve; it stays off. V32 corrects and passes the future-head reference/candidate comparison (368 assertions). OPT22 owned tensor descriptors pass nine lifetime/bounds checks, 1,130 full-model assertions and 370 MTP assertions. Five valid serving pairs show a 0.01280% median latency regression and only two improve, so direct handles also stay off. These reject the measured short-serving speed hypotheses, not the ownership and checked-read foundation. Evidence: [[sources/runs/2026/09/2026-09-05-optimization-compiled-normalization]] and [[sources/runs/2026/09/2026-09-05-optimization-owned-read-handles]].

**OPT10/15 V33–V36:** compact block IDs pass exact reference-mask reconstruction, including exceptional scores and partial blocks. The D256 online-softmax prototype passes 208 corrected component assertions but fails 52 of 807 full-model numerical assertions. Five clean component pairs per shape are 4.84×, 21.98× and 7.07× slower at the three declared dense/sparse shapes despite lower allocator peaks. This scalar kernel is rejected; bounded native attention remains the dependency path. Short queries retain their exact existing path. A matrix-tiled successor with preserved BF16 rounding remains a separate conditional candidate, with no implied quality or speed result. Failed initialization, short-query and test-scope revisions are preserved in [[sources/runs/2026/09/2026-09-05-optimization-selected-attention-rejection]].

**OPT35 V37/V38:** explicit terminal demand passes two 810-check numerical families (2,051/4,096 tokens), 462 cancellation/image-span/full-output lifecycle assertions and 370 MTP-exclusion assertions. State/upstream routing is exact; final-row MoE logits satisfy the existing numerical bands. Seven clean real-serving pairs produce identical output and a 2.02711% median client improvement, with six improving and round 7 regressing 9.10707%. The frozen 5% speed gate fails, so the prototype stays off. It removes intermediate terminal queries and unused MoE rows; final-pass QSA query pruning and full quality/integration gates remain open. Evidence: [[sources/runs/2026/09/2026-09-05-optimization-terminal-prefill-demand]].

### Stage 0 — establish trustworthy instruments and baselines

| Item | Concrete deliverable and code surface | Dependencies | Acceptance |
|---|---|---|---|
| OPT00 — reproducible benchmark manifest | Freeze production/dependency hashes, actual token IDs, image hashes/preprocessing settings, sampler seeds, context and output lengths, MTP setting, cache state, pool and total-memory limits. Repair and reuse `Tools/prefill_bench.sh` under N6 Phase 0, including immutable fixtures, arm interleaving, fail-closed metrics, effective-override accounting and a real serving cell; build this harness once. | None | A/A establishes variability; all results identify cold/warm filesystem, cold/warm expert pool and prefix reuse separately. No model-speed result inferred from the earlier contended probes. |
| OPT01 — correct timing and memory observations | Add request-stage timing and optional sampled physical-footprint high-water tracking in `ProcessMemory.swift`, `Generate.swift`, `Engine.swift`, `ExpertStore.swift` and `Server.swift`. Include queue, images, text prefill, first byte, steady decode, speculative reconciliation, socket stalls, read bytes/counts and GPU waits. | OPT00 | Verify timer boundaries and overhead against disabled instrumentation. Report RSS, sampled physical footprint, allocator active/cache/peak separately; never derive GPU time as wall minus overlapping I/O. |

Start performance work with existing bounded 8.1–10 GB targets; a fixed smaller diagnostic pool may be used when its footprint is separately bounded. Before every heavy step inspect running model processes and reclaimable memory. Run one model process at a time and keep several GB headroom. Use existing policy simulation for governor checks, not memory-hog stress. No context-cap or measured scheduling-bound increase belongs in this stage.

The baseline manifest covers nonrepeating prose, code, tool exchanges, short length-capped replies, long outputs, reused and edited prefixes, and known-content images. Run only the subset relevant to an individual change; reserve the integrated matrix for Stage 6. Begin long-context tests at a fitting rung and advance to 8k/32k only when measured headroom permits.

### Stage 1 — remove confirmed waste and repair resource accounting

| Item | Concrete deliverable and code surface | Dependencies | Acceptance |
|---|---|---|---|
| OPT02 — compact GDN and PLE state ownership | In `Layers.swift`/`Model.swift`, give retained convolution windows their own compact storage once their parent is no longer needed. Handle ordinary prefill, subsequent decode and speculative state references. | OPT00; OPT01 for memory claims | Exact cache values and logits for ownership-only changes; state persists correctly into further tokens. Reproduce allocator reduction, measure footprint over time, test cancellation and recording, and remove full-chunk retention across layers. |
| OPT03 — compact MTP retained rows | Materialize the retained final multi-stream row in `MTP.swift` and audit checkpoint/rollback slice ownership in `Model.swift`/`Generate.swift`. | OPT02 | Exact retained values and continued logits; no stale or oversized parent in normal prefill, acceptance/rejection, cancellation or prefix storage. Do not claim that `eval(slice)` copies it. |
| OPT04 — tail-aware bounded prefill schedule | Make `Context.swift`, `Generate.swift` and planner reporting use one function that checks the actual final remainder against the existing bound. | OPT00 | Boundary arithmetic, reused offsets and tails pass; fewer actual passes in the identified cases; memory remains inside the envelope and logits satisfy the rechunking contract. Final-scope expert admission stays correct; N6 3.1 tests rebalanced or explicitly swept short prefill tails against decode-pool churn, with numerical and fixed-suffix gates. |
| OPT05 — avoid unused last forward | Stop ordinary decode after the last permitted emission without calculating unused next-token logits. Preserve the pending/unconsumed token in exact prefix accounting. | OPT00 | One-token/zero-token/length/EOS/stop/disconnect cases are correct; next-turn reuse consumes the pending token exactly once. Lower completion latency where this forward was wasted. |
| OPT06 — compact sampler work | In `Generate.swift`, reduce candidate sorting/CDF work and CPU synchronizations. Keep top-K threshold ties, top-P exclusive CDF, min-P, penalties, token-ID ordering and RNG draw behavior. | OPT00 | `Tools/sampler_gates.sh` and its numpy oracle agree, including tied/boundary distributions and invalid-parameter sanitization; measure the sampled rather than only greedy path. |
| OPT07 — compact n-gram cache and queue | In `NgramStore.swift`, test UInt16 storage of already BF16-rounded rows, pooled allocation/index storage and bounded FIFO/ring maintenance. | OPT00 | Identical row IDs and dequantized values, eviction/reuse correctness; lower owned memory and no meaningful lookup/prefill regression. Account object overhead as well as payload. |
| OPT08 — allocated-capacity accounting and planner recalibration | Update prefix state, KV growth, staging, MTP/vision allowances and pass cost in `PrefixCache.swift`, `Plan.swift` and `Governor.swift` using accepted measurements. Reserve active growth on prefix hits as well as misses, include rollback coexistence and price effective overrides; N6 1.4/2/3.4 define the affected cases. | OPT01, OPT02, OPT04; OPT03 when MTP is enabled. Refresh after later memory-layout changes | Charge actual owned capacities without double-counting shared buffers. Regenerate the whole estimator family together, including throughput/cost coupling; simulated policy tests, fitting real targets and derived projections agree. Do not spend a theoretical allocator saving before real footprint is verified. |

Stage exit: accepted ownership changes are correct across continued state, the extra tail pass and final forward have dispositioned results, and memory reporting/planning reflects measured behavior. This stage is useful independently of custom kernels.

### Stage 2 — bound prefill work and reuse each expert over more rows

| Item | Concrete deliverable and code surface | Dependencies | Acceptance |
|---|---|---|---|
| OPT09 — incremental completed indexer blocks | Cache pooled/normalized/rotated completed blocks in `Layers.swift`; append only newly complete blocks and maintain the raw partial tail. Separately test replacing old raw storage with completed keys plus bounded raw undo/tails (N6 U3); extend trim/checkpoint behavior before releasing raw data. | OPT02 | Preserve FP32 mean/BF16 normalization/RoPE sequence. Test every block-boundary remainder, chunk split, prefix continuation and rollback. Old blocks are not recomputed on each decode step. |
| OPT10 — bounded indexer selection and masks | Tile the indexer by queries and expose compact selected-block IDs with causal/tail metadata, retaining a reference mask path. N6 U2/U3 add absolute-visibility dense bypass and separately gated fused positive-head score aggregation; OPT36 supplies an optional selector. | OPT09 | Selections match the reference, including ties and early contexts. Memory scales with the chosen tile, not a full scope-by-context mask or score matrix. |
| OPT11 — shared positions and partial RoPE | Cache/share angle tables where parameters match and evaluate a fused partial rotation in `Layers.swift`/`MTP.swift`. | OPT00; coordinate with OPT09 | Correct positions for text, images, reused prefixes and rollback. Preserve the existing rounding path for the exact version; any alternate reduction goes through the numerical-change gate. |
| OPT12 — deterministic expert output placement | Replace retained groups plus concatenate/inverse-gather with disjoint placement in original router-rank order; this still owns N×K×H. Compare tiled/fused exact reduction with a separate deterministic expert-order fold, using N6 U1's explicit A/B/C storage alternatives. | OPT02 | First variant preserves contribution/reduction order and cache-invariant output; demonstrate actual copy/peak savings. Account any remaining N×K×H and FP32 product. Reject nondeterministic atomics; deterministic reordered folds need separate numerical evidence and adversarial row-liveness tests. |
| OPT13 — bound expert routed rows | Separate routed-row tiles from expert group size in `Layers.swift`/`ExpertStore.swift`; keep the loaded group available across all its row tiles. | OPT12 | No hidden unbounded row buffer; no extra expert reads solely due to row tiling; backend dispatch and padding depend on routing, not cache contents. |
| OPT14 — tile PLE/resident work and overlap n-gram lookahead | Bound PLE/projection activations by token tile and begin bounded n-gram prefetch when IDs become known. | OPT02, OPT07 | Preserve n-gram history, output and convolution semantics across tiles; lower peak or exposed read latency; cache capacity bounds all lookahead. |
| OPT15 — fused text attention for actual shapes | Prototype selected-block, 256-wide tiled online-softmax attention for prefill, with appropriate short-query handling. Integrate compact selections from OPT10. | OPT00/01 for fixed-input probes; OPT10 for production integration | Validate GQA, causal visibility, partial tails and sparse/dense boundary. Bound both indexer and attention memory. Pass numerical/state/task gates; show a relevant end-to-end gain, not only a synthetic kernel win. |
| OPT16 — larger layer-major expert-read scope | Implement N6's bounded layer-major/superchunk path in `Generate.swift`, `Model.swift`, `Layers.swift` and `ExpertStore.swift`. Distinguish read scope, token tile, expert group and row tile. | OPT08, OPT10, OPT12–14 capabilities with an explicitly selected N6 U1 workspace; OPT04 or validated existing schedule; bounded attention or accepted OPT15 | Measured expert records/bytes fall at matched total footprint. Gate causal state, chunking numerics, MTP/vision handoff, prefix reuse, final admission, progress and cancellation through `serve`; checkpoint/cleanup in OPT30 is part of this feature's acceptance, not a deferred serving task. Keep a bounded scope when a whole prompt does not fit. Read reduction alone does not establish latency improvement. |
| OPT35 — compute only demanded prefill outputs | Add an internal state-only/last-logit/full-multi demand contract in `Model.swift`/`Generate.swift`. Remove unused final mixer work first, then test terminal QSA query and MoE pruning under N6 U4. | OPT00/01/02; independent of OPT16, port any accepted path when scope scheduling changes | Every required KV/indexer/recurrent update is materialized, whole-state continuation and prefix/cancel behavior pass, public diagnostic/full-output paths remain. MTP disables terminal pruning; frozen numerical gates cover changed row/kernel shapes and admission/decode warmth. Measure executed work and full-request gain. |
| OPT36 — specialized router and indexer top-K | Replace full-sort-backed partial selection with bounded selection appropriate to top-10/512 routers and block-top-512 indexers. Preserve scoring initially; N6 U2 defines semantics and backend boundaries. | OPT00/01 for independent fixed-score probes; production callers retain their existing validated fallback | Same ordered router IDs/logits, exact required indexer keep sets, verified ties/infinities/signed-zero/exception behavior, repeatability and bounded workspace. Separate selector timing from scoring and final request impact; reuse in OPT06 only under its sampler-specific contract. |

OPT15/19/26/36 fixed-input or schedule-only probes can proceed before OPT16. Their production integration requires the relevant ownership, numerical and memory gates. Initially keep the existing chronological GDN in a new scope path, then compare any independently accepted OPT26 implementation. OPT35's unused-mixer substep is also independent. This permits earlier experiments, not concurrent model processes.

Stage exit: N6's deterministic record-count and memory gates pass; the selected scope/tile policy improves its declared workloads at fixed total memory. Recalibrate OPT08 after choosing the implementation. Do not extrapolate uniform expert routing, an I/O residual, or a single read-scope bound into universal throughput.

### Stage 3 — shorten shared prefill and decode critical paths

| Item | Concrete deliverable and code surface | Dependencies | Acceptance |
|---|---|---|---|
| OPT17 — eliminate repeated router promotion | Compare cached FP32 routers with a mixed-input kernel in `Layers.swift` and the MTP resident MoE. Account for references retained by `Weights.swift`. | OPT01, OPT08 | Exact routing/logits for the chosen equivalent implementation; measure at fixed total memory, including displaced expert slots. No claim of net savings based only on eliminating per-call temporaries. |
| OPT18 — fuse pure compute regions | Prototype gate/up projection fusion, SiLU/product, GDN input projection groups, normalization/gates/residuals, and compiled pure islands. Profile actual quantized/NAX kernel dispatch and tile occupancy first. | OPT01 | Preserve intended rounding or pass the declared numerical contract. Reduce measured launches/critical-path time without increasing footprint beyond budget; benchmark real shapes and both decode/prefill where affected. |
| OPT19 — overlap independent expert work with misses | Launch shared and resident-expert computation while missing weights are read; reassemble original routing order in `Layers.swift`/`ExpertStore.swift`. | OPT01 and the existing path's lifetime proof; OPT12/13 only when integrating changed row/reduction scheduling | No cache-content-dependent arithmetic or kernel changes; identical cold/warm results where applicable. Events show useful overlap and latency improves under mixed misses. |
| OPT20 — stable arena and device-visible slot mapping | Prototype reusable buffers, device expert-to-slot mapping and compact miss notifications. Replace broad layer synchronization only with verified explicit lifetimes/events. | OPT19; profile must show residual handoff/scatter cost | No slot reuse before GPU readers finish; stale asynchronous completions are rejected by pool/scope generation identity; donation/resize/cancellation safe. Lower all-hit and mixed-hit critical-path time. Keep existing synchronization until the replacement passes. |
| OPT21 — cache admission and bookkeeping | Test trace-driven frequency/reuse-aware admission, per-layer stall distributions, recent-tail warming, compact lookups, pinned-ID tracking and bounded read-worker scheduling. | OPT00, OPT01 | Improve actual stall time on varied held-out routing traces at equal bytes; no scan pollution regression or routing changes. Do not optimize aggregate hit rate while critical layers slow down. |
| OPT22 — verified local record/row layout | Independently test expert-major contiguous records and n-gram row/page layout against actual checkpoint offsets; include read scheduling, reusable aligned staging and any scatter elimination. | OPT01, OPT21; coordinate with OPT16 | Exact tensor-byte equivalence, bounded memory and explicit disk-space cost. Require atomic construction, identity/version checks and interrupted-build/read-failure tests from the recovery contract below. Benchmark prefill and random decode separately. Direct reads into shared GPU storage require OPT20's lifetime proof. Retain only layouts with a meaningful net benefit. |

Stage exit: a profile explains the remaining single-token bottleneck. Existing zero-copy staging, grouped quantized kernels and intralayer overlap are preserved unless measured replacements are better. A frontend language rewrite is not a substitute for these results.

### Stage 4 — make speculative decoding and recurrence cost-effective

| Item | Concrete deliverable and code surface | Dependencies | Acceptance |
|---|---|---|---|
| OPT23 — fused GDN state recording | Add a bounded recording kernel in `Vendored/GatedDelta.swift` that writes the intermediate states required by verification without repeated one-step launches. | OPT02, OPT01 | Exact recorded state/output against the existing recording path for every accepted length; no rerun of accepted target tokens; reduced recording overhead. |
| OPT24 — preserve valid MTP cache work | Reuse the first provisional draft-cache entry when its true main-model input is valid, and rebuild only entries needing verified hidden states. | OPT03 | All acceptance/rejection/stop lengths preserve token counts, offsets, MTP validity and future drafts. Measure executed work; lazy-pruned head graph nodes are not charged as full head forwards. |
| OPT25 — adaptive speculation and bounded tails | Cap draft length by remaining output; test short-query attention splitting/accepted OPT15; choose depth/off using observed acceptance, verify cost, context and cache pressure, with explicit draft-state invalidation/rebuild on mode transitions. | OPT01 and the state-validity contract below; OPT23–24 and OPT15 are optional accelerators | `mtp-check`, parity where required, sampler contract, mode-transition checks and quality pass. Improve time per emitted target token at matched total memory; revert to plain decode when the draft head is not worthwhile. |
| OPT26 — chunk-parallel GDN algorithm | Prototype N6 P5's bounded chunkwise recurrence formulation independently on the current prefill path, retaining the current kernel as the numerical reference. Include coefficient preparation, zero decays, partial blocks and state propagation; tune recurrence block length independently from the expert-read scope. | OPT00/01; OPT02 ownership for integration and a profile identifying recurrence as material; OPT16 is not required | Validate FP32 state drift, compensated sums, normalization, long sequences and final task quality. Show a net prefill gain after added intermediate work. Reject naive dense-transition scans if their added work dominates. |

### Stage 5 — vision, prefix reuse and serving efficiency

| Item | Concrete deliverable and code surface | Dependencies | Acceptance |
|---|---|---|---|
| OPT27 — correct vision attention dispatch | Compare a fused 72-wide kernel with carefully scaled padded alternatives in `Vision.swift`. | OPT01 | Confirm dispatch, bounded peak and actual image-request TTFT. Validate tower outputs and known-content image tasks. Padding is not adopted solely because it reduces a synthetic allocation. |
| OPT28 — image preparation, feature reuse and startup | Profile cold tower loading, preprocessing/layout conversions and duplicate-frame handling; add a bounded feature cache keyed by content plus model/processor identity where useful. Inspect download/load/tokenizer startup only as measured cold-start costs. | OPT01, OPT08 | Warm/cold image requests and changed preprocessing invalidate correctly; live memory is charged; pixel/layout changes preserve the declared numerical contract. No redundant work when prefix reuse already covers the image. |
| OPT29 — valid prefix checkpoints | Add intentional reusable system/common-prefix checkpoints with explicit GDN state and paged/COW KV ownership; preserve multiple client entries and canonical token/image identity. | OPT08–09 | Edited prefixes, interleaved conversations, auxiliary requests, long followups and eviction pass. Lower TTFT after charging state memory against expert capacity. Never infer rewindable recurrent state from a string LCP. |
| OPT30 — bounded output and responsive governor | Decouple socket writes via a bounded queue and make memory-policy action possible at safe execution boundaries in `Server.swift`, `Engine.swift` and `Governor.swift`. | OPT01, OPT08 | N6 3.2/3.3 requires eligible committed-prefix preservation on prefill cancellation, absolute-base progress and bounded cleanup before scope adoption. Slow/disconnected clients cannot stall unrelated inference indefinitely or create unbounded buffers. Cancellation, SIGPIPE handling, generation serialization, pool resize and prefix ownership remain correct. Test real serving paths and simulated policy. |
| OPT31 — optional continuous batching | Profile demand first; prototype scheduling multiple independent recurrent states only if aggregate throughput is a product bottleneck. | OPT20, OPT29–30 | Explicit per-client state/routing/pinning and fairness; improved aggregate throughput with a separately stated per-user latency tradeoff. Otherwise record a deferred disposition; this is not required to improve single-user inference. |

### Stage 6 — qualify the combined runtime and close every finding

| Item | Concrete deliverable and code surface | Dependencies | Acceptance |
|---|---|---|---|
| OPT32 — integrated performance and correctness matrix | Combine verified changes incrementally, rerun the relevant baseline cells at fixed total memory, and complete the full existing acceptance battery on the final candidate. | Adopted items from Stages 1–5 | No quality, API, cancellation or memory-policy regression; paired improvements are beyond A/A noise for the declared target workloads. Report regressions and startup/steady-state tradeoffs. No unattended large-memory tests. |
| OPT33 — coherent planner, artifacts and delivery readiness | Finalize estimator calibration, compatibility/fallback dispatch, temporary-flag cleanup, run records, measurement sections, claims and generated projections. | OPT32 and refreshed OPT08 | One coherent measured parameter family; source/dependency and weight identities recorded; standalone CLI/server compatibility maintained. Build/static/brain/projection/claim checks pass. Record hardware coverage limits; local M5 evidence does not certify every Mac. |
| OPT34 — disposition all broader alternatives | Maintain the register below with measured outcomes or explicit revisit conditions. | OPT00; later profiles identify any useful successor | Every audit recommendation has an implemented, rejected, conditional or deferred outcome. Quality-changing alternatives remain distinct from equivalent runtime optimization. The plan never marks an unexplored candidate as a demonstrated improvement. |

Completion of OPT33 makes an implementation ready for delivery; this planning task does not claim implementation, benchmarking or release completion. Subsequent performance work starts from the measured remaining bottleneck rather than automatically enabling every prototype.

### Broader-alternative register for OPT34

| Candidate | Conditional/deferred disposition and reason to revisit |
|---|---|
| Lower-bit expert weights, reduced top-K, expert pruning, smaller/different base model | Separate quality/compute comparison. Revisit with frozen task-quality criteria and byte math; never call deleted model contributions an equivalent optimization. |
| BF16/quantized recurrent state or quantized KV | Quality-sensitive. Revisit only if measured state/KV bandwidth or capacity warrants it; GDN drift requires long-sequence tests. |
| Lossless expert compression | Low-priority experiment after read reuse/layout. Revisit with representative data, codec/decode cost and end-to-end traffic measurements; the limited compressed-weight sample is insufficient. |
| BF16-dequantized hot experts | Conditional reuse-heavy compute experiment; charge lost cache capacity and conversion time. |
| Streaming more resident weights / row-caching embeddings | Conditional bounded prototype: source confirms separate lookup embedding and dense lm_head. Exact packed lookup payload357,580,800B;8192packed rows11,796,480B. Theoretical payload difference345,784,320B (125expert records), before buffers/overheads. No planner credit until checked main/MTP reads, exact row parity, public tensor compatibility, resource and fixed-total-memory serving gates pass. |
| mmap-based storage / direct CPU-to-GPU arena writes | Mapping alone does not manage residency. Any useful direct-write variant belongs under OPT20/22 and must prove lifetimes. |
| Cross-layer expert prediction/read-ahead | Conditional; requires measured prediction utility and bounded wasted reads/staging, since exact routing is not available early. |
| Another draft model or speculative tree | Conditional after OPT25; charge extra memory and acceptance/verification cost. |
| Faster/multiple SSDs or another compute device | Hardware study only if measured storage/compute bounds justify it. Existing hardware results do not predict latency for another machine. |
| Activation spill for a larger read scope | Conditional OPT16/34 alternative after in-memory scopes; charge every frontier write/read, disk space, cleanup and prompt-data custody. No once-read total-traffic claim that excludes spill. |
| Full frontend/backend language rewrite | Deferred unless a profile identifies a concrete bottleneck the targeted OPT18/20 changes cannot address. |
| Lower image resolution | Explicit input-quality tradeoff; separate from OPT27's kernel equivalence. |
| Energy optimization | Measure joules per emitted token and useful request when a reliable measurement method is available; no energy-efficiency claim from elapsed time alone. |
| Weight hosting/download changes | Cold-install scope, not steady inference. Revisit only from a measured install/startup bottleneck; OPT28 profiles the relevant cold path. |

### Test and evidence contract

Existing commands verified in source include `Tools/sampler_gates.sh`, `Tools/planner_gates.sh`, `slotstream prefix-check`, `sweep-check`, `elastic-check`, `mtp-check`, `mtp-parity`, `context-check`, and `Tools/verify.sh`. Use their actual option parsing at implementation time. Optional MTP/vision fixtures must be present before claiming their gates passed. A missing optional fixture does not block text-only work, but an affected feature cannot be marked verified or enabled by default until its checks run. Keep missing-fixture skips explicit. `Tools/verify.sh` is a final integrated gate, not something to rerun after each prose or low-impact change.

Add focused checks for mechanisms not covered today: ownership/retention across continued tokens, per-request footprint sampling, exact tail scheduling, indexer block rollback, compact sampler ties, deterministic tiled output placement, event-safe pool mutation, final-token prefix accounting, and bounded slow-client backpressure. Each check must exercise a behavior that could regress, not merely mirror the implementation.

Two numerical classes govern acceptance:

- **Unchanged arithmetic:** ownership, byte layout, admission, scheduling of independent operations and lookup changes must retain exact values/routing where operation shapes and arithmetic remain the same. Check full state when the change touches it, not only the final answer.
- **Changed grouping/kernels:** prefill rechunking, fused attention, projection/reduction changes and recurrence reformulations need the existing rechunking control plus state/logit/routing/quality checks. Matching greedy text alone is insufficient; do not hide drift by refreshing goldens.

For each kept candidate, capture the baseline/candidate command, binary/source/dependency identities, run order, input identities, total-memory settings, VM state, primary metric and full result. Use interleaved paired runs and report medians plus tails with the A/A comparison. Mark loaded/swapping runs discarded as timing evidence. Measure both fixed pool (mechanism) and fixed total memory (product effect) when the candidate changes resident storage. Cache-content invariance and prefix/MTP correctness are separate from numerical differences caused by batching.

### Whole-engine interaction and recovery contract

These are mandatory acceptance details for the affected OPT items and OPT32. Several are already specified for prefill in N6; they now apply to the combined runtime as well.

**MTP validity is a state invariant, not a non-null pointer.** Current `Generate.swift` selects a cached draft state using `state.mtp != nil`, while the plain decode path advances the main model without updating that draft cache. This was designed for a fixed serving mode. OPT25 must not introduce dynamic off/on transitions on top of that assumption. Track whether draft state covers the exact consumed main prefix, including the required previous multi-stream row. Disabling speculation must either keep draft state correctly maintained or invalidate/discard it. Re-enabling requires a fresh state or an explicitly bounded, verified reconstruction; a stale non-null cache is never eligible. Start with request-boundary mode selection. A later mid-request fallback may occur only at a committed verification boundary and must establish the validity state for the next request. Include the cost of maintenance, rebuild, resident head and any displaced expert slots in the policy measurement.

**Only a coherent whole-model boundary may be published as reusable state.** During layer-major work, different layers may cover different token counts. A progress percentage is not a reusable prefix. On cancellation/error, restore the last committed boundary or discard the request state, join owned jobs, and restore recording mode, allocator limits, admission flags and offsets. Do not resume partially advanced layers or cache them as complete tokens. N6 defines the bounded scope checkpoint strategy. Pool resize and numerical/state-layout changes occur at compatible committed boundaries or after abort-and-restore, not merely when the host has temporarily stopped launching work.

**Queued output is distinct from delivered output and consumed tokens.** OPT05/30 must track these separately. A socket failure after enqueue must cancel work promptly without duplicating emitted tokens or storing a prefix the model did not consume. Preserve tool-call/UTF-8/stop-sequence framing and completion reasons. Report time to first token ready, first protocol delta, and first visible answer text separately; a reasoning/special token need not be user-visible output.

**Derived layouts and asynchronous reads need failure recovery.** OPT20/22 must test short/interrupted reads, read errors and stale completions after cancellation/resize. Stage completion publishes a slot mapping only after the complete intended tensor record is ready. Attach pool/scope generation identity so an old completion cannot overwrite a recycled slot. An interrupted repack or disk-full condition leaves the verified original checkpoint usable: construct into temporary storage, verify lengths/content/layout/model/quantization identity, then atomically publish the completed artifact/manifest. A stale or corrupt derived layout is rejected; safe fallback uses the verified original only when the request can restart from a coherent state. Simulate failures through bounded seams, not by filling the user's disk or corrupting original weights.

| Interaction to exercise | Required invariant | Owning items |
|---|---|---|
| Prefix reuse, MTP on → off → on, including a length-capped pending token | Main/draft offsets and previous multi agree, or the draft cache is explicitly invalid and cannot be reused | OPT03/05/24/25/29 |
| Layer-major prefill canceled in each layer family and across an image span | Restore/discard at a whole-stack boundary; retry has correct tokens, image identities and n-gram history | OPT02/09/14/16/29/30 |
| Speculative rejection across a four-token indexer boundary and a KV capacity-growth boundary | Exact accepted prefix, raw tail, completed block cache and storage ownership survive trim | OPT09/23–25 |
| Pool shrink or canceled scope with delayed I/O completion | No stale completion, mutation of a GPU reader's slot, leaked pin or unjoined worker | OPT19–22/30 |
| Queue saturation, split UTF-8 or tool-call output, stop sequence and disconnect | Bounded buffers; exact token/byte framing; consistent consumed/pending/delivered accounting | OPT05/06/30 |
| Repeated requests, image changes, prefix eviction and regrowth | Memory reaches a bounded steady state at fixed maxima; no stale feature/state reuse | OPT02/03/07–09/28–30 |
| Optional MTP absent, image feature disabled, and capability-specific kernel unavailable | A validated supported path remains; skipped feature checks are not claimed as passes | OPT15/18/23/25/27/33 |

Use cheap state/shape/fault-seam checks for combinatorial coverage and representative bounded real-model runs for integration. A bounded repeated-request soak is needed to catch lifetime growth that a single prefill misses; freeze its duration and request sequence before running it. This is not a large-memory stress experiment.

### Benchmark comparability and hardware coverage

OPT00 freezes both development workloads and a separate confirmation set before tuning. Do not select policies or regenerate fixtures based on the confirmation results; a changed policy gets a newly declared confirmation protocol. Use a fixed teacher-forced decode suffix as a mechanism control and real free generation as the product/quality check. They answer different questions. Count actual output tokens and EOS/stop behavior; different generated continuations are not identical decode work. An all-hit test must demonstrate actual hit counts with a fitting working set or a clearly labeled layer-level replay; it must not allocate the entire expert bank on this Mac.

A new process does not establish a cold filesystem cache. Label cache state from the actual setup and observations; do not globally purge the user's machine to manufacture a cold run. Record power mode, power source, thermal state where available, background load, warmup and sustained-run conditions. Freeze repetition and tail-estimation requirements after the A/A pilot; do not report a reliable p95 from too few independent observations or treat correlated token samples as independent runs. Predeclare nonregression limits for affected short/long, warm/cold, text/image and sampled/greedy cases, not just the primary winning cell.

Report the physical-footprint sampling interval and its overhead. A sampled high-water mark can miss short peaks: combine it with event-boundary/live-allocation accounting and conservative memory bounds rather than claiming continuous peak coverage. Keep allocator memory and process measurements distinct.

The public package currently declares macOS 14 support, and shader loading distinguishes macOS 14, 15 and 26 families (`Package.swift`, `Tools/fetch_metallib.sh`). A new kernel must be gated by actual supported capabilities/shapes with a tested existing fallback. Test dispatch and initialization failure before state mutation. Do not switch to a fallback and continue a possibly corrupted request after a GPU execution failure. Validate known short-query/head-dimension/tile boundaries and separately confirm the packaged metallib/JIT path. Local M5 timing is scoped to that machine; activation on an untested older GPU/OS remains restricted until compatibility evidence exists. Core text improvements that retain the established portable path need not wait for optional GPU-specific accelerators.

Kernel changes can preserve algebra while changing softmax, normalization or reduction rounding. For OPT06, first remove redundant sorting/synchronization while retaining normalization/CDF behavior where required by the exact sampler contract; a compact reduction is a separate numerical candidate. Likewise, an upstream MLX alternative must be pinned and evaluated independently, not mixed into a kernel comparison as an unrecorded dependency upgrade.

### Coverage review and readiness limit

All audit areas have an owner: cost/measurement (OPT00/01/08), retained state (OPT02/03), scheduling and read reuse (OPT04/12–16), attention/indexer/RoPE and selection (OPT09–11/15/36), decode/router/storage (OPT17–22), GDN/MTP (OPT23–26), n-grams (OPT07/14/22), generation/sampling and unused prefill work (OPT05/06/35), prefix/governor/serving (OPT08/29–31), vision/startup (OPT27/28), and broader alternatives/delivery (OPT32–34).

The latest review unifies N6 and the whole-engine program, adds OPT35/36, makes the reduction-storage alternatives explicit and removes artificial probe dependencies. Earlier MTP validity, recovery, benchmark and hardware gates remain binding. The first ownership/instrument slice remains appropriate; the following independent probes now have a clearer route. Readiness means that the identified work has a sound implementation and validation route. Numerical tolerances, practical kernel wins, final scope sizes and combined latency gains are still experiments; they cannot be certified by reviewing a plan.

### First implementation slice

1. Freeze OPT00's small representative baseline and add OPT01's bounded observations.
2. Implement OPT02, then verify exact continued-state behavior and footprint before recalibrating any memory allowance.
3. Extend the ownership correction to OPT03 when the MTP fixture is available; otherwise keep that feature explicitly unverified and proceed with text-only work.
4. Implement OPT04 as a separate change; validate the existing scheduling envelope and actual pass/read counts.
5. Land or reject each result independently. Add OPT35's unused-mixer substep and OPT36 fixed-score probes alongside OPT05/06/09; test P1/P2 overlap, attention and GDN according to the measured exposed stalls.
6. Once local bounds pass, compare N6 U1 workspaces under OPT16 at matched memory, then integrate winning kernels/overlap and run a combined A/B. Record each OPT status here and its detailed evidence in N6 without counting predecessor gains twice.

The starting point, dependency order, tests and completion criteria are defined. Execution can begin without another planning pass; experimental branches are resolved by their stated measurements rather than assumed to be improvements.

The V38–V41 all-hit investigation is preserved in [[sources/runs/2026/09/2026-09-05-optimization-all-hit-compute]]. A 24 GB reference calibration proves the selected request can be all-hit, but the cross-process comparison delivered no candidate cells because the unchanged 30/30.3 GB headroom requirement did not fit. Its V39 bounded-wait successor started no model; a 22 GB V40 calibration still read experts and cannot qualify as all-hit. The bounded V41 full-model next-token replay instead fits 640 slots and passes 109 checks, including exact logits/routes and zero expert reads in every measured cell. All five candidate pairs are valid: router caching reduces median replay time by 2.08716%, compiled normalization by 0.562315%, each with four positive pairs. Both fail the unchanged 5% gain threshold and remain off. This closes the missing small-pool all-hit mechanism measurement for these prototypes; fixed-total memory, cold-start and broader compute fusions remain unqualified. No production integration or whole-program completion is claimed.

OPT28 source preparation now has bounded metadata admission before CGImage creation/EXIF rotation, a cumulative one-GiB source charge, checked resize errors, failure cleanup and exact request-local source sharing under the default-off image-reuse control. V42–V44 raw evidence is [[sources/runs/2026/09/2026-09-05-optimization-image-source-admission]]. The failed initial CLI invocation and overly specific malformed-PNG assertion are retained. The corrected catalogue passes all thirty groups (132 vision assertions); failure recovery passes twelve checks, source refusals pass nine live checks across Ollama/OpenAI/Gateway, and both source-reuse serving sequences pass twelve checks. The preserved JPEG pixels and full tower embeddings are byte-identical. Duplicate preparation counts change from two to one without sharing different image bytes; the reduced admission charge is not a physical-memory or latency gain claim. Maximum-image resource qualification, actual allocator/GPU failure and integrated adoption remain open.

OPT21 now fixes duplicate missing-key slot ownership and validates expert keys and total pin capacity before mutation; the checked API exposes bounded request errors while the existing API stays source-compatible. V45 passes 160 real-weight checks across container modes and resize/retry boundaries, plus all thirty catalogue groups. V46 serving retains six clean exact pairs after one swap-excluded pair and passes the explicitly frozen 5% latency non-regression gate (median reduction 0.423885%, not a speed claim). Raw evidence, the failed V45 protocol delivery and the sixteen-test cross-binary harness are preserved in [[sources/runs/2026/09/2026-09-05-optimization-duplicate-pool-requests]]. Checkpoint I/O recovery, asynchronous completion lifetimes, admission policy and integrated defaults remain open.

OPT06 now corrects the exact-zero inverse-CDF boundary: the frozen predecessor selected a filtered-out token for a valid seed, while the corrected path preserves RNG evolution and returns the only legal top-k-one token. The independent oracle and all 647 sampler assertions pass. The separate default-off GPU draw path removes one host scalar readback and passes the native component gate: 13.9789% median filtered-sampler reduction alone and 17.4517% with threshold selection, with exact sequences in every pair. Full sampled serving retains all seven clean pairs per candidate and passes the separately frozen 5% latency non-regression gate; whole-request median reductions of 0.1060%/0.1266% are not meaningful speed claims. All evidence, exact identities, seventeen harness tests and the original counterexample are preserved in [[sources/runs/2026/09/2026-09-05-optimization-sampler-device-draw]]. Positive-draw semantics and prior goldens remain intact. Confirmation, MTP integration and production optimization defaults remain open.

### Adaptive speculation V49–V51: lifecycle passes, transparent timing policy rejected

[[sources/runs/2026/09/2026-09-05-optimization-adaptive-speculation-counterexample]] preserves 129 initial model assertions, all 31 catalogue groups, the expanded 256/257 result and the subsequent 32-token timing-sensitivity counterexample. The sole expanded failure compares greedy outputs against plain decode; the sensitivity probe then changes only timing observations and obtains different continuations. Fixed-depth MTP also differs from plain, so this is a batching/reproducibility limit, not a task-quality measurement. The state machine, bounded tails, EOS/callback cancellation and invalidated-head cache lifecycle pass their checks. ADAPTIVE_MTP stays off and the frozen V50 serving study is not run. A deterministic request-boundary policy, explicit numerical/quality qualification and fixed-total-memory benefit remain open for OPT25. The same source captures the passing V48 static/planner/installer transcript; the full integrated real-model acceptance suite remains open.

### Fixed-schedule MTP serving V51: exact outputs, no accepted request gain

[[sources/runs/2026/09/2026-09-05-optimization-fixed-mtp-serving]] preserves seven clean pairs each for first-entry reuse and fused GDN recording at the same 10 GB target and fixed depth one. All twenty-one cells satisfy resource checks and all IDs/text match. Median client reductions of 0.0432633% and 0.1919425% fail the five-percent gain gate; neither candidate meets the eighty-percent-positive gate. Controls remain off. High-residency/component performance and combined qualification remain separate; OPT23/24 are not closed by this short serving result.

### Runtime allocation V52–V55: protected reservations and measured cache benefit

[[sources/runs/2026/09/2026-09-05-optimization-runtime-budget]] preserves 561 planner assertions, all 32 catalogue groups, 24 live lifecycle checks, two predecessor diagnostic failures and both serving studies. The default-off RUNTIME_BUDGET path transfers only unused explicit prefill/prefix reservations to expert slots, persists its policy through metadata/vision/governor changes, and prevents restoring spent reservations through cache toggles. An active correctness fix separately prevents first-image loading from regrowing previously donated capacity. At 10 GB, the plan moves from 961 to 1217 slots while retaining the previous expected-peak envelope; constants and full active-context charges stay unchanged. Seven clean exact-output 8.1 GB development pairs all improve, with median 4.9062062% client reduction and decode records 4722 → 4177, but miss the frozen 5% gain gate. The independent 10 GB chat confirmation has four clean exact pairs after round-four swap exclusion, median 4.1457244% reduction, and fails both five-pair and gain requirements. No replacements run; the option remains off. Larger-target prefill reservation recovery and full OPT08/integrated qualification remain open.

### Cache admission V56–V58: rejected broad policies and a bounded successor

[[sources/runs/2026/09/2026-09-05-optimization-cache-policy-screen]] preserves exact replay scripts, trace bytes and all results. Bounded frequency eviction fails five of six cells. Broad layer-local replacement reduces reads at 640 slots but regresses the larger-capacity development cases, so neither broad policy enters inference. A newly frozen 640-slot-only successor passes two fresh reference trace screens after replay exactly reproduces actual decode records, including prefill warmth: compiler 21414 records and containers 20280, with predicted reductions 5.7486% and 6.3166%. These are causal offline record predictions, not wall-time or quality claims. V59 starts a default-off native FLOOR_CACHE path restricted to exactly 640 slots and ordinary demand loads; larger pools and staged sweep admission retain CLOCK. Its state, lifecycle and performance qualification remain open.


### Native floor cache V59–V60: correctness passes, serving gate fails

[[sources/runs/2026/09/2026-09-05-optimization-native-floor-cache]] preserves 226 pure victim, 1128 state assertions at each of 255/256 tokens, 367 MTP, 33 actual allocation-boundary checks and all 33 catalogue groups. Development is invalid delivery evidence with zero model pairs because another process held the model lock. The independently frozen confirmation has three clean exact-output pairs after two whole-pair swap exclusions; median client reduction 2.3462096% fails the five-percent gain and five-pair requirements. Candidate decode reads are 9398 versus 9965, with 8938 observed local victims in every candidate cell. No replacement, threshold change or production-default adoption follows. All raw excluded cells and both frozen source closures remain preserved.


### Responsive governor V61–V63: safe boundaries and overhead qualified

[[sources/runs/2026/09/2026-09-05-optimization-responsive-governor]] preserves the default-off pressure latch, nonblocking busy poll, explicit-plan immunity, queued allocation refusal, safe cancellation/resize, floor prefix/cache release and serialized lifecycle. All 34 catalogue groups and 59 live lifecycle assertions in each of plain/MTP mode pass, including partial scope rollback, chronological prefill, streamed/nonstreamed decode, exact retry and stop while the governor waits for generation. Controlled scope pressure-to-boundary observations are 5.52/6.85 ms; streamed callbacks observe pressure in microseconds after avoiding an unnecessary next forward. These are diagnostic observations, not hardware preemption guarantees. V62 serving failed before inference from a malformed arm envelope; the unchanged seven-pair design runs as delivery correction V63. Six clean exact-output pairs pass the declared 5% median overhead limit with 0.3313024% median client regression; round two remains swap-excluded. No request speedup is claimed. Image preparation observation is now mandatory for encoded/reused/prefix-skipped image work; all 17 harness tests pass. Real serving pressure/final-frame behavior, an in-flight image encoder boundary and integrated defaults remain open for OPT30.


### Compact main indexer history V64–V67: ownership and recovery pass; resource study inconclusive

[[sources/runs/2026/09/2026-09-05-optimization-compact-indexer-history]] preserves the default-off completed-key/raw-tail implementation, checkpoint storage, arbitrary recording-span rollback, terminal key-only updates and cached-state option toggles. Draft raw history remains intact for its public arbitrary trim contract. All 35 catalogue groups pass; native main checks expand from 1128 to 1468 and cached MTP passes 526, with exact logits, routes and continuation. At 8192 component tokens the indexer allocation is 589824 versus 2621440 bytes (77.5% lower); at 2051 model tokens total allocated sequence bytes fall by 7077888 (8.3333%). The frozen 2509-token code resource study has zero clean pairs because the first three reference intervals swap; it stops for futility during the third candidate once five clean pairs become impossible. Candidate allocation observations match actual MLX active savings, but no process-memory or latency qualification follows. The separate 20 GB runtime-reservation calibration has 4834 slots and still performs 2933 prefill plus 1479 decode reads, so it fails its all-hit criterion without a larger-cap retry. Compiler delivery failures, partial cells and all source identities are preserved. The harness suite passes 18 tests after arm-schema validation and 19 after per-pair allocation gating. No reservation or default changes; real resource qualification, vision-boundary integration and full acceptance remain open.


### Default-path acceptance V68–V69: broad correctness passes; long-memory qualification remains open

[[sources/runs/2026/09/2026-09-05-optimization-default-acceptance]] preserves the complete frozen default-path verification and targeted harness corrections. V68 passes 21 top-level gates, including all model hashes, numerical and state references, elastic/prefix/sweep/MTP checks, 15 behavioral probes and 25 real image-serving checks. Four failures exposed missing physical sampling in two commands, a long-recall answer truncated inside reasoning, and a Bash 3.2 empty-array failure before API launch. V69 enables sampling, preserves the same long prompt and 16-token limit under the ordinary non-thinking template, requires a completed exact recalled value, and fixes the empty server arguments. Short memory passes at 6,397,939,144 sampled physical bytes; all 74 API robustness checks pass. Long recall passes with 7,972 prompt tokens and four output tokens, but swap excludes the long-memory interval. No replacement or threshold relaxation ran. The 20-test Python harness suite passes. All experimental controls remained default off, so this is default-path acceptance evidence and does not qualify combined optimization defaults or end-to-end speed.


### Actual-offset expert layout V70–V72: qualify a pool-read prototype; reject sweep adoption

[[sources/runs/2026/09/2026-09-05-optimization-actual-expert-layout]] preserves the matched per-piece parallel benchmark, byte-verified bounded repack, cache counterexample and cold correction. V70's apparent gains were file-cache measurements: V71 recorded zero packed disk-read bytes for a 5.66 GB requested payload. V72 instead constructs uncached packed and sparse column copies at the original tensor offsets and requires disk-read coverage in every accepted cell. Six bounded fault/identity/atomic-publication tests pass. Seven clean exact pairs each for random batches of 1/4/10 misses show median component reductions of 19.0802305%/16.8597787%/10.6965409%; six pairs for 32 misses show 9.8008323% after one swap exclusion. These pass the frozen component gate, include deinterleave cost, and support a pool-path runtime experiment only. The bounded artifacts occupy 11,337,527,296 filesystem bytes. Dense sweep retains only one cold pair, 25.7104351% slower; subsequent reference cells are cache-resident. Half-density sweep also exceeds the packed scratch physical-memory bound and has no qualified pair. No sweep-packing or runtime default is adopted. Full-model packing, startup verification, failure recovery and end-to-end benefit remain open.


### Synchronous request read recovery V73–V76: lifecycle, HTTP and overhead gates pass

[[sources/runs/2026/09/2026-09-05-optimization-request-read-recovery]] preserves checked expert batch/run reads, atomic row-cache insertion, completed-record slot publication and error propagation through ordinary prefill, read scopes, decode and speculative verification. All synchronous workers join before partial staging is freed. On failure the generator drains previous valid pool copies/GPU readers, drops the partially advanced request state, restores allocator limits and resets pins/admission/recording; emitted tokens remain in an explicit error result and partial state is never cached. Existing nonthrowing library APIs remain available; low-level checked callers must discard or checkpoint-restore state after errors.

V73 passes 1,322 real-weight storage/pool recovery assertions and 702 ordinary generation assertions; V74 also passes all 702 generation checks after propagation. V75 passes 151 plain and 160 MTP request-failure assertions, including cache hits, second-pass failure and exact retry. V76 passes 120 real HTTP-handler socket-pair checks across Ollama/OpenAI/Gateway, streamed and JSON responses and both output writers. Every stream error follows delivered text, has valid termination without false success and permits a subsequent request. These are handler-level checks, not TCP admission tests. Both V73 and V76 pass all 35 catalogue groups. The frozen seven-pair fixed-pool serving study retains six clean exact pairs after round-six swap exclusion: median client latency regresses 0.1935286813%, passing the declared 5% non-regression gate. No replacement or speedup claim. Recovering MLX allocator/GPU faults, optional descriptor-open compatibility behavior, asynchronous write epochs, packed runtime layout and combined defaults remain open.


### Packed per-read verification V77: rejected after cold-qualified measurement

[[sources/runs/2026/09/2026-09-05-optimization-packed-read-verification]] preserves unchanged V72 criteria with SHA-256 inside every timed packed read. Random batches of 1/4/10/32 misses retain 6/6/5/7 clean exact pairs but regress median component duration 127.5204296%/47.8034520%/21.7805559%/0.9773315%. All speed gates fail. Dense sweep has only one cold pair and sparse sweep none; cache and footprint exclusions remain binding. Six builder tests and two native corrupt/truncated digest failures pass. No replacements, original-weight changes or runtime adoption. Moving verification to load time is still a distinct candidate, requiring measured startup cost, owned-descriptor identity, post-load change rejection and end-to-end benefit.


### Native load-verified expert layout V78–V79: correctness passes, serving and startup reject adoption

[[sources/runs/2026/09/2026-09-05-optimization-native-packed-layout]] preserves the native atomic builder, complete load-time verification, config/tensor/source fingerprint, owned descriptor, mutation checks and joined error-to-original recovery. Sweeps remain on original tensor runs. The independent artifact contains 67,947,724,800 payload bytes and occupies 67,947,728,896 disk bytes; construction completed in 54.6058510 s but global swap excludes strict construction cost/resource qualification. All 226 component, 36 catalogue, 993 real-weight storage and 1,130 state/router/rollback assertions at each of 255/256 tokens pass. Plain/MTP failures after delivered text pass 16/18 lifecycle assertions with exact original retry. The 22-test measurement harness now preserves startup separately and gates its amortization estimate on equal work and positive paired steady savings.

All seven exact serving pairs are clean and slower: median client regression 2.96969496% fails both declared speed criteria. Five clean startup pairs add a median 9.6189655 s to ready and 9.7550058 s to the first completed job. No positive steady saving exists to amortize these costs. Experimental packing stays explicitly opt-in and is not adopted; no sweep/default changes or replacements. File stamps detect ordinary changes, not adversarial immutability or silent bit rot between load and read. V72 component gains remain scoped to their cold I/O study and cannot be presented as serving gains. N-gram row/page layout and asynchronous prefill I/O are separate open work.


### Bounded n-gram lookahead V80–V82: exposed waits shrink, full-request speed gate fails

[[sources/runs/2026/09/2026-09-05-optimization-ngram-lookahead]] preserves immutable CPU read tickets, exact history/generation binding, original insertion order/accounting, admission within unused cache capacity and cancellation/join through every read syscall. No worker mutates model state or GPU slots. V82 passes 37 catalogue groups, 530 row/cancellation assertions, 1,130 full-state/router/rollback assertions at each of 255/256 tokens, 160 ordinary and 169 speculative failure assertions, and exact layer-zero/one Python parity. V81's scalar-view diagnostic error is retained and corrected without changing the model's arithmetic.

Seven fresh-process 440-token prose pairs are clean and exact. Each arm reads 22,016 expert records; candidates consume 6,736 asynchronous rows. Median exposed n-gram prefetch falls from 119.494917 to 14.428833 ms, but median request time regresses 0.198106304%, failing the 5%/80%-positive gate. Peaks remain 6.562–6.663 GB under the 8.1 GB target. This stage accounts for only about 1.6–2.0% of reference request time. No replacement, warmup relabeling or default adoption. OPT14's lookahead speed hypothesis is rejected for this workload; its bounded PLE tiling and larger-regime qualification remain separate. The next priority is a measured material cost, including the OPT26 recurrence profile and remaining pool handoff/lifetime work.


### V83 — recurrent-layer attribution and OPT26 disposition

Evidence: [[sources/runs/2026/09/2026-09-05-optimization-gdn-phase-profile]]. A package-only observer times GDN preparation, recurrence and finish with explicit evaluation boundaries. Five alternating traced/untraced full-model replays at each of 1/256/1024 tokens pass all 3,600 assertions, including exact logits, ordered routes and every state byte; all extents are complete, nominal and whole-process swap-clean. Catalogue: 37 groups pass. The actual register-resident chronological recurrence remains unchanged.

At 256/1024 tokens, median paired serialized recurrence divided by untraced forward time is 3.078158%/4.486361%, below the frozen 5% materiality screen; tracing overhead is 1.528215%/7.682230%. Existing expert-read time is 1.491718/1.936804 seconds of median 2.334185/3.704381-second untraced forwards. Single-token tracing adds 58.14%, so its phase attribution cannot establish a decode speed opportunity by itself. These are fixed-forward component observations, not request throughput, and phase forcing changes overlap.

**OPT26 disposition: conditionally deferred for the measured 256/1024-token regimes because its material-cost prerequisite is not met.** Revisit after a qualified reduction in read/other costs or a measured larger compute-bound prefill. A successor still needs zero/underflow-safe coefficient construction, partial-block and FP32-state/compensated-reference tests, and end-to-end gains after triangular/intermediate work. No untested chunk algorithm is represented as implemented, rejected by measurement, or faster. Continue OPT20/22 with actual allocation/handoff/scatter attribution before replacing existing managed-buffer staging.


### V84–85 — actual expert staging and scatter attribution

Evidence: [[sources/runs/2026/09/2026-09-05-optimization-expert-transfer-profile]]. V84's first probe stops before timing on a diagnostic final-pin guard; V85 releases completed-forward bookkeeping pins at the explicit boundary, retaining the guard. V85 then passes 4,860 exact state/logit/router/payload assertions across five clean alternating all-miss forward pairs at each of 1/32/256/1024 tokens. V84's 37 catalogue groups pass. No constructor copy fallback occurs: all tested aligned staging buffers use MLX's managed wrapping.

For one token, untraced replay is 198.756625 ms; forced scatter is 59.483752 ms while allocation/wrapping/eval are only 0.167335/2.699542/0.103377 ms. The frozen transfer-materiality screen passes at 31.316746% for one token and 23.165804% for 32, with 6.301978%/4.588327% tracing overhead. At 256/1024 the sweep performs no pool scatter and total staging fractions are only 0.964122%/0.785319%, failing that screen. These are serialized attribution, not attainable gains; overlapping timings must not be summed twice.

OPT20/22's residual-scatter prerequisite is now demonstrated on bounded all-miss decode/short-prefill replays. First test contiguous slot-range updates when CLOCK reserves one/two destination runs, with ordinary MLX ownership/dependencies and generic scatter fallback. Reusable staging arenas are conditionally deferred in the measured prefill regime; direct CPU-to-GPU writes still require their separate explicit lifetime proof. No arena, mapping rewrite or measured inference gain is claimed by this profile.


### V86 — contiguous slot-copy candidate

Evidence: [[sources/runs/2026/09/2026-09-05-optimization-contiguous-slot-copies]]. Default-off `SLOTSTREAM_OPT_SLOT_SLICES` replaces generic scatter only when unique reserved slots form one/two adjacent runs in original source order. It preserves normal MLX dependency/lifetime behavior and falls back for fragmented batches. Catalogue38, component158, actual-pool164, storage/error1322, state255/state2561129 each, request recovery151 plain/160 MTP all pass.

All seven fresh-server 8.1 GB / 640-slot request pairs qualify, with exact IDs/text and3359prefill/4722decode records in both arms. Candidate decode uses153slice batches /271runs and566ordinary scatters; reference has719ordinary batches. Median client reduction is -0.0000257988361 (0.002579884% regression), with3/7positive pairs. The frozen5%/80%-positive speed gates fail; keep the candidate disabled. No replacement or threshold change ran.

A distinct successor follows actual backend code: BF16 indexed assignment uses a packed-word compare/exchange loop, whereas UInt32 stores are native atomics. Test copying paired BF16 values as unchanged UInt32 words and restoring dtype/shape through ordinary MLX graph nodes; retain original scatter when row geometry cannot be packed. This is an unqualified hypothesis until byte/state/lifetime and real-request gates pass. It does not involve a precision change or unsafe direct device-buffer mutation.


### V87 — BF16 word-copy candidate

Evidence: [[sources/runs/2026/09/2026-09-05-optimization-bf16-word-slot-copies]]. Default-off `SLOTSTREAM_OPT_SLOT_WORDS` copies paired BF16 values as unchanged UInt32 words, using standard MLX indexed updates and restoring exact shape/dtype; ineligible row geometry falls back. Catalogue39, component350, actual-pool164, storage/error1322, state255/state2561130 each, request recovery151 plain/160 MTP all pass, including byte patterns and retained readers.

Seven clean exact serving pairs at the same8.1GB/640slots all execute the mechanism (719decode word batches,4314BF16 pieces; reference zero). Both arms retain3359prefill/4722decode reads. Median client reduction is -0.0110398983492 (1.103989835% regression), with2/7positive pairs. The frozen5%/80%-positive gates fail; keep the candidate disabled, preserve all14cells, no replacement. No causal attribution of the residual overhead is asserted from these timings. A custom Metal kernel is not automatically an efficient in-place replacement: the pinned custom-kernel backend allocates new outputs. Full direct-write pool lifetime/generation work remains required for that distinct successor. Continue required OPT29 reusable common-prefix ownership and budget work next.


### V88 — OPT29 prefix-fork ownership

Evidence: [[sources/runs/2026/09/2026-09-05-optimization-prefix-state-fork]]. `State.forkForPrefix()` forks exact materialized committed state into distinct mutable cache objects and distinct MLX contexts. GDN/PLE windows are compact; KV and raw/pooled indexer buffers share initial data and use ordinary MLX COW at allocated-buffer granularity. Both branches must reserve full allocated sequence capacity and fixed state. Draft caches and retained multi row remain aligned; recording transactions are rejected.

Four complete full-model probes pass12100assertions:256/1023/2051main prefixes and256with MTP. Independent references preserve exact seed/append boundaries. The tests overwrite common future rows in separate siblings, grow caches, mutate the parent, fork descendants, and match whole logical state plus full continued logits. They explicitly include pooled-indexer metadata/data and retain only SHA256 dtype/shape/byte digests between writes so a diagnostic reader cannot mask an aliasing bug. No latency or peak-memory improvement is claimed. These bounded correctness probes check end footprints below10GB; sampled runtime memory and performance belong to the retention/serving successor.

OPT29 remains in progress: implement and qualify budgeted reusable checkpoints, exact token/image identity, ordinary-entry coexistence, eviction/toggle/pressure/cancellation and actual serving reuse. This is not a rewindable GDN cache or a paged-KV claim.

### V89 — exact budgeted reusable prefix retention and generator integration

Source: [[sources/runs/2026/09/2026-09-05-optimization-prefix-checkpoint-retention]]. The optional configured checkpoint is captured only at an already committed whole-stack prefill boundary, with no re-chunking or GDN rewind. Full allocated capacity of both retained and active branches plus the existing fixed-state quota is charged; maximum four states remains. Ordinary longest entries and auxiliary-client capacity are preserved. If active reservation evicts the selected checkpoint, it transfers exclusively rather than creating an extra state. Cancellation at the checkpoint boundary cannot replace its private ownership, and input checkpoints never feed assistant-output splicing.

Plain/MTP native tests pass128/132assertions, including repeated forks, seven budget boundaries, eviction/transfer/drop/toggle/released-budget behavior, invalid token/recording/image metadata, exact continued generation, cancellation and actual second-pass read-failure recovery. Catalogue41PASS, Python harness23PASS. Metadata-only image tests do not certify tower integration.

The seven-pair serving protocol completed14cells at8.1GB/640slots, with both caches enabled,440prose/16output tokens, compact-state/final-forward common and chunk256. Every pair has exact output, actual256token checkpoint reuse and21511→10352prefill reads (decode4793 unchanged). Candidate sampled peaks6.899–6.970GB. Only pair1 is clean:9.324664125→7.066419709s (24.2179706% reduction). Pair2 has swap/non-nominal thermal state;3–7are thermally`fair`. Frozen minimum-five-clean gateFAIL; no replacement or relaxed criteria. Default remains off. Continue real-image and divergent/interleaved-client cases, then independently qualify under restored conditions. No accepted aggregate checkpoint speed claim yet.

### Broader-alternative source/math pass and OPT31 disposition

Source: [[sources/runs/2026/09/2026-09-05-optimization-broader-alternatives-audit]]. OPT31 optional batching is deferred: the existing engine records queue time, but no representative product demand trace establishes aggregate throughput as a bottleneck. A primitive batch dimension is insufficient; main/draft token counts, recurrent state, n-gram history, independent positions/RNG/cancellation and pin ownership must be separated. Revisit with actual queued-client traces and frozen throughput/per-client latency/fairness/memory gates. The illustrative uniform-independent routing formula is labeled an assumption, never measured routing utility.

OPT34 alternatives are preserved with explicit triggers. A BF16expert is9,830,400B versus2,764,800B packed, so a640packed-slot byte budget holds180BF16experts; conversion must earn displaced residency. Lookup embedding rows are a distinct quantified opportunity because lm_head is separate, but the345,784,320B theoretical payload difference is not usable planner credit or a measured speedup. Quality-changing precision/pruning/model/resolution work requires separate quality gates; compression/prediction/drafts require useful-work and traffic/cost evidence; hardware and energy claims require the actual devices/meters. No whole-program completion is inferred from this disposition pass.

Build-guard evidence: [[sources/runs/2026/09/2026-09-05-optimization-build-lock-recovery]]. V90's attempted build was stopped after its shell continued past a failed model-lock preflight (owned build group terminated143; other task's server untouched). The new optimization_build.py driver holds that same model lock throughout preflight, compilation and freezing, eliminating the launch race. Four pure driver tests pass; V91 demonstrates refusal before invoking make while the unrelated server on18134 is active. New real-image and divergent/long-followup diagnostics remain unbuilt/unverified until that lock is available.

### V92 — divergent clients, long followups and real-image prefix forks

Source: [[sources/runs/2026/09/2026-09-05-optimization-prefix-client-and-vision-integration]]. Corrected locked build succeeds. Plain/MTP retention146/152assertions pass, including17/259/1795-token divergent suffixes,2051-token followup, auxiliary requests between clients, exact independent outputs and preserved reusable capacity. Real PNG/tower/full48-layer integration passes32plain/42MTPassertions: a wholly covered image skips the tower; an image crossing256re-encodes and consumes the exact suffix; changed pixels with the same placeholder IDs refuse old state. Every represented state field plus continued logits matches the independent reference, and MTP remains aligned. Total372PASS; correctness-only endpoints are not latency/peak qualification.

An independent seven-pair speed confirmation is frozen with unchanged acceptance gates plus symmetric60second cooldown before each cell, defining an intermittent-request regime. V89 remains preserved and underpowered; no replacement/exclusion relaxation. No confirmation result yet. A bounded packed lookup-embedding component is being prepared from the quantified OPT34 opportunity; runtime embedding and planner capacity remain unchanged.

### V93–V95 — embedding row component and committed prefix boundaries

[[sources/runs/2026/09/2026-09-05-optimization-lookup-embedding-rows]] preserves the V93 scalar-reference failure, V94 matrix correction, a lock-refused attempt and147passing component assertions. Exact packed rows survive forced eviction and five injected partial-read failures. The resident lookup table is357,580,800B;8192packed rows are11,796,480Bbefore overhead. Main/MTP runtime integration is now under preparation, default off and unqualified; neither measured memory saving nor planner credit follows from this geometry.

[[sources/runs/2026/09/2026-09-05-optimization-prefix-committed-boundaries]] records the V95 validity safeguard and1496passing native assertions, plus31T0catalogue groups. Real layer0/3read failures cannot publish or fork a partially updated prefix even when the global token count still looks committed; exact checkpoint restoration recovers validity. Scope rollback and plain/MTP request recovery pass. A separate T1 catalogue attempt is refused before execution by another task's model. The independent seven-pair V95 serving protocol keeps the original exactness,5percent speed, minimum-five-clean,80percent-positive and8.1GB/nominal-power gates with reserved symmetric60second cooldown; it is unrun at this update. Prefix speed, embedding runtime/resource qualification and integrated OPT32/33 remain open.

### V95 — repeated-prefix serving qualification passes

[[sources/runs/2026/09/2026-09-05-optimization-prefix-serving-confirmation]] records all seven prospective pairs. Six clean pairs all improve: median paired client latency falls **35.7637%**, with reference/candidate medians of **9.4411/6.0312 seconds**. Round 4 remains swap-excluded; every one of the 14 cells emits identical token IDs and wire text. At 440 prompt / 16 output tokens, the exact retained 256-token prefix reduces prefill records from 21,511 to 10,352 while decode remains 4,793. Maximum qualified candidate sampled footprint is 6.9331 GB within the fixed 8.1 GB plan; retention adds roughly 128.5 MB active allocation, fully charged. The separate five-pair startup comparison has median first-job excess 0.0993 seconds. This passes the frozen minimum-five-clean / 5% gain / 80%-positive / exact-output gate for repeated prompts in the declared reserved-cooldown regime; it establishes no general decode, unique-request or cold-filesystem speedup. Driver exit 1 preserves the invalid cell even though completion.json records acceptance passed. OPT29 is ready for scoped combination; default activation and OPT32/33 remain pending.

### V96/V97 — checked input-embedding rows reach full generation

[[sources/runs/2026/09/2026-09-05-optimization-embedding-runtime-integration]] preserves the lock-refused build, diagnostic compile failure, successful V97 build and **527 passing assertions** (147 row component, 172 plain full-model, 208 MTP). The optional runtime now preserves public tensor access and propagates checked row errors through main/draft generation. CPU IDs avoid unnecessary GPU upload/readback. Full state, continued logits, greedy output, image splicing, retained-prefix reuse and read-failure retries match the resident-table reference. Sequential load snapshots show about 358 MB less MLX active allocation and 330–331 MB less physical footprint; these are diagnostic endpoints, not serving qualification. The frozen resource study requires at least 300 MB active / 200 MB sampled physical peak savings in every qualified pair and no more than 5% median latency regression. It remains unrun; row mode is default off and the planner grants no memory credit.

V97 also preserves a real TCP output test with two failed disconnect assertions: normal peer FIN did not establish failed local writes. The successor is being corrected to observe an abortive reset and check the actual failure latch. The 16 GB larger-prefill-scope feasibility protocol is separately frozen and unrun. No TCP pass, larger-scope win or combined runtime result is inferred.


### V98 — embedding serving resource gate passes; TCP reset test corrected

[[sources/runs/2026/09/2026-09-05-optimization-embedding-resource-serving]] preserves the frozen V98 build, 40 TCP / 38 Unix output assertions, 593 row-enabled scope-lifecycle / 240 row-enabled MTP-vision assertions and all 42 T0/T1 catalogue groups passing. The TCP diagnostic observes an abortive reset before checking failed writes and the actual failure latch. It preserves the public diagnostic signature; full Server/Engine slow-client pressure remains open.

All 14 embedding study cells complete; five whole pairs qualify (2–6), with 1/7 swap-excluded and no replacements. Outputs and expert work are identical in all pairs. Every clean pair saves at least **357.58 MB MLX active / 326.47 MB sampled physical peak**, passing the declared 300 MB / 200 MB thresholds. Qualified row-mode peaks are 6.242–6.323 GB under the fixed 8.1 GB plan. Median paired request latency improves 4.3334%, with four of five positive; this passes the frozen memory-benefit/nonregression gate, not a separate positive-speed claim. Measured requests hit the warmed 278-row / 400,320-byte packed cache with zero misses. Default activation, planner credit and combined OPT32/33 qualification remain pending. The separately frozen V98 16 GB larger-prefill-scope feasibility pilot is next and remains unrun at this update.


### V98 — larger-scope reference warmup cannot qualify

[[sources/runs/2026/09/2026-09-05-optimization-larger-prefill-feasibility]] preserves the frozen 16 GB / 3719-token code pilot. The 2576-slot reference warmup reaches 13.796 GB sampled physical peak, but four generator-interval swap-ins and nominal-to-fair thermal state invalidate it. The driver obeys its resource stop before either measured request or candidate launch. No comparative speed/fit result exists; no replacements or relaxed exclusions ran. Larger scopes remain disabled, with qualification dependent on a clean sustained-prefill regime. This does not invalidate the separately qualified short-request memory/prefix results.


### V99–V102 — real HTTP/TCP lifecycle and explicit pressure failures

[[sources/runs/2026/09/2026-09-05-optimization-tcp-serving-and-pressure-errors]] preserves all failed test setups and the corrected V102 candidate. The Engine now reports memory-pressure interruption as a runtime error at queued refusal and in-flight cancellation, so existing HTTP handlers cannot emit false successful completion. V102 passes **306 native assertions**: 58 real TCP/HTTP, 64 ordinary / 64 MTP governor, and 120 HTTP read-failure/recovery checks, with row embeddings enabled. All 42 T0/T1 catalogue groups pass; a wrapper schema-reading error is corrected against the same immutable raw output without rerunning. The Python harness passes 28 tests with an optional prospective equal-work gate.

All four production streaming handlers recover from actual kernel backpressure induced by one declared roughly 512 KiB framed diagnostic backlog in the unchanged 1 MiB / 64-frame / five-second writer. Normal model generation continues through the actual Engine; health, serialized queued inference, exact retry, bounded storage and handler join pass. Socket waits are 5.040–5.046 seconds, queued completions 6.150–6.232 seconds and owned peaks 525426–528424 bytes. These are correctness timings, not paired performance. Gateway pressure cancels at a committed boundary, drains the real shrink-only governor queue and emits an explicit error without finish/[DONE]; pending JSON pressure fails before work. The natural-short-reply probe never filled TCP buffers, and its limitation remains explicit. Default queue/governor activation still awaits combination. A separately frozen seven-pair V102 queue nonregression confirmation is next; the V11 failure is preserved, with no replacements or loosened criteria.


### V102 — bounded-output latency nonregression qualifies

[[sources/runs/2026/09/2026-09-05-optimization-output-queue-confirmation]] preserves all seven clean pairs, exact IDs/text and eight matching work counters. At 17 prompt / 16 output tokens, both arms read3359prefill/4722decode records and execute15decode model tokens; there is no change in model work. Median paired client reduction is **0.5006%**, with6/7positive; the frozen minimum-five-clean / maximum5%median-regression gate passes. No replacement/exclusion occurred. Queue sampled peaks5.0865–5.0868GB fit the8.1GBtarget and MLX active memory matches pairwise. Six clean startup pairs have median first-job excess -0.0518seconds. This qualifies latency nonregression for the separately proven bounded-client behavior; it is not a positive-speed qualification. V11 remains a failed prior experiment. OPT30 queue and responsive-governor candidates are ready for combined qualification, with explicit pressure errors. OPT32 now enters incremental combination; OPT33 remains pending.


### V103 — first combined ordinary-runtime correctness pass

[[sources/runs/2026/09/2026-09-05-optimization-combined-first-pass]] binds the explicit integration candidate and151passing ordinary assertions: all retained state/representation bytes and continued logits,17/255/256/257/1025tokens, real images before/crossing256, two sampler modes/two seeds, interleaved and edited prefixes, cancellation/retry and exact outputs. Ordinary eight-token completion executes8reference versus7candidate decode forwards. Models are sequential and only hashes/scalars/IDs survive across arms. MTP and the combined serving matrix remain pending; defaults remain unchanged. A new pre-mutation public-state/cache-offset guard and atomic rejection tests are awaiting the V104 build reservation. No performance gain is inferred from this correctness run.


### V104/V105 — native source-closure correction and component successors

[[sources/runs/2026/09/2026-09-06-optimization-native-source-closure]] preserves the successful V104 build and its provenance limitation: its package includes newly added native C code omitted by the former Swift-only archive. V102/V103 packages do not include that target. V104 is diagnostic feedback only, not a reconstructible final benchmark candidate. The builder now captures native sources/headers/resources, rejects unexplained symlinks, verifies bytes during archive creation and invalidates stale receipts. Four provenance and five build-reservation tests pass. Existing evidence is unchanged.

OPT18 gains an unbuilt trained-weight projection/SiLU component probe and prospective timing protocol. OPT20 gains an unbuilt CPU stream slot-update component using MLX's own alias/donation/dependency machinery; it tests separate Swift/view/GPU readers and invalid inputs. The distinct-stream assertion avoids a source-observed bug in MLX's explicit-stream wrapper by using its scoped default-stream facility. These are pending prototypes, not production dispatch, qualified speedups or a complete arena/device-mapping implementation. V104 ordinary correctness remains queued behind another task's model; successor build, MTP and integrated performance remain pending.


### V104 — combined ordinary state-entry regression passes

[[sources/runs/2026/09/2026-09-06-optimization-integrated-state-entry]] preserves 167 passing assertions, including 16 new atomic rejections of invalid public offsets and blank state. Exact ordinary outputs, complete state/continued logits, images, sampling, prefix interleaving and cancellation still pass. This is diagnostic feedback under V104's disclosed native-source archive limitation; this qualifies neither performance, defaults nor complete source closure. The corrected V105 builder and isolated component probes are queued; rebuilt ordinary/MTP integration and combined serving remain pending.


### V107 — complete source closure, CPU update component and combined MTP

[[sources/runs/2026/09/2026-09-06-optimization-components-and-mtp]] preserves two diagnostic build failures and the successful V107 archive, including native C sources. The CPU slot-update component passes 174 assertions and observes exclusive storage reuse, with exact byte copies, retained Swift/view readers, a distinct GPU stream and a GPU consumer. This does not measure the hardware overlap interval or qualify full pool/inference performance. V108 adds a default-off integration, mechanism counters and the existing pool/state/read-failure matrices.

The compute component passes 69/71 assertions. The dense GDN B/A concatenation fails exact bytes at 256 rows in both input layouts and remains a rejected numerical counterexample. The two quantized groups and SiLU/product candidate pass all 55 corresponding assertions; a separately named successor excludes the dense group entirely. No compute timing has run. The serving harness passes 29 tests and can prospectively require exact or bounded work counters per arm.

The combined MTP candidate passes all 181 state, continued-logit, image, sampling, prefix/interleaving and cancellation/retry assertions. Defaults, planner credit and combined performance remain pending, as does ordinary integration on the final rebuilt candidate.


### V108 — CPU slot integration passes its correctness prerequisites

[[sources/runs/2026/09/2026-09-06-optimization-cpu-slot-integration]] binds the default-off CPU update and all 3,113 passing component, configuration, pool, storage/read-failure, complete-state/route/continuation and ordinary/MTP request-recovery assertions. Existing synchronization, reservations and publication remain. The explicit quantized/SiLU successor separately passes 55 assertions. V108 now exposes per-request CPU batch counters; no numerical change is introduced by slot copying.

A seven-pair CPU serving study is frozen only after its native prerequisites pass against the same binary. It requires at least five clean pairs, 5% median client reduction, 80% positive pairs, exact outputs, eight matching work counters, zero reference and positive candidate CPU batch counts, nominal thermal/power, no swap and the 8.1 GB physical limit. No replacement rounds are allowed. It remains unrun at this update. Combined ordinary V108 validation is underway; default activation, combined performance and planner credit remain pending.


### V108 — CPU serving rejected; rebuilt ordinary integration passes

[[sources/runs/2026/09/2026-09-06-optimization-cpu-serving-and-integrated]] preserves all 14 CPU study cells and 167 passing ordinary integration assertions against the complete rebuilt source closure. Combined state, images, sampling, prefixes and cancellation/retry pass. Combined performance, defaults and planner credit remain pending.

Only round 6 qualifies as a whole clean serving pair; rounds 1–5 and 7 remain swap-excluded with no replacements. Its reference/CPU latencies are 3.5791/17.5222 seconds, a 389.57% regression, but one pair does not support a population estimate and cannot pass the required minimum of five. All pairs retain identical outputs and eight model-work counters; CPU mechanisms execute 129 prefill and 719 decode batches. Keep the CPU candidate disabled. Pinned MLX's generic CPU scatter walks every element individually; this motivates a separate bounded contiguous-copy successor, not a claim of proven causal attribution or full-model donation. The original result remains a failed experiment.


### V110 — contiguous CPU correctness and compute screen

[[sources/runs/2026/09/2026-09-06-optimization-compute-and-cpu-successors]] preserves V109's source-change refusal and V110's complete native archive. The CPU successor uses at most 32 contiguous copy runs and passes all 3,348 native component, pool, storage, state and ordinary/MTP recovery assertions. Its separately frozen seven-pair serving protocol is unrun at this update. No latency benefit is inferred and CPU writes remain disabled.

The stronger quantized/activation component passes 63 assertions, including non-power-of-two and varying finite BF16 multipliers. Its first timing screen stops for interval resource failure at shared gate/up 1024 rows. Whole-process swap increases by 198 pages, excluding all timings; GDN timing is not reached. No compute benefit qualifies, and dense B/A remains a preserved numerical counterexample.

OPT29 gains a concrete full-prompt followup: retain the exact complete prefill state together with its compact raw last logits so an identical retry can avoid all prefill. Preserve the public strict-prefix API and independently bind complete reuse to model identity, computation settings, pass schedule and draft mode. Keep seeds/filtering outside the raw-logit key; charge both state branches plus bounded logit storage. Require exact state/continued logits, ordinary/MTP sampled and greedy output, image identity, aliases, zero/one-token limits, EOS, cancellation, read-failure retry, eviction/pressure and interleaving. Qualify repeated-request latency and cold/unique-request retention overhead in separately frozen protocols before combination. The implementation is default off and outside integrationCandidate; native validation is in progress and no result is claimed yet.


### V114 — complete-prompt reuse passes ordinary and MTP integration

[[sources/runs/2026/09/2026-09-06-optimization-complete-prompt-integration]] preserves the initializer build failure, stopped zero-limit diagnostic mistake, pinned-residency test precondition and their corrections. V114 passes 272 ordinary / 280 MTP assertions for full state, continued logits, greedy/sampled output, text boundaries, real images, exact complete reuse, ownership, budget thresholds, cancellation, edited/interleaved clients and read-failure recovery. Repeated prompts execute zero prefill tokens/passes/records. An MTP transition guard stamps the actual producing mode and rebuilds a missing head. Public strict-prefix and zero-limit semantics remain unchanged.

The harness passes 33 pure tests after correcting a macOS test-path comparison. Two frozen seven-pair 8.1 GB protocols now cover repeated 440-token prompts and unique 440-token prompts after distinct 17-token warmups. The repeat gate requires minimum five clean pairs / 5% median gain / 80% positive; the unique gate requires minimum five clean pairs / at most 5% median regression. Both require exact outputs, observed mechanisms, nominal thermal/power, no swap, reserved 60-second cooldowns and no replacements. They remain unrun at this update. Full-prompt reuse remains default off and outside integrationCandidate. CPU-copy serving, compute confirmation, total combined comparisons, final acceptance and OPT33 remain open.


### V114/V115 — repeat timings excluded and competing-work guard

[[sources/runs/2026/09/2026-09-06-optimization-complete-repeat-contention]] preserves all 14 cells, identical IDs/text in every pair, exact 440-token complete hits and prefill records reduced from 21,511 to zero. Decode model work matches; actual decode records differ slightly, as permitted. Sampled peaks fit 8.1 GB. However, there are zero clean pairs because of interval swap-ins. Raw 9.35–9.92 / 2.43–2.64 second request ranges are excluded descriptive observations, not a qualified speedup. No replacements or rescoring occur.

Post-run timestamps prove a separately launched checkpoint pull overlapped every measured interval. Its precise contribution to swap/latency is unassigned. Checkpoint reads already request uncached I/O with read-ahead disabled. The new optional known-storage/build-job guard passes 34 pure tests and a live refusal before model launch, while preserving all existing resource gates. Independent guarded repeat confirmation, unique-request overhead, CPU-copy serving, compute confirmation and integrated/final delivery remain pending; no other task is stopped and no defaults change.


### V115/V116 — guarded refusal and maximum vision-attention geometry

[[sources/runs/2026/09/2026-09-06-optimization-guarded-vision-capacity]] preserves the first guarded repeat confirmation: another Swift build appeared during cooldown, so no model or measurement launched. The compute guard likewise refuses before loading. Existing excluded timings remain unchanged.

The complete V116 native archive passes all 48 vision-attention capacity assertions across 1–9216 patches, all 16 heads, actual transposed BF16 layout and two score regimes. Full padded output is finite; sampled query rows satisfy independent Float64 and original-path numerical controls. Largest-grid sampled component/oracle peaks are below 0.74 GB. This does not establish maximum complete-tower/request memory or a latency benefit.

The image harness passes 36 pure tests and now pins inline bytes and charges the separately observed image-preparation peak. Frozen unrun 512/1024-pixel image protocols require at least five clean pairs, exact one-token outputs and at most 5% median client regression; 1024 also requires 200 MB peak saving per clean pair. Refreshed complete-tower/known-content checks, maximum-image serving, repeat/unique/CPU/compute comparisons, combined performance and final delivery remain pending. Defaults stay unchanged.


### V116/V117 — rebuilt ordinary integration and vision-tower numerical counterexample

[[sources/runs/2026/09/2026-09-06-optimization-vision-tower-counterexample]] preserves 167 passing rebuilt ordinary integration assertions and the complete V117 archive. Padded vision output is finite and exactly repeatable through the maximum 1536-pixel square; its maximum-grid correctness-run footprint is about 2.012 GB. However, two of 19 trained-tower checks fail the declared 0.998 same-dtype similarity threshold: 512/1024-pixel red/blue fixtures yield 0.997832/0.997284. Component algebra and other images do not waive this counterexample. Padding remains off and paired image studies stay unrun pending a separate independent Float32/dtype-band investigation. No threshold is relaxed or result rescored; no complete-request or maximum-grid whole-tower numerical qualification is claimed.


### V118 — repeat confirmation stops for renewed competing work

[[sources/runs/2026/09/2026-09-06-optimization-complete-repeat-guarded-stop]] preserves five exact raw pairs. Only rounds 1 and 4 are clean; their 75.377% median paired reduction is a two-pair descriptive observation, not a qualified gain. Reference rounds 2/3/5 remain excluded for 4/12/4 global swap-ins. A new checkpoint test appears during round 6 cooldown; the guard stops before model launch. The minimum-five-clean acceptance fails, with no replacements. All full hits still reuse 440 tokens and reduce prefill records from 21511 to zero.

OPT27 now tests a separate source-derived query-prescaling successor: preserve the fallback's BF16 scale/query rounding before a unit-scale padded kernel. It is outside production dispatch and retains the same tower threshold; score/softmax differences remain a numerical risk. All adoption, unique-request, combined-performance and delivery gates remain open.


### V119–V123 — query-tiled original vision attention passes broader native checks

[[sources/runs/2026/09/2026-09-06-optimization-vision-query-successor]] preserves the V119 source-change refusal and rebuilt V120 prescaling counterexample: 48 component passes, but three of 19 trained-tower assertions fail without any threshold change. Prescaling stays outside production dispatch.

The separate original-attention query-tile path evaluates at most 256 query rows at once. V121 passes its initial 67 assertions. Expanded V123 passes 193 component/tower/configuration/prefix-identity assertions. All seven full reference comparisons, including narrow images and partial query tiles, are byte-identical. Maximum-grid candidate-only correctness-run footprint is about 1.851 GB; full maximum-grid reference, complete-request memory and performance are not yet qualified. The default-off control has its own prefix identity and rejects combination with padding. Larger-image ordinary/MTP integration is underway; known-content serving and paired qualification remain pending.

The 37-test harness adds a sampled initial quiet interval. A new unrun repeat-prompt protocol fixes sixteen alternating pairs after 180 quiet seconds, with unchanged minimum-five-clean, gain, exactness and resource gates. Prior observations are neither pooled nor replaced. Defaults and integrationCandidate stay unchanged.

### V123–V125 — full maximum-image reference, executed tiling and known-content serving

[[sources/runs/2026/09/2026-09-06-optimization-vision-query-mechanism]] preserves V123 ordinary/MTP integration (181/195 assertions), the V124 maximum-tower comparison (5 assertions), and V125's rebuilt mechanism closure. V125 passes 104 attention-capacity, 47 trained-tower, 6 maximum-reference, 76 identity, 195 ordinary integration and 209 MTP integration assertions (637 total). The 9,216-patch full original tower and query-tiled tower are byte-identical; intermediate dimensions and query-tile boundary cases remain exact. A callback after each evaluated query tile distinguishes execution from a merely configured flag. Full images execute the expected tile count; prefix/feature reuse skips encoding.

V125 known-content serving passes all 25 existing dog/tree, image-order, changed-pixel, strict URL/input and multi-API checks at a 10 GB plan. These are correctness results, not paired latency qualification. V124's maximum candidate-only sampled peak was 1,730,627,264 bytes; the subsequent full-reference comparison reached 5,185,652,416 bytes while retaining candidate outputs. They are different correctness intervals, not a valid paired saving.

Three protocols are frozen before execution: independent seven-pair 512-pixel nonregression and 1,024-pixel resource/nonregression comparisons, plus a single maximum-size whole-request feasibility probe. Each pins source/weight/image identities, actual tile counts, preparation plus generator footprint, nominal power/thermal state and zero swap, with strict sampled competing-work checks and no replacements. The 1,024-pixel comparison requires at least 200 MB sampled peak saving in every clean pair; both paired studies require five clean pairs and at most 5 percent median client regression. The maximum probe does not establish a latency effect. All are unrun at this update. Query tiling remains default off and outside integrationCandidate; padding stays rejected.

A further source audit found ordinary-prefix model identity and checkpoint-branch ownership gaps. The unqualified V126/V127 successor adds model-bound state reuse, checked restore/rollback, weak checkpoint validity tickets, and atomic invalid-input rejection tests. V126 failed to compile a diagnostic report call; V127 builds. Native recovery and existing MTP/cancellation regressions are pending, so this is not yet claimed as verified. The new checks retain ancestor checkpoints but reject later snapshots permanently after their KV rows can have been overwritten by another branch.

### V126/V127 — model ownership and checkpoint branch validity

[[sources/runs/2026/09/2026-09-06-optimization-state-lineage]] preserves V126's diagnostic compile error and V127's complete build/source closure. V127 passes 43 direct lineage, 151 ordinary read-failure, 160 MTP read-failure, 593 scope cancellation, 240 image/MTP scope, 272 ordinary complete-prompt, 280 MTP complete-prompt and 209 combined vision/MTP assertions (1,948 total). This qualifies the covered recovery behavior; these diagnostic durations are not performance measurements.

State now carries its loaded-model identity; ordinary generator prefix lookup misses a same-geometry foreign model, and checked model entry rejects its state before cache mutation. Prefix forks preserve the model identity but receive independent checkpoint ownership. Checkpoints have weakly registered validity tickets that retain no extra tensor data. A restore or recorded rollback permanently invalidates later tickets even after different tokens grow KV offsets past the old numbers. Older valid ancestors remain restorable. Checked restore/rollback refuses foreign/discarded/incomplete input before mutation; rollback additionally binds the recorded token sequence, base and history-window geometry. Public nonthrowing wrappers remain source-compatible and fail on invalid internal contracts. Discarded snapshots do not accumulate live tickets.

V128 adds a bounded misaligned-draft capture check and an independent, default-off OPT19 resident-overlap prototype. It only partitions the batch axis of one-token QMV operations; larger token batches keep existing grouping, and router-rank reduction order is restored. All requested resident slots are pinned before launch; synchronous read completion is followed by an explicit resident-reader join before any pool write, including read errors. The pinned backend selects gather-QMV from unchanged M/N/K and uses the expert count only as grid depth for this one-token case. The build passes, but native exactness, error-path join evidence and serving benefit are pending. CPU host join time is labeled as such; no actual GPU-overlap event timing or speed claim is inferred from submission order.

### V128/V129 — bounded resident-expert computation during miss reads

[[sources/runs/2026/09/2026-09-06-optimization-resident-overlap]] preserves both builds and the unchanged-arithmetic mechanism checks. V128 passes 45 lineage and 597 component assertions; its full-model test passes all numerical assertions but fails the required execution counter because the immediately preceding reference made every candidate one-token call all-hit. V129 explicitly preloads half of each observed reference route for correctness comparisons. It passes 45 lineage, 627 component, 1,128 full-model state/routing/continuation, 151 ordinary read-recovery, 160 MTP read-recovery and 240 image/MTP scope assertions (2,351 total). Two actual mixed-cache read errors each submit resident work, join it exactly once, release temporary pins and retry with exact output. The lineage successor also rejects a checkpoint captured with an unaligned draft cache.

The new RESIDENT_OVERLAP control stays off and outside integrationCandidate. It separates only independent one-token expert QMV batch rows, keeps the original larger-batch path, pins the whole intended request before GPU submission, and joins resident readers after synchronous I/O but before any pool mutation or error unwind. Reassembly preserves original router rank. The pinned backend source is captured: these M=1, at-most-ten-row calls keep the same quantized kernel selection and reduction geometry. The submission/join counters show execution and completed lifetimes; host join seconds do not establish GPU event duration or useful GPU overlap.

The 38-test serving harness now requires matched completed joins for every resident submission. A seven-pair 17-prompt/16-output, 8.1 GB/640-slot protocol is frozen before execution, with exact outputs/eight work counters, minimum five clean pairs, at least 5 percent median client gain, at least 80 percent positive pairs, bounded footprint, nominal power/thermal state, zero swap and no replacements. It is unrun at this update. A serving win still needs combined/event qualification before activation. The three frozen V125 vision studies, complete-prompt confirmation/unique overhead, CPU-copy confirmation, compute islands, remaining prefill qualification and final OPT32/33 are also pending.

### V130/V131 — maximum-image observation and small-image nonregression

[[sources/runs/2026/09/2026-09-06-optimization-vision-serving-nonregression]] preserves the maximum-image delivery correction, both complete studies and the V132 measurement successor. The first single-arm maximum protocol was refused before model launch because its sole arm was not named reference; the corrected frozen protocol retains identical candidate settings and explicitly establishes no paired comparison. Its 1,536-pixel request emits the expected Red token, executes 972 query tiles and consumes 2,329 prompt tokens/104,279 expert records. Sampled generation/preparation peaks are 7,016,468,656/6,816,010,416 bytes within the 10 GB plan. Twelve global swap-ins during generation exclude the cell from qualification. It remains a correctness/resource observation with no qualified latency or capacity claim.

The independent seven-pair 512-pixel study completes all fourteen cells. Round one remains swap-excluded; six whole pairs pass the frozen minimum-five-clean / maximum-five-percent median regression gate, with exact token IDs/wire text in every raw pair and unchanged 16,681 prefill records. Median paired client latency regresses **1.15583 percent**; this is a qualified nonregression result, not a speedup. Candidates execute exactly 108 tiles per request, references zero, and qualified candidate sampled generator peaks are 6.7304–6.8032 GB. No replacement round, settings change or output difference occurred. The driver exits 1 to preserve excluded cells while completion.json records the paired gate passing.

A pre-execution audit corrected the resource comparison to take max(generation sampled peak, image-preparation sampled peak) for each process. Both stages already had independent absolute-budget gates, but the saving calculation previously used generation alone; a larger preparation peak could conceal a regression. The new counterexample test proves that case, and all 39 harness tests pass. The unrun 1,024-pixel study receives a new frozen V132 protocol with the original 200 MB-per-clean-pair saving, minimum-five-clean and five-percent nonregression criteria unchanged. No earlier resource claim is rescored to obtain a pass. V125's small-image study had no saving threshold. Query tiling remains off pending larger-image and maximum-size qualification; OPT32/33 and the remaining program are still open.


### V132 — larger-image whole-request resource gate fails

[[sources/runs/2026/09/2026-09-06-optimization-vision-resource-counterexample]] preserves four complete pairs and the interrupted fifth-round reference. Only pair one is clean; pairs two through four are excluded for global swap-ins. At that point no more than four clean pairs could remain in the fixed seven-pair study, below the required five, so the owned driver was stopped for futility and its server cleaned up. There are no replacement rounds or changed thresholds. The completion fails both minimum pairs and minimum sampled peak saving.

The clean pair preserves exact Red output, 50,247 prefill records and 138,922,905,600 read bytes. Query tiling executes 432 tiles. Whole-request sampled peak falls from 6,962,024,432 to 6,859,952,232 bytes: **102,072,200 bytes**, below the required **200,000,000**. Its 15.4488 / 13.7388 second client latencies are an isolated descriptive 11.0692 percent reduction, not a qualified speedup. Image encoding itself is slower (0.6644 / 0.7328 seconds); the observed request difference occurs in prefill with equal expert-read work, and its cause is unproven. All completed raw outputs remain exact and sampled peaks fit the 10 GB plan.

V131's independent small-image nonregression remains valid, while V130 maximum-size serving is swap-excluded and this larger-image resource qualification fails. Query tiling remains off and outside integrationCandidate. Future investigations must be new prospective hypotheses, not revised scoring of this study. Complete-prompt confirmation/unique overhead, resident-overlap and CPU-copy serving, compute islands, remaining prefill qualification, combined acceptance and OPT33 remain open.


### V133 — full-prompt reuse passes independent repeat confirmation

[[sources/runs/2026/09/2026-09-06-optimization-complete-repeat-confirmed]] preserves all 32 cells from the prospectively frozen V122 sixteen-pair study against V114. Ten whole pairs (2, 3, 4, 7, 10, 11, 12, 13, 15, 16) qualify; six remain swap-excluded with no replacements or pooled predecessors. Every raw pair has exact IDs and wire text. The unchanged minimum-five-clean / five-percent median gain / eighty-percent positive gate passes with all ten clean pairs positive and **71.95368447 percent median paired client reduction**. Separate arm medians are 8.11722 / 2.27677 seconds; they are descriptive and are not substituted for the paired statistic.

This is incremental complete-prompt reuse on the fixed combined base: 8.1 GB / 640 slots, raw 440 input tokens / 16 greedy outputs, MTP off. All complete hits reuse 440 tokens and remove 21,511 prefill expert records. Decode model tokens and forwards remain 15; actual decode reads are 4,793 / 4,815, as permitted by the frozen residency-changing protocol. Qualified candidate sampled peaks are 6,403,231,464–6,405,213,880 bytes. The checkpoint adds about 117.5–117.6 MB active storage and stays fully charged. No total-engine, unique-request or p95 claim follows from this study.

Unique-request retention overhead and final combined/default acceptance remain pending. A follow-up source audit found duplicate-store matching omitted loaded-model identity, and optional unused checkpoints could displace useful conversation entries. Add counterexample traces and model-aware duplicate matching; make never-used checkpoints yield before ordinary entries, while allowing checkpoints with actual hits to retain normal LRU value. Verify four distinct clients, auxiliary requests, proven common prefixes, budget/pressure and same-model/different-model ownership before combination. This extends OPT29 correctness/retention coverage without changing the just-qualified one-prompt measurement. All remaining program gates remain open.


### V134–V136 — model-bound prefix stores and multi-client capacity

[[sources/runs/2026/09/2026-09-06-optimization-prefix-client-capacity]] preserves V134's 17 failed counterexample assertions. Unused input checkpoints reduced four ordinary conversations to two, causing three of four sequential follow-up misses. Model-blind duplicate matching also conflated distinct loaded owners. V135 repairs both and passes 1,214 native assertions, including ordinary/MTP retention, full-prompt and combined image/MTP behavior. Its standard catalogue fails only nine assertions in the old runtime fixture group, where thirteen dummy states incorrectly declare zero consumed tokens. V136 corrects those logical fixtures, adds checkpoint promotion priority, and passes all 43 T0/T1 groups plus 656 targeted owner/capacity/ordinary/MTP assertions.

Duplicate detection now includes loaded-model identity; complete-prompt keys must match that owner; ordinary storage rejects a mismatched consumed-ID count. A new optional checkpoint may replace its exact same-owner duplicate or another unused checkpoint, but cannot evict an unrelated ordinary conversation or a checkpoint with demonstrated reuse. Failed admission plans change nothing. Unused checkpoints yield first during later pressure; actual hits, ordinary returns and promotions retain normal LRU standing. The physical four-state and shared token-capacity bounds stay unchanged. Four independent clients preserve baseline followup hits and the same three survivors after a fifth auxiliary request.

The V136 full-prompt unique-request successor is frozen before any unique-request measurement. It retains the original 8.1 GB / 440-token unique prompt / 16-output, exact-work and five-percent median nonregression gates, with minimum five clean pairs. Sixteen fixed alternating pairs and an initial 180-second sampled quiet interval address observed exclusion attrition; V115 remains unrun and unchanged, and no prior result is pooled or replaced. Protocol SHA-256: 53a711fc8b628c356af3055b8700b9b10b254578e903c9b3ddffef4fcb9f236c. Complete-prompt reuse still awaits this overhead result and final combined/default acceptance.

OPT11's separate source-derived partial-rotation component is now implemented outside production dispatch and awaiting V137 build/native tests. It preserves explicit BF16 angle/product/sum rounding and original arbitrary strides; it is not yet a qualified fusion or speedup. Resident overlap, CPU copy, compute islands, terminal QSA pruning, remaining prefill qualification and final OPT32/33 remain open.


### V137–V139 — exact BF16 partial-rotation component

[[sources/runs/2026/09/2026-09-06-optimization-partial-rotation-component]] preserves two diagnostic/component build corrections and the complete V139 frozen source closure. The component uses one pointwise Metal pass with the original angle casts, individually rounded BF16 products and final BF16 addition. It retains arbitrary input and broadcast-angle strides and copies untouched dimensions exactly. It is outside production dispatch.

V139 passes all 208 component assertions across actual QSA/indexer layouts, 1–1,024 rows including kernel/tile boundaries, 1/2/24 heads, rotary dimensions 2/64/128/256, signed zero, small/large finite operands, rounding boundaries, offset slices, broadcast batches, repeatability and FP32 fallback. All covered BF16 bytes match the original ropePartial path exactly. The 2.6453-second correctness duration is not a benchmark. Full-model/MTP and serving performance remain pending for OPT11, including the earlier shared-table candidate.

The raw record also preserves the V136 unique-prompt protocol before execution. Its first independent sixteen-pair run is the next active measurement; no unique-request overhead or full-prompt activation is claimed yet. The rest of the unified program and final OPT32/33 remain open.


### V140 — complete-prompt retention passes unique-request nonregression

[[sources/runs/2026/09/2026-09-06-optimization-complete-unique-confirmed]] preserves all 32 cells from the independent V136 study. Six whole pairs (1, 3, 9, 10, 15, 16) qualify; ten remain swap-excluded with no replacements or pooled predecessors. All sixteen raw pairs have exact IDs and wire text. The unchanged minimum-five-clean / maximum-five-percent median regression gate passes: median paired client latency regresses **0.00067214 percent**, effectively flat at this precision. This is nonregression, not a unique-request speed claim.

At the fixed 8.1 GB / 640-slot plan, both arms prefill 440 new tokens after a distinct 17-token warmup, read 21,472 prefill / 4,915 decode records and execute 15 decode model tokens/forwards for 16 outputs. There is zero prefix or complete reuse; the candidate stores one measured complete checkpoint. Holding both warmup and measured states adds **261.1–261.2 MB active memory**. Qualified candidate sampled peaks are 6,470,110,784–6,524,898,952 bytes, 89.5–236.9 MB above paired reference peaks, within the plan. Retention remains fully charged and earns no planner credit.

Together with V133's independent 71.95368447 percent repeated-prompt reduction and V136's native ownership/capacity closure, full-prompt retention is ready for combined qualification. V141 adds it to explicit integrationCandidate and distinguishes complete hits from an independently exercised edited-tail partial-prefix fallback. Public defaults remain off; joint native/performance acceptance is pending. The V104 resource drafts already disable prefix caching in stateless cells, so retention cost is not silently subtracted from their resource objective. Retained-serving cases must report that cost separately.

The raw record also freezes V140's resident-overlap protocol description correction before any timing. V129's executable settings and native/work/gain/resource gates were already resident-specific, but copied descriptive fields mentioned CPU copying. Only those descriptions and predecessor identity change; seven fixed pairs and all acceptance thresholds remain. Its protocol SHA-256 is 267cc85f54f6984c5a85a75be1e6d3d1d74c63836026a6289e729419f8c81da4. Resident serving, CPU-copy serving, compute islands, RoPE integration/performance, terminal QSA pruning, remaining prefill qualification and final OPT32/33 remain open.


**OPT29/32 V141: complete-prompt reuse passes joint native qualification.** [[sources/runs/2026/09/2026-09-06-optimization-complete-integrated]] captures the exact V141 source/build and all three independent reference/candidate runs: 180 plain, 194 MTP and 222 vision-query/MTP assertions, 596 total. The joint candidate now explicitly includes complete-prompt checkpoints. A same-prompt request exercises full reuse, a complete-hit counter and zero prefill. A separate changed-tail request exercises exactly 256 retained tokens and no complete hit, preserving real partial-fallback coverage. State and continued logits, sampled replay, image boundaries/identity, interleaved reuse, cancellation and retry pass. The optional query-tiling variant remains outside integrationCandidate. These are correctness runs, not joint serving measurements; their elapsed time and VM activity are not speed evidence. All public/environment defaults remain off.

V133's 71.95368447 percent paired repeated-prompt client reduction and V140's effectively flat unique-request result support inclusion in this candidate, not a total-engine speed claim. Final all-reference combined serving, default adoption and OPT33 remain pending. The independently initialized BF16 partial-RoPE kernel is being integrated under its own default-off control and has not joined this candidate. Resident overlap, CPU copy, compute-island confirmation, terminal last-query pruning and remaining prefill/capacity qualification are still open.


**OPT11 V142: exact partial RoPE now passes full-model and MTP integration.** [[sources/runs/2026/09/2026-09-06-optimization-rope-integration]] preserves the frozen build and 2,665 passing native assertions: component208, fused-only2051-token state1129, shared-plus-fused2051-token state1130, and integrated MTP198. Every text QSA/indexer rotation uses the independent dispatch; the draft head inherits the same Rope. Both controls are prepared before cached MTP entry as well as ordinary main forwards. Shape/state/logit/continuation checks cross the sparse-indexer boundary; integrated MTP additionally covers images, complete/common-prefix reuse, sampling, cancellation and retry. Unsupported inputs keep the original path. The shared table remains bounded at two entries and clears when disabled. Counters report table builds/hits and scheduled rotations, not GPU event time.

RoPE remains default off and outside integrationCandidate; component and fixed-memory serving performance are pending. V143 separately implements the residual terminal final-QSA query candidate and a four-arm production-Rope component screen, with no result at this update. The final-query candidate retains full projection/key/value/indexer update geometry, slices only demanded output computation, preserves the original one-row sparse mask, and is gated off for full-output APIs or a loaded MTP head. Its layer-major path writes earlier tiles' keys before the final output tile. Existing numerical bands and exact state requirements remain unchanged; a kernel-shape change is not assumed exact. Tests cover final-demand cancellation and one-token sparse continuation. All other open program gates remain open.


**OPT35 V143: single final-query version rejected by existing numerical gates.** [[sources/runs/2026/09/2026-09-06-optimization-terminal-query-counterexample]] preserves the frozen source/build and all3491 assertions (3488PASS,3FAIL). The1024/2051-token families each change final router rank, although the sets match. The layer-major/image/cancellation lifecycle output differs by0.01156069338 of the reference logit spread, above the unchanged0.01 threshold. The4096 family passes810 and loaded-MTP bypass passes370. Persistent state, teacher-forced continuation, rollback, full-output API bypass, cancellation after the first terminal key tile and enabled one-token sparse masking all pass. These do not waive the failed gates. The single-query prototype has no performance result and is not adopted.

A separate V14464-row tail successor is awaiting build/native validation. It keeps matrix operations for final attention/output/second-hyperconnection, preserves every row of shorter passes, and accounts for the actual64 computed rows; all numerical and ordered-route thresholds remain unchanged. The pinned implementation uses distinct vector/matrix dispatch and64-row NAX tiles, making kernel-family preservation a source-backed hypothesis, not an exactness claim. Final OPT32/33 and all other unresolved qualification items remain open.


**OPT35/11 V144:64-row terminal tail passes; first RoPE timing screen excluded.** [[sources/runs/2026/09/2026-09-06-optimization-terminal-tail-and-rope-screen]] captures the frozen successor and3491passing native assertions: three810-check numerical families,691lifecycle checks and370MTP-bypass checks. All original ordered-route and numerical limits pass, while persistent state and continuation remain exact. This resolves the single-query predecessor's failures by retaining matrix-shaped terminal output computation and every row of shorter passes. Cancellation after an earlier key-only tile, image splicing, rollback/retry, full-output APIs, one-token sparse masks and loaded MTP are covered. Terminal-tail serving, quality and integration remain pending; it stays off.

The four-arm production-Rope screen passes421exactness/mechanism/interval assertions with a326435728-byte maximum sampled footprint, but12whole-process swapins exclude every timing result. No component or serving gain qualifies from V144. An independent V145confirmation is frozen prospectively with the same executable/helper, five rounds, whole-process zero-swap gate and no replacement/pooling. The41-test harness now enforces distinct-tail partial reuse and RoPE/terminal mechanism counters; the445-token fixture shares309tokens with the440-token warmup, so its intended256checkpoint cannot be confused with a full hit. Final combined drafts remain unbound. Resident/CPU serving, compute islands, RoPE confirmation/serving, terminal quality/performance, remaining prefill and OPT32/33 are still open.


**OPT11 V145: independent confirmation also excluded.** [[sources/runs/2026/09/2026-09-06-optimization-rope-confirmation-excluded]] preserves the same-protocol confirmation:274reported assertions,273PASS and one interval-swap failure at256rows/round5/fused. Four global swapins exclude the entire process; the1024-row family is unrun. Exactness and observed mechanisms pass, but no component gain qualifies and neither excluded run is pooled or replaced. RoPE remains off, with serving qualification still open. The next timing block is the existing V140 resident-overlap serving protocol, unchanged and unrun at this update. No whole-program completion or combined/default speed claim is made.


**OPT19 V140 serving completed (V146 evidence): resident overlap misses the gain gate.** [[sources/runs/2026/09/2026-09-06-optimization-resident-serving-counterexample]] preserves all14cells under the unchanged frozen protocol. Six clean pairs1,2,4,5,6,7; pair3swap-excluded. All seven raw pairs have exact IDs/wire text and matching eight work counters (3359prefill/4722decode records,17input/16output/15model tokens/15forwards, no draft/verify). Each candidate submits613resident operations and joins613; reference0/0. Active bytes are4490729648in every arm; qualified candidate peaks5086907536–5090086008bytes. Host join waits2.629–2.784ms are not GPU event durations.

Median paired client reduction is0.1972128117percent, five of six clean pairs positive. Minimum sample count, exact outputs and80percent-positive pass, but the5percent gain gate fails. No replacement or threshold change; overlap remains off/outside integrationCandidate. Its conditional joint/GPU-event adoption work is not earned by this result. CPU-copy serving, compute islands, RoPE optimization/qualification, terminal-tail quality/performance, remaining prefill and final OPT32/33 are still pending.


**OPT11 V147: direct-stride kernel passes2687native assertions.** [[sources/runs/2026/09/2026-09-06-optimization-rope-direct-strides]] preserves the new3Dlaunch/direct signed-stride implementation, unchanged BF16 rounding and230component/1129fused-full-state/1130combined-full-state/198integrated-MTP assertions. The component now includes negative and nonunit input/angle strides and empty fallback. Full-state checks cross2051tokens; MTP integration includes images, sampling, prefix reuse, cancellation and retry. No speed/adoption claim yet. A separate same-gate component run is frozen with180seconds of sampled quiet before preflight and no pooling of V144/V145exclusions.

The same raw record freezes V148's paired maximum1536image study before execution, protocolSHA86d4741560883dbfd0de6ea18054ce94fd1bc4f03790fe3b82fff540eeb031b8. It retains10GB/sevenpairs/min5clean/200MBwhole-request saving each/5percent median nonregression/exact-output gates. The logical attention-domain reduction at9216patches motivates this independent case; it is not measured saving. Earlier1024resource failure and maximum single-arm exclusion stay intact. CPU-copy serving, compute islands, terminal-tail quality/performance, remaining prefill, final combination/defaults and OPT33remain open.


**OPT11 V147 component qualifies (V149record).** [[sources/runs/2026/09/2026-09-06-optimization-rope-component-qualified]] captures five clean fixed pairs per shape,421native assertions, whole-process zero swap and a325993336-byte sampled peak. Direct-stride fusion gives12.2243/14.9280/30.1819percent component reductions at1/256/1024rows; shared-plus-fused gives18.2054/17.5395/35.0286percent, every pair positive. Both pass5%/80%at all three shapes. Tables alone pass at1row (9.6845%), not256/1024 (2.8552/3.3304%). These are bounded QSA component gains, not whole-engine throughput. The same executable passes43catalogue groups; the numeric-count wrapper receipt correction is preserved with unchanged native output.

V149short17/16 and prefill440/16 serving nonregression protocols are frozen before observation at8.1GB/640slots. They compare the qualified shared-plus-fused candidate to the common qualified base, require exact outputs/eight work counters and actual mechanisms, minimum5of7clean pairs, at most5percent median client regression and unchanged memory/power/VM/quiet/cooldown gates. ProtocolSHA71a29134eb403dbd410cd051768d5bf86dc23bbd3545382a3ac813824859958b(short) and3aef933120c567ea3a1b99f4cd3e05652206a1993bf940a80b0316cef19646cd(prefill). This gate is prospective after component qualification, not a retrospective threshold change or a5percent serving-speed claim. Both are unrun; RoPE stays outside integrationCandidate/defaults. CPU-contiguousV115is next; max-imageV148, compute islands, terminal-tail quality/performance, remaining prefill and OPT32/33remain open.


**OPT19 V150: contiguous CPU writes fail the serving gain gate.** [[sources/runs/2026/09/2026-09-06-optimization-cpu-contiguous-counterexample]] preserves all14cells from the unchanged V115protocol/V110executable. Six clean pairs1,2,4,5,6,7; pair3swap-excluded. All seven raw pairs have exact IDs/wire text and matching3359prefill/4722decode records,17input/16output/15model tokens/15forwards, no draft/verify. Every candidate executes129prefill/719decode CPU batches; active memory4490729648bytes matches reference. Qualified candidate peaks5088136384–5088382144bytes.

All six clean pairs are slower. Median paired client regression6.5949435346percent; generator6.5982340310percent. Minimum-five and exact-output pass, but five-percent gain and80percent-positive fail. No replacement or threshold change: contiguous CPU writes remain off/outside integrationCandidate. This closes this successor's adoption branch without erasing its3348passing native assertions or the earlier indexed-CPU failure. RoPE serving, maximum-image V148, compute islands, terminal-tail quality/performance, remaining prefill and final OPT32/33remain open.


**OPT11 V151/V152: both serving nonregression gates pass.** [[sources/runs/2026/09/2026-09-06-optimization-rope-serving-qualified]] captures28cells on frozen V147. Short17/16 has seven clean exact pairs,0.17229738115percent median paired client reduction (four positive),3359prefill/4722decode records and unchanged work. Candidate384fused rotations/176tablehits/16builds versus0/0/192; active+1024bytes, candidate peaks5086776464–5090053336bytes.

Prefill440/16 has five clean exact pairs1,2,4,5,6;3and7swap-excluded. All seven raw output pairs match. Median paired client regression0.20421633983percent (two positive),21511prefill/4793decode records with identical eight work counters. Candidate408fused rotations/187hits/17builds versus0/0/204; clean candidate peaks6206590696–6242782928bytes. Both prospectively frozen minimum-five/exact/5percent-nonregression gates pass without replacements or threshold changes. The prefill helper's aggregate exit1 preserves excluded cells and does not change its distinct passing acceptance result.

RoPE earns integrationCandidate inclusion from2687native assertions,43catalogue groups, independent component gains and these two serving nonregression gates. Request latency is approximately flat;18.205/17.540/35.029percent remains component-only. Public defaults, compatibility restrictions and final combined gates remain pending. Maximum-image V148, compute islands, terminal-tail quality/performance, remaining prefill and final OPT32/33remain open.


**OPT18 V153: quantized compute screen qualifies, most fusion benefits fail.** [[sources/runs/2026/09/2026-09-06-optimization-compute-islands-qualified]] preserves303passing assertions, five eligible pairs per cell, zero whole-process swap and2882046520-byte maximum sampled footprint. The explicit180second quiet interval is a prospective independent-confirmation condition; original V110exclusions and dense B/A numerical failure remain intact. Only3of32outcomes pass the5percent/80percent-positive gate: shared pointwise GLU at8rows5.87387percent and32rows5.69191percent, and GDN QKV/Z at1row5.44636percent. All shared projection/combined cases fail. Pointwise regresses5.3851percent at1row and2.10846percent at256; no broad activation or interpolated shape policy follows. A narrow GLU successor is conditional on a relevant measured workload/profile.

One-token GDN projection earns a model-level prototype, with separate projection fallback and exact named weight tensors. Naive all36-layer duplication costs849346560bytes, so a proposed single concatenated backing must prove alias ownership, full bytes, supported dispatch and startup/resource cost before serving qualification. No default or planner credit changes. RoPE is now included in the unbuilt joint candidate configuration and its integrated mechanism checks; public defaults remain off. Maximum-image V148, terminal-tail quality/performance, remaining prefill and final OPT32/33remain open.


**OPT18 V154: shared-backing GDN projection passes2296native assertions and43catalogue groups.** [[sources/runs/2026/09/2026-09-06-optimization-gdn-projection-shared-backing]] preserves source/build and773packing/1131full-state/189ordinary/203MTP assertions. All recurrent named tensor bytes/strides remain exact. Reference and packed resident increments both equal2576975896active bytes; the849346560-byte projection payload is shared, not additional. OnlyB1/S1/BF16 fuses; unsupported metadata, other shapes and disabled dispatch preserve the original projections. MTP integration explicitly compares a fused one-token main-model state/logit path. No public defaults or planner credit; RoPE is now in the built integrationCandidate.

V156short/prefill protocols are frozen before observation, SHA70173fa0f7bbe6da33b729f289be5b398599617548acab13ae394b48df4c2d4c(short), a6a29d28c6f389fabffa998eee09f1053ac06c98aa7c7b890a16247ec5a74602(prefill). Each has7pairs,8.1GB/640slots, exact outputs/eight work counters and actual540fusions/36layers/849346560shared bytes. Require separately min5clean warmed pairs and min5clean exact first jobs, each at most5percent median regression, plus fixed resource/quiet/cooldown gates. A harness initialization-order failure was found before benchmarking, preserved, and corrected;43harness tests pass.

V155terminal64-row-tail serving is also frozen on V144, SHA2f9bcab3484dd03c92058d066b17436cb1c40362c5f2365c884ab28f4618f49a:10GB/sevenpairs/min5clean/5percent client gain/80percent-positive/exact-output, plus actual skipped-query proof. Quality and combined gates remain necessary if benefit earns adoption. V148maximum image, these serving runs, remaining N6 qualification and final OPT32/33are still open.


**OPT27 V157: target-driven maximum-image reference exceeds the resource ceiling.** [[sources/runs/2026/09/2026-09-06-optimization-maximum-image-reference-stop]] preserves the automatic stop after1of14planned cells. Reference peak10144682400bytes exceeds10GBand8swapins exclude timing. Nominal power,2329prompt/104279prefill records/288310579200read bytes, Red[3605], no decode reads; client32.3865s and encoder5.0225s are excluded observations. No candidate or valid pair exists. No memory or acceptance gate is relaxed.

Independent V158is frozen before observation: exactly640slots via native --pool-gb1.769472, unchanged10GBmeasured whole-request ceiling,12fixed pairs/min5clean/200MBpeak saving in every clean pair/5percent latency nonregression/exact outputs and work. SHA b05e23354bf3fb96a49499f58e41d50b1583b271a6af6b8b6768b488eb22c9a4. This is a fixed-pool mechanism comparison with additional headroom, not a passing --memory-gb10planner result or pooled replacement for V157. The narrow helper validates640slots and actual capacity;44tests pass. Target-driven image accounting and combined/default acceptance remain open, alongside V155terminal/V156GDN serving, remaining N6 qualification and OPT32/33.


**OPT27 V158: second resource stop and correction of the pool-size assumption.** [[sources/runs/2026/09/2026-09-06-optimization-maximum-image-pool-correction]] preserves the stop at10251604336physical bytes plus4swapins,1of24cells/no candidate/no pair. Both V157andV158actually used640slots in warmup ANDmeasurement, with identical2329tokens/104279records/5458031224active-end bytes/8.599892626GB MLXpeak. The previous statement that explicit640reduced a larger V157pool was incorrect; it provided no actual capacity reduction. Historical source/protocol bytes stay intact, while this correction supersedes that interpretation. No10GBresult qualifies.

V159is independently frozen at a NEW12GBmeasured ceiling and the SAME640slots,12fixed pairs/min5clean/200MBsaving each/5percent median nonregression/exact outputs/work,18GBhost headroom and unchanged strict guards. SHA c7dcbf0dc06a700d7fb772fd115493c4919bea3e567ee94049fcbb7f9277c8d7. V160separately freezes seven query256candidate-only --memory-gb10capacity cells with min5clean exact Red[3605]/972tiles, SHA249763ff5d17a05382545b33fc840e01855599229d955bee150ba7a2133e5325. Its explicit capacity_gate is a separate post-run assessment, never paired timing evidence. Both are unrun; the helper's bounded12GBvision declaration passes45tests.

The model/build interval has been released to the concurrent authorized context task; shared Sources remain atV154while it qualifies an isolated delta. Vision transient accounting is coordinated with that task and remains separate from resident/load allowances. V155terminal/V156GDN, maximum-image successors, remaining N6 and final OPT32/33are still open.


**OPT32 V161: full protocol preflight catches and fixes the large-vision loader inconsistency before any run.** [[sources/runs/2026/09/2026-09-06-optimization-vision-preflight-correction]] preserves the original failing regression, corrected helper and all 46 passing harness tests. The image loader now reuses the complete narrowly declared 12 GB / 640-slot / one-image / one-output resource contract; ordinary image studies keep the existing envelope. All five pending V155/V156/V159/V160 protocols pass static preflight through binary/source and native-prerequisite identities, fixtures, images and request serialization with their original protocol hashes. No model/build or performance result is implied. The context task still owns the model/build interval, and shared Sources remain unchanged.


**OPT27/32 V162: the frozen capacity decision is executable.** [[sources/runs/2026/09/2026-09-06-optimization-vision-capacity-gate]] preserves `Tools/vision_capacity_gate.py` and five passing focused tests. The post-run tool applies V160's original seven fixed cells / minimum five clean cells, exact output and physical resource/mechanism contract separately from paired performance acceptance. It rejects incomplete/replacement studies and rechecks evidence behind valid flags; a wrong clean output fails rather than being filtered away. V160 remains unrun and its frozen protocol is unchanged. No model source or default changed; the context task is finishing its bounded build/component interval before returning the reservation.


**OPT32 V163: bounded repeated-request lifetime qualification is prepared, not yet run.** [[sources/runs/2026/09/2026-09-06-optimization-combined-soak-harness]] preserves the new `Tools/optimization_soak.py`, seven passing focused tests and an unrun fixture-pinned draft. Separate sequential plain/MTP servers will use 10 GB, two warmup plus eight measured cycles of six requests, a 900-second bound and explicit input/output/cache/retained-memory limits. Exact per-position replay remains required even when swap excludes resource observations. At each position five clean observations, including two early and two late, must bound active and physical end growth. The driver rejects incomplete/replacement sequences and unfrozen drafts and cleans up its owned server on failure/interruption. Final binary/control/prerequisite binding and both live soaks remain open; fresh-server paired benchmarks cannot substitute for this gate.


**OPT35 V155: terminal-tail serving stops for futility and does not qualify adoption.** [[sources/runs/2026/09/2026-09-06-optimization-terminal-tail-serving-stop]] preserves nine measurements plus the round-five candidate interrupted during cooldown, before launch. Pairs 3/4/5 have swap activity, leaving at most four possible clean pairs versus the frozen minimum five; no replacements run. All nine observed outputs match, every candidate skips 637 final queries / 700 MoE rows, and actual 10 GB serving uses 961 slots, 701 input / 16 output / 15 decode-model tokens. Prefill records fall 35,776 → 35,185 while decode records rise 3,980 → 3,995.

Only pairs 1/2 qualify, with 3.0887458% / 0.6544169% reductions and a 1.8715813% paired median, below the 5% criterion. Candidate sampled peaks are 35.55 / 19.10 MB higher, though all observed peaks remain below 10 GB. Native V144's 3,491 passing assertions remain correctness evidence. Terminal-tail stays OFF and outside integrationCandidate; no new study or default adoption follows. The owned driver exits 130, cleanup confirms no local model and a free lock, and the interval returns to the context task's bounded diagnosis. V156 GDN, V159/V160 vision, remaining N6 and final OPT32/33 remain open.

The same source captures V164's final Tools-only soak correction: enabled prefix caching and at least one retained state are now mandatory, so a disabled/empty cache cannot manufacture a retention pass. Seven focused tests pass. The frozen lifetime draft still has no live run or final binary binding.


**OPT32 V165/V166: seven final-combination drafts cover first-job and simultaneous prefix costs.** [[sources/runs/2026/09/2026-09-06-optimization-final-matrix-preflight]] preserves seven non-executable drafts in `/tmp/slotstream-optimization-execution/integrated-protocol-drafts-v165`: short-one, unique-prose resource, sampled-short, MTP resource, distinct-tail, complete-repeat and unique-with-retention. Each declares sixteen fixed alternating pairs, unchanged resource/exactness/latency criteria and an additional min-five-clean / at-most-five-percent first-job regression gate. This prospective count follows observed whole-pair contamination, not replacement of a measured candidate. All 54 current controls are explicit; GDN, vision query, terminal-tail and scopes remain excluded pending independent qualification. Every binary path is intentionally `FINAL_BINARY_REQUIRED`; final source and native prerequisite binding remain open.

The complete-repeat case enables partial and complete checkpoints together. V166 corrects the harness assumption that complete reuse implies zero partial warmup stores by adding an explicit bounded per-arm expectation; that case requires reference zero / candidate one. Historical protocols keep their original behavior. All 48 harness tests and seven static draft checks pass. No model or performance result follows. The context task continues a bounded four-layer numerical diagnosis and keeps its failing successor outside shared Sources.


**OPT18 V156 short: both serving and first-job nonregression gates pass, without a request-speed gain.** [[sources/runs/2026/09/2026-09-06-optimization-gdn-short-serving]] preserves all fourteen cells and exact outputs/eight equal-work counters in every raw pair. Actual 8.1 GB serving uses 640 slots, 17 input / 16 output / 15 decode-model tokens, 3,359 prefill and 4,722 decode records. Every candidate schedules 540 one-token projection fusions across 36 packed layers; its 849,346,560-byte payload is shared. All active end allocations equal 4,490,730,672 bytes.

Six clean warmed pairs (1/3/4/5/6/7) pass the five-percent nonregression gate at 0.8571868% median client regression; five of six are slower. Round two remains excluded for eight candidate swapins. Five exact eligible first-job pairs (1/3/5/6/7) pass at 1.0658950% median regression; round four's startup/warmup is independently excluded. Candidate clean physical peaks are 5.08550–5.08791 GB. The exit code one reflects the excluded raw cell, while both structured acceptance gates pass. No request-speed or physical-memory saving is claimed. GDN stays OFF/outside integrationCandidate until the prefill study and final adoption decision.

Cleanup confirms no local model and a free lock. The model/build/local-storage interval is handed to transport task `01a073d9-b583-7d71-916c-1835d53af327` for its bounded rebuild, full fresh compressed-Hugging-Face download and short reconstruction smoke; context task `01a07425-9a6a-7d42-86a2-717abb929252` is notified. No new local model/build here until release. Transport reserves v0.2.11; this task makes no publication. The context successor's remaining nine numerical failures stay isolated.


**OPT30/32/33 V167–V169: lifetime cleanup and compatibility preparation.** [[sources/runs/2026/09/2026-09-06-optimization-lifetime-platform-preparation]] preserves ten passing Python soak tests, the real wall-clock deadline and mocked full-driver cleanup on success, HTTP failure, cancellation, timeout and startup failure. A fake-response coordinate mistake in the first mocked success test is preserved and corrected. No model/build or live soak ran.

The independent context-task pin fix is now in shared Generate.swift: every nonempty request joins GPU users before returning expert pins. Expanded integrated and real HTTP retry assertions are added, but remain unbuilt/unrun here. The context task's reported 462 passing HTTP assertions do not substitute for the new combined build. Its remaining numerical context changes are still isolated.

The prospective deployment candidate restricts automatic fused RoPE to native ARM64 Mac17,9 / Apple M5 Pro / OS build25G83; missing, different or updated identities keep the original rotation and portable candidate work. Seventeen new pure assertions cover this policy, pending native execution. Explicit experimental qualification remains available and all public/environment defaults stay OFF. Final automatic wiring, source identity, seven paired cases, two live soaks and full acceptance remain required.

Transport continues owning the interval for fresh Mac reconstruction/acceptance and reserves v0.2.11. Its separately published e498b2d0162cefa314ba9931ebd38c3a0c47f371 HF transport change must be preserved when combining the final source. Frozen V154/V125 binaries/protocols remain unchanged for pending V156 prefill and V159/V160 image studies. No local model/build starts until explicit release.


**OPT16/32 V170: long-prefill combination is prepared for a fresh 10 GB qualification.** [[sources/runs/2026/09/2026-09-06-optimization-scope-combination-preparation]] preserves the source review, expanded native diagnostic variants and an unrun fixture-pinned draft. V22 already observed candidate footprints below10GB, but reference swap prevented a paired result; V98's separate16GB stop is not rerun or erased. The new common base includes bounded embedding rows and the qualified core/RoPE controls, plus the exact bounded-indexer/PLE path. Candidate adds4096read scope/1024workspace tile/compact frontier while retaining256-token compute and the same native10GB target. No pool override or planner credit.

The3730-token code request has16greedy outputs and MTP/prefix off. Sixteen prospective fixed pairs require min5clean/5percent client gain/80percent positive, exact IDs/text, six equal logical/model-work counters, reference prefill records>=150000 and candidate<=50000, strict10GB/power/VM and independent first-job nonregression. Initial quiet180seconds, reserved cooldown60seconds, no replacements;20.112GBcandidate preflight headroom. Static checks pass, all54controls are explicit, and FINAL_BINARY_REQUIRED prevents execution. Combined2051/4096family, scope lifecycle and MTP/image assertions are unbuilt/unrun prerequisites. Short-prefill guard, automatic activation and planner coherence remain separate if benefit qualifies.

V169 also receives its explicit package-visible platform initializer for the separate diagnostics module; no compiler pass is claimed. Transport retains the interval; frozen V156prefill/V159/V160 and the final combined matrix/soaks/full acceptance remain open.


**OPT20/22/32/33 V171–V172: current combined source builds and weight-free regressions pass.** [[sources/runs/2026/09/2026-09-06-optimization-bounded-build-and-staging-review]] captures the single explicitly permitted `-j2` build during the transport download and all32 T0 groups /20,145 assertions passing without failures or skips. The source receipt binds the V168 request-end join/unpin fix and its expanded diagnostics, V169 platform fallback policy and17 new assertions, and V170 combined scope diagnostic variants. Those full-model pin/scope assertions are compiled but still unrun. Six Python build-helper tests and an actual Makefile dry run validate the new1..8 job bound, command recording, contention, headroom and identity failure. Ordinary SwiftPM defaults are unchanged. No model ran; overlap timings and memory observations are excluded from inference evidence. Both owned processes exited and the shared source freeze/model reservation were returned to transport.

V172 re-analysis of the existing V85 profile puts median allocation/wrapping fractions at1.431909/0.856673/0.923457/0.750643percent for1/32/256/1024tokens; including staging evaluation gives1.483223/0.889356/0.964122/0.785319percent. The largest of all20 observations is1.499931percent. These serialized attribution fractions are not attainable speedups and overlapping waits cannot be charged twice. The reusable-arena materiality trigger remains unmet in these measured regimes. Arena and device-mapping rewrites remain conditional, with explicit lifetime proof required before broad synchronization can be removed; no such implementation or gain is claimed. Separately tested contiguous/word/CPU copy alternatives retain their rejected serving outcomes. Revisit if a qualified change makes residual handoff material.

Frozen V156prefill/V159/V160, V170full-model scope qualification, final combined pairs/soaks/full acceptance and activation remain open. Transport still owns the overall download/acceptance interval. All optimization defaults remain OFF.


**OPT33 V173: explicit fallback remains available when defaults are eventually selected.** [[sources/runs/2026/09/2026-09-06-optimization-default-overlay-preparation]] preserves an unbuilt package-only environment overlay resolver and34 added native assertions. The public environment function/signature and reference initializer remain unchanged. All48 booleans preserve their selected default when absent, explicit0/1 changes only the requested field, numeric0 can disable prefix retention, and inherited read scopes revalidate prerequisites after overrides. Tests cover each current candidate flag in both directions, complete reference restoration, malformed/unknown controls and function-value compatibility. Static mapping checks pass; Swift compilation/native execution remain pending and V171 does not cover this newer source. No public default, new control, planner credit or performance result changes.


**OPT32 V174: lifetime acceptance requires the final native and paired evidence.** [[sources/runs/2026/09/2026-09-06-optimization-soak-prerequisite-binding]] captures23 passing focused Python tests and fail-closed prerequisite enforcement before output/server creation. Three native reports (combined plain/MTP and real HTTP recovery) plus all seven distinct final paired studies must bind to the same verified binary/source archive/Metal identity. File hashes, actual passing assertions, correct diagnostic variants, all32 unique cells in each16-pair study and original workload-specific thresholds are checked. Request/first-job acceptance is recomputed from frozen rows and must match completion; skipped/failed/old-build/incomplete/duplicate/weakened evidence is rejected. Final workload/control binding still requires the planned pre-run review.

The soak also requires actual embeddingRowsEnabled metrics, preventing disabled row storage from qualifying on zero bytes. Existing prefix, memory, exactness, deadline and clean early/late rules remain unchanged. No live soak/build/model ran; V163/V164 awaits final native/paired bindings, and V173 Swift remains unbuilt. Transport's verified HF resume and context's newly granted bounded correctness sequence own the interval; all final model/performance/default gates remain open.


**OPT18 V156 prefill: warmed nonregression passes; first-job qualification fails and GDN stays OFF.** [[sources/runs/2026/09/2026-09-06-optimization-gdn-prefill-first-job-failure]] preserves all14 cells from the single frozen7-pair study. A personally drained/verified-resumable HF pause supplied the explicit17:20–17:42UTC interval;180seconds quiet and60second cell cooldowns remained intact. At8.1GB/640slots and440input/16greedy output tokens, every raw pair has exact IDs/text and eight equal-work counters, including21,511prefill and4,793decode records. Every candidate schedules540fusions over36packed layers/849,346,560shared payload bytes; reference0/0/0.

Pairs1/7 each have4candidate measured swapins and stay excluded. Five clean warmed pairs2–6 pass nonregression at1.7600558percent median client reduction;4/5 favor candidate. Clean candidate peaks6.20489–6.32246GB remain below8.1GB; the small variable active/peak changes establish no memory saving. First-job qualification fails: only pair6 is eligible versus minimum5. Reference startup/warmup swap occurs in2/7 and candidate in3/4/5; no contaminated interval is borrowed from its clean warmed observation. The sole eligible first-job reduction0.5509584percent is insufficient evidence. Exit1 and all exclusions remain preserved, without replacements.

GDN remains outside integrationCandidate and automatic defaults. Its5.44636percent component gain/2296native assertions and short-serving nonregression remain scoped evidence; the small prefill warmed reduction plus failed first-job gate do not earn adoption or a new study. The driver/server exited, zero model processes/free lock were confirmed and the interval returned promptly to transport's exact verified HF resume. Context now owns a separately granted bounded correctness sequence. Pending V159/V160vision,V170scope and final OPT32/33acceptance remain open.


**OPT08/29/30/32 V175: context integration review catches dispatch and allocation ownership gaps.** [[sources/runs/2026/09/2026-09-06-optimization-context-component-review]] preserves the unapplied 29-file component proposal and three-way staging. The sole Generate conflict is resolved in staging by retaining V168's GPU join/unpin before the new admission refusals. Shared Sources remain untouched, and V173/V170 work is preserved. The extracted proposal is unbuilt and cannot inherit its full-snapshot test result.

Review caught an odd-override path that could halve 511 rows to 255 within the existing context cap. The context owner corrected v2 to preserve the original 256-row floor whenever it fits. The independently inspected build13 HTTP report passes all 835 assertions after the pending-pressure ticket fix and actual retained-ID fixture correction; its permitted overlap excludes timing/capacity claims. Separately, review found that aggregate main/draft/indexer capacity subtraction can credit spare bytes in the wrong buffer. The context owner confirmed this and is adding per-buffer full replacement-allocation charges and actual pending/draft-span guards. Those new fixes are not covered by the 835-assertion report and must pass identified tests before integration.

The isolated C07 first projection witness passes 1,605 criteria, as reported by its owner; the wider numerical family and larger capacity remain unqualified. No new public context cap or numerical dispatch is adopted. Pending image/scope studies and final combined acceptance remain open while transport/context own the bounded interval.


**OPT27 V160: maximum-image capacity remains unqualified.** [[sources/runs/2026/09/2026-09-06-optimization-maximum-image-capacity-incomplete]] preserves the unchanged frozen candidate-only 10 GB study. Four cells completed; only cell 2 is clean, with exact Red token [3605], 972 evaluated query tiles, 104,279 prefill records and a 7,045,566,688-byte whole-request sampled peak. Cells 1, 3 and 4 have 24, 524 and 8 swapins and remain excluded. All completed outputs match, but excluded intervals do not establish capacity or a paired saving.

After three exclusions made five clean cells impossible, the owned driver was interrupted for futility during cell 5. Exit 130 and the incomplete five-record result are preserved; cells 6 and 7 did not run. The separate assessor fails both fixed-study completion and minimum clean count. There are no replacement cells, changed thresholds, seven-cell completion claim or default adoption. V159's separate 12 GB paired comparison remains pending. Driver/server exit, zero model/build processes and a free shared lock were confirmed before returning the interval at 18:42 UTC; transport resumed its exact verified download.


**OPT08/29/30/32/33 V176: context ownership is integrated and the merged build passes T0.** [[sources/runs/2026/09/2026-09-06-optimization-context-integrated-build]] preserves the 30-file three-way merge, its original bytes, the one Generate conflict resolution and complete frozen source. V168's single GPU join/unpin precedes all nonempty admission failures. Per-buffer full replacements, actual draft/verify/reconciliation spans, compact-tail copies and checkpoint sharing are charged before dispatch. An Engine-owned atomic budget keeps concurrent tokenization/image preparation and pending dispatches from spending the same headroom; request/vision ownership retains preparation charges through queueing and releases them with the final owner. The tiny concurrent-budget fixture creates no real memory stress.

The merged executable passes all 33 T0 groups / 22,242 assertions, with zero failures/skips, including the new context gates, V169's 17 platform assertions and V173's 34 overlay assertions. Source identity binds the preserved V170 variants. The separately captured full-snapshot HTTP report passes 835 assertions, but the extracted merged binary still needs its own full-model/HTTP gates. Build/T0 ran only under the specifically permitted HF overlap; timing/memory are excluded, no model ran, and the build/check process reservation was returned at 18:48 UTC. No C07 arithmetic, context-limit increase, default activation, version change or publication follows.

The closure audit keeps the following original substeps open explicitly: OPT07 isolated compact n-gram owned-memory and serving qualification; OPT10/N6 U2's fused four-head positive-part score prototype; OPT25 standalone bounded-output MTP-tail qualification and deterministic request-boundary policy disposition; and OPT28 duplicate-image serving benefit plus the conditional feature-cache disposition. Adjacent mechanism tests do not close these substeps. Their next protocols must bind source and correctness evidence before running; this audit creates no measured benefit or adoption. V159, the combined N6 scope, final paired matrix, two live soaks and full acceptance remain pending.


**OPT10/N6 U2 V177: four-head score prototype prepared, unrun.** [[sources/runs/2026/09/2026-09-06-optimization-indexer-score-preparation]] binds the standalone Metal/Python prototype, frozen component contract and five passing pure protocol tests. Four independent positive parts precede the head sum; original visibility/top-512 masks must match exactly. Float64 checks cover boundary/tie/exceptional inputs and every exact timing input, including 8192 blocks. Five fixed alternating pairs per shape, five-percent median benefit, eighty-percent positive pairs, no replacements, fifteen-minute deadline and explicit memory/VM/power bounds are prospective. No GPU/model/performance has run, no production dispatch changed and no benefit is claimed. Transport has not granted the clean component interval.

**Combined context correction remains required.** The context task's separate V176-based build16 reports broad C07/context checks passing, then prefix-retention-mtp aborts on Swift exclusive access. This does not qualify shared V176 MTP. A minimal correction and rerun are pending; final source, native integration, paired comparisons, two soaks and activation remain open.


**OPT25/29/30 V178: shared MTP cancellation correction applied.** [[sources/runs/2026/09/2026-09-06-optimization-mtp-fix-and-cache-qualification]] preserves the exact V176-based three-hunk fix and original build16 exclusivity abort. Caller cancellation is recorded outside GenStats' exclusive speculative borrow and folded in after it ends; explicit request failures keep precedence, and legacy committed-prefix cancellation retains its compatibility semantics. Separate context build17 passes prefix-retention-mtp198, including18 new typed cancellation/recovery assertions, and lifecycle138. Its adaptive258/259 result reproduces the exact old1002-versus3431 token16 counterexample; no threshold changed or redundant baseline model ran. Shared corrected source remains unbuilt and final shared MTP qualification remains open.

**OPT07/25/28 V179: five isolated serving drafts are concrete and unrun.** Compact n-gram serving, bounded MTP tail at two outputs, a seeded sampled tail guard, two identical512 images and a unique-image guard all bind explicit work and resource contracts, with final binary/native prerequisites still unbound. Each uses native10GB, sixteen fixed alternating fresh-process pairs, minfive clean, no replacements, whole-request resource observations and an independent first-job gate. The MTP work diagnostic retains its public signature and now covers seeded sampling and an explicit integrated base; new native code is unbuilt.

**OPT07 V180: full-cache storage and FIFO comparison prepared.** The real row-store component fills from32768 tokens, times128 warm256-token assemblies and4096 tokens of actual eviction, hashing all BF16 outputs. Four fresh-process arms cross FP32/UInt16 rows and array/ring bookkeeping. FIFO already evicts40000 rows at once, so no per-miss400000-key shift is assumed. Seven fixed rounds, minfive clean and no replacement separate compact memory benefit (100MB payload and80MB absolute/baseline-adjusted physical saving in every clean pair) from ring eviction speed (5percent median/80percent positive), with nonregression and5GB process bounds. Exact same-source native prerequisites are enforced before launch. Six component and49 serving-harness pure tests pass; no component/native/performance run has occurred. Pooled row storage remains conditional on measured residual allocation overhead.

**Conditional policy dispositions are explicit.** The original adaptive policy investigation is rejected by its exact output counterexample. Existing off/on/auto planner selection and full head/context charges remain; a new deterministic request-boundary successor is conditional on its own numerical/quality and fixed-total-memory evidence. Bounded output-tail work is still being qualified separately. Persistent cross-request image features remain conditional on demonstrated edited-prompt benefit and charged content/model/processor ownership; no cache is implemented or credited. Existing V131/V132 phase fractions are preserved as workload-specific cost observations, not universal bounds or new speedups. Request-local duplication has an active V179 qualification draft. Transport retains the machine through installed-release acceptance; our source work continues, and final joint/native/paired/scope/vision/soak/activation gates remain open.


**OPT07/10 V181: unrun component activation and numerical bounds tightened.** [[sources/runs/2026/09/2026-09-06-optimization-probe-review-and-pressure-recovery]] preserves direct selection/observation of compact rows and FIFO on the standalone n-gram store, since that diagnostic deliberately skips model.forward. It also closes a vacuous infinite-bound case in the indexer oracle: negative-infinite heads have zero positive part and cannot inflate the finite heads' error budget to infinity. Finite scores require finite bounds; exceptional-pattern and exact-mask gates remain. Five indexer, six final-prerequisite and eleven soak-harness pure tests pass. No native/GPU run occurred and neither component is qualified.

**OPT08/30 V182: typed pressure recovery delta merged.** Only Engine pressure observations and the recovery diagnostic changed; their exact bytes and protected-source hashes are recorded. Typed guards preserve pressure cancellation/latency when they preempt the legacy callback. The diagnostic explicitly refuses work while infeasible, then uses a real-bounded pure-policy reading to recover at the same pool floor and retry exact outputs. No physical-memory override or guard bypass exists. Separate context build18 passes governor-boundary-mtp80 and HTTP835; its earlier missing-control invocation is preserved as a pre-check delivery failure. Shared source after V176 is still unbuilt, and final joint/serving/capacity/soak/activation work remains pending.


**OPT04/08/29/32 V183: bounded C07 arithmetic merged with a prompt-cache identity fix.** [[sources/runs/2026/09/2026-09-06-optimization-c07-source-integration]] binds the fourteen-file proposal merge and protected existing source. Late sub-256 passes preserve a canonical projection/attention domain while cropping before state mutation and charging padded Q×K. Normal scheduling activates the family only after 256 rows no longer fit; the public65536/default32768 limits remain unchanged. Seven pure capacity-evidence tests pass. Separate short numerical witnesses support the family, but shared native and late-context capacity remain unqualified. Source review found the proposal's epoch1 Generate key mismatched epoch0 initializer/ownership lookups; a centralized current epoch, two pure identity checks and explicit old-epoch native refusal fix that integration error before build. The native complete-prompt regression gate remains mandatory.

**OPT18/33 V184: supported stride metadata and accurate CLI help.** The unchanged exact shared-backing stride assertion now uses the pinned MLX no-copy metadata API, removing the external-consumer deprecation gate failure without weakening the test or creating contiguous tensors. CLI help now describes memory feasibility separately from the prefill wait policy. The shared source is unbuilt after V176; context's isolated follow-up build is supporting evidence only. No transport/version files, public defaults, release, performance claim or context-cap increase are included. Pending components, exact-source native integration, final paired matrix, N6 scope, image capacity, two soaks and full acceptance remain open.


**OPT07/32 V185–186: first shared compile stopped, fixture expression corrected.** [[sources/runs/2026/09/2026-09-06-optimization-ngram-compile-correction]] preserves the actual failed -j2 build,33.56864-second wrapper, zero native launches and free-lock/process return. Swift could not type-check the compound synthetic n-gram fixture expression. V186 splits the same Int/Int64 formula into explicit typed steps; token counts, EOS positions and qualification gates do not change. The successor queue is prepared but unbuilt and requires the next bounded correctness grant. No successful native result or replaced performance cell is inferred.


**OPT10 V187: the unrun indexer prototype preserves exceptional score arithmetic.** [[sources/runs/2026/09/2026-09-06-optimization-indexer-exceptions-and-child-cleanup]] records the pinned MLX Maximum branch and the matching explicit NaN/signed-zero handling in the fused prototype. Its Float64/exceptional-pattern/exact-mask gates are unchanged; five pure protocol tests pass but no GPU result exists. The same source/math pass confirms OPT04 tail-aware scheduling changes no default256-token logical passes inside the public65536 window:16,777,216 remains below the32,833,536 product bound. Larger-pass tail benefit remains conditional on separately qualified chronological arithmetic; C07's late capacity family is distinct and unqualified for a higher cap.

**OPT00/30 V188: timeout cleanup follows owned descendant process groups.** Numeric parent/group snapshots precede root termination; only groups led by proven owned processes receive signals. TERM-ignoring descendants are killed even if their root exits first. Snapshot and root waits are bounded; failed enumeration still drains the root and explicitly leaves full cleanup unverified. Normal completion adds no scan. Three real-process test groups, seven capacity-driver groups and49serving-harness groups pass, with original raw outputs and source identities retained. The prospective V186 build wrapper uses the same cleanup. This is harness correctness only; all native/performance/capacity work is held pending the next grant.


**OPT07/25/32 V186–189: shared build succeeds; sampled tail shortening fails and gains an explicit fallback.** [[sources/runs/2026/09/2026-09-06-optimization-mtp-tail-sampled-counterexample]] preserves the exact new shared build/source/Metal, all33T0 groups/22251 assertions, native ngram1160/cache-bookkeeping1163 passes, and258/260 MTP-work assertions. At depth1/seed7/temperature0.7/five outputs, tail and both variants emit final token328 versus reference352 after the same four leading IDs. The batch stops, no further native check or timing runs, and the reservation is returned after142.749180625seconds with an empty compiler audit/free model lock. This is an observed sampled-output counterexample, not a failed core bundle (tail remains OFF there).

V189 restricts optional tail shortening to greedy generation; sampled requests keep the reference requested verification shape, with new exact draft/target/verify-count assertions in addition to output identity. Mandatory context bounds still apply. Greedy work assertions remain. The successor is unbuilt and needs native and serving qualification. The V179 sampled-tail serving draft is now explicitly a fallback-overhead/nonregression gate, with no sampled-tail speed claim. Full combined, scope, remaining component, capacity, soak and acceptance gates remain open. The final acceptance script's existing elastic-drill--slots1000 also needs an explicit bounded plan before launch, because the command internally raises its arena to the floor plus3GB; no such drill ran under the small correctness grant.


**V190 — qualification verification exclusion.** Full checkpoint verification now holds the real per-user model lock across the child, then releases it before inference. All nine provider-free rejection/lock tests pass, including actual competing lock descriptors and a preserved busy-lock refusal. The exact delta and output are in [[sources/runs/2026/09/2026-09-06-optimization-qualification-verification-lock]]. This changes only the capacity qualification driver and its checks; no native source or measurement changed. Final integrated execution and all outstanding performance/capacity gates remain open.


**V189 — shared combined correctness passes its bounded batch.** [[sources/runs/2026/09/2026-09-06-optimization-merged-runtime-correctness]] preserves the serial build and every raw report. Frozen binary `e5558fec2e086689383272457f786c035200ec6034238c6b4cd72e1bc4b920af`, source archive `89f741d052ab8fb330f9e6d8ef90b3bab2f7d5b7bed56324bb90908001dc7147`, Metal `198488eb61359e953580a9c4530400feee1a06dd2f28a930a6ffa58aec66a597`. T0 passes33groups/22251assertions; ngram1160, cache-bookkeeping1163, corrected mtp-work-integrated284, ordinary integrated242, integrated-MTP256, complete-prompt273/MTP281, prefix-retention172/MTP198, and read-failure-serving134 all pass. The sampled-tail counterexample is repaired by exact reference fallback, including identical drafted-token, target-token and verification counts. No output tolerance changed. Ordinary/MTP combined state, continued logits, images, sampling, cancellation, pending tokens and pool pins pass.

The batch ended at its predeclared boundary after1153.407361542seconds, with context-serving, image-reuse and both governor-boundary variants still unrun. The model lock was free at explicit return, and the separate download was untouched. All timing and capacity observations from this authorized overlap are excluded; this is correctness evidence only. The whole optimization program, final paired matrix, soaks, calibration, default activation and acceptance remain incomplete.

**V191 — required diagnostics price their real work and report actual schedule geometry.** [[sources/runs/2026/09/2026-09-06-optimization-bounded-diagnostics-and-schedule]] preserves the10file merge, before/after identities and syntax checks. Elastic-drill now refuses an insufficient explicit ceiling before loading; its full unchanged shrink/cooldown/growth gate declares13GB with16GB preflight and independently checks target-plus3GB, replacement-pool headroom and one complete sampled/VM/physical/RSS interval. Four mandatory-head MTP diagnostics now price the head through startup planning; MTPCheck uses Engine's guarded generation, requires executed vision in full acceptance and validates its entire memory interval at the explicit12GB/15GB preflight combined-mode gate. These required gates remain unrun and held until clean transport handback; skips cannot qualify them.

Prefill schedule reporting follows Generator's existing canonical late alignment, including padded query rows and masked key columns. An odd4095 override from200000 now reports the actual64-row dispatch instead of nominal127. This does not alter Generator arithmetic, published context limits, scheduling bound or optimization defaults. The V193 shared build subsequently passes the added Swift/CLI refusal and schedule assertions; V189 cannot certify subsequently changed source.


**OPT16/29 V193 — preserve common-prefix reuse when read scopes are combined.** [[sources/runs/2026/09/2026-09-06-optimization-scope-checkpoint-interaction]] records the source counterexample and its source-only correction; the subsequent V193 build and pure checks pass. Cold1280-token input with256-row compute and1024-row read groups commits first at1024, stepping past the configured256 reusable checkpoint. Optional read groups now stop at that existing compute boundary only when retention is enabled with a positive budget; compute passes are never divided/rebatched. Reused/interior/invalid boundaries and disabled/zero-budget cache objects retain the old grouping. Engine always passes a cache object, so non-nil alone was correctly rejected during review.

Seven added T0 assertions pass in V193. The cold combined-scope lifecycle/state/divergent-followup test and disabled-cache native cases remain unrun. V193 executed and stopped on its first plain-governor failure; V192 never ran. The existing V170 cache-off serving draft retains its mechanism and needs final source binding. Retained-prefix scope adoption additionally requires this new native interaction gate and a real-request resource/latency guard; source reasoning is not a measured benefit. All optimization defaults remain unchanged. Post-V191 claim/projection checks pass, with their captured output in this run.


**V193 — bounded batch stops on a real plain-governor feasibility gap.** [[sources/runs/2026/09/2026-09-06-optimization-plain-governor-feasibility-counterexample]] preserves the identified build and every raw report. Binary `8fdff8a06ade9a51aca4712ffb5a71ab0491b2bb3495aec8a47427f80c5ea5b9`, source archive `6ef88b2c68e838e64ee338011424ace9178be4a381076acac4e1a414595de592`, Metal `198488eb61359e953580a9c4530400feee1a06dd2f28a930a6ffa58aec66a597`. T0 passes33groups/22263assertions, context CLI116, context-serving835 and image-reuse76. Plain governor passes72/80: scope/prefill/decode/nonstream each fails the infeasible-admission and zero-read assertions. Startup Planner's ordinary-context advisory floor was being accepted as a feasible live plan; MTP's mandatory-head rejection hid that gap in its earlier separate proof. The fixture's eight refusal assertions remain unchanged. Governor-MTP, ngram and cache-bookkeeping were never launched in this batch. All overlap timing/capacity observations are excluded. The driver exited and explicitly returned a free lock at21:44UTC; no automatic follow-on slot exists.

**V194 — full physical feasibility at every live context.** [[sources/runs/2026/09/2026-09-06-optimization-live-governor-feasibility]] binds the reviewed two-file fix, exact preimages and32 added pure regression assertions. desiredPlan checks its complete resolved peak against working set, credited availability minus slack, and the total target. Startup advisories, loaded component identities, pressure and resize policy are preserved. Caps1/1024/32768/65536 cover plain/MTP, infeasible floor and feasible recovery. This source is unbuilt/untested; both live pressure variants and the existing pure governor catalogue must pass on the successor. All build/model/performance/capacity/storage work remains held for transport's explicit clean handback. No optimizer default or public release/version changed.


**OPT00/32/33 V195 — bind the final workloads and controls before execution.** [[sources/runs/2026/09/2026-09-06-optimization-final-matrix-workload-binding]] preserves the source gap, correction and21 passing focused Python tests. A same-build passing report in a distinct directory was insufficient to identify its workload. The lifetime prerequisite gate now requires a separately frozen contract binding the exact final build, all three native command/environment pairs, seven complete paired protocols, and both complete soak configurations. Changed prompts, targets, MTP, selected controls, work requirements, mode definitions or contract bytes are rejected. The full parsed contract is retained with the receipt; prior hash/source/coordinate/recomputed acceptance gates remain mandatory. The first test failure was error-precedence only and is preserved; the original missing-prerequisite message remains first. No native source, performance result or default changed. The contract itself remains unfrozen until final candidate decisions, and must precede the first final matrix cell. Final studies, soaks, calibration, defaults and full acceptance remain open.


**OPT33 V196 — exercise the combined portable deployment path.** [[sources/runs/2026/09/2026-09-06-optimization-portable-integration-gates]] binds two diagnostic-only files adding integrated-portable and integrated-portable-mtp. The full existing state/logit/image/sampling/continuation/cancellation/embedding/pin suite now has an entry through the actual unqualified-platform deployment selector: shared angles execute, while fused rotation dispatch must remain zero. The existing public diagnostic signature and ordinary candidate checks are preserved by a private shared implementation. These gates remain unbuilt/unrun. Production controls/defaults, memory accounting and timing estimates are unchanged; forcing fallback on this Mac cannot certify another chip or OS. Native fallback and the eventual actual-default build remain explicit final qualification work, not inferred from pure policy checks or explicitly enabled candidate runs.


**V197/198 — one aligned source for the next shared correctness build.** [[sources/runs/2026/09/2026-09-06-optimization-public-transport-source-alignment]] preserves exact preimage-checked import of public e498 DownloadHTTP/PinnedTransport/Version bytes and the unrun successor queue. The existing owner-approved v0.2.11 value is preserved; this task made no independent version or publication decision. All143 shared/context Source files now hash-identically. Transport retains its public docs/tools/brain reconciliation and installed-acceptance work; source equality is not yet a built executable identity.

V198 is prepared only: -j2/13GB build, T0/context CLI/sampler+governor/planner, corrected real plain/MTP governors first, then all combined prerequisite variants and V193's cold scope lifecycle. Stop first failure/no retries;55min work+1cleanup; incomplete queue cannot pass. Scope lifecycle uses640slots/1024readgroup/256compute/1280prompt/1output with conservative20.112GB reclaimable preflight and no10GB capacity claim. Native portable/long scope/P5/performance/capacity/full-acceptance/default-activation remain outside this batch. No heavy launch before transport's explicit handback. After successful return, the context task consumes the same actual frozen candidate for its remaining full-mode and P5 gates, preserving defaults.


**OPT32/33 V199/200 — qualify the actual deployed defaults and retain historical evidence.** [[sources/runs/2026/09/2026-09-06-optimization-activation-and-evidence-preservation]] preserves the four-file UNAPPLIED activation draft and its exact preimages. Automatic environment selection, embedding-row default and corresponding native default/explicit-disable assertions must be built into an isolated final candidate before freezing the V195 matrix and running final native/paired/soak/full acceptance. Promotion must preserve that exact tested source/build identity. The current shared defaults are unchanged, no activation build ran, and final candidate decisions/context P5 return still precede this work.

Evidence closure also found three Hermes0.2.8 raw records already absent at both tasks' initial dirty baseline, with no known relocation. Original HEAD bytes were restored at the same coordinates for client acceptance, context/gates and published-install evidence; all three known SHA-256s match after dbmd source-index repair. Original IDs/timestamps/body bytes are retained, with no reference or index staging/commit/publication. Validation has0errors and the existing historical log warning. These historical records do not qualify the new runtime. The37-item working inventory still covers OPT00–OPT36 exactly once; completeness of the inventory does not mark the remaining implementation/measurement gates complete.


**V198 — shared build passes and stops at an advisory-fixture counterexample.** [[sources/runs/2026/09/2026-09-06-optimization-advisory-fixture-counterexample]] preserves binary b5ee2cbab9117aeb5178594b72a3cfc8629ad714070365bdf1fd505b4cde1ed4/source093a7512b77cc9df972647ae72dbc183953a793a83bac95b15de968248010b6c and all raw reports. T0 passes32/33groups with20 failed assertions among22295; the32 new live exhaustion/recovery cases pass. No model group launches. Four legacy10GB whole-availability fixtures expected settled plans whose7,919,289,600/7,921,999,104byte peaks exceed their7,425,000,000byte physical budget. Actual frozen doctor independently refuses all four with insufficient_memory/exit2. This is an invalid settled-fixture assumption, not reason to weaken production feasibility. The89.710951-second attempt is closed, preserving every failed assertion.

**V201/202 — preserve refused advisories and add feasible near-floor coverage.** [[sources/runs/2026/09/2026-09-06-optimization-governor-advisory-fixture-correction]] preserves the single diagnostic-file correction and unrun successor. Original10/18/44GB rows remain; feasibility is classified from exact initial ledger and original physical/target budget independently of desiredPlan. All four original unphysical advisories must refuse live, settle to arena floor and remain unavailable after returning owned pool bytes. Added12GB rows preserve feasible near-floor settle/cooldown/recovery checks. Production guards, deadbands, context caps, numerical goldens and optimization defaults remain unchanged. V202 retains the19-group bounded queue, new exact source/build identity, stop first failure/no retries and prior resource ceilings. Transport explicitly returned the clean machine after installed-release acceptance; context consumes the successful frozen candidate next for remaining full-mode/P5 work. All final component/paired/soak/calibration/activation/acceptance gates remain open.


**OPT08/33 V203 — one cost family prepared without claiming new calibration.** [[sources/runs/2026/09/2026-09-06-optimization-planner-family-preparation]] preserves an UNAPPLIED three-file draft centralizing the existing exact fixed/MTP/vision/transient allowances, prefill ladder and coupled decode anchors. Public planning, integer admission ledgers and next-dispatch workspace guards will derive their costs from that one family. Existing arithmetic order and parameter values are preserved; no hypothetical memory saving is reassigned. A441-case public doctor comparison covers target sweeps, modes, availability, caps, retention and pass overrides; the helper syntax passes but no comparison or candidate build has run. Capture baseline immediately before the eventual family-only application, after context P5/public-cap source disposition. The final actual resource envelope and estimator decision remain required; this consolidation is not empirical recalibration.


**V202 — complete shared correctness queue passes.** [[sources/runs/2026/09/2026-09-06-optimization-merged-correctness-v202]] preserves the full19-group queue and every raw report under binary `b64b07cff525003546694e8a129d7996a11987fb9b5350b47e0a882b9c848daf`, source archive `38707bb9cd38ddefe5a793451b55da56012dac1e03332b129d88577831622664`, Metal `198488eb61359e953580a9c4530400feee1a06dd2f28a930a6ffa58aec66a597`. T0 passes33groups/22346assertions, contextCLI116, sampler/governor17 and planner64. Actual plain/MTP pressure boundaries each pass80, integrated242/MTP256, correctedMTP-work284, complete-prompt273/MTP281, prefix-retention172/MTP198, read-failure-serving134, context-serving835, image-reuse76, ngram1160, cache-bookkeeping1163 and combined scope-lifecycle832 all pass. V194 physical feasibility, V201 refused-advisory matrix, V189 sampled reference fallback and V193 cold-scope checkpoint interaction are now verified together. No goldens, numerical tolerances, public caps or optimization defaults changed.

The batch completes in1309.274244seconds under its original3360-second bound. All143 source and7 driver hashes are unchanged; explicit23:15:57UTC return audit confirms a free model lock and no compiler/native processes. Exact candidate and machine interval are returned to context for full resource/client/P5 work. These diagnostics establish correctness only, not10GB scope capacity or timing improvement. Portable variants, larger scope family/MTPvision, optional component/serving decisions, final7pairedmatrix, two soaks, coherent planner resource validation, actual-default candidate and delivery acceptance remain open.


**OPT10 V204/206 — preserve a component input-delivery failure.** [[sources/runs/2026/09/2026-09-06-optimization-indexer-input-delivery]] records the exact181.357014-second synthetic-only interval, including180seconds of quiet, unchanged V202 source anchor and all raw artifacts. MLX0.31.1 rejected a NumPy float32 scalar nested in the denominator initializer list; zero score comparisons and zero timing cells ran. This is input delivery evidence, not a numerical/selection failure or measured speed result. Model lock/native/compiler return was clean at23:40:55UTC; the context task owns the machine again.

V206 converts that already-computed float32 scalar to an exactly representing Python float and explicitly constructs mx.float32. The sqrt, denominator value, shader, score oracle, selected-mask contract, shapes, repetitions and all gates are unchanged. The one-line owned Python probe correction and V207840s-child/60s-cleanup successor pass syntax checking only. V207 remains unrun and requires a new explicit synthetic-only interval; no automatic retry or replacement data exists. Full vision client acceptance concurrently exposed four honest10GB image-workspace refusals; context is deriving an explicit sufficient diagnostic budget from the unchanged fixtures. The full zero-swap resource gate remains unqualified after an8-page interruption and a separate48-page idle swap-in observation. No production guard, default or numerical tolerance is relaxed.


**OPT08/33 V208 — keep cost-family consolidation free of new dispatch machinery.** [[sources/runs/2026/09/2026-09-06-optimization-planner-family-refinement]] preserves the UNAPPLIED successor to V203. Direct switch dispatch is retained with scalar anchors, avoiding a new array/loop solely to centralize the current cost parameters. All existing values, original arithmetic order,441 comparison cases and comparison-helper bytes remain unchanged. V203 remains preserved; V208 is the eventual reviewed draft, still awaiting context P5/public source disposition, baseline capture, build, exact equality and final measured resource reconciliation. No current runtime or default changed.


**OPT27/28/32/33 V209 — full-image profile and acceptance false positives corrected.** [[sources/runs/2026/09/2026-09-06-optimization-vision-acceptance-profile]] preserves the reviewed four-file hunk import, protected143-source identity, exact profile2 receipt and nine passing provider-free checks. The original photographs pass25/25 at a scoped14.5GB/3072-token prefill reservation after20.5GB real reclaimable preflight. Ordinary10GB quality passes15/15; actual pinned Hermes tool/followup/title/image and Ollama image pass. These are correctness results only while global swap-in activity continues. The10GB image workspace refusals and the old emitted21/25 report remain preserved. A different-image answer now requires bothHTTP200 and a nonempty second answer; an empty error response can no longer appear to prove content sensitivity.

Full verify also increments FAIL when either the independent vision-reference environment is absent or the required full-image suite cannot launch with headroom. Actual shell-branch tests prove nonzero acceptance and zero model launch; six predicate cases preserve successful content differences while rejecting errors/empty/same-subject responses. Only matching human-doc hunks are imported, preserving unrelated context and public release material. No runtime guard/default/image fixture changes. Full no-skip acceptance, clean resource/capacity gates and final optimization measurements remain incomplete.

### V210–V213: typed context integration and OpenAI wire counterexample

[[sources/runs/2026/09/2026-09-07-optimization-typed-context-wire-counterexample]] preserves the independent V202 four-case Ollama reproduction,
reviewed V210 matching-hunk integration, first V211 build and all outputs.
The Ollama branches preserve error strings while adding code/details; the new
twenty-one assertions also cover all seven dialect variants before streaming
headers. CLI/API vision prose now reflects reservation inside the original
process memory target. No default, cap, allowance or success path changes.

V211 passes T0 33groups/22346assertions and CLI116, then fails2/856context-serving
assertions: OpenAI JSON and SSE expose the code in error.type but omit
error.code. All four Ollama structured-code assertions pass. The queue stops
before actual API74 and overflow4; neither ran. This is an additive error-schema
gap with unchanged tests, not numerical/resource evidence. Preserve this run;
the successor must add code alongside existing message/type and retain all856
assertions. Source143, drivers9 and queue-driver hashes remain exact; clean
drain confirms no native/compiler process, free ports and released model lock.
V212 is a preparation-only48Boolean control-disposition inventory. PlannerV208
and activationV199 remain unapplied; optional components, final matrix/soaks,
resource/P5 and delivery remain open. No inference speed or completion claim.

### V214–V215: typed context refusal and affected final-source correctness

[[sources/runs/2026/09/2026-09-07-optimization-typed-context-correctness]] preserves the independent four-case V202 counterexample,
reviewed matching-hunk import, one frozen successor build and first raw outputs.
Ollama chat/generate now retain the legacy HTTP400 error string while adding
structured context_length_exceeded code/details through the common refusal
path. V211 exposed two missing OpenAI error.code fields; its failed run remains
immutable in [[sources/runs/2026/09/2026-09-07-optimization-typed-context-wire-counterexample]].
V214 adds code alongside unchanged message/type in the local v1Chat helper. All seven native dialect variants also require refusal before streaming
headers and no success payload. CLI/API vision prose correctly describes the
reservation inside the process memory target; existing public history remains.

V215 passes all five affected groups: T0 33groups/22346assertions, CLI116,
context-serving856, actual API robustness74, actual typed overflow4. Binary
`9002c67e0094ebb14b22efed27168e513b37639a43b2a28bb8fb5a28e1c74cf9`, source archive
`dbdef8e89d59e853fabca4661eff67851fd097c8680beed9fab5fb465a0b819d`, Metal
`198488eb61359e953580a9c4530400feee1a06dd2f28a930a6ffa58aec66a597`. All143runtime source and9driver hashes remain
exact after the queue; the queue driver is unchanged, owned processes drained,
ports11641/11642 free and model lock released. This is correctness evidence;
no latency/capacity claim, resource/P5 trial or optimizer-default change follows.
The complete V202 nineteen-group result remains preserved on its own identity.
The planner-family V208 and activation V199 drafts remain unapplied. Remaining
optional components, combined matrix, soaks, final acceptance and OPT33 remain
open. V212 is only a temporary48Boolean control-disposition inventory; it has
changed no runtime control and is not a completion receipt.

### V216: swapping-machine disposition correction for V211/V215

The store contract requires `discarded: true` for a run made on a swapping
machine even when only correctness is claimed. The two newly created, still
unpublished V211/V215 sources now carry that metadata. Their complete body
bytes, native results and raw receipts are unchanged. V211 records12swap-ins
during native context serving; V215 records8during native context serving and
28during actual API robustness. Both have zero swap-outs in those intervals.
Preserve V211's854/856failure and V215's complete five-group correctness pass
as observed; neither supplies timing or capacity qualification. The previous
`discarded:false` metadata was a registration error, not a reason to rerun or
rescore either batch. Exact before/after metadata and unchanged body hashes:

```json
{
  "reason": "DB.md requires discarded:true for swapping-machine runs. Both sources were newly created, unpublished and Git-untracked. Only metadata changed; complete body/raw bytes preserved. Correctness remains observed; timing/capacity excluded.",
  "records": [
    {
      "path": "/Users/carlos/Projects/slotstream/db/sources/runs/2026/09/2026-09-07-optimization-typed-context-wire-counterexample.md",
      "before_sha256": "80a27fe44208113dacf637c68d65139b3dc9c3b425336fff88d8d2995ddc58b3",
      "body_sha256": "bdb9c9ca9d2b4a1be2c346a5586cd38cb060a508d4a288b35929c09f13459afb",
      "before_frontmatter": "\ntype: run\nid: 01m1wkt9mdetwr5eak39kcpb5y\ncreated: 2026-09-07T00:20:45.836587+00:00\nupdated: 2026-09-07T00:20:46.115446+00:00\nsummary: Typed context integration reveals OpenAI error.code gap\nbinary: /Users/carlos/Projects/slotstream/.build/optimization/typed-context-build-v211/candidate/slotstream\ncaptured_at: 2026-09-07\ncommand: python3 /tmp/slotstream-optimization-execution/run_typed_context_v211.py\ndiscarded: 'false'\nmachines: '[[records/machines/macbook-pro-m5-pro-48gb]]'\ntitle: Typed context integration reveals OpenAI error.code gap\ntool: Native context serving diagnostics\n",
      "vm_delta": {
        "context_swapins": 12,
        "swapouts": 0
      },
      "after_sha256": "e653fb8f747cabc1cb3f85da40c7cfcf8f884683a55888098cf7794255f099f9",
      "after_frontmatter": "\ntype: run\nid: 01m1wkt9mdetwr5eak39kcpb5y\ncreated: 2026-09-07T00:20:45.836587+00:00\nupdated: 2026-09-07T00:44:55.279011+00:00\nsummary: Typed context integration reveals OpenAI error.code gap\nbinary: /Users/carlos/Projects/slotstream/.build/optimization/typed-context-build-v211/candidate/slotstream\ncaptured_at: 2026-09-07\ncommand: python3 /tmp/slotstream-optimization-execution/run_typed_context_v211.py\ndiscarded: 'true'\nmachines: '[[records/machines/macbook-pro-m5-pro-48gb]]'\ntitle: Typed context integration reveals OpenAI error.code gap\ntool: Native context serving diagnostics\n",
      "body_unchanged": true
    },
    {
      "path": "/Users/carlos/Projects/slotstream/db/sources/runs/2026/09/2026-09-07-optimization-typed-context-correctness.md",
      "before_sha256": "ae87d19d6e6270277cb9fb56c034c83d10a6ad20a7b11c64d322f57932e7ccf6",
      "body_sha256": "0cfa9a33b8a9c81d8f594b539d3f262b3f80006794c177c204a7f0965d05f15c",
      "before_frontmatter": "\ntype: run\nid: 01m1wm7f5pg26wpsywb9rrsfh6\ncreated: 2026-09-07T00:27:57.493594+00:00\nupdated: 2026-09-07T00:27:57.794975+00:00\nsummary: Typed context refusal and shared affected correctness V215\nbinary: /Users/carlos/Projects/slotstream/.build/optimization/typed-context-build-v215/candidate/slotstream\ncaptured_at: 2026-09-07T00:27:57.477369+00:00\ncommand: python3 /tmp/slotstream-optimization-execution/run_typed_context_v215.py\ndiscarded: 'false'\nmachines: '[[records/machines/macbook-pro-m5-pro-48gb]]'\ntitle: Typed context refusal and shared affected correctness V215\ntool: Native diagnostics and live HTTP probes\n",
      "vm_delta": {
        "context_swapins": 8,
        "api_swapins": 28,
        "swapouts": 0
      },
      "after_sha256": "78818971df80327ddd8b1c889514451c0e55dfaedc9c9eaa0227a2862ad8c7d5",
      "after_frontmatter": "\ntype: run\nid: 01m1wm7f5pg26wpsywb9rrsfh6\ncreated: 2026-09-07T00:27:57.493594+00:00\nupdated: 2026-09-07T00:44:55.316444+00:00\nsummary: Typed context refusal and shared affected correctness V215\nbinary: /Users/carlos/Projects/slotstream/.build/optimization/typed-context-build-v215/candidate/slotstream\ncaptured_at: 2026-09-07T00:27:57.477369+00:00\ncommand: python3 /tmp/slotstream-optimization-execution/run_typed_context_v215.py\ndiscarded: 'true'\nmachines: '[[records/machines/macbook-pro-m5-pro-48gb]]'\ntitle: Typed context refusal and shared affected correctness V215\ntool: Native diagnostics and live HTTP probes\n",
      "body_unchanged": true
    }
  ]
}
```

### V217–V218: final actual-default guard and release-script acceptance

[[sources/runs/2026/09/2026-09-07-optimization-default-and-release-acceptance-review]] preserves the reviewed source, exact first shell
counterexample and all pure outputs. V217 adds an unapplied eighth final
comparison; retain all original seven. The actual-default arm has no
optimization/embedding override and prefix caching stays enabled. Cold
short17/output1 first-job plus distinct prose440/output1 guard minimal-reply
retention overhead with max5percent median regression, min5clean among16fixed
pairs, no replacements, exact outputs and unchanged resource/thermal gates.
Its complete workload must join the mandatory pre-run contract before any
final study. All13pure receipt tests pass, including explicit-override,
disabled-retention and larger-reply false-claim rejection. Source-resolved
workspace preflight must be reviewed after optional default choices. The
harness/protocol draft remains unapplied and the actual benchmark is UNRUN.

V218 fixes the installed release script's typed-doctor pipelines: with
pipefail they incorrectly rejected correct exit2. The tested correction
requires exact exit2 plus insufficient_memory, rejecting0/1/3/termination
and unrelated errors. Over-length construction uses the real discovered
window; failed discovery counts as failure and sends no request. The
subsequent400 must also have context_length_exceeded. All20actual-shell
fragment cases pass at32K/65K/262K and invalid boundaries. Tools/e2e_release.sh
now hashes `05c181cfaa4e23855a949e57f6d7d1f998cc1ece5e5a26c2051b2ca7451a5824`; no runtime source changed.
Installed/model e2e acceptance remains UNRUN. This record is discarded for
timing/capacity use; exact correctness observations remain preserved. V208
planner and V199activation drafts remain unapplied; V207component held by
the context task's interval. Whole-program implementation remains in progress.

### V219: eighth actual-default qualification gate applied

[[sources/runs/2026/09/2026-09-07-optimization-actual-default-final-gate]] preserves the applied V217 validator/test patch and13passing
pure tests. Both final soaks now require all8paired studies: the original7
plus the no-override actual-default minimal-reply guard. The new guard checks
cold short17/output1 first-job and distinct prose440/output1 unique retention,
with min5clean among16fixed pairs and max5percent median regression in both
intervals. Explicit overrides, disabled retention or larger replies cannot
borrow the actual-default claim even if declared in a newly frozen contract.
No original gate is removed or weakened. All143runtime source hashes still
match V215; no model/build/GPU/default/cap/budget change. The actual8study
matrix and bothsoaks are UNRUN. Bind final chosen source/native prerequisites
and all8complete workload/control objects before the first final cell; review
source-resolved workspace preflight after optional defaults are chosen.
V208planner and V199activation remain unapplied. Final qualification, resource
calibration and artifact/control closure remain open.


### V220: same metadata correction for preserved V202 correctness

The same audit found167swap-ins across10native intervals in V202, all with
zero swap-outs. Its still-unpublished source now carries discarded:true, as
required by DB.md. The full body hash and every raw/check result are unchanged.
All19groups still pass as observed; this remains correctness-only evidence,
never timing/capacity qualification. No model was rerun and no criterion
changed. Exact disposition receipt:

```json
{
  "source": "/Users/carlos/Projects/slotstream/db/sources/runs/2026/09/2026-09-06-optimization-merged-correctness-v202.md",
  "before_sha256": "6f5ac38e5b6f00548c40390164c760365128d7851d9e24a3050fdec35bae20f9",
  "after_sha256": "837b6c2f787a5bba64712d2dc93433ed0b59773cdc4e8351c7019b7371e2ac60",
  "body_sha256": "6649eee4b7afb034022e2a65f574a976659df3ec3b13b96ee87c615bd28acc79",
  "body_unchanged": true,
  "correctness": true,
  "groups": 19,
  "vm_active_intervals": [
    {
      "group": "governor-boundary-mtp",
      "swapins": 8,
      "swapouts": 0
    },
    {
      "group": "integrated",
      "swapins": 32,
      "swapouts": 0
    },
    {
      "group": "integrated-mtp",
      "swapins": 40,
      "swapouts": 0
    },
    {
      "group": "mtp-work-integrated",
      "swapins": 11,
      "swapouts": 0
    },
    {
      "group": "complete-prompt-mtp",
      "swapins": 12,
      "swapouts": 0
    },
    {
      "group": "prefix-retention",
      "swapins": 8,
      "swapouts": 0
    },
    {
      "group": "prefix-retention-mtp",
      "swapins": 20,
      "swapouts": 0
    },
    {
      "group": "read-failure-serving",
      "swapins": 4,
      "swapouts": 0
    },
    {
      "group": "context-serving",
      "swapins": 12,
      "swapouts": 0
    },
    {
      "group": "scope-integrated-lifecycle",
      "swapins": 20,
      "swapouts": 0
    }
  ],
  "reason": "Same DB.md rule as V216: preserved swapping-machine correctness queue requires discarded:true. Source was still unpublished/untracked. No raw bytes, assertions or actual pass results changed; timing/capacity excluded."
}
```

### V207 completed: reject general prefill score fusion; one-query screen only

[[sources/runs/2026/09/2026-09-07-optimization-indexer-score-screen]] preserves the first actual execution after V206's initializer
correction. All25numerical/visible-mask cases pass. Each shape completes
5swap-clean/nominal timed pairs under the fixed memory caps, without
replacement. One query/2048blocks reports34.095783percent median paired
component reduction,5/5positive. The256query prefill shapes are3.781782times
and6.130565times as slow at2048/8192blocks and fail their benefit gates.
Max cell MLX/RSS are46661664/466321408bytes. Whole wrapper183.781797seconds
includes180secondsquiet,761swap-ins and0swap-outs; discarded:true preserves
the distinction between local timed-cell observations and an unqualified
whole-machine/inference result. No production dispatch was added.

This prototype is rejected for general prefill. A one-query successor remains
a conditional, prospective followup, requiring actual-runtime score-cost
attribution, trained-state/routes and serving qualification. Its special
input selection must include more than512visible blocks: current8/513special
fixtures never reach that cutoff. Preserve V207 unchanged; no rescoring or
inference/default speed claim. Drivers remain exact, model lock released and
all owned processes drained within the explicit15-minute interval. Other
optional components, final8matrix/soaks, resource/P5 and delivery remain open.

### V221–V223: resource prerequisite and final lifetime harness closure

[[sources/runs/2026/09/2026-09-07-optimization-cache-prelaunch-and-soak-harness]] preserves V205's first outer-launch refusal. The unchanged
12 GB reclaimable preflight observed only 10.72 GB and stopped before creating
a child process or study directory. Zero cells ran; the 0.004829541-second
wrapper is neither a cache-performance failure nor qualification. The protocol
SHA remains `5c5e74e4da45811cce9acff02ae25acd8adb5b20422d481ed1d512cde60ff61b`.
The machine interval was returned immediately. No automatic retry, memory-gate
relaxation, unrelated process termination or production activation occurred.
Any first actual study execution requires observed sufficient headroom and a
new coordinated interval, preserving this prelaunch receipt separately.

V222 passes all 11 existing pure soak-driver tests on V219's applied harness,
including 60-request completion, per-position coverage, memory/growth/charge
limits, failure preservation, deadline/cancellation and server cleanup. Together
with V219's 13 prerequisite tests this closes the affected pure harness checks;
the eight paired studies and two real soaks remain unrun. No model or GPU is
used by these tests. The remaining actual work is still optional-component and
serving qualification, context/resource P5, coherent planner calibration,
actual-default candidate qualification and full delivery acceptance.



### V224: current source and final execution reconciliation

[[sources/runs/2026/09/2026-09-07-optimization-current-default-source-review]] preserves the source review and all37item dispositions.
No current candidate default enables the three experimental resource expanders
(read scope, layer workspace, router cache), so the present preflight calculation
requires no additional allowance for this family. Re-review before contract
freeze if optional choices change it. This is not a native/default qualification.
V199 remains unapplied; its old final7 prose cannot omit V219's eighth study.
Generated llms-full.txt now reflects the previously recorded vision reservation
and full-image acceptance-profile corrections. No hand-authored public claim,
runtime source, numeric constant, frozen study or optimizer default changed.


### V225–V226: prevent static acceptance of the wrong executable

[[sources/runs/2026/09/2026-09-07-optimization-static-candidate-selection]] preserves the original six failing shell-selection
contracts and the corrected six passing fixture cases. Static acceptance now
uses SLOTSTREAM_TEST_BINARY, then legacy BIN, then the release default; both
names are forwarded consistently, direct calls are quoted, and missing or
failing candidates never fall back to another executable. The durable test is
run by the static battery itself. All143runtime source hashes remain V215-exact;
the frozen cache/prefill/serving drivers are unchanged. Shell syntax passes.
This is acceptance-path correctness only: no actual native/static full battery,
model, GPU, build or performance cell ran, and no default or gate was relaxed.


### V227–V229: final correctness independent of timing exclusion

[[sources/runs/2026/09/2026-09-07-optimization-paired-correctness-independent-of-timing]] preserves a read-only55study/445comparison audit with
zero observed output mismatches, plus the first pure counterexamples and final
correction. No old result is rescored or reclassified. The prospective final
prerequisite now requires all32first/measured cells complete, the same captured
workload and outputs irrespective of timing eligibility, valid mode/chunk/pool
and equal-work metadata, and both first/measured absolute footprint ceilings.
All32warmup.json files per paired study require receipt digests and must match
the recorded first requests. Missing cells/preflight refusals leave the final
matrix incomplete; runtime failures cannot become ordinary discarded samples.
VM/power still determine timing eligibility under the unchanged minimum-pair and
latency bounds. All25prerequisite and11soak-driver pure tests pass on shared
source. All143runtime and all3frozen component/serving drivers remain exact.
Final8studies,2realsoaks, resource/P5 and delivery acceptance remain unrun; no
optimizer/default activation or new performance claim follows.

### V230–V231: coherent selected artifacts and durable static execution

[[sources/runs/2026/09/2026-09-07-optimization-selected-artifacts-and-static-suites]] preserves the reviewed five-file context packet import,
25passing focused fixture tests, then the three pre-change suite-execution
counterexamples and correction. Static acceptance now runs all nine existing
optimization suites:117tests pass, plus9static entry-point tests. Missing or
failed suites prevent native checks. Binary/Metal byte identity is enforced in
isolated installer fixtures; paths with spaces/quotes and same-version wrong
artifacts are covered. No real model, compiler or user installation ran.

The context owner's actual planner evidence remains58/64under insufficient
headroom: intermediate304033normal and finalf869quoted. It is not a final
normal-path or64/64receipt. All143runtime sources and all3frozen study drivers
remain V215-exact. Full shared context evidence now lives in this same db and is
linked through its owned context plan; optimization selection and execution
remain governed here. Final8studies,2soaks, resource/P5, optional serving and
full delivery acceptance stay open; defaults remain off.

### V232: preserve the remaining context client fixtures

[[sources/runs/2026/09/2026-09-07-configurable-context-shared-client-fixtures]] is imported byte-for-byte from the context owner.
The shared consumer, Hermes and gateway Tools after-images match its receipt.
The owner records seven new syntax/bounds checks only; earlier actual consumer
and32K/65Kclient runs retain their original identities. No new native build,
model or live client call is implied. This supplies the missing shared fixture
paths without closing final candidate consumer/client or installed acceptance.

### V233: full-verification selected paths and current execution disposition

[[sources/runs/2026/09/2026-09-07-optimization-verification-selected-paths]] preserves the real-shell call-site tests and all37current
OPT dispositions. The final fixture reproduces5failures on the original script
and passes9/9after correction; static-entry9/9also pass. A separate initial
macOS path-canonicalization fixture error is preserved explicitly. Model checks
retain their13GB preflight; selected paths and template/vision arguments remain
literal even with spaces/quotes. The static battery invokes the new fixture.
Mandatory vision, context and installer gates are preserved. All143runtime
sources and3frozen drivers remain exact. Full native/model/installed acceptance
has not run, and optimizer defaults remain off. API/sampler edge closure is a
separate pending context-owner handoff, not silently counted here.

### V234: reject erroneous completion metadata in final receipts

[[sources/runs/2026/09/2026-09-07-optimization-final-completion-metadata]] preserves three synthetic receipt counterexamples and the
corrected30/30prerequisite plus11/11soakdriver results. First and measured
responses must be error-free, their counters must match captured prompt/output
IDs, and computed plus reused tokens must account for the whole prompt. No
runtime, frozen driver, original benchmark or acceptance threshold changes.
The static nine-suite set now contains122tests; affected suites were rerun,
unchanged suites retain V231 results. Final8studies/2soaks, real model/resource
and delivery qualification remain open; no new performance/default claim.

### V235: mandatory sampler test and final shared acceptance handoff

[[sources/runs/2026/09/2026-09-07-optimization-mandatory-sampler-and-final-handoff]] records8/8sampler fixture and9/9static-entry fixture passes,
three shell syntax passes, exact owner after-images and the byte-exact immutable
sampler/API evidence import. The new sampler fixture is now mandatory. Owner
V215native17/17sampler/NumPy/governor results remain separately identified; no
new full API or model run follows. All143runtime and3frozen drivers stay exact.
No reviewed Tools/client/source handoff remains pending. The source includes
all37current dispositions and the unchanged remaining execution order.

The program is not complete: remaining component/serving and resource/context
qualification, planner-family parity/calibration, actual-default candidate,
eight paired studies, two soaks, portable/consumer and full installed/delivery
acceptance remain open. Insufficient headroom blocks model execution; no
resource gate is weakened and no optimizer default is enabled to manufacture
completion. Continue from the preserved next unrun protocol after its actual
resource prerequisite and coordinated machine interval are available.


### V236–V237: restored environment and isolated planner implementation

[[sources/runs/2026/09/2026-09-07-optimization-resource-readiness-and-restoration]]
preserves the resource snapshots, restored optional VM configuration and empty
container list, source143/frozen-driver audit and zero actual cache cells. The
active browser was kept open. Stopping the optional VM did not meet the next
12GB component prerequisite, and the original environment was restored. Private
application/session inventories are excluded from this public evidence.

[[sources/runs/2026/09/2026-09-07-optimization-typed-planner-baseline-and-isolation]]
preserves the original V208 comparator, prospective type-preserving successor,
seven focused fixtures and every one of441 actual V215 doctor responses. The
original Python equality operator produces six failed assertions; the new
comparator passes7/7, rejects non-finite JSON and retains bool/int/float/signed
zero distinctions while ignoring key order. This is a synthetic validation
counterexample, not a native planner failure. The bounded metadata-only baseline
finishes in30.910408583seconds without Engine construction or model inference.

The V208 three-file consolidation is applied to a reconstructible isolated
V215 source tree after that baseline, with exact preimages and no additional
source differences. The scalar cost family passes Swift typechecking. Actual
build, typed441 comparison, T0/CLI/context/planner and measured resource gates
remain required. The unchanged13GB build preflight is unmet at9.769910272GB;
no make or SwiftPM build is launched. Original V215 and all P5 protocols remain
byte-exact and unconsumed. Shared source/default activation remains pending.

This advances implementation and captures the prospective reference; it does
not complete the optimization program. Optional component/serving studies,
resource/context qualification, final candidate selection and exact build,
eight paired studies, two soaks, portable/client/full installed acceptance and
planner/calibration/delivery reconciliation remain open under their original
gates. No clean-machine claim or inference speed/memory gain follows.


### V238–V240: serial compiler and first-cache refusal evidence

[[sources/runs/2026/09/2026-09-07-optimization-serial-compiler-guard-failures]]
preserves the original cleanup-masked build failure, synthetic primary-error
counterexamples, corrected11test guard and the separately observed3GBRSS stop.
No compiler failure is converted into an inference result; V238's primary
trigger remains unknown. [[sources/runs/2026/09/2026-09-07-optimization-full-cache-first-cell-refusal]]
preserves V239's zero native cells after settling and insufficient headroom.
Neither failure weakens the prospective resource contract.

### V241–V244: a real planner build, exact parity and incomplete cache study

[[sources/runs/2026/09/2026-09-07-optimization-cached-planner-build-and-typed-parity]]
records159.103749542seconds for the whole successful temporary cached build,
publication and exact restoration, plus all441typed comparison rows. The build
uses existing third-party objects and is not a fresh-build or inference speed
claim. [[sources/runs/2026/09/2026-09-07-optimization-full-cache-two-cells-and-refusal]]
then records two exact-output native cache cells and the third-cell headroom
refusal. Reference timing is VM-excluded, so no complete clean pair exists and
no component qualifies. Original protocols and incomplete attempts remain intact.

### V245–V248: release completion and actual model-free planner checks

[[sources/runs/2026/09/2026-09-07-optimization-serial-and-installed-gate-integration]]
records the mandatory serial suite and the first installed-response correction.
[[sources/runs/2026/09/2026-09-07-configurable-context-openai-release-completion]]
preserves the next nine OpenAI false passes and all28passing successor fixtures.
These are process/response fixtures, never actual installed acceptance.
[[sources/runs/2026/09/2026-09-07-optimization-planner-metadata-and-context-boundary]]
preserves actual T022346/33groups and CLI116on the exact V241candidate, the
V24613GBpreflight refusal, source/protocol context-boundary review and one-file
executable-mode restoration. The completed metadata checks do not spend a
memory credit or qualify a new context size.

### V249–V250: context correctness, failed API acceptance and scope correction

[[sources/runs/2026/09/2026-09-07-optimization-planner-context-pass-and-api-pressure-failure]]
records planner64and context856passes, followed by API38pass/36fail during
reported memory-pressure503s and new swapouts. Typed overflow is unrun under
stop-on-failure. Both source and candidate remain exact; no acceptance pass or
regression absence is certified from this failed interval. The seeded-output
predicate's matching-error false pass is preserved for correction.

The linked source/protocol boundary review separates the unfinished full262K
context-expansion campaign from unchanged-cap optimization. Its original
existing-range predecessor settings are not silently imposed as new optimization
release prerequisites. Required existing-cap safety, behavior, resource and
client/install/rollback acceptance remain open and mandatory. The unified
37OPTitems and all N6prefill work are retained. The optimization program is
still not complete; no source/default activation or combined speed/memory
claim follows from these steps.


### V251–V254: completed API output and observed concurrency are mandatory

[[sources/runs/2026/09/2026-09-07-optimization-api-generation-completion-validation]]
preserves the original 22 completion false passes, corrected producer/content/
terminal checks and seven passing fixture groups. The adjacent real concurrency
block had six more false passes, including failed and already-finished work.
[[sources/runs/2026/09/2026-09-07-optimization-api-concurrency-validation]] requires
a successful complete stream with content observations spanning all metadata
probes; the full nine-group API fixture suite passes. These are acceptance-script
corrections, not proof that the actual model API currently passes.

[[sources/runs/2026/09/2026-09-07-optimization-os-pressure-readiness]] adds exact
normal/warning/critical observations without changing OS state or applications;
12 guard, four readiness and 13 static tests pass at that revision.
[[sources/runs/2026/09/2026-09-07-optimization-api-pressure-preflight-refusal]]
preserves V253's unlaunched 13 GB preparation refusal. Its old driver identity
is retained; later code changes do not silently update that protocol.

### V255–V257: build the automatic candidate and verify its unchanged math

[[sources/runs/2026/09/2026-09-07-optimization-combined-default-candidate-build]]
preserves the prospective seven-file candidate lease and exact complete
restoration in 158.529884583 seconds. This uses the existing dependency cache;
it is not a fresh-build speed benchmark. The separately frozen executable and
source archive are named in the current boundary. No optional scope, cache,
tail, image or selector control has joined the selected family, and original
context caps and planner coefficients remain unchanged.

[[sources/runs/2026/09/2026-09-07-optimization-combined-default-metadata-and-runtime]]
contains all 22,346 T0 assertions, 116 CLI checks, 441 exact typed planner cases
and 104 weights-free runtime assertions on that candidate. Complete JSON types,
explicit opt-outs and platform fallback policy remain verified. New automatic
model execution, empirical recalibration, physical capacity, actual hardware
portability, full integration and default adoption remain unqualified.

### V258–V261: preserve the failed guard; separate functional and resource evidence

[[sources/runs/2026/09/2026-09-07-optimization-correctness-memory-policy]] preserves
V258's stop after 3.706682834 seconds and 64 new global swapout pages, before any
API assertion. Sampled owned RSS never exceeds 144,261,120 bytes and reclaimable
never falls below 13,406,191,616 bytes; neither observation establishes who
caused the global swap. Descendants are drained and the candidate remains exact.
V259's 14 guard fixtures make an explicit correctness-only VM policy effective
while keeping original build defaults, actual memory bounds and every frozen
performance/resource criterion. V260 retains normal pressure and 13/3/10 GB
startup/live/RSS bounds, records VM activity, and expressly cannot qualify
performance or capacity. It refuses 11.32 GB before any child. Neither attempt
closes actual API74 or typed-overflow4; V249 remains failed historical evidence.

[[sources/runs/2026/09/2026-09-07-optimization-combined-default-sampler-installer]]
records a real V255 pass through 17 sampler/NumPy/governor checks (26 governor
branches) and the actual installer using a local temporary release. Exact
binary/Metal/version bytes and filesystem activation/failure transitions pass.
No user installation changes, model inference, publication or final installed
client/resource acceptance occurred.

### V262–V263: external consumer status is checked; real compile still needs headroom

[[sources/runs/2026/09/2026-09-07-optimization-consumer-build-contract]] captures
one demonstrated false pass after a failed Swift build left a runnable consumer.
Compiler success is now required; the original two-worker default is retained
with a validated 1–8 override. The consumer also compiles the public optimization
function value and checks explicit reference construction/opt-out behavior.
Four consumer and 15 static fixture tests pass. V263 verifies and extracts every
one of V255's 149 source inputs for a real one-worker external SwiftPM build,
then refuses at 8.62 GB against its unchanged 9.5 GB build requirement. No
compiler or model launches; native standalone-library acceptance remains open.


### V265–V271: executable final campaign, source compatibility and guarded build stop

[[sources/runs/2026/09/2026-09-07-optimization-combined-bounded-components]]
records actual V255 read, socket, prefix and RoPE correctness. All363 assertions
pass; fast-group sampling limitations remain explicit. No model is loaded.

[[sources/runs/2026/09/2026-09-07-optimization-campaign-and-active-throughput-reporting]]
preserves new campaign/reporting code, original workload origins, complete
contract preparation, positive/adversarial fixtures and the first static-fixture
failure. The runner does not consume a stage on prelaunch readiness refusal,
does not repeat attempted stages, and requires raw exact-build evidence before
advancing. The native-only intermediate boundary cannot bypass the eight paired
prerequisites at the actual soak entry point. Reporter output cannot create a
performance claim for an unqualified workload. Actual V269 status contains no
measurements and all thirteen stages remain unrun.

[[sources/runs/2026/09/2026-09-07-optimization-planner-device-extraction-rebase]]
contains the exact planner seam merge, V267's unlaunched competing-compiler
refusal and V270's real964167-assertion policy pass. Device observations are
inert only in the proxy; no measured coefficient or public context limit changes.

[[sources/runs/2026/09/2026-09-07-optimization-current-source-external-consumer]]
closes real source-consumer compatibility for the proposed combined Swift/native
closure plus current device extraction. The original serial guard passes with
60swapins and no new swapouts. This qualification is not the final executable
build and cannot supply model/API/performance evidence.

[[sources/runs/2026/09/2026-09-07-optimization-current-source-build-resource-stop]]
preserves V271's failed release build. Its guard catches the real6GB live-floor
crossing and drains the owned processes, then restores the complete lease.
Only a fresh successful current-source build can replace this boundary. There
is no new combined speedup, steady active-token-rate or memory-saving percentage.


### V273–V279: current-source build, actual API/native passes, and checkpoint counterexample

[[sources/runs/2026/09/2026-09-07-optimization-current-source-candidate-build]]
contains the successful V273 serial release lease with exact full restoration.
[[sources/runs/2026/09/2026-09-07-optimization-current-candidate-metadata-and-api]]
contains actual T0/CLI/typed-planner/runtime checks and API74 plus typed-overflow4.
The API run records VM activity and is correctness evidence only.

[[sources/runs/2026/09/2026-09-07-optimization-native-comparison-pressure-stop]]
keeps V276's incomplete first native attempt. The precise pressure level was
not retained, so it is not retrospectively classified as warning or critical.
[[sources/runs/2026/09/2026-09-07-optimization-pressure-failure-cause-reporting]]
records V277's narrow cause-reporting correction and four passing tests.

[[sources/runs/2026/09/2026-09-07-optimization-native-pass-and-read-failure-checkpoint-counterexample]]
preserves V278's exact242ordinary and256MTP passes,134read-failure assertions
with14failed empty-cache expectations, and the566.087331125second complete
coordinated interval. All owned jobs drain and the interval ends before handback.
The individual native passes do not turn the entire campaign into a pass.
V279's test-only successor must prove exact independent checkpoint state/logits
and same HTTP retry with checkpointing both enabled and disabled. No generation,
cache, HTTP or planner runtime rule changes merely to satisfy the old assertion.


### V279–V284: exact checkpoint recovery and complete same-build native prerequisites

[[sources/runs/2026/09/2026-09-07-optimization-committed-checkpoint-read-failure-recovery]]
preserves the original diagnostic, expanded source and actual 522-assertion pass.
Its independent cold successful request defines the checkpoint state/logits and
output oracle before failure. Both cache modes and queue modes remain covered;
no stale failure is waived without a stronger exact-state and retry check.

[[sources/runs/2026/09/2026-09-07-optimization-final-candidate-native-and-build]]
contains V280's exact guarded build/restoration and V281's ordinary 242/MTP 256
passes. V284 completes the previously unstarted recovery stage in the same
campaign. The old failed campaign remains immutable; no partial or false pass
was substituted. All three native prerequisites now pass on V280.

[[sources/runs/2026/09/2026-09-07-optimization-current-candidate-complete-static]]
contains actual T0 22,346, CLI 116, the complete static gate and bounded 363
component assertions on V280. Small transport-fixture compilation is identified
explicitly; no model or performance claim comes from those fixtures. All final
paired studies, soaks and remaining full resource/client acceptance stay open.


### V283, V286–V290: real full verification, portable shell acceptance and MTP recovery

[[sources/runs/2026/09/2026-09-07-optimization-full-verification-counterexample]]
preserves the whole exact V280 verification, all pinned-weight hashes, 22 passes,
three failures, original tool closure and actual per-gate evidence. Its full-suite
failure remains historical evidence; independent successful gates retain their
precise scope.

[[sources/runs/2026/09/2026-09-07-optimization-governor-and-mtp-recovery]]
preserves the two real shell parser failures, their 15-test correction, actual
successful governor drill and complete MTP-plus-original-image pass below 12 GB
without any VM activity. It also preserves the V287 omitted-constant orchestration
error, V287/V289 swap-excluded attempts and V290/consumer pre-launch refusals.
All required workloads, limits and no-swap criteria remain unchanged. The long
resource gate, fresh full-suite pass, portable/client/consumer/delivery and all
paired/soak qualification remain open; no final speed percentage or activation.


### V288, V293, V295–V297: first combined paired result and current external consumer

[[sources/runs/2026/09/2026-09-07-optimization-first-combined-pair-and-throughput-reporting]]
preserves every fixed cell, raw request/response, startup row and acceptance
receipt for V293's first complete combined study. Completion improves 12.9725%
and sampled peak decreases 6.2374% on its 15 clean one-token pairs. First protocol
output is 3.6128% later; a whitespace-only token supplies neither visible preview
nor active-rate evidence. V296 preserves the original inflated cleanup-only
throughput report and fixes its cause, with all nine reporting checks passing.
No raw row, eligibility criterion, frozen driver or runtime/default is changed.

[[sources/runs/2026/09/2026-09-07-optimization-external-consumer-and-long-preflight]]
records V288's actual separate Swift package build and 22 diagnostic assertions
against the exact 150-file source archive. It also preserves V295's inherited
full-suite preflight error and V297's correction to the original 13 GB long-run
headroom requirement followed by its quiet-VM refusal before model load. Long
resource and fresh full verification remain open. Four of thirteen final campaign
stages pass; seven paired stages and two lifetime soaks remain unrun. This is
progress toward qualification, not full implementation acceptance or activation.


### V291–V292, V298–V301: current prose, client and portable acceptance

[[sources/runs/2026/09/2026-09-07-optimization-combined-prose-serving-pass]]
preserves the second complete fixed32-cell study:15 clean request pairs,14 startup
pairs and exact16-token outputs. Its measured gains are3.7195% completion,1.8134%
visible text,1.4569% active emission and7.3960% sampled process peak, with all frozen
criteria retained. Six paired studies and two lifetime soaks remain unrun.

[[sources/runs/2026/09/2026-09-07-optimization-current-clients-and-portable-pass]]
records real Ollama/AI SDK at32K/65K, actual Hermes tool/follow-up/title at65K,
and complete portable ordinary242/MTP256 assertions on unchanged V280. It also
preserves the prospective portable scheduling adjustment and the unrun V299 client
extensions/V300 long-resource preparation, including fresh output-path correction.

[[sources/runs/2026/09/2026-09-07-optimization-retained-window-sync-component]]
records the48 clean component pairs and12 edge cases for grouped evaluation.
Native Swift and whole-model qualification remain pending; no inference source
is changed. [[sources/runs/2026/09/2026-09-07-optimization-sync-probe-memory-label-correction]]
corrects its unsupported active-plus-cache wording without modifying the original
evidence. No isolated component percentage is added to a serving result.


### V302–V306: native synchronization result and actual runtime implementation

[[sources/runs/2026/09/2026-09-07-optimization-native-retained-window-sync]]
preserves the initial compile failure and corrected native component: 48 clean
fixed pairs, 12 byte-exact edge cases and roughly 46–48% less component time.
[[sources/runs/2026/09/2026-09-07-optimization-joined-state-eval-implementation]]
contains both source images, implementation hash, exact new build/restoration and
242 ordinary/256 MTP native passes. It explicitly records recovery and the extension
wrapper as unrun, all current-build gates still outstanding and the drained handback.
The earlier V280 sources and measurements remain unchanged and separately scoped.


### V307–V308: current-build checks, first paired result and lifetime contract repair

[[sources/runs/2026/09/2026-09-07-optimization-joined-eval-current-gates]] preserves
522 recovery, 22,346 T0, 116 CLI, complete static, 363 components, real clients,
78 API/context checks, and 242/256 portable assertions. It retains the failed
original long no-VM gate, correct long recall, uninvoked consumer, and drained
handback. [[sources/runs/2026/09/2026-09-07-optimization-joined-eval-short-request-pair]]
contains all 32 current-build paired cells, the original startup exclusion and
descriptive latency/memory report. It explicitly reports no one-token TPS or
visible-preview estimate. [[sources/runs/2026/09/2026-09-07-optimization-lifetime-mtp-vision-contract-correction]]
preserves the impossible unrun predecessor, initial helper-schema failure,
source-derived 12 GB successor, all 12 byte-identical wire requests and 16 negative
cases. No lifetime model run or new planner credit is claimed. Every OPT00–OPT36
item remains in the execution inventory, including the conditional dispositions.


### V309–V314: actual consumer proof, lifetime executor and full-verification failure handling

[[sources/runs/2026/09/2026-09-07-optimization-corrected-lifetime-composition]]
preserves the first uninvoked executor. Its minimal V312 successor, failed V310
consumer fixture, corrected V311 compilation/execution and actual public-planner
receipt are in [[sources/runs/2026/09/2026-09-07-optimization-public-consumer-lifetime-preflight]].
No runtime change or empirical memory credit results from the fixture correction.
[[sources/runs/2026/09/2026-09-07-optimization-full-verification-context-exit]]
contains the exact V304 full battery, each original resource failure, unreported
2K exclusion, historical-output timestamps, real shell correction with all 16
tests, and the subsequent tail's refusal before model load. No failed gate is
replaced by a passing source-only check, consumer proof or lower sampled peak.


### V307 prose, V315–V319: full tail and installation close; timing remains inconclusive

[[sources/runs/2026/09/2026-09-07-optimization-joined-eval-prose-qualification-exclusions]]
preserves all 32 cells/warmups, exact execution and absolute-ceiling checks, the
18 fair thermal observations, five VM exclusions and insufficient four/three paired
samples. [[sources/runs/2026/09/2026-09-07-optimization-joined-eval-installed-and-full-tail]]
contains the five installed upgrade/rollback phases, 31 installed checks, original
quality15/API74, independent tower and vision25 results, both guarded refusals and
the early drained handback. [[sources/runs/2026/09/2026-09-07-optimization-foundation-thermal-preflight]]
and [[sources/runs/2026/09/2026-09-07-optimization-thermal-static-suite-integration]]
record the additional direct Foundation observation, eight unit cases, missing
fixture failure and corrected 19-case mandatory static dispatch. Original failed
resource/timing gates and every OPT00–OPT36 disposition remain explicit.


### V320–V322: fixed confirmation, lifetime provenance and complete control inventory

[[sources/runs/2026/09/2026-09-07-optimization-fixed-confirmation-and-lifetime-preparation]]
preserves the distinct V320 packet, byte-identical original qualification contract,
explicit import inventory for the four qualified original executions, nine passing
prelaunch/proof/one-attempt tests, and the stopped original prose cohort. V321 binds
that exact provenance into the original-length corrected lifetimes, verifies the
actual V311 consumer proof, and passes 15 negative checks. Neither confirmation nor
lifetime launches. V322 independently hashes all 150 source inputs and checks every
48 Boolean/five numeric control, three construction choices, 54-key reference arm,
retention distinction, empty-environment default arm and inherited-environment
isolation. These are source and preparation results; remaining runtime/resource,
performance, calibration and activation acceptance remains explicit.


### V323–V324: original resource subset and exact activation patch prepared

[[sources/runs/2026/09/2026-09-07-optimization-resource-subset-and-exact-activation-preparation]]
records the byte-identical original elastic/MTP/long/context source sections,
seven unchanged required assertions, validated shell and prelaunch refusal,
13,308 exact client dependency files, and the unapplied seven-file archive-derived
activation patch. All 143 other source inputs match; dry-run patch application
passes and every shared source hash remains unchanged. Both resource/client
execution and final activation remain unrun, without a new machine interval.


### V320 execution / V325–V326: qualified current prose and exact report cohort

[[sources/runs/2026/09/2026-09-07-optimization-current-prose-confirmation-and-startup-reporting-parity]]
preserves the initial thermal refusal, full 32-cell/32-warmup confirmation, original
15-pair request/startup acceptance, all16 exact output pairs and the one four-swap-in
exclusion. Current preview is 1.8215% lower; active emission TPS is 0.4359% higher;
sampled process peak is 7.4314% lower. These describe this fixed short prose workload,
not a broad or sustained gain. The startup reporter now uses the unchanged acceptance
cohort and passes11 focused test methods; its superseded report remains preserved.
Current native cache prerequisites pass2323 assertions. All owned jobs drain and the
Mac is handed back at20:07:43UTC. Six paired workloads, two corrected lifetimes and
remaining resource/client/optional/calibration/activation work remain open.


### V327: current full-cache component preserves original work and thresholds

[[sources/runs/2026/09/2026-09-07-optimization-current-full-cache-component-preparation]]
records the exact V304/V325-bound protocol, unchanged original28-cell contract,
byte-preserved stoppedV244 evidence and14 passing model-free test methods. No model
runs and no attempt is claimed. The prospective interval must include the original
90-minute study allowance plus one-minute cleanup; component and serving decisions
remain separate, with no automatic adoption.


### V327–V329: compact cache qualifies and ring variants are rejected

[[sources/runs/2026/09/2026-09-07-optimization-compact-cache-component-and-serving-qualification]]
captures the original 28-cell component, 32-cell serving guard, fixed exclusions,
all exact source/build/driver bindings and independent reporting/math audit. Both
studies execute in the separately granted September 7 20:25–22:10 UTC interval and
drain before explicit early handback at 21:58:03 UTC. The source also corrects the
generic command-frontmatter label in the two prior immutable V325/V326 and V327
preparation wrappers; their exact bodies, commands and raw bytes remain unchanged.

Complete the still-unrun optional MTP, image and bounded-scope/vision studies before
freezing the final control composition. Compact rows are now a qualified addition;
ring eviction is rejected on measured benefit. Running the outstanding final matrix
before resolving additions would qualify an obsolete composition. Existing V304
results remain valid evidence for their exact controls and workloads; they cannot
be silently relabeled as tests of subsequently changed defaults. User apps stay
open. No default, shared binary, user installation, commit or public release changes.


### V330: original MTP and image prerequisites prepared

[[sources/runs/2026/09/2026-09-07-optimization-original-mtp-image-gates-preparation]] records the current-source native command bindings and all four original V179 serving workloads. Seven model-free test methods pass, including absent-proof, retry, deadline, contention, thermal/power, identity and failed-worker cleanup cases. Current native MTP work, corrected lifecycle and real image-reuse results must actually pass before any serving protocol is bound. No model or serving attempt has occurred. All original counts, targets, input bytes, output/work rules and fixed paired criteria remain unchanged. Automatic controls and current source are unchanged. This preparation advances OPT24/28 without claiming either candidate qualifies.


### V331: current bounded read-scope qualification is prepared

[[sources/runs/2026/09/2026-09-07-optimization-current-read-scope-gates-preparation]] preserves the four current native prerequisites and original V170 long-prefill serving workload. Family tests retain 2,051/4,096 tokens, the original 256-token compute schedule, 1,024-token routed tiles and compact frontier. Lifecycle and MTP/vision remain separate required proofs. All four must pass before the 32-cell serving protocol is frozen. Its original 10 GB cap, 3,730 prompt tokens, 16 outputs, read-count bounds, exact outputs and fixed benefit/startup criteria are unchanged. Seven model-free adapter tests pass. The preparation-only base-versus-extra headroom assertion failure is preserved and corrected without changing the original 20.112 GB allowance. No native or serving attempt exists. If the long study qualifies, short-prefill and runtime/planner policy still precede adoption.


### V332: current maximum-image native prerequisites are prepared

[[sources/runs/2026/09/2026-09-07-optimization-current-vision-native-preparation]] preserves five original query256 native commands on the exact V304 build and five passing model-free adapter tests. All original attention-oracle, tower, maximum-reference and ordinary/MTP integration assertions remain required. Eight full-tower comparisons additionally require their original observed byte equality. No image-serving or capacity proof is inferred. The original 13 GB native startup, 10 GB owned, 3 GB live and 900-second work limits stay unchanged. External admission after the explicit Mac handback is below that startup bound, so none of V330/V331/V332 starts a model attempt. The shared source, default, executable and user installation remain unchanged; every user app remains open.


### V333: full-image qualification profile and mandatory static checks corrected

[[sources/runs/2026/09/2026-09-07-optimization-vision-qualification-profile-correction]] preserves the actual model-free CLI refusal of the already-required 14.5 GB full-photo profile. The qualification command now selects that same full profile: 3,072-token prefill, vision on, MTP off, no elastic, existing 32,768 context and unlimited prefill wait, with 20.5 GB startup admission. Smaller refusal/reuse cases retain their original 8.1–10 GB targets and schedules. Five profile regression tests pass; the suite is mandatory in static_gates.sh, and twenty-one static-runner methods pass after preserving and correcting the missing fixture stub. Insufficient admission creates neither a model process nor a partial output directory. Frozen runtime/source/build and original paired drivers remain unchanged. The actual full-photo query256, maximum-image paired/resource/capacity and final full static gates still require execution; no native/serving attempt or new speed claim follows from this harness correction.


### V334: current full-photo and original maximum-image studies are prepared

[[sources/runs/2026/09/2026-09-07-optimization-current-maximum-image-gates-preparation]] binds the corrected full-photo query256 command and unchanged V159/V160 workloads to current V304, current native/image-identity prerequisites and exact source/fixture/driver evidence. Seven adapter tests and two independent report-validation tests pass. Original 12 fixed mechanism pairs retain the 12 GB cap, 640 slots, minimum five clean pairs, 200 MB saving in each and five-percent client nonregression; original seven capacity cells retain target-driven 10 GB, five clean cells and exact Red output/mechanism. Full-photo correctness requires the original 25 assertions and observed query-tile execution. All 54 paired controls are explicit, with query tiling the sole arm difference. No actual stage runs or serving protocol freezes before its current prerequisites pass. The unused model interval is returned at 23:17:53 after admission remains below the unchanged minimum; next grant is September 8 00:30–02:30 UTC pending explicit peer handback. User apps remain open and final activation remains pending.

### N6 — Prefill: bound the pass, then read each expert once
**Standalone plan superseded: execute through [[records/plan/whole-engine-optimization-2026-09-04]].** N6 remains its detailed prefill chapter; its optimization work is preserved and mapped to the OPT register below. Do not start a separate N6 queue, baseline or implementation. Implementation is now in progress through the OPT register; this chapter does not keep a second completion ledger.

 The September 4 review traced
prefill through `Generate.swift`, `Model.swift`, `Layers.swift`,
`ExpertStore.swift`, `NgramStore.swift`, the planner, prefix cache, MTP, serving,
and the pinned MLX kernels. It corrected the experimental contract as well as
the implementation sequence. The subsequent first-principles pass checks causal
dependencies, existing concurrency, backend submission locking and chunkwise GDN
algebra. No new controlled end-to-end speed A/B ran in these reviews. A separate native audit
confirmed retained-allocation savings with identical final logits at 1024/2048
tokens; its timings were discarded. See
[[records/analyses/2026-09-04-first-principles-inference-audit]],
[[sources/runs/2026/09/2026-09-04-first-principles-native-retention]],
[[sources/runs/2026/09/2026-09-04-prefill-parallelism-algebra]], and the earlier
code/tensor evidence [[sources/runs/2026/09/2026-09-04-n6-code-and-tensor-audit]]. Historical
shape probes and storage timings remain in
[[sources/runs/2026/09/2026-09-03-prefill-transient-probes]] and
[[sources/runs/2026/09/2026-09-03-expert-record-read-layout-ab]]; their original
bytes are preserved, including interpretations corrected below.

The central opportunity survives review: amortise each expert read over more
prompt tokens. First establish trustworthy instruments, remove unnecessary
retained allocations, and bound local work. Then enlarge the read scope without
enlarging every intermediate. Explore the compute alternatives below early in
small probes; their adoption is gated independently, not promised by this plan.

### Cost model and limits

Use **read scope** N for the new tokens sharing an expert sweep; **token tile** C
for attention, recurrence, PLE and resident projections; **expert group** G for
staged weights; and **routed-row tile** R for the expert GEMMs. These are four
separate quantities. Initially G remains 32. Today N and C are effectively the
same outer pass and R is unbounded inside a group.

```
expertBytes = recordBytes × sum over scopes and layers of fetched experts
recordBytes = 2,764,800
one full layer = 512 records = 1,415,577,600 bytes
48 full layers = 24,576 records = 67,947,724,800 bytes

wall time = host preparation + critical path through reads and GPU work
            + non-overlapped tails + request setup and output handling
```

When every layer's routed union is read at most once in **one scope**, main-model
expert reads are at most 67.95 GB, less where resident experts are reused. For
J scopes the bound is J × 67.95 GB. It excludes resident trunk loading, n-gram
rows, MTP/vision weights, decode, and any optional activation spill or deliberate
read amplification. It is **not a permanent 68 GB bound for arbitrary prompts**:
whole-prompt activations must fit, the context cap still applies, and a smaller
scope may be preferable for latency or memory.

The prior plan cited 826 GB at the floor and 273 GB at 16 GB. Keep these as
historical anchors to reconcile, not a frozen matched baseline. In particular,
`256 ×31 + 137 = 8,073`, not 7,960; 7,960 ends in a 24-token tail. The raw/chat
rendering, tokenizer revision and exact token IDs must travel with every number.
Reported ~208/~311 routed experts are not sufficient to infer the joint union,
cache misses or density at another shape. Measure them separately. Neither
uniform routing nor a decode-only union is a substitute for a prefill trace.

A larger scope usually reduces repeated reads, but changing arithmetic and
batch shape can change later routing. Calculate the bound from the actual
schedule and trace, including resident copies, tails and admission. Do not
assert a universal ratio. For 32,768 tokens, the current 4096-cap schedule is
25 passes; a constant 4096 schedule would be 8. A 256-cap schedule already has
128 constant passes, so removing context-driven halving offers it no such gain.

The old predictions of ≥222 tok/s at the floor and ~300 at 16 GB are withdrawn
as bounds. `ioSeconds` includes host read/staging calls that can overlap GPU
execution; `prefillSeconds - ioSeconds - scatterSeconds` is a residual, **not
measured GPU compute time**. Holding that residual fixed after reordering the
pipeline does not produce a lower bound. Likewise, 2.4–3.7 ms/token is not an
established compute ceiling, and a 2.5× reduction in records is not a 2.5× speedup.

The layout probe makes repeated reads a stronger target than more read lanes on
this Mac. It does not prove there is no remaining bandwidth on any device.
Existing queue-depth and buffer-recycling nulls stand for their tested regimes;
revisit only with a changed regime and a specific trace-based hypothesis.

### Unified program: ownership and implementation order

N6 is now the detailed prefill chapter of
[[records/plan/whole-engine-optimization-2026-09-04]], not a competing backlog.
That record owns the single OPT status register and program-wide experiment
lifecycle. This chapter owns prefill algorithms, substeps and acceptance detail;
phase exits link the corresponding OPT result rather than maintaining a second
completion or numerical ledger. The combined program has 37 items, including
OPT35 for demanded prefill outputs and OPT36 for specialized selection. All
production implementation status is now tracked in the unified program. The following review describes the pre-implementation evidence.

| N6 detail | Single execution owner |
|---|---|
| Phase 0 harness, baseline, trace and timing repair | OPT00/01 |
| 1.0 GDN/PLE compact carry; MTP last row | OPT02/03 |
| 1.1 PLE/resident tiling and n-gram assembly | OPT07/14; compatible projection fusions OPT18 |
| 1.2 completed indexer blocks, bounded selection, attention | OPT09/10/15; shared positions OPT11; selection kernel OPT36 |
| 1.3 deterministic MoE placement/fold and bounded rows | OPT12/13 |
| 1.4 capacity, growing prefix hits, feature and rollback reservations | OPT08; checkpoint extensions OPT29 |
| 2.0 remaining-aware scheduling | OPT04 |
| Remaining Phase 2 workspace bounds and estimator family | OPT08/10/14/16 |
| 3.1 short-tail scan resistance and decode admission | OPT04 for schedule; OPT21 for admission policy |
| 3.2 canceled prefill and committed-prefix recovery | OPT30; scope implementation OPT16; reusable checkpoint extension OPT29 |
| 3.3/3.4 truthful progress, effective knobs and overflow bounds | OPT01/04/08 |
| Phase 4 read scope and explicit workspace alternatives | OPT16, consuming OPT12/13/14 bounds |
| P1 shared work and P2 resident/missing/read-group overlap | OPT19; optional reusable-arena work OPT20 |
| P3 exact n-gram lookahead and aggregate read budget | OPT14/21 |
| P4 selected-block text attention; P5 chunkwise GDN | OPT15/26 |
| Unused final mixing and terminal-layer query/MoE pruning | OPT35 |
| Phase 5 expert/row layout and gap-aware read runs | OPT22; scheduling/bookkeeping OPT21 |
| Request-specific scope/pool trade and temporary headroom | OPT08/16, with explicit-pool semantics preserved |
| Prefix checkpoints; cross-request batching; activation spill and quality changes | OPT29; OPT31; OPT34 respectively |
| Combined acceptance, planner/claims/projections and delivery readiness | OPT32/33 |

Use one baseline manifest and repair the existing harness once. The execution
order is: instruments and compact ownership; tail correction and independent
waste removal; bounded indexer/PLE/row work; select a read-scope workspace;
combined qualification. P1/P2 overlap, P4 attention, P5 GDN and OPT36 selection
probes may start from fixed inputs before the broad scope implementation.
Integration requires their actual state/memory gates. In particular, OPT26 does
**not** require OPT16: it may first improve the current token-tiled path. A small
P1 schedule-only experiment does not require a new MoE reduction either.

### Additional first-principles findings from joining both plans

The latest implementation check uses source HEAD
`6e66a367f93e636c3729179e24007aa99c97b9a6`; inference sources and package pins
are unchanged from the initial audit. Raw source identities, derivations and
CPU checks are in
[[sources/runs/2026/09/2026-09-04-prefill-unification-first-principles]].
No model benchmark ran in this pass.

**U1 — distinguish bounded row computation from bounded reduction storage
(OPT12/13/16).** The existing output axis is router rank, not expert ID:
`experts[B,S,K,H] * weights[B,S,K,1]`, then the pinned reduction over K.
Preallocating disjoint output positions removes concatenate/inverse-gather
copies but still owns `N × K × H` values. At N=32768, K=10, H=2560, that buffer
alone is 1,677,721,600 BF16 bytes. An unfused weighted FP32 tensor is another
3,355,443,200 bytes if simultaneously live. Neither is a bounded R-tile buffer.
The current 56,320-bytes/token frontier example includes an FP32 accumulator;
it is only sufficient for that part of a design which actually implements a
compact fold. Add the full output axis when the selected algorithm keeps it.

There is a real scheduling tradeoff, not a missing `scatter` call. Expert-major
visitation produces contributions for many tokens out of router-rank order.
Without an additional storage/liveness proof, a bounded per-token K tile cannot
preserve the existing sum while arbitrary earlier/later expert groups remain
unvisited. Choose and measure one of these explicit arms:

| Arm | Storage/read trade | Numerical contract |
|---|---|---|
| A: canonical output placement | Keep scope-wide BF16 N×K×H, fuse or tile weighting/reduction to avoid a full FP32 product; select a smaller fitting N | Preserve existing contribution values and reduction tree for the exact arm; changed GEMM row tiling still has its own gate |
| B: deterministic expert-order fold | FP32 N×H accumulator plus bounded R scratch; execute fixed expert-ID order, reduce duplicate token destinations deterministically within each group | A new summation algorithm, even when repeatable and pool-invariant. Must pass frozen logit/state/routing/quality bands; no claim of exact reference summation |
| C: one-layer routed-weight workspace | Up to 1,415,577,600 weight bytes, then chronological token tiles with bounded C×K×H combine | Retains more existing tile arithmetic; projections/GEMM shapes and continuation still require checks |

An alternative which stores partial reduction-tree nodes must publish its
worst-case live-node bound on adversarial routing, not only a typical trace.
At 27,648 tokens the full 512-expert weight payload equals the single BF16
N×K×H output payload. This is payload arithmetic, not a runtime crossover:
resident copies, actual routed union, staging, scratch and decode-pool displacement
still decide. Benchmark A/B/C at fitting budgets; do not force B if its numerical
or scatter cost loses. Preserve G outside R to avoid rereads in B, and retain
loaded layer weights across C in C. Admission must neither change the arithmetic
order nor secretly reload an expert after its workspace was freed.

**U2 — partial selection is currently a full sort (OPT36 with OPT10/17/06).**
Pinned `backend/metal/sort.cpp:342` routes `ArgPartition::eval_gpu` to
`gpu_merge_sort`, as does `Partition`. `Layers.swift:230/565` and `MTP.swift:94`
therefore sort the entire selection axis despite retaining only 512 indexer
blocks or ten router experts. The router has 512 candidates; the indexer has
up to 8192 completed blocks at the current cap. Multi-block sorting also owns
values/indices scratch. Merely changing the Swift spelling to another wrapper
of the same primitive is not an optimization.

Prototype top-10/512 router selection and bounded block-top-512 separately.
A blockwise local-top-K plus merge can discard candidates that cannot enter the
final top-K under one fixed total order. The CPU check establishes this identity
in 60 finite synthetic cases, including ties, not pinned GPU parity. Derive and
verify the actual comparator, tie order, masked infinities, signed zero and
exceptional-value behavior across single/multi-block backend boundaries. Router
acceptance requires the same **ordered** IDs and logits: changing order can
change softmax and the later weighted sum even with the same expert set.
Keep routing weights/normalization separate initially. A changed tie policy is
not an exact optimization. Reuse a proved primitive in the sampler only under
its different threshold-tie/CDF contract.

For the indexer, initially keep the existing score arithmetic and substitute
selection only. Separately prototype a tiled score kernel that combines the
four `max(q_h dot k, 0)` terms without materializing all four head-score planes.
Do not move the positive-part operation after summation or sum queries first:
`max(1,0)+max(-1,0)=1`, whereas `max(1-1,0)=0`. Changed dot/sum grouping is a
numerical candidate. Compact selection eliminates dense mask expansion only
when attention consumes it directly; it does not eliminate scoring all visible
completed blocks. Preserve a query-tiled full-score/old-selector fallback so
fused attention need not wait for a new selector to win.

**U3 — visibility-based dense shortcut and compact persistent indexer
(OPT09/10/15).** Let absolute query position be t, compression ratio r=4,
and block budget k=512. The number of fully visible blocks is
`floor((t+1)/r)` and the own partial tail has `(t+1) mod r` keys. All causal keys
are retained whenever that block count is <=k, even if later keys in the same
scope make total context exceed 2048. For finite scores, positions through
2050 inclusive need no indexer scoring/selection; 2051 is the first position
with 513 complete visible blocks. Preserve raw K projections/cache updates for
future selection and split query tiles at the actual visibility boundary.
The independent 510-case mask transcription covered ties, reused absolute
positions and partial blocks; production mask/output equivalence is still required.
If only K projection rows are used, a smaller quantized projection is a separate
shape/numerical experiment, not an assumed benefit from lazy slicing.

Completed raw indexer blocks have no future consumer once the exact FP32 mean
→ BF16 → normalization → RoPE result is cached. Consider replacing old raw
storage with completed pooled keys plus a raw tail, rather than keeping both
forever. Across the main model's 12 indexers at 32k, raw payload is 100,663,296
bytes and completed payload is 25,165,824: a derived 75,497,472-byte difference
before alignment, growth, rollback and MTP. This is a smaller capacity opportunity
than expert reuse, not a measured footprint saving.

The new cache cannot implement arbitrary trim using offsets alone. A checkpoint
must retain its partial raw block and completed-block count; speculative verify
must retain enough accepted/rejected raw positions to rebuild a cut block.
Provide bounded undo/tail state for maximum draft length, or keep the old raw
storage until that proof passes. The CPU append/whole-checkpoint experiment
covers 156 cases and does not prove BF16, normalization, RoPE or arbitrary trim.
Test every four-token remainder through prefix restore, cancellation, KV growth
and speculative rejection; immutable completed blocks may be shared only with
correct ownership/capacity accounting.

**U4 — ask prefill for only the outputs its caller consumes (OPT35).**
`Model.hiddenStates` returns every mixed row, and nonfinal prefill calls explicitly
`eval` that result. Its final `eval(h)` also forces all final-layer MoE/residual
rows before a last-row slice can prune them. Introduce an internal demand
contract—state only, last logits, or full multis/diagnostics—while leaving public
full-output methods compatible. First skip the unused final mixer on nonfinal
scopes and mix only the demanded last row on the final scope.

The pinned model ends in QSA. With MTP disabled and no full-output hook, final-layer
state depends on K/V and raw indexer K for **all** new positions. It does not
depend on the final layer's earlier query outputs or token-local MoE/residuals.
After all earlier layers and final-layer input/required caches are complete,
a state-only scope can omit those queries and terminal MoE entirely; a last-logit
scope needs only its final query, attention residual, MLP HC, MoE and mixer.
Q projection is stateless, while K/V/indexer updates are not. This is stronger
than skipping only the final mixer, and only affects the terminal layer. Keep
all earlier layers intact. A diagnostic truncated stack that ends in GDN still
needs every chronological recurrent update; query-only QSA pruning cannot apply.

MTP needs full final multis to build its cache and therefore disables the terminal
MoE pruning arm. `MTPHead.consume` already materializes only its state dependencies;
do not count its discarded lazy MoE/mixer graph as executed work being removed.
Provide explicit full-output fallback for diagnostics/parity/hooks and compare
continued state, fixed inputs, pool warmth and generated quality. One-row expert
work must not silently switch to a cache-content-dependent kernel: preserve the
chosen sweep/grouped arithmetic where feasible, or classify the shape change
under numerical gates. Materialize every required cache/state write explicitly when omitting `eval(h)`; preserve the release barrier before pool mutation and do not let lazy pruning discard necessary state. Choose admission explicitly; removing terminal routes
changes warming/counters, not model mathematics. Report its limited one-layer
scope rather than promising a stack-wide multiplier.

**U5 — select a measured schedule, not the largest possible N (OPT08/16/21).**
The saved reads from merging adjacent scopes are governed by their actual routed
set intersection and resident misses. Compute the union benefit from fixed
traces as a mechanism model, then remeasure live routes after numerical changes.
Beyond near-saturation, larger N can continue saving repeat scans but also expand
frontiers, prolong cancellation/retry work and displace decode cache. Select
among fitted A/B/C schedules after the reused prefix and remaining prompt are
known. Do not assume future-layer routes are available at planning time.

Keep N, C, G and R distinct. Inside C, attention query/key tiles and the GDN
recurrence block length may differ from resident-projection and PLE tile sizes;
forcing all kernels to use one shape can discard the best compute regime.
Use a small measured candidate set rather than an uncontrolled Cartesian tuning
grid. Freeze weights for TTFT, full-request latency and cancellation requirements;
include fixed decode suffix cost and cold/warm MTP/image startup. Choose a policy
on development traces, confirm on held-out requests, and charge all persistent
and overlapping buffers. Progress remains work progress until a whole-stack
scope commits.

The review also resolves an acceptance mismatch: a verified ownership/capacity
improvement may ship with measured latency **nonregression** and an explicit memory
benefit; it need not await a speculative larger-scope speedup. A change advertised
as a speed improvement still needs a full-request timing win. Correctness,
truthful reporting and missing budget enforcement are fixes regardless of whether
a custom kernel wins. No evidence here certifies the fastest possible runtime;
the program identifies and tests the remaining alternatives against one baseline.

### First-principles parallel execution — the dependency and resource contract

Prefill knows all input token IDs; it does not know every layer's activations or
expert routes. Consequently it has much more token parallelism than decode,
but the 48-layer dependency chain remains. The objective is minimum exposed
critical-path time at a bounded total footprint, not the largest thread count.
For fixed work, necessary lower bounds include storage bytes/effective storage
bandwidth, GPU work/effective compute throughput, memory traffic/effective memory
bandwidth, and the causal critical path. Use the maximum of resource bounds,
not their sum, where resources overlap; release times and contention can make
actual elapsed time larger. CPU, SSD buffers and GPU share unified-memory
capacity and bandwidth. More streams cannot multiply those resources.

For one layer, the relevant dependencies are:

```
prior-layer hidden -> optional PLE -> attention HC -> GDN or QSA -> residual
  -> MLP HC -> x2
       x2 -> router/top-K -> CPU IDs -> sort/lookup -> weight reads -> experts
       x2 -> shared gate and shared expert ------------------------------+
       router weights + routed outputs -> ordered weighted reduction ----+
                                                                  combine
                                                                     |
                                                              next layer
prompt IDs + preceding IDs -> exact n-gram rows -> PLE when reached
```

**Existing parallelism must remain in the control.** Expert `readRuns` already
uses dynamically assigned concurrent read lanes (12 by default); n-gram prefetch
already uses 32 lanes. Sorted grouped expert GEMMs parallelise rows and output
dimensions, and `asyncEval(d)` overlaps a group with the following CPU read.
Replacing these with generic async functions does not create a new mechanism.
For an ideal two-stage pipeline of m identical groups with read time r and GPU
time c, elapsed time is `r + c + (m - 1) × max(r,c)`, rather than `m × (r+c)`.
The current path already captures some of this saving. Remaining bubbles and
resource contention, not the serial formula, define further overlap headroom.

**P1: launch shared work while exact routed weights are arriving.** In
`MoELayer.callAsFunction`, the shared branch is currently constructed after
`sweep` returns. It depends on x2, not on expert IDs or routed output. First
prototype submitting the unchanged shared branch with `asyncEval` immediately
after router ID readback, before CPU sorting and the first staged read. Keep
router readiness first: launching a large shared projection ahead of the router
can delay every required disk request. A more advanced ordering can submit the
router to its own completion event, then shared work, and wait only for the
router event; verify that ordering on the pinned backend before using it.

Use the existing GPU stream initially. This overlaps GPU computation with host
sorting/SSD work without requiring simultaneous GPU kernels. Charge the shared
output and its live intermediates; in a large scope submit bounded tiles with
admission limits instead of an entire extra scope graph. Moving the graph earlier
can itself change peak/donation behavior. Preserve shared gating, intermediate
rounding and the original routed combine. Measure time until the next layer can
start, including any expert GEMMs delayed by shared work. No win is promised on
a warm, GPU-saturated layer.

**P2: overlap the first missing read with resident computation, and preserve
read-ahead across routed-row tiles.** The current sweep handles all resident
groups first, using blocking `gatherResident`, before it starts missing reads.
Once CPU IDs and residency are known, a bounded raw-I/O producer can start the
first required missing group while the GPU copies/computes resident groups.
Compute/reduce in the same canonical order; early read completion must not
change group membership, dispatch or arithmetic. In Phase 4 the producer can
read group G+1 while all bounded R tiles reuse group G, rather than waiting for
the final row tile before beginning that read. Retain at most the current and
one next weight group unless a separately budgeted experiment earns more.
At 32 experts per group those two weight sets alone occupy 176,947,200 bytes,
before row buffers and admission copies. An all-resident layer has no missing
read to hide; report cold/mixed/warm cases separately.

This needs an ownership boundary, not background calls to the current
`pool.readStaged`: that method also wraps/evaluates arrays and mutates stats.
Workers should own raw aligned buffers and immutable read tickets; a single
generation owner wraps completed buffers, submits MLX work, updates pool/cache
metadata and publishes counters. Pinned `Transforms+Eval.swift` and
`MLXArray.eval()` serialize evaluation calls with the same `NSRecursiveLock`,
including blocking evaluations. Worker `eval` calls therefore need not provide
parallel submission and can prevent the owner from progressing. This is a host
API constraint, not proof the GPU cannot overlap commands. The C++ backend has
per-evaluation events and inter-stream fences, so do not call every `eval` a
device-wide synchronization either; already-available raw arrays need no GPU
wait. Inspect actual event waits and command order.

A stage slot moves through `free -> reading -> ready -> GPU in use -> free`.
Its completion must include every row consumer and any admission copy, not
merely the first GEMM. Enforce bounded ready queues, generation/scope identity,
read-failure propagation, cancellation joins and exactly-once ownership
transfer. Keep pool writes and resizing behind the existing release guarantee
until a narrower fence proves the same lifetime. A CPU worker returning from
`pread` is not permission to overwrite a buffer the GPU is still reading.

**P3: start exact n-gram lookahead before layer 1 needs it.** Hashing and row
reads depend only on prompt/history IDs, so bounded prefetch can overlap layer
0 and, for image requests, independent vision work once IDs/history are fixed.
Merge completed rows on the owner; retain the cache/EOS semantics and avoid
prefetch-then-evict-then-reread. Share an aggregate I/O budget with expert reads,
prioritising data that is blocking the next operation. Independently enabling
12 expert lanes plus 32 n-gram lanes does not establish useful extra bandwidth.
Measure nonrepeating prose, warm rows and concurrent vision separately.

**P4: turn attention's token independence into an appropriate GPU kernel.**
Given projected Q/K/V, each query row and query head can be evaluated
independently against its exact causal selected keys. For this model, ordinary
prefill falls outside the pinned fused SDPA dispatch at head dimension 256.
The main opportunity is bounded query/head/key tiling, online softmax and direct
selected-block traversal. A dense boolean mask does not skip the dense fallback
GEMMs. Preserve selection and process one query tile's indexer output directly;
consider sharing K/V loads across the 12 query heads using each KV head, without
assuming that the largest head tile wins on register pressure. Each query may
select different blocks, so cross-query K/V reuse is a measured tradeoff.
Fusing compatible Q/K/V or GDN projections and pointwise epilogues can reduce
launches and memory traffic; separate Swift tasks for each projection usually
add no compute resource to the same GPU. Kernel reassociation has its own
numerical gate. The existing <=2-query vector path is a diagnostic control,
not a proposed long-prefill strategy with thousands of small launches.

**P5: parallelise within GDN chunks without deleting recurrent context.** The
current kernel already launches `48 × (128/4) = 1,536` threadgroups for B=1,
parallel across value heads/value rows with 32 lanes reducing key dimensions.
It keeps FP32 state in registers and loops serially over T. It is therefore
not a one-thread or necessarily underoccupied kernel. A faster formulation must
earn its extra work/temporary traffic by using matrix operations more efficiently.

For one value head, with value-by-key state, the implemented update is
`S_i = g_i S_(i-1) + delta_i k_i^T`, where
`delta_i = beta_i (v_i - g_i S_(i-1) k_i)` and `y_i = S_i q_i`.
Within a block define `d_ij = product(g_(j+1)..g_i)` for i>=j, `d_ii=1`,
`d_ij=0` for i<j, `gamma_i = product(g_1..g_i)`, and unit-lower-triangular A with
`A_ij = beta_i d_ij (k_i dot k_j)` for i>j. Stack token vectors as rows:

```
U = solve(A, diag(beta) V)
W = solve(A, diag(beta * gamma) K)
Delta = U - W S_in^T
Y = diag(gamma) Q S_in^T + (d * (Q K^T)) Delta
S_out = gamma_last S_in + Delta^T diag(d_last,:) K
```

Here `*` inside the parentheses is elementwise multiplication.
U/W and local coefficients depend only on block inputs: their preparation can
be parallel over blocks/heads. Incoming/outgoing states still connect blocks;
the first implementation should retain chronological boundary-state propagation
and use matrix operations within blocks. Use a structured triangular/WY
algorithm, not a generic explicit inverse or a dense 128x128 transition-matrix
scan. Start with bounded block sizes such as 32/64/128 and compare full layer
time including preparation, state transfer and projection—not only the inner
recurrence. Do not materialise coefficient matrices for the entire scope without
a memory budget; bound precomputation lookahead independently of N.

The CPU FP64 derivation check in
[[sources/runs/2026/09/2026-09-04-prefill-parallelism-algebra]] passed 56 cases,
including nonzero S_in, partial blocks, masks, collinear keys, extreme gates and
128-dimensional state. Maximum output/state absolute discrepancies were about
1.95e-15/6.56e-15. This validates the synthetic algebra, not FP32/Kahan parity,
Metal performance or model quality. GDN's decay can underflow to zero, so a
production implementation must avoid dividing cumulative products or forming
undefined log differences across zero decays. Preserve BF16 q/k/v and outputs,
FP32 state/gates, the three value heads per key head, convolution carry and all
positions. Test long continuation/state drift and routing under frozen numerical
bands. Retain the current decode and MTP recording paths during the first prefill
experiment. The [Gated Delta Networks paper](https://arxiv.org/abs/2412.06464)
provides the chunkwise algorithmic precedent; it supplies no Metal speed estimate.

**Parallelism that does not follow from the dependency graph.** Independent
halves of a prompt lack the correct KV and recurrent starting states. Later
layers cannot obtain exact routes before the preceding computation produces
their inputs. A causal wavefront across token tiles and layers is possible, but
it competes with this plan's broad expert reuse, can hold several layer working
sets and uses the same GPU. Consider it only against a demonstrated exposed
bubble, charging rereads, buffers and the much harder partial-state cancellation
contract. Speculative next-layer reads need a measured predictor/waste budget.
CPU offload is likewise an experiment: shared RAM does not make CPU numerical
execution, GPU migration or DRAM contention free. More devices, a CPU rewrite
or concurrent requests are not prerequisites for these single-Mac opportunities.

**Experiment selection.** Run small attention/GDN kernel probes early while
ownership and instrument fixes proceed; their adoption need not wait until all
storage work is complete. Use the following measured decision rule rather than
a blanket priority based on layer count:

| Exposed critical-path observation | First experiment | Mechanism required before full-request acceptance |
|---|---|---|
| Repeated expert bytes dominate | Phase 1 ownership, tail-aware schedule, Phase 4 read reuse | Fewer actual repeated reads, no hidden amplification |
| GPU idle during sort/first read | P1 shared work; P2 resident/missing overlap | Independent useful GPU work fills the wait, next-layer readiness improves |
| Gaps between row tiles/groups | P2 bounded producer | Next group ready sooner without unbounded queues or altered combine |
| PLE waiting for rows | P3 exact lookahead | Fewer exposed row waits without slowing critical expert reads |
| Dense attention fallback dominates | P4 fused selected-block attention | Correct selections, less score traffic/work and bounded indexer memory |
| GDN recurrence dominates | P5 chunkwise recurrence | Faster complete GDN phase after setup, correct persistent state/quality |
| GPU stays busy on expert GEMMs | Larger weight reuse and shape-specific kernel/epilogue tuning | More useful rows per weight load or less memory/launch work; more read lanes alone has no mechanism |

Capture router-ready, first-read start/completion, resident-copy and GPU group
start/completion, shared completion, row-tile waits, next-layer readiness, queue
occupancy and live stage bytes in a separate trace. Test cold/mixed/all-hit
layers, skewed/low-occupancy routes and short/long scopes. Schedule-only arms
preserve shapes and require exact output/state/routing; numerical kernel arms
use the frozen numerical contract. Then perform untraced paired whole-request
A/B at matched memory. Combining individually winning candidates needs a further
combined A/B: two optimisations can remove the same wait or compete for the same
bandwidth. There is no supported universal speed multiplier yet.


### Validation contract

**Separate mechanism, correctness, memory and user latency.** Correctness, bounded memory and latency nonregression must pass before adoption. A memory/capacity optimization also needs a demonstrated resource benefit; a speed optimization needs an actual latency win. A byte reduction with unchanged latency is a mechanism result, not a user speedup. It may be retained for a declared memory/traffic benefit only after its costs pass; attribute the remaining critical path and record that limited outcome. Do not claim the byte model itself was
falsified merely because overlap or compute absorbed the gain.

* **Mechanism:** actual expert records and bytes, distinct IDs per layer/scope,
  duplicate fetches, resident copies, admission/churn, routed rows and actual
  scheduling dimensions. Exact repeatability applies to a pinned path and
  initial state; it is not guaranteed across numerical changes. Keep a
  routing-replay microbenchmark to isolate I/O from changes in the model's
  own routing. Never use replayed routing as a model-quality test.
* **Correctness:** distinguish byte-preserving changes from reassociation.
  Exact-preserving arms compare full outputs, state and routing; numerically
  different arms use frozen, same-shape reference/control logits and state
  tolerances plus task checks. Do not require identical expert records after a
  changed sum has perturbed later routers. Preserve exact independence from
  pool contents for a fixed computation path; memory residency is not a licence
  to change reduction order.
* **Memory:** untraced process lifetime peak plus current physical footprint,
  MLX active/cache diagnostics, retained-state capacities and allocation
  lifetimes. `ProcessMemory.peakResidentGB` combines lifetime `ru_maxrss` with
  current `phys_footprint`; it is not a resettable per-prefill peak. A decode or
  startup high-water can hide a prefill improvement. Fresh processes and
  phase attribution are both needed. `MemTrace` adds evals and resets MLX peaks;
  its results cannot stand in for the untraced memory or timing gate.
* **Latency:** request arrival → generation-lock acquisition → first token
  ready → first visible answer delta → completion. Also record prefill wall
  time, actual decode count/time and cancellation latency. Vision encoding is
  currently before the generator's prefill timer, and reasoning/special tokens
  need not produce a visible delta, so tok/s alone misses both costs.

The existing `sweep-check` requires exact repeatability and cold/warm pool
identity. Its logit threshold is `max(3 × measured control, 0.01)`, not the
historical 3.32% observation or 5.09% control. Freeze the unmodified control and
report observation, control and bound separately; do not let a modified control
widen its own allowance. Test full depth, more than one token position, early
and late QSA layers, and continuation state. The current short
`quality_probe.sh` prompts do not reliably execute the sweep; retain those
smoke checks and add frozen long prose/code/tool/retrieval cases that do.
A generated-text hash detects differences but cannot certify logits or state.

**A/B protocol.** One model process; explicit small memory sizes; current
reclaimable memory with several GB spare before each launch. Interleave at least
three paired A/B rounds with balanced order, not all A then all B. Two A/A
rounds are the minimum initial sanity check, not a reliable variance estimate;
repeat ambiguous cells rather than declaring a small difference real. Record
per-cell load and swap/pageout changes before and after, preserve invalid runs
with their reason, and report paired medians and spread. Trace runs are separate.

**Flag lifecycle.** The unified program's execution rules own this policy: typed, validated performance controls start default off, effective values enter every receipt, the verified control survives qualification and one release after adoption, and redundant experimental switches then disappear unless measured dispatch requires them. Correctness/reporting fixes do not retain a mode restoring the bug.

### Phase 0 — repair the instruments, then freeze the baseline

**0.1 Repair `Tools/prefill_bench.sh` before using it to accept anything.** Its
parser has been exercised, but its present implementation does not implement the
experiment promised here:

* Prose is `head -c 34000 PLAN.md`, which changes as this very plan is edited.
  Freeze a UTF-8 fixture and exact IDs; preserve the acceptance fixture too.
  Record raw/chat mode, template/thinking, tokenizer/checkpoint hashes, prompt
  hash, actual token count and ID hash. Do not rename a new fixture "10,490
  tokens" or "7,960 tokens" without checking. Command substitution strips final
  newlines; hash the exact bytes and IDs that reach the engine.
* Defaults omit 512 and 2048; the prose/acceptance × pool × chunk grid alone is
  not an A/B driver. The harness has one hardcoded binary and one global flag
  set, no arm manifest or within-cell A/B interleaving, no server cell, and no
  record-repeatability assertion. Implement these explicitly. Capture binary,
  source/dirty-tree, metallib and package hashes, effective flags, pool slots,
  cache limits, schedule, read scope, tile sizes, MTP and vision modes.
* Its availability formula sums inactive/speculative/purgeable pages and
  hardcodes 16 KiB; it differs from `Planner.deviceAvailableGB()` (free +
  purgeable + file-backed pages using the real page size). Use the canonical
  reading. Its `pool × 0.133 + 5.3 + 2` need estimate omits the forced chunk,
  prefix reservation and feature costs. `--experts-per-layer 13` is actually
  floored to 640 slots; explicit pools are not total-memory caps. Pin effective
  cache limits, charge overrides and check each arm, not just the nominal pool.
* A prefill-chunk environment override bypasses the plan's chunk while the
  printed expected peak and cache-cap selection still use the plan. Record both
  and price the effective arm. First use matched pools for causal attribution;
  then use matched total targets for the user-facing decision. Pool 20 does not
  represent a 16 GB target, so the proposed matrix cannot prove a 16 GB claim.
* Missing metrics become null and a failing child can be followed by a successful
  final script exit. Fail the run on missing required fields, nonzero status,
  token truncation or the wrong effective configuration. Preserve stdout/stderr
  and full output/token hashes; the current temporary directory is deleted.
  Serialize labels/paths as data rather than interpolating them into Python or
  handwritten JSON. Validate arguments and stop/clean up owned children on
  interruption. `pgrep` is advisory; the engine lock is the allocation guard.
* The CLI prints peak to 0.1 GB and timing splits to hundredths. Prefer structured
  unrounded stats, including request/phase labels. The current `compute_s`
  field must be renamed as residual until actual GPU timing exists. N-gram
  counters are reset at the prefill/decode boundary; expose both phases.
  A 48-token limit need not produce 48 tokens: record EOS and actual counts.
  Use a frozen teacher-forced decode suffix as an additional identical-work
  performance control, alongside free generation for quality.

**0.2 Instrument validation.** Repeat a small fresh-process cell with identical
IDs, effective settings and cold expert/prefix state. Verify repeatable routing
and fetch counts. Validate counters against the trace and record-byte arithmetic.
If they vary, inspect the counter, kernels and initial state; do not simply
replace them with noisy timing. Unit-test fixture identity, failure propagation,
parsing and the arm scheduler without loading the model. Attach phase, absolute
position and scope/tile IDs to router traces before changing loop order; the
current format infers passes from layer visitation and has no explicit phase.
Bound trace buffering and capture it separately from performance runs.

**0.3 Baseline matrix.** Freeze acceptance and ordinary prose first. Use matched
640-slot and 960-slot pools, chunks 256/512/1024/2048/4096 only where the arm
actually fits, MTP off and a fixed allocator/prefix policy. Begin with a small
screening subset; expand only candidates that survive it. Add true total-memory
8.1/10 GB and, on a sufficiently quiet machine, 16 GB acceptance cells. Include
one fresh `serve` cell and one controlled warm/prefix-hit cell. Extend the server
harness: `api_test.sh` alone is not the required metrics/cancellation experiment.
Do not compare a warm server with a cold CLI and call the difference an arm.

Record several lengths including 255/256/257, chunk−1/chunk/chunk+1, indexer
budget 2048±1, partial compression blocks, the 8,016 product boundary, and a
nonzero reused prefix. Cheap shape/state tests cover boundary combinatorics;
real model runs cover representative long cells. Test 8,192 then 16,384 then
32,768 progressively for any claimed long-context schedule. At the exact cap,
`Engine.generate` returns without prefill when no completion room remains:
measure full-cap prefill through `context-check`, and serving with prompt + reply
inside the cap. Never mistake an immediate cap response for a fast prefill.

**0.4 Shape probes and attribution.** Correct `PrefillProbe` before relying on
its numerical conclusions. Production router logits and softmax weights are
float32; bf16 expert output × float32 weights and the reduction are float32.
The old probe A explicitly converted weights to bf16, whereas B used the original
float weights, and its reported reference comparison did not apply the same
final cast to both outputs. Its 0.49–0.56% is synthetic expert-output spread,
not full-model logit spread. Thus "4× more accurate than shipped" and "shipped
sum is wrong" are unsupported. Memory results remain measurements of those
synthetic graphs, not a production saving. Probe PLE uses ordinary synthetic
matmuls rather than the production quantized projections; full PLE parity and
carry state remain gates. Its per-shape retained references and allocator state
also differ across arms; do not treat the printed peak difference as matched
process RSS. Record seeds correctly: constructing a random key
without installing or passing it does not seed the following draw.

Use identical tensors, production dtypes and final casts, fixed seeds and warmup,
separate memory/timing runs, equal retained references and clean allocator state.
Validate repeatability against fresh processes. Measure actual phase outputs and
carried state, and verify that a profiler/eval has not removed the allocation it
claims to attribute. Inspect the allocation plateau before presuming one of the
old synthetic savings will lower it.

**0.5 Storage and routing.** On a quiet machine capture the 51,200 / 819,200 /
2,764,800 / 26,214,400-byte read curve, QD and read-run distribution, and whether
`F_NOCACHE` actually succeeded. The present code ignores that fcntl return.
Re-anchor the 17.3 GB/s public claim only with comparable evidence and update
all of its surfaces. Trace actual prefill routing at 1024 and 4096, per-layer
unions, resident intersections, frequency and suffix frequency. Measure bytes,
read-call wall time, GPU time and overlap separately. These are baseline tasks,
not requirements to redo already closed cache-policy experiments.

### Phase 1 — bound retained state and local working memory

**1.0 Implement compact ownership before changing arithmetic.** `GDNLayer`
saves `convInput` tail slices, `PLELayer` saves `full` tail slices, and
`MTPHead.consume` returns a slice of `chunkMulti` after `eval(last)`.
Evaluation is not detachment. The later native intervention used the pinned
backend's `contiguous` copy behavior on retained GDN/PLE views and reduced
end-of-prefill MLX active allocation by 775,979,008 bytes at 1024 tokens and
1,551,892,480 bytes at 2048, with all final vocabulary logits byte-identical.
See [[sources/runs/2026/09/2026-09-04-first-principles-native-retention]].
This was a deterministic synthetic-ID prompt with a 640-slot pool and 512 MiB
MLX cache. It establishes allocation ownership and those final logits, not
all-state equality, MTP/vision coverage, a process-RSS saving or a speedup.

The pinned backend explicitly copies a contiguous view with an oversized
backing allocation; verify the primitive's ownership behavior rather than
assuming that either `eval` or a contiguity name detaches storage on every
backend. Put compact copies at the production lifetime boundary where the
parent's last necessary consumer has finished. Check GDN ssm outputs and sibling
graph lifetimes, PLE carry, MTP lastMulti and recording arrays after normal
prefill and rollback. Audit KV/indexer allocated capacity, not just offsets.
Measure retained bytes at layer end, request end and after prefix retention;
compare full outputs, state and routing before accepting the production patch.

At S=4096 the derived GDN parent term is about 3.02 GB across 36 layers, plus
PLE; that larger-shape total remains unmeasured. Confirm the real footprint and
feature combinations before repricing the planner. Increasing N without resolving
retention could multiply a small logical carry by sequence length and layer
count. Ownership fixes can recover capacity without changing the MoE reduction,
so they still go before the fold.

**1.1 Bound PLE and resident projections.** Tile PLE with its nine-row dilated
conv carry; slice n-gram history from the original scope with its preceding two
IDs, respecting EOS boundaries, and update the global history once. Do not
compute every tile from the final scope suffix. Quantized projection, norm,
gate and carry outputs must pass the production-shaped parity checks. Evaluate
bounded outputs before advancing and avoid collecting tile intermediates into
an ever-growing lazy graph. If production quantized dispatch changes rounding
when tiled, classify that arm as numerical and run the corresponding gates;
per-token mathematical independence does not prove bit identity. Include
hyper-connection norms/projections, GDN projections, shared experts and final mixer in the liveness inventory: bounding
only the three originally named transients is not a whole-layer memory proof.

**1.2 Stream indexer masks directly into attention.** Split projection/cache
update from mask calculation. Cache each new raw key once; pool and normalise
completed compression blocks once per layer/scope, preserving the partial tail.
For each query tile compute its causal block selection and immediately consume
it in SDPA, then release the mask. Concatenating bounded masks back into a full
`N × context` mask restores the original problem. Slice keys to the tile's
absolute causal end and preserve the own-block tail, ties and partial blocks.
Test cached offsets and the <=2048 dense path explicitly. Mask equivalence with
fixed q/k is a separate gate from projection and full-layer equivalence.

**1.3 Bound expert output with deterministic reduction.** The existing sweep
retains every group's output in `outs`, concatenates, gathers by `invOrder`,
then weights and reduces. Remove those whole-scope `N × K × H` lifetimes without
silently changing the computation according to cache contents.

The proposed scatter-add is a candidate, not the committed implementation:
duplicate token destinations use atomic addition in pinned MLX; float addition
order is not fixed by atomicity. Moreover the current sweep schedules resident
experts first. Accumulating in that visitation order makes arithmetic depend on
residency, even if every individual GEMM is identical. Test a deterministic
segmented reduction with a canonical per-token expert order and bounded workspace;
if necessary retain a bounded tile's K axis. Route I/O by residency but preserve
canonical compute/reduction order with bounded buffering. Prove buffer donation
or charge an old and new accumulator per update; `at.add` does not itself prove
an in-place operation. Maintain bounded double buffering rather than replacing
every overlap with a synchronous eval.

U1 above makes the reduction choice explicit: router-rank placement still owns the full K axis, and deterministic expert-order folding is a numerical alternative rather than the reference sum. Require production-dtype micro parity for exact arms, exact repeatability across runs and
cold/warm/admitted pools, and full-model logit/state checks. A changed but
accepted numerical algorithm can perturb later routing; measure that separately.
If the compact fold cannot satisfy the invariants, keep the canonical combine
and evaluate the full-layer weight workspace alternative in Phase 4. Do not
weaken exact residency independence to force the desired memory result.

**1.4 Capacity and reuse accounting.** Prefix retention must evict other entries
before a hit grows, not only before a miss: `PrefixCache.take` currently returns
a hit without applying `reserveTokens` to the remaining entries. Charge actual
KV/indexer capacity rounding, compact carry buffers, MTP state (currently not
fully included in bytesPerToken), and simultaneously retained conversations.
Exercise four conversations with auxiliary title/tool requests and a long growing
hit. Avoid double-charging the active context already in the fixed footprint.
Memory released by ownership fixes must be remeasured before enlarging any budget.

*Exit:* exact-preserving substeps have identical routing, state and outputs.
Numerical substeps meet the separately frozen bands and exact residency and
repeatability gates. At the matched 4096 cell, seek at least 0.5 GB less untraced
process peak or demonstrate a bounded transient beyond the old envelope without
claiming a peak reduction. A winning memory arm must pass the predeclared latency-nonregression limit and demonstrate its resource benefit; it need not wait for a later larger-scope speedup. The old scatter probe was slower in isolation and needs its own decision. The 0.5 GB figure is a proposed 4096-cell target, not a requirement imposed on smaller ownership-fix cells.

### Phase 2 — let a measured pass stay large

**2.0 Test the actual remaining tail before reducing its candidate size.** The
current `chunk(at:maxChunk:)` tests the full candidate against the query-key
limit; `Generate` only then clips it to remaining IDs. With maxChunk=4096 and
7960 new tokens, current passes are `[4096,2048,1816]`, although the actual
remaining 3864 fits: `3864 × 7960 = 30,757,440`, below `4096 × 8016`.
Tail-aware sizing permits `[4096,3864]` without raising that product bound.
This is a smaller independent experiment than a larger scope; preserve the
per-token/capacity budgets and the numerical rechunking contract. Implement one
shared remaining-aware schedule for generation, reporting and estimation, with
boundary, reused-prefix, sub-256 final-tail and no-completion-room cases. Do not
promise one-third less elapsed time merely because there are two rather than
three passes. The derivation is also recorded in
[[records/analyses/2026-09-04-first-principles-inference-audit]].

After Phase 1's liveness and memory gates, decouple the **outer scheduling cap**
from the **inner attention/indexer workspace budgets**. Today
`AttentionTuning.queryKeyBudget` aliases
`PrefillSchedule.measuredQueryKeyProduct`; raising that shared value raises
both limits and can leave blocking a no-op. Give the inner bounds independent,
measured values and assert they are actually engaged where the outer pass grows.

Re-anchor `prefillCostGB`, context-dependent workspace, prefix costs and
`estPrefillTokS` together from effective configurations. Re-evaluate the current
quarter-budget filter using measured request latency; do not blindly remove it.
The old flat/falling process peaks versus a linear activation model are a
question of liveness, allocator/cache policy and phase high-water, not proof
that either old measurement must be false.

Gate 25 → 8 passes only on a proven constant-4096 configuration at 32,768.
Other tiers get their own predicted schedules and observed record changes.
Measure the longest context whose behaviour is being changed, not merely 8k and
16k while claiming 32k. Preserve context ceilings until their own gates pass.
Keep matched-pool diagnostics separate from matched-total-memory release results.

Regenerate the entire estimator family: `MemoryPlan.expectedPeakGB`, planner
objective, CLI banner, `doctor` and its tier table, API plan JSON, context errors,
`prefill-schedule`, progress ETA, governor controls, measurements and claims.
Tests include tier monotonicity, effective overrides and shrinking/growing state.
The current objective assumes 2000 prompt + 400 reply tokens; test a long first
turn and short cached follow-ups as well. A faster prefill must not make the
common complete request slower by shrinking the decode pool too far.

### Phase 3 — independent request-behaviour fixes

**3.1 Tail scan resistance and decode admission.** A final pass shorter than
256 currently takes `cached/ensure`, ignores `admitOnSweep` and loads many
experts through the decode pool. Moving admission to the preceding sweep does
not solve that churn: the tail can evict what was just admitted. A nonzero
admission count is therefore an inadequate exit.

Compare (a) rebalancing the final two passes so both take the sweep where the
measured envelope allows, and (b) an explicit prefill-tail sweep mode with bounded
expert-row padding. Never pad actual model tokens to reach 256. Preserve the
cheap pool path for genuinely short requests and follow-ups; sweep selection
uses request/scope semantics, never residency. Both changes need numerical gates
because pool and sweep kernels differ. Measure post-prompt pool overlap with the
first fixed decode suffix, admissions, evictions, extra reads, first-token and
48-token decode time. Whole-prompt frequency is not necessarily a better predictor
than suffix frequency: compare current final-pass, suffix and scope-wide admission
from the same trace. Retain the fair quota and avoid eviction of unread residents.
Do not publish the old 24.7%/11.5% affected-length rates without an explicit
prompt-length distribution and actual schedule enumeration.

**3.2 Cancellation retains only a valid completed prefix.** Today early return
loses the state removed by `take`. At an existing full-stack pass boundary,
store exactly `promptIds.prefix(i)` with `state.tokenCount == i`, coherent
GDN/conv/PLE/KV/indexer/MTP offsets, and image segments whose starts precede i.
Retain the full digest metadata for a partly consumed image. Exclude future
images; otherwise `imagesAgree` can reject an otherwise valid shorter retry.
Always restore the allocator cap, `admitOnSweep`, pending I/O/admission state and
stats on every exit, including cancellation, via scoped cleanup.

The state must fit the existing retention budget: a 90%-complete long prompt
can exceed the floor tier's prefix limit, in which case dropping it is correct.
Do not invent rewind to trim GDN. Test cancellation before any work, between
passes, across an image run, with MTP and immediately before first generation.
An exactly full-prompt cache entry cannot hit the current strict-extend API;
retaining final logits or an earlier complete snapshot is a separate explicitly
budgeted extension, not something `store` alone fixes. Repeatability compares
identical resumed schedules first; if cancellation causes new batching, apply
the established rechunk band plus task quality, not universal text equality.

Phase 4 changes the safe boundary: after layer 10 of a scope, only ten layers
have consumed the new IDs. That state is **not a reusable token prefix**.
Checkpoint the previous committed whole-stack boundary, restore on mid-scope
cancel, and discard the unfinished activations; preserve the old boundary at
most, not the percentage printed by work progress. `StateCheckpoint` supplies
the starting machinery but its shared arrays, KV growth and lifetime cost need
validation. Do not allocate per-token GDN snapshots to save partially completed
layer-major work. Poll cancellation between bounded I/O groups/compute tiles;
wait for owned in-flight jobs before releasing buffers or the generation lock.

**3.3 Truthful progress and ETA.** Pass the absolute reused base position to
the estimator. Price a short pass with a measured fixed-overhead plus routed-set
curve, not `tail/full-pass-tok-s`; a tail neither pays zero overhead nor always
reads a full pass's experts. Under layer-major execution distinguish work
completed from tokens committed across all layers. Preserve existing progress
callback semantics and add a compatible stage/work event if needed. Include
startup/queue/vision in user wait reporting without relabelling them as prefill.

**3.4 Effective knob validation.** Refuse or clearly report sub-256 outer chunks
and mismatches across CLI/env/library paths. The schedule command accepts up to
65536 while `PrefillTuning` clamps the environment to 4096; distinguish a
hypothetical schedule from a measured supported runtime shape. Validate inner
query blocks separately, use overflow-safe arithmetic for tokens × context,
and never advertise an ignored override. No performance experiment is needed
to prove these validation/reporting fixes.

### Phase 4 — amortise expert reads across bounded computation

**Dependency:** instrument repair, ownership/capacity checks, and production
workspace/parity gates. Phase 2's new default is not a prerequisite: Phase 4 may
win directly with its own measured budgets. Pure Phase 3 fixes can land first;
compute probes in the alternatives section can run before the structural work.

`Model.hiddenStates` already visits layers in order within a call. Prefer an
internal scope-aware prefill entry point reusing those layer implementations,
not a second model implementation. Keep public `hiddenStates`, diagnostic hooks,
serving APIs and the independently usable CLI/library compatible.

```
for each new-token scope N, from the current committed prefix:
    save a bounded checkpoint; embed/splice this scope
    for layer L:
        run PLE/attention/recurrence/HC in chronological token tiles C
        materialise the scope's MoE input and required residual/injection data
        route those inputs, in bounded projection tiles if needed
        build the layer's canonical expert-row index over the whole scope
        for staged expert group G:
            obtain this group's once-read weights; optionally prefetch G+1
            for bounded routed-row tile R belonging to this group:
                compute grouped gate/up/down; reduce by the canonical policy
            release weights only after all their rows finish
        join bounded shared work submitted during I/O where useful
        finish residual work; materialise next-layer state (terminal demand per OPT35)
    consume final-layer multis through MTP in chronological tiles, if enabled
    commit the scope only when all main and MTP state is coherent
```

P1/P2 above define how bounded shared computation and next-group raw reads can
overlap this schedule without expanding the two-group weight budget. Their
timing benefit is independently gated; a synchronous control remains necessary.

The nesting of G **outside** R is essential: `for token tile { read experts }`
reintroduces rereads. All routes for layer L depend on its attention/HC output,
not directly on input IDs; they cannot be precomputed for later layers before
those outputs exist. The current router already runs once per pass: a scope-wide
router replaces those calls, not an inherently extra matmul. Bound its
`N × 512` f32 logits and retain only needed top-K IDs/weights once feasible.
Preserve the reference's full-precision routing and stable tie semantics.

**Rows, not just experts, must be bounded.** A group of 32 hot experts may own
most of `N × 10` assignments. Retaining all its x/g/u/down rows makes memory
scale with N despite bounded weight staging. Cap R independently, including
padding, output and combine workspace. Keep sorted indices and the pinned
MLX grouped-kernel conditions (at least 16 rows, four per staged expert).
Preserve a canonical computation independent of resident/missing groups. A row
cap changes dispatch/padding and cannot borrow the old parity result untested.

One concrete deterministic option is fixed ascending expert-ID groups containing
both resident and missing experts, assembled into canonical weight-row positions,
with segmented per-token sums in fixed expert-ID order. This is U1 arm B: deterministic and residency-independent, but not the current router-rank reduction and therefore separately numerical. The I/O assembly may
fetch/copy in a different order, but may not reorder reduction. Defer admission
until all resident weights that could be evicted have been consumed, or protect
those slots; if hot candidates must be retained for later admission, charge that
bounded weight buffer. Do not add a hidden reread at admission and still claim
the per-scope once-only bound. Compare the extra copy/segmentation cost with the
full-layer workspace before choosing this option.

**Memory ledger.** One bf16 hyper tensor is 20,480 bytes/token: 167.8 MB at
8192 or 671.1 MB at 32768. That is not the entire new footprint. A straightforward
schedule with two hyper buffers, bf16 x2 and an f32 accumulator already represents
56,320 bytes/token: 461.4 MB at 8192 and 1.845 GB at 32768, before routes,
injection weights, live tile temporaries, shared/GDN projections, masks, staging,
allocator cache, saved rollback state, KV growth, n-gram data and MTP/vision.
U1 requires adding the full N×K×H output axis if arm A retains it; the FP32 accumulator example is not proof that arm B preserves the reference sum. These are explicit-buffer arithmetic examples, not a required minimum or measured
peak; aliasing/reuse can change the ledger only when verified.

Document each buffer's dtype, size, owner, live interval, evaluation and copy
behaviour. Preallocate bounded outputs and prove writes do not copy an entire
scope/pool while an alias remains. Do not collect tile/group outputs and then
concatenate them into another full copy. Reserve rollback and prefix coexistence
before N is chosen. Explicitly budget indexer-cache and KV growth copies;
reserve capacity when the known scope size makes that cheaper and safe, and
never treat a large unused capacity as free. Select N from both target memory and live availability;
use smaller super-chunks when needed, account for their repeated expert scans,
and describe the actual bound. An N=old-pass fallback restores the old execution
path explicitly; merely using equal dimensions in a different algorithm does
not prove bit identity.

**State and feature coupling.** Step each GDN/conv/PLE tile in token order with
absolute offsets; update n-gram history exactly once per committed scope. Feed
MTP tiles only after final-layer multis exist, with previous multi shifted by one,
`mtp.offset == tokenCount - 1`, and aligned image splices. Budget its extra work
and retained multis. Vision runs remain absolute until each consuming tile clips
them; do not infer identity from placeholder IDs. Preserve tower/prefix reuse,
changed-image misses and multi-image tails. Keep speculative recording off during
ordinary prefill. Verify checkpoint/restore after failures in every layer family,
not only after a completed scope. Defer pool resizing to safe request boundaries;
under pressure, abort a bounded scope and recover a coherent state rather than
letting a waiting governor imply protection it cannot provide mid-request.

**Alternative workspace to compare before committing to a global fold.** Once
layer-major activations exist, a temporary workspace holding all *routed* weights
for one layer (at most 1.416 GB) allows chronological token tiles and the canonical
per-tile MoE combine. It can preserve more of the existing arithmetic while
reading each expert once. Fill it in bounded batches, without also keeping an
unbounded staging copy; charge it alongside the decode pool and activations.
It may be unsuitable at the floor but worthwhile at a larger tier. It does not
avoid the cross-layer activation frontier or permit a naive token-major loop to
reuse every layer's weights. Compare this against the small-G, expert-major
row-tiling approach on both latency and total peak before choosing per tier.

*Exit:* for a no-retry main-model sweep, no expert is fetched twice within a
layer/scope, and fetched records are at most scopes × 24,576, excluding explicitly
separated pool-tail/decode work. Freeze stronger fixture-specific ceilings from
real measurements, not arbitrary margins or promised 4×/2.5× ratios. Compare
Phase 4 against the accepted Phase 1/2 path as well as the original baseline so
earlier gains are not counted twice. Require a user-visible latency win past the
A/A spread, bounded peak at the true target, no material fixed-suffix decode
regression, all numerical/ownership/state gates, cold and warm server cells,
MTP/vision integration, cancellation recovery, and full acceptance. A conditional
win stays scoped to the configurations that passed.

### Other angles — ordered experiments, not unmeasured promises

This queue complements fewer reads. A small shape probe and a profile can change
its order; a faster isolated kernel still needs a full-request A/B before shipping.

| Candidate | Why the current code leaves room | Discriminating check / limitation |
|---|---|---|
| Compact carry ownership | Native 1024/2048-token intervention confirms oversized retained buffers | Phase 1.0; allocation savings and final-logit equality established, production state/features, process footprint and latency still to gate |
| Fused SDPA at head dimension 256 | Pinned MLX full-prefill fusion supports 64/80/128; this model uses 256 and materialises dense score intermediates | Prototype an exact-mask tiled online-softmax Metal kernel or a separately pinned upstream candidate; preserve 24-query/2-KV GQA, bf16 I/O, scale, RoPE and causal offsets; long-context parity/state and timing |
| Sparse execution of the existing QSA selection | Indexer retains about a 2048-key budget but downstream boolean-masked SDPA still attends over dense storage | Consume selected block IDs plus own-block tail directly, without a global boolean mask; canonical order, no duplicate keys, correct empty/partial causal sets; do not confuse computational sparsity with a new approximate attention policy |
| Chunk-parallel GDN prefill | Already parallel over heads/value rows, but sequential over T inside each workgroup | P5 derives and algebra-checks the block formulation; benchmark setup plus full GDN layer, fp32 state, long continuation, zero decay and Kahan-sensitive cases; retain decode/MTP paths initially |
| Bound/query-tile fusion before a new kernel | Indexer mask, SDPA, gate and projection are separate full-pass lifetimes | Stream mask → attention → output tile, hoist pooled keys once; prove the working set falls without additional rereads or enough launches to erase the saving |
| Persistent pooled indexer keys | `QSAIndexer` re-pools/norms/rotates all complete raw-key blocks on each invocation | Append only newly completed blocks; retain the incomplete tail; checkpoint/trim invalidates the right blocks; byte parity first, bounded additional cache second |
| GDN/resident projection fusion | inQKV/inZ/inA/inB and shared gate/up issue separate quantized projections over the same input | Test compatible concatenated projections or fused epilogues on the pinned kernels; charge extra/packed weights and do not silently change quantization groups |
| MoE gate/up fusion and row-index overhead | Each group gathers rows and runs gate/up separately, with CPU counting-sort and index uploads | Profile first; preserve `sortedIndices`, canonical ties and bounded R; a custom gather/SiLU/weighting epilogue may remove intermediates without changing expert bytes |
| Specialized bounded selection | Pinned Metal `argPartition` performs a full merge sort for router and indexer | OPT36 / U2; ordered router IDs and backend tie/exception semantics before memory or latency claims |
| Avoid unused final mixing | Intermediate `hiddenStates` calls evaluate the final mixer even when only cache state is needed; MTP needs multis but not every mixed row | Internal prefill API omits unused mixer work while preserving public return semantics; confirm MLX was actually evaluating the work before claiming a saving |
| Prune final-layer token-local work when MTP is off | The final QSA layer needs all K/V/indexer cache writes but only demanded queries and token-local MoE/residual/mixer outputs | OPT35 / U4; earlier layers and all terminal state updates remain complete, MTP/full-output hooks disable pruning, and removed terminal routes need explicit admission/decode checks; one-layer work, no universal multiplier |
| N-gram storage and assembly | CPU f32 row cache uses ~640 payload bytes per row for 160 bf16-rounded values; embedding reassembles a large Swift array | Packed bf16 cache/staging can halve payload, not necessarily total heap; exact dequant/EOS hashing and timing; separate prefetch I/O from CPU assembly |
| N-gram prefetch window | The 400,000-row cache cannot guarantee every row of a whole 32k scope remains resident after bulk prefetch | Keep a bounded window or request-local row arena, avoid evict-then-refetch; flatten/deduplicate in bounded storage; a fully unique 32k prompt can ask for >400k rows |
| Earlier exact n-gram reads | N-gram row IDs depend only on known prompt IDs and history, unlike future expert routes | Bounded prefetch before the PLE layer can overlap earlier compute; no concurrent unsynchronised dictionary writes; measure contention with expert I/O, not merely nominal overlap |
| Request-aware scope/pool trade | Planner assumes 2000 new prompt + 400 reply tokens even on a large prefix hit | Choose read scope after reuse is known while preserving the generation budget; compare TTFT and total request time across long-first-turn and short-tool-loop workloads |
| Temporary use of decode-pool headroom | Prefill and decode need different working sets | Only when measured beneficial: explicitly budget resize transients, avoid double allocation and charge later warmup; do not silently resize an explicit user pool |
| Exact prefix checkpoints | A later edit/retry can reuse only a saved whole-state prefix, not rewind GDN | A separately budgeted immutable checkpoint cache can save more work on branching prompts; exact token/image/model identity, copy-on-write cost and four-conversation retention; not required for N6 |
| Shared computation and bounded read-ahead | Shared work starts late; missing reads start after resident groups; future R tiling can expose new read bubbles | P1/P2 define one MLX owner plus raw-read workers, narrow completion events and bounded lifetime; exact schedule-only parity and next-layer readiness are gates |
| Adaptive I/O run formation | Excluding residents fragments runs; reading small gaps can trade a few extra bytes for fewer calls | Replay the actual scope trace on the target disk, record useful/read-amplified bytes and total latency; speculative extra reads are outside the 68 GB bound |

Three broader alternatives were considered too. **Activation spilling** can
trade bounded RAM for temporary I/O when a larger read scope cannot fit; it adds
reads and writes for every layer frontier and changes the storage bound. At a
32k bf16 hyper frontier, merely writing and rereading one 671 MB tensor over
48 layers is about 64.4 GB of extra traffic, before other intermediates. Compare
that concrete cost with smaller in-memory scopes first, and define cancellation,
cleanup and prompt-data handling before any implementation. **Cross-request
prefill batching** can share expert reads but replaces single-flight scheduling
with several independent state machines, queue fairness and memory reservations;
it is a separate serving proposal, not a free extension of N6. **Lower precision,
expert dropping, approximate routing or prompt compression** change the model
computation/content and need separate quality evidence; none may be hidden in an
exact execution optimisation. Changing the model/backend is outside this plan.

The fused-attention direction is informed by
[FlashAttention](https://arxiv.org/abs/2205.14135); the GDN direction by
[Gated Delta Networks](https://arxiv.org/abs/2412.06464). These papers establish
algorithmic alternatives, not Slotstream/Metal speedups. Do not replace the model
with DeltaNet-2 or another recurrence. Do not simply split 256 head dimensions
into independent 128-dimensional attentions: that changes softmax. The pinned
vector SDPA path supports dimension 256 only with additional sequence/GQA limits;
with a 12× GQA factor its `query_length × factor <= 32` condition permits at most
two queries. This is a small diagnostic alternative, not evidence that an
8-query block reaches fusion or is fast enough for prefill.

A faster storage path or a faster GEMM is no longer the only plausible next
step. The compute queue may dominate after read amortisation, and on a fast SSD
may be worthwhile before it. Use actual GDN/QSA/MoE timing to decide; do not
label the entire engine compute-bound by subtracting overlapping host counters.

### Phase 5 — optional record layout work, primarily decode

The nine-piece layout is three 819,200-byte weights and six 51,200-byte
scale/bias pieces, not nine equal ~307 KB reads. The historical run-length-1
ratio of ×1.94 is a read-path result; it is not a decode speedup. Using the old
44% read-time share as a non-overlapped Amdahl illustration gives about
`1 / (0.56 + 0.44/1.94) = 1.27×`, and even that is not a current prediction.
After Phase 4 changes run density, remeasure before deciding a repack is worth
its roughly 68 GB extra copy, conversion cost and persistent-format maintenance.

If pursued, derive the packed file from the pinned checkpoint with a manifest,
exact hashes, interruption-safe creation and a compatible original-layout
fallback. Keep expert values, routing and decode records/token unchanged. Seek
at least a 1.2× paired decode gain at the target configuration without a prefill
or peak regression; otherwise leave the existing format as default. Preserve
original checkpoint verification and downloads. An n-gram row repack is a
separate storage/custody cost; the tiny-read and packed-cache alternatives above
must be timed before declaring its remaining cost negligible.

### Acceptance and shipping checklist

| Stage | Must establish | Do not accept |
|---|---|---|
| 0 | Frozen fixtures/IDs and code, fail-closed harness, repeatable mechanism counters, genuine A/A and paired A/B, correct memory guard | Null fields, mutable prose, unloaded/cap-short-circuited serving results, mislabeled pools/targets or residual GPU time |
| 1.0/1.1 | Compact owned carry and bounded production PLE/resident work, exact state/output/routing for exact arms | An evaluated view counted as a compact allocation, synthetic-only parity or a traced peak used as an untraced one |
| 1.2/1.3 | Bounded mask-to-attention flow and expert-row/reduction workspace, preserved numerical and residency contracts | Full-mask concatenation, unbounded hot-group rows, atomic-order or resident-order dependence, historical synthetic spread called logit error |
| 1.4 | Active-plus-retained budgets hold on both cache hits and misses, including growth and enabled features | Charging token offsets as allocation capacities or relying on an old margin after expanding N |
| 2 | Remaining-aware schedule tested against the unchanged bound; measured effective schedule at each claimed tier/context, independent workspace bounds, consistent estimator family | Two-pass tail arithmetic called a speedup, 25→8 demanded at the floor, a shared threshold raised on both sides, or 16k evidence presented as a 32k gate |
| 3.1 | Tail no longer destroys useful decode warmth; fixed-suffix and full-request nonregression | Merely observing admission before the tail flushes the pool |
| 3.2 | Exact committed state retained when eligible, cleanup on every exit, bounded cancellation latency, repeated continuation valid | Partially advanced layer state cached as a token prefix, retention outside its budget or unconditional resumed text identity after rebatching |
| 3.3/3.4 | Base-aware honest progress, effective validated knobs, overflow-safe scheduling | Work-through-layers reported as fully committed tokens or unsupported settings shown as real configurations |
| 4 | Per-scope fetch uniqueness/bound, incremental and total latency gain, peak at matched total memory, complete request/feature gates | Bytes alone called speed; predecessor gains counted twice; unmeasured whole-prompt footprint |
| P1/P2/P3 overlap | Exact same-shape output/state/routing, ownership and cancellation closure, bounded total I/O/queues, earlier next-layer readiness and matched-memory full-request A/B | More threads or nominal overlap called a win, a worker blocked in global MLX eval, resident eviction changing arithmetic, or a GPU-in-use buffer recycled early |
| U1 reduction/workspace choice | Explicit A/B/C ledger, adversarial routing liveness, reference-order or declared numerical gate, matched-budget scope comparison | N×K×H placement called R-bounded, fixed expert order called the existing sum, or payload crossover called measured performance |
| OPT35/36 added work removal | Required state materialization, terminal MTP/diagnostic dispatch, exact ordered selection/ties and full-request checks | Lazy graph nodes counted as removed execution, unchanged set called unchanged router arithmetic, or CPU identity called GPU parity |
| P4/P5 compute alternatives | Complete-phase mechanism proof including preparation, long-context state/quality and full-request A/B | Synthetic FP64 algebra called production parity, ignored zero decay or a paper's CUDA/training result called a Metal inference improvement |
| 5 | Byte-preserving format, verified recovery/fallback, decode gain beyond noise and paid disk/memory cost | Read-layout microbenchmark ratio called an end-to-end gain |

Install a deterministic fixture-specific expert-read ceiling in `verify.sh`
after Phase 4 is measured, plus a structural per-layer/scope duplicate-fetch
assertion. Keep performance regressions in the controlled benchmark lane rather
than a noisy universal CI tok/s cutoff. Validate rejected controls, prefix reuse,
MTP, vision, HTTP disconnects and metadata responsiveness as well as numerical
checks. One `Tools/verify.sh` run at its small standard sizes is the final engine
acceptance; broader cases are justified by the specific changed paths above.

Update the canonical records and regenerate `PLAN.md`/`MEASUREMENTS.md` with
`Tools/projections.py`. Any earned public number moves its claim and all surfaces
in the same change; update `llms-full.txt` only when its source docs change.
Correct misleading code comments when their implementation phase lands, including
the old bf16-sum claim in derived prose, chunk equality, the supposedly detached
MTP slice, uniform expert coverage, equal-size record pieces, old sampler/rebuild
comments encountered in touched paths, and partial-pass ETA. Preserve immutable
source evidence and historical nulls; link corrections instead of rewriting runs.

The September 4 reviews leave **Phase 0 instrument repair, production integration
of the demonstrated Phase 1.0 ownership fix, and the remaining-aware tail schedule**
as the first implementation steps. Small P1/P2 overlap and P4/P5 attention/GDN
probes can proceed independently; measured exposed stalls determine which to
integrate next. The broad scope design remains the main read-reuse experiment.
The reviews do not mark an engine feature or speed claim delivered. Repeated
passes covered causal dependencies, resource contention, buffer ownership,
numerical contracts, zero-decay/partial-block cases, budget arithmetic and whether
each proposed gate can distinguish useful overlap from merely more concurrency.

### Configurable context window — 2026-09-06
**Status: software implementation and proxy acceptance delivered; native qualification and release deferred (2026-09-07).** This is the
engineering execution authority for configurable context. The complete review
was handed off on approval to implement all phases. The command-center record
becomes a rationale/status pointer. No private brain content is required here.

The implementation is being developed against an isolated snapshot of HEAD
`cf5bc50c11aed8eb7b01f1d5d7b9270d1e20dc7d` and the concurrent optimization
source closure, preserving other sessions' work. The public implementation
ceiling stays at 65,536 until the full qualification gate passes. The default
remains 32,768. Hermes's bounded request override remains supported.

Source ownership: this feature owns the context delta across planning,
governor, request control, generation, CLI and dialect delivery. The separate
[[records/plan/whole-engine-optimization-2026-09-04]] owns optimization candidate
activation. Builds and model runs share the existing single-process lock;
measured runs may not overlap. The final delta is reviewed against intervening
shared changes before landing on main.

P0 baseline evidence: [[sources/runs/2026/09/2026-09-06-configurable-context-baseline]].
`Tools/fixtures/context-default-v1.json` freezes an explicit stable allocation
projection from the installed 0.2.10 binary. Four simulated tiers each returned
identical JSON on two runs. New policy fields have an explicit additive schema.
This is regression evidence, not full-window capacity qualification.

The following reviewed contracts and C01–C22 acceptance cases remain binding.
No phase is closed merely because its code has been written.

## 1. Product and compatibility contract

The goal remains a **32768-token default**, with an explicitly chosen server
or `run` window up to the pinned model's **262144-token configured limit**
when the implementation and machine can support it. No automatic context
shrinking/growing, model catalog, RoPE extension, implicit summarization,
history truncation or sliding-window semantics are introduced.

Use separate, named values:

- **Model limit:** read from, and validated against, the pinned checkpoint's
  configuration. This is a configuration limit, not a quality guarantee.
- **Implementation limit:** highest window the current schedule, capacity
  accounting and release qualification support. It remains 65536 until the
  relevant gates pass; the full goal is not complete while this is below 262144.
- **Configured window:** user's startup choice, never silently lowered by the
  planner, governor, lazy vision load or client discovery.
- **Request window:** at most the configured window. Preserve existing OpenAI
  `options.num_ctx` and the ordinary unset/null behavior.
- **Measured evidence:** actual prompt and output counts, exact build/model,
  options, hardware and measurement method; never a universal scalar called
  "verified here" on a machine where the test did not run.

Context counts the final tokenized input, including templates, system/tool
history and expanded image tokens, plus generated tokens. Retain the existing
completion-budget clamp to available room. Over-limit input fails before model
work; it is never silently trimmed. Preserve the legacy zero-room behavior
where already documented, while OpenAI continues to reject a request with no
reply room. Freeze those exact boundaries rather than accidentally changing
them while sharing the admission code.

Keep the published `ContextPolicy.maxTokens`, `defaultTokens`, existing
initializers and planner overloads source-compatible. Add named fields and
overloads rather than requiring the old plan's destructive symbol rename.
Existing overloads delegate with the actual legacy default; all engine paths
use the new explicit configuration. A Swift consumer compatibility fixture is
required, not only successful compilation of this repository.

`Engine.maxContextTokens` is currently publicly mutable. New configuration must
be validated before allocation. Preserve source compatibility for legacy
callers, but detect an invalid assignment or an attempted increase beyond the
engine's allocated plan before generation and return a typed failure through
the checked API/legacy result. Do not merely alter metadata or retain an
unchecked setter as a way to bypass the limit. This is not a hot-resize API.

## 2. Shared memory and feasibility model

One pure calculation owns planning, feasibility, `doctor`, live metadata,
lazy vision reservation and governor controls. It prices the same controls
the allocator uses. Compute in checked integer bytes where possible and only
format GB at the boundary. Rounded JSON must not decide fit.

The ledger distinguishes:

1. Fixed resident/runtime allowance, including the already-budgeted default
   active state and one fixed recurrent state.
2. Active **allocated sequence capacity**, including allocation rounding,
   indexer storage and the MTP sequence when loaded; charge growth above the
   included baseline once. Reuse the capacity arithmetic already present in
   `Qwen4ExpModel.sequenceCapacityBytes`, extracting a pure geometry helper if
   needed so planning requires neither weights nor a model allocation.
3. The four-entry cache's shared retained-capacity ceiling and up to three
   additional fixed recurrent states; actual runtime charges must be bounded
   by the same reservation. Count prompt checkpoints and their logits too.
4. The selected prefill/attention/indexer workspaces, expert staging, allocator
   cache envelope, and separately qualified long-context transient reserve.
5. MTP resident weights, lazy vision resident/load/workspace costs, and other
   resolved runtime reservations. Flags must not make charged controls diverge
   from active controls or take the same credit twice.
6. The existing planning margin and current-availability slack, each once and
   with its own meaning. A declared target is not available physical memory.

The current `extraContextMemoryGB` derives its reserve from `maxTokens`.
Decouple the measured reserve anchor from the public maximum **before raising
that maximum**: otherwise changing one constant reprices the existing 65K
configuration without evidence. Preserve its established envelope until a
replacement measurement justifies a different one. Do not interpolate an
unmeasured transient peak or credit default-off optimizations as free memory.

Every path through `prefillChunkFor`, `slotsForTarget`, `prefixCacheGB`,
`finish`, `expectedPeakGB`, runtime-policy application and governor fallback
must receive the resolved cap and use the same ledger. A locally correct
`finish` alone leaves the actual pool selection incorrect.

Resolve a **feasibility result**, not the old one-line inversion of
`target - minMemoryGB`. It contains the requested window, maximum feasible
window under the same policy, limiting resource, selected controls and byte
breakdown. Determine it using the real discrete candidate calculation;
binary search is allowed only after proving its fit predicate monotone.
Clamps, rounded capacities, minimum pool, MTP choices and pass transitions must
not make the reported maximum accept a configuration the planner rejects.
Pool slot count itself need not be monotone in context: a smaller prefill pass
can free room for more slots. Account for the total, not that misleading proxy.
Report this as a memory-feasible ceiling; it is distinct from the cold-prompt
length allowed by the time policy. `doctor` shows both costs without implying
that memory feasibility guarantees admission under the deadline.

Policy handling is explicit:

- **Auto:** preserve the selected window and existing RAM-share/working-set
  policy. Adjust pool, pass, retention and optional MTP around it; refuse if
  no feasible configuration exists. Never take `.hold` after an infeasible
  replan as permission to start another unsafe request.
- **Total-memory target:** fit the whole resolved plan inside that target.
  Also check real reclaimable memory before allocation; a large explicit
  target does not defeat the request safety guard.
- **Pinned experts/pool:** honor that pool, calculate full cost, and refuse a
  raised context it cannot support. Do not quietly shrink the requested pool.
  Preserve the documented legacy advisory behavior at ordinary caps; the new
  request guard still applies even with explicit sizing or `--no-elastic`.
- **MTP:** explicit `on` fails if it cannot remain on; `auto` may choose off
  with a note explaining the context tradeoff. A live governor never unloads
  an already resident head by pretending it is off in the next plan.
- **Vision:** replan before the first tower load with the same cap, respecting
  current donated pool capacity. If the image/window combination cannot fit,
  reject the image request and leave ordinary text usable at the same cap.

Qualification may initially support 262K text with MTP off while retaining a
smaller qualified range for other modes. Freeze and publish those mode limits:
forced MTP at an unsupported startup cap fails, automatic MTP explains why it
stays off, and an unsupported long image request fails before tower work. Never
advertise an unqualified 262K vision/MTP capability from the text result or
silently reduce the server's configured text window. This disposition must be
explicit in C12/C19 before a text-only qualification can close P5.

For governor credit, use a coherent snapshot of memory actually owned and
releasable, distinguishing it from unused reservation and memory already
included in reclaimable availability. Credit each byte at most once. Include
held additional sequence capacity when appropriate; never add the worst-case
future context or a no-longer-live prefill transient merely because it is in
the plan. Preserve the conservative fixed/resident treatment until its own
replacement is measured. Test no allocated long state, filled state, prefix
eviction, allocation rounding, pressure shrink and recovery. Repeated replans
must converge without growing into reservations or progressively ratcheting down.

Refund below-default **retention** over-reservation as a correctness fix.
Refunding the baseline active-context allowance is separate, deferred work:
the fixed footprint has not been proved to shrink by exactly 27648 bytes per
token. It is not required for completing the main 262K feature.

## 3. A safe prefill schedule before opening 262K

The selected path is to keep every actual query-by-key product within the
existing bound and support smaller late-context passes. At the model ceiling,
a 128-token pass is too large for that bound; a power-of-two schedule reaches
64. Passes below 256 use a different existing execution/eviction path and lack
current throughput anchors, so they require component correctness and memory
qualification. The comment about the floor is not proof either of feasibility
or impossibility: inspect the path and test it.

Implement the bounded candidate schedule first behind diagnostic qualification.
Cover fresh prefill, a reused prefix at an arbitrary position, non-power-of-two
tails, MTP, image boundaries and any enabled grouped read scope. A read scope
cannot turn several bounded passes into one unbounded attention operation.
Check `from + tokens`, products and capacity rounding for overflow before
allocating a pass list. No exemption of the form `product fits OR chunk ==
minChunk` is permitted in the new full-window gate.

Measure valid sub-256 cost anchors before pricing them. If the selected path
fails correctness or acceptable bounded operation, keep the public cap at the
qualified limit and revise P4 using evidence; do not raise the product bound or
claim completion. A tiled attention alternative belongs to the existing
optimization program and needs its own measured integration, not an assumed fix.

`context-check` must request the candidate cap through the same planner before
loading. A diagnostic-only checked path may plan above the currently published
implementation limit, up to the model limit, to avoid a circular qualification
gate. It still enforces physical feasibility and cancellation. Remove the
post-load `engine.maxContextTokens = target + 16` escape and the `maxTokens * 8`
input bound. Explicit prompt plus reply reserve must fit the model limit;
the diagnostic must actually complete the requested prompt and output before
reporting `fits`. Partial work and allocation failure are counterexamples.

## 4. Admission, wait and interruption contract

Expose `--max-context` and `--max-prefill-wait <minutes>` consistently on
`serve`, `run` and `doctor`, backed by shared library configuration. Proposed
wait default: **30 minutes**, `0` disables only the time policy. Reject NaN,
infinity, negative values and duration overflow before loading. Neither `0`
nor `--no-elastic` disables memory or context bounds.

Define the wait budget as **request-to-first-model-token wall time after the
complete request is accepted for processing**: it includes queueing, prompt
preparation, image work and prefill, excludes request upload, model startup and
decode after the first sampled token. CLI starts at its equivalent post-load
request boundary. A monotonic clock controls it. Publish queue/preparation and
prefill timings separately; neither keepalives nor reasoning visibility resets
the clock. A queue wait must remain cancellable rather than taking an
uninterruptible generation lock.

The estimate is an early refusal aid, not the deadline or a guarantee. At the
generation lock, resolve the actual current plan and exact reusable state,
then price only missing input tokens **from their absolute context position**.
Include known preparation work and elapsed queue time. If the estimate exceeds
the remaining budget, reject before prefill; the wall-clock deadline catches
underestimation. An unknown estimate must be labeled unknown and remain subject
to the deadline, never represented as zero or measured throughput. Cold input
can fail while a valid reused continuation succeeds. If a cache lookup is only
a preview, revalidate/reserve it under the same lock before admitting work.
The controller must exist before preparation begins. Compute bounded expanded
image/token geometry before expensive tower inference; pass cancellation into
the preparation path itself. A guard first created inside `generate` cannot
retroactively bound earlier image work or queueing.

Default preservation means the same context default and ordinary successful
behavior/allocation, not a promise that a new 30-minute deadline never affects
an old request. Slow storage, queueing and other apps can exceed an M5 estimate.
Document the intentional new bounded failure behavior and exercise both 32K
and the already-supported Hermes 65K path. No stale 13.7-minute figure is a proof
that the default cannot fire on every machine.

Add a typed request outcome/controller shared by `Engine` admission and the
generator's existing cancellation seams. Distinguish pressure/insufficient
memory, deadline, client cancellation, inference error and successful stop.
Do not make a generic `shouldContinue == false` mean normal completion.
Preserve legacy nonthrowing APIs with additive error/status data or checked
variants; do not turn recoverable input/resource errors into a process trap.

Memory checks run before image/tower allocation, state reservation, each bounded
prefill pass/scope, and during decode at safe commit boundaries. Test sufficient
headroom for the **next allocation** plus slack, not just a fixed free-memory
threshold after allocating. Unknown memory readings cannot authorize new
above-default context growth. A checkpoint check cannot guarantee that another
app will never allocate concurrently; say that honestly and bound the work until
the next cancellation point. Preserve the existing responsive-governor seam
without enabling unrelated experimental optimizations as a side effect.

On abort, join readers and synchronize outstanding GPU users before releasing
buffers/pins. Memory-pressure abort drops active/retained state needed for
recovery and must not immediately store the partial state back into the cache.
Other cancellation may retain only a validated whole-stack checkpoint within
the budget; no incomplete recurrent/MTP/image state can be reused. No tool
proposal from an unsuccessful turn becomes executable. The next short request
must either run normally or receive an explicit still-unavailable error.

### HTTP and client behavior

| Failure point | Required behavior |
| --- | --- |
| Invalid cap/input or predicted wait above policy, before headers | Typed HTTP 400 with actual limit/estimate and actionable remediation |
| Memory/deadline refusal before headers, including a queued request | Typed HTTP 503 naming the resource or deadline; no generation |
| Memory/deadline/inference failure after streaming headers | Dialect-valid terminal error, then close; never try to change HTTP status |
| Client disconnect | Stop bounded work and release state; do not attempt a success response |
| Normal completed inference | Existing dialect finish semantics, exact usage and bounded output |

Freeze codes such as `context_length_exceeded`, `prefill_wait_exceeded`,
`insufficient_memory` and `prefill_deadline_exceeded`, with elapsed/limit/estimate
fields when available. Keep existing unknown/inference errors distinguishable.
Stream headers must follow checked admission; errors after
that point still require the in-stream path. Failed OpenAI turns emit no
successful finish or success `[DONE]`; Ollama emits an error without a normal
`done: true`; the gateway emits its defined failure terminal. Preserve the
error-before-success behavior already added in the working tree.

Expose machine-readable policy/estimates before work through `doctor` and
discovery. Keep logs/progress current after governor changes; do not capture
the startup chunk forever. Existing SSE keepalives preserve transport liveness
but are not ETA or evidence the model progressed. Do not insert arbitrary
progress JSON into a client's content stream. Document required client timeout
settings, including Hermes, without expanding this feature into a new job API.

## 5. Implementation order and gates

These are dependency gates, not time estimates. P0–P4 can improve ordinary
and Hermes serving while the public ceiling stays at 65536. P5 opens larger
windows only after qualification; P6 releases the complete feature.

| Phase | Work | Exit evidence |
| --- | --- | --- |
| P0 — freeze and handoff | Capture actual source/binary/model identities and current successful/error fixtures; project this engineering plan into Slotstream's `records/plan/` without private paths; make this record a pointer at handoff | Four deterministic default-tier snapshots, source-compatible consumer fixture, dialect baselines and named ownership of overlapping files |
| P1 — accounting | Shared byte ledger, explicit cap threading, feasibility result, governor ownership credit and unavailable-state handling | C01–C05; unchanged frozen default allocation fields; exposed original counterexamples now pass |
| P2 — admission/API | Shared context/wait configuration, `run` parity, checked engine admission and metadata | C09–C12; existing library callers and Hermes remain compatible |
| P3 — interruption/wire | Memory/deadline controller, cancellable queue and safe cleanup, typed errors in all dialects | C13–C17; fake failures plus a small live interrupted request and successful recovery |
| P4 — schedule | Bounded late-context passes and checked diagnostics; measured costs and memory for the new path | C06–C08; no unqualified product or fabricated throughput anchor |
| P5 — full-window qualification | Preregistered incremental capacity runs against the exact candidate, then raise the implementation limit to the maximum proved within the model limit | C18–C20; all claimed endpoint/configuration combinations have evidence; no claim of full completion below 262144 |
| P6 — documentation/release | Update authoritative records and public surfaces; complete relevant existing acceptance, installed-binary and client gates; publish through normal CI workflow | C21–C22; released and installed build identity, compatibility and limitations recorded |

### Acceptance cases

| ID | Required case and observable result |
| --- | --- |
| C01 | Default 16/24/32/48 GB simulated tiers: pin RAM, working set, availability, model capabilities, MTP, vision and environment. Canonicalize an explicit versioned projection of existing allocation/sampling fields; repeat twice and compare byte-for-byte. Additive fields are separately schema-checked. Never refresh a failing golden automatically. |
| C02 | Cap matrix 1, 1024, 4096, 8192, 32768, 32769, 65536, 128256, 131072, 262144 and neighbors; floor/knee/busy targets, MTP off/on/auto, vision absent/allowed/resident, cache enabled/disabled and pinned pool policies. Accepted plans fit exact ledger limits; rejected plans name their actual constraint. |
| C03 | Solver maximum is accepted and its next token is refused under identical fixed inputs, unless already at model/support maximum. Include allocation-step boundaries and forced MTP failure. No expected answer is hardcoded from RAM-tier marketing. |
| C04 | Prefix reservation equals runtime's bounded allocated-capacity cost; lowered caps recover retention over-reservation; raised caps never spend that reserve on experts. Four interleaved conversations, prompt checkpoints, image histories and MTP capacity are covered. |
| C05 | Governor under quiet/busy/pressure/recovery inputs preserves the cap and loaded components, credits only owned/releasable memory once, converges, respects cooldowns and blocks new work when infeasible. Test both no long state and allocated long state; old 1024-cap tests alone are insufficient. |
| C06 | Every actual schedule pass from arbitrary prefix positions satisfies the product bound, including the final pass at 262144, 64/128/256 transitions, short tails and any enabled scopes; totals are exact with no floor exemption. |
| C07 | Sub-256 model path: deterministic repeatability, bounded state/capacity, correct offsets, MTP rollback, prefix extension and numerical behavior within the preregistered rechunking control. Do not require byte-equality across different batch geometries or loosen tolerances after observing output. |
| C08 | Diagnostics reject negative/overflow/out-of-model lengths and missing reply reserve before load, charge the candidate context before allocation, enforce the same guard, and mark abort/incomplete prefill/no output as failure. |
| C09 | `serve`, `run`, `doctor`, direct checked Engine calls and applicable legacy adapters resolve the same configuration. An invalid legacy cap assignment cannot allocate or silently alter the advertised server contract. |
| C10 | Final templated and image-expanded token counts govern cap-minus-one/exact-cap/cap-plus-one requests. Output clamp and zero-room behavior match each frozen public contract. Integer overflow and null/default forms fail or pass deliberately. |
| C11 | Hermes/OpenAI bounded `options.num_ctx` passes at/below the served cap, rejects zero and inflation, and never changes another request's cap. Ollama still rejects unsupported non-default controls; gateway output allowance stays valid. |
| C12 | `doctor`, `/api/show`, `/v1/models` and gateway catalog report the configured usable window rather than the model maximum. Policy and evidence metadata are additive and consistent after resizing and lazy vision loading. |
| C13 | Fake monotonic clock proves estimate refusal, deadline despite a too-fast estimate, zero-disabled time policy, queue/preparation accounting, no timer reset on keepalive, cancellation before first allocation and no timeout on a valid fast reused continuation. |
| C14 | Fake availability/pressure at admission, image work, each prefill boundary and decode causes typed failure with bounded stop latency. Fixed-size, no-elastic and unreadable-memory paths remain covered without inducing real system pressure. |
| C15 | Cleanup proves joined readers, completed GPU users, released pins and restored allocator limits; invalid state is not retained, pressure-aborted state is not immediately re-cached, and a fresh short request recovers. |
| C16 | HTTP/SSE/NDJSON tests before and after committed headers for all dialects: right status or terminal error, no success tail, no extra output after terminal, disconnect/backpressure does not hang. Existing successful text/tools/vision behavior survives. |
| C17 | Strict consumer fixture rejects tool calls followed by error, timeout, EOF or `length`, even when arguments are valid JSON. Engine release gates its own terminal contract and existing real clients. The actual db.md/Rust receiving-side gate remains owned by Sevra H2/F2; it is required before claiming that end-to-end integration safe, not a new dependency on shipping Sevra Desktop. |
| C18 | Increasing supported prompt lengths with reply room, including a near-ceiling prompt and nontrivial decode reaching the configured total cap. Preserve exact tokens/configuration, full completion, sampled physical footprint, RSS, MLX peaks, swap and abort state. A one-token 65K diagnostic does not prove all decode/vision/MTP combinations. |
| C19 | Cold state and already-filled/interleaved retention; minimum-feasible, pass-transition and ordinary targets; MTP and vision combinations actually advertised at the new cap. Unsupported combinations fail explicitly rather than inheriting a text-only measurement. |
| C20 | Live small controlled deadline/guard interruption and recovery; one model process at a time with real reclaimable memory checked and several GB spare. Use injected guard triggers, not a memory hog or simulated RAM to authorize real allocations. |
| C21 | Existing relevant `brain_gates.sh`, `planner_gates.sh`, `sampler_gates.sh`, `static_gates.sh`, catalogue, prefix/MTP/vision/HTTP suites and installed `e2e_release.sh` pass on identified candidate builds. Claims and generated docs agree. No generic passing suite is substituted for C01–C20. |
| C22 | Public Swift consumer, actual Hermes and applicable Ollama/gateway clients; ordinary 32K and existing 65K remain usable. CI release, installer, version, binary/model hashes and a rollback preserving the prior 65K-capable install are recorded. |

P5's protocol is frozen before measurements: candidate source/options, lengths,
memory targets, MTP/vision/cache modes, required output tokens, hard stop
conditions and per-run time ceiling. Advance one rung only after the previous
one completes within its budget; stop after a failed rung and preserve it.
Wait may be disabled deliberately for a bounded diagnostic, but the independent
measurement wall-clock ceiling and memory guard remain active. There is no
obligation to hold a loaded model for hours merely to label the plan reviewed.

A complete-window qualification separates **capacity/memory**, **latency
estimate calibration**, **numerical parity**, and **answer quality**. It does
not claim quality from a synthetic filler; general long-context quality work
remains separate. No default changes or planner credits are earned by unrelated
experimental optimization results.

## 6. Documentation, ownership and completion

Update hand-edited `README.md`, `docs/CLI.md`, `docs/API.md`, `docs/HERMES.md`,
`docs/TROUBLESHOOTING.md`, `docs/HARDWARE.md`, `docs/TESTING.md`, `CHANGELOG.md`,
`llms.txt` and the context rule in the repository's `CLAUDE.md`/`AGENTS.md`.
Generate `MEASUREMENTS.md` and `PLAN.md` from the engineering db.md records;
generate `llms-full.txt` through its existing script. Source evidence stays
append-only, with measurements, public-number claims and release references
updated together. Do not leave a shipped context feature under `Unreleased`.

Coordinate the owning changes with the current whole-engine optimization plan
(`db/records/plan/whole-engine-optimization-2026-09-04.md` in Slotstream).
Reuse its capacity, checked-read, cancellation, output and prefix primitives;
do not enable its full candidate bundle to implement this feature. Freeze the
actual source closure, preserve other sessions' edits, and rerun only affected
acceptance when overlapping code changes. H1 owns the common engine turn;
H2 owns safe consumption; this plan owns configurable context and its guards.

**Completion requires every applicable C01–C22 gate and a qualified
262144-token implementation limit.** The current 65K feature, this design
review, a simulated acceptance, or an updated status field is not completion.
The below-default active-state credit remains explicitly deferred. If measured
hardware or latency prevents full qualification, report the precise partial
limit and preserved counterexample instead of changing the goal silently.


### Implementation evidence and open gate (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-components-v1]] records
stable installed-default projections, shared accounting/admission/controller
implementation, and passing component/HTTP cleanup checks after fixing retained
expert pins and vision budget fit. These are bounded implementation gates;
they do not close the whole C01–C22 matrix. Public source compatibility and
remaining live mode/lifecycle checks are still pending.

[[sources/runs/2026/09/2026-09-06-configurable-context-small-pass-counterexample]]
and [[sources/runs/2026/09/2026-09-06-configurable-context-small-pass-diagnosis]]
preserve failed numerical gates and the mechanistic follow-up. The original
small-pass path and its grouped-expert successor both fail the frozen state
and continuation contract. A four-layer probe finds exact first-layer inputs
and attention but differing expert output. Routing/shared-expert shape
controls are being investigated; no thresholds or frozen outputs changed.
The observations in [[sources/runs/2026/09/2026-09-06-configurable-context-observations]]
exclude contaminated timing/capacity inferences. P4 remains open. P5 has not
started; the public implementation limit stays 65536. P6 publication and
installed-client acceptance remain open. This feature is not complete.

### Expanded component checks and preserved counterexamples (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-components-v2]] and
[[sources/runs/2026/09/2026-09-06-configurable-context-cli-policy-and-driver-v1]]
record the expanded CPU/governor/CLI contracts and the capacity driver's
fail-closed rejection tests. They preserve the ordinary allocation fixture.
The expanded live HTTP matrix is still pending. Source review subsequently
moved HTTP request acceptance before JSON parsing and put CLI prompt-file
reading and cumulative image preparation under that same request's guards;
those later changes are not covered by build 11.

[[sources/runs/2026/09/2026-09-06-configurable-context-router-shape-counterexample]]
and [[sources/runs/2026/09/2026-09-06-configurable-context-key-domain-counterexample]]
preserve the router-padding and reference-key-domain successors. Both retain
nine continuation/rollback failures under the original C07 criteria. Candidate
repeatability passes but does not override those failures. A prospective
successor preserves the reference row shape of token-wise resident projections,
including the plain indexer projection. It never pads recurrent updates or
logical attention query rows. Its additional workspace is explicitly priced.
It remains unbuilt and unqualified at this entry; no numerical tolerance or
expected token was changed. P4, P5, and P6 remain open, and the public cap stays
65536. All overlapping-run timing and capacity interpretations are excluded.

### First exact small-pass witness and HTTP pressure correction (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-projection-shape-first-pass]]
records build 12 passing all 1605 frozen assertions for the first 64-row
projection-shape witness, with every recorded candidate drift exactly zero.
This supersedes the preceding unbuilt status for that exact witness only.
The 128-row, partial, reused-prefix, short-tail and sparse-indexer cases remain
pending. Later source edits preserve the original 256-row floor at ordinary
caps, align short terminal router projections, and bound late passes against
their actual reference key domain; these are not yet built or verified.

[[sources/runs/2026/09/2026-09-06-configurable-context-http-pressure-counterexample]]
preserves 114 failures in the expanded build 11 HTTP matrix. Build 12 fixes
the supplied-controller pressure bypass and the actual-length prefix fixture,
but the corrected expanded matrix still requires a live run.
[[sources/runs/2026/09/2026-09-06-configurable-context-components-v3]] records
build 12's 21171 CPU, 95 CLI and 64 planner assertions. These passing component
checks do not replace the open HTTP, numerical or public-consumer gates.
All overlapping-run timing and capacity interpretations remain excluded.
P4, P5 and P6 remain open; implementation limit 65536 and default 32768 persist.

### Expanded HTTP pass, six exact witnesses and remaining tail failure (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-http-pressure-correction]]
records build 13 passing the corrected 835-assertion HTTP matrix, with CLI95
and planner64 also passing. The 114-failure predecessor remains preserved.
[[sources/runs/2026/09/2026-09-06-configurable-context-projection-prefix-pass]]
records both 64/128-row candidates passing all1605 frozen assertions, with
zero candidate drift, for each ordinary, partial-terminal and reused-prefix
witness. The sequence then stopped on two failures in the one-row-tail case:
[[sources/runs/2026/09/2026-09-06-configurable-context-short-tail-counterexample]].
No sparse-prefix witness or capacity rung ran in that sequence.
The eight obsolete minimum-floor test expectations are separately preserved in
[[sources/runs/2026/09/2026-09-06-configurable-context-floor-fixture-counterexample]].

A prospective successor prices separate physical replacement allocations for
main, draft, raw and pooled caches, guards the complete provisional draft span,
and atomically reserves concurrent preparation and pending dispatch ownership.
Prepared-image ownership retains its reservation through queueing and use.
Tiny matrix-prefill tails pad only attention query work, crop before state,
and report physical query rows independently of logical token counts.
Those later changes require new identified CPU, live HTTP and numerical runs.
No failed criterion was loosened. P4/P5/P6 remain open, with default32768 and
public implementation65536 unchanged. Overlapping-run timing and capacity
interpretations remain excluded.

### Allocation ownership and complete bounded shape matrix (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-allocation-ownership-pass]]
records build 15 passing 22191 T0, 95 CLI, 64 planner and 835 HTTP assertions.
Per-buffer replacement guards, provisional MTP span bounds and atomic
simultaneous preparation/dispatch reservations now have bounded regression
evidence. Actual MTP-head and large-context capacity remain separate gates.

[[sources/runs/2026/09/2026-09-06-configurable-context-short-tail-and-sparse-prefix-pass]]
records four more C07 cases, each passing all 1605 unchanged assertions.
The 64-row short-tail case has one nonzero logits measure within the frozen
band; every greedy token matches. With build 13's six exact cases, this
completes the bounded predeclared shape/prefix matrix for the diagnostic family.
All overlapping timing and capacity interpretations are excluded.

Normal Generator family activation, exact runtime-control binding in the
capacity driver and odd late-chunk canonicalization are later unbuilt edits.
The optimization owner has three-way merged the extracted non-numerical v3
component into newer shared source; that combined source requires its own
build and regression gates. P4 integration and P5/P6 remain open. The default
32768, public implementation ceiling 65536 and separate MTP/vision caps persist.

### Combined source and actual MTP cancellation correction (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-combined-shape-and-http-pass]]
records all ten fixed C07 witnesses on build 16, each passing 1605 unchanged
assertions, plus 22249 T0, 102 CLI, 64 planner and 835 HTTP assertions. The
combined source preserves the optimization owner's V176 changes. The following
[[sources/runs/2026/09/2026-09-06-configurable-context-mtp-cancellation-exclusivity-counterexample]]
then stops that batch: caller cancellation touched stats while speculative
decode held an exclusive inout borrow. The failed binary and stderr remain.

Build 17 separates callback cancellation from the borrowed statistics and adds
real typed MTP cancellation at zero, one and three delivered tokens, cleanup
and recovery. The corrected actual MTP-retention gate passes 198 assertions;
lifecycle passes 138. Remaining tests in this identified batch are ongoing.
The exact public e498b2d transport source and published Hermes documentation
are incorporated in the isolated candidate, preserving the reserved v0.2.11
release. No shared source, public tag or installed binary is changed here.
The optimization owner independently applied the minimal correction to shared
v3 source. All download-overlap timing and capacity interpretations are excluded.
P5's quiet full-window and retained-state capacity runs have not started; P6
public consumer, actual clients and release/installed acceptance remain open.

### MTP recovery pass and bounded pressure follow-up (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-mtp-cancellation-recovery-pass]]
records build 17 passing actual MTP retention/cancellation/recovery (198),
lifecycle (138), T0 (33 groups / 22249), CLI (102) and planner (64).
[[sources/runs/2026/09/2026-09-06-configurable-context-unchanged-adaptive-mtp-counterexample]]
preserves the sole, exact known V50 adaptive-candidate mismatch. That option
remains disabled. The separate
[[sources/runs/2026/09/2026-09-06-configurable-context-governor-invocation-prerequisite]]
is an invocation error, followed by prospective pressure/recovery fixture and
observability corrections. Build 18 is pending verification at this capture.
No numerical tolerance, old failure, or optimization adoption gate changes.
P5 and P6 remain open; overlap correctness is not capacity or timing evidence.

### Pressure recovery pass and actual four-conversation witness (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-pressure-feasible-recovery-pass]]
records build 18 passing fixed-MTP pressure cancellation and feasible recovery
(80 assertions), HTTP (835), and T0 (33 groups / 22249). New work stays refused
while the configured plan is infeasible; a physically bounded recovery poll
clears the latch without growing the arena, then preserves exact output.
[[sources/runs/2026/09/2026-09-06-configurable-context-retained-functional-swap-exclusion]]
records eight completed, correctly reused warm-up turns followed by the entire
4096+16-token main request. Its strict capacity verdict remains excluded due
to eight system swap-in pages during overlapping download. This is functional
wiring evidence only. C19 still requires a clean identified capacity interval.
The public Swift consumer build is in progress; no P5 run or release occurred.

### Actual clients and external public consumer (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-actual-32k-client-contracts]]
records actual Ollama and published AI SDK gateway success at the ordinary
window, and Hermes's own expected minimum-context refusal before inference.
[[sources/runs/2026/09/2026-09-06-configurable-context-actual-65k-client-pass]]
records all three clients passing at the documented Hermes window, including
real tool dispatch, follow-up, title and bounded request context. Hermes CLI,
forced compression and image extensions remain unrun on this candidate.

[[sources/runs/2026/09/2026-09-06-configurable-context-external-swift-consumer-pass]]
records build19 passing original public API signatures from an external
SwiftPM package and the unchanged strict warning gate. The preceding
[[sources/runs/2026/09/2026-09-06-configurable-context-external-consumer-deprecation-counterexample]]
remains evidence. Build19 includes a shared prompt-checkpoint arithmetic epoch
and explicit old-epoch rejection; T0 passes33groups/22251assertions. The native
complete-prompt MTP gate remains in progress at this capture. P5, the remaining
C21 native gates, public release, installation and rollback remain open.

### Complete-prompt epoch integration passes (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-complete-prompt-epoch-pass]]
records all281native MTP/vision/cache lifecycle assertions passing on build19.
The centralized arithmetic key now agrees across Generator and direct callers,
and stale epoch0 identities are refused. This supersedes the preceding
in-progress status for this exact bounded gate only. The full-window capacity
ladder and its retained/minimum/transition variants remain unrun; existing C21
native suites and public release/install/rollback remain open.

### Static regression passes; native continuation prerequisite (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-static-and-sampler-prerequisite]]
records the full static suite passing on build 19, including installer fixtures
and planner gates. The sampler then stopped before execution because the
isolated checkout did not select the existing NumPy parity environment. The
environment is now identified; the next bounded batch supplies PYTHON
explicitly. No numerical or model result is inferred from that failed launch.
The following native suites, P5, release and installed acceptance remain open.

### Existing native gates and mandatory-MTP planning defect (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-native-regression-and-mtp-plan-counterexample]]
records build19 passing sampler/governor, n-gram/template/layer parity, exact
8.1GB/10GB output, live cache resizing, prefix reuse, sweep and MTP-head parity.
The following MTP check was intentionally stopped after source review exposed
an unpriced head loaded after an MTP-off plan. Four diagnostic entry points
are being corrected to reserve the head before Engine construction. Their
combined image leg and the full governor drill have explicit larger bounded
test targets; neither has passed on the prospective source yet.

The qualification driver's full-file hash operation now holds the inference
exclusion lock. Nine provider-free rejection/locking groups pass, including
busy-lock refusal and release. The P0 handoff is also reflected in the private
planning pointer; Slotstream's engineering store owns execution and evidence.
No full-window protocol has run. P5 and the remaining C21/C22 release gates
stay open, and the public context ceiling remains65536.

### Explicit resource contracts and real Hermes extensions (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-budget-and-schedule-component-pass]]
records build20 compiling the reviewed diagnostic-budget and actual-schedule
corrections, with T0 33/22256, CLI116, sampler/governor and the full static
suite passing. Required MTP heads are reserved before allocation; the full
governor and image/MTP checks require explicit larger targets and validated
whole-interval memory receipts. Their native execution remains pending.

[[sources/runs/2026/09/2026-09-06-configurable-context-actual-hermes-cli-pass]]
and [[sources/runs/2026/09/2026-09-06-configurable-context-actual-hermes-compression-pass]]
record the actual pinned client's successful CLI and forced compression at65K.
The summary finishes normally, reduces history and preserves the exact fact
in a real follow-up answer. Both extensions finish within their frozen bounded
ceilings; owned children exit and the shared lock is verified free afterward.
Timing and capacity interpretations remain excluded during download overlap.

The optional V193 read-scope checkpoint delta is imported only after this
identified group ends. It preserves existing compute boundaries, affects only
enabled positive-budget retention, and remains unbuilt in the context worktree.
P5's clean full-window/retained/minimum/pass-transition protocols, remaining
vision/native gates, and P6 release/install/rollback remain open. No public
context increase or qualification protocol execution is claimed.

### Ordinary-context live feasibility gap (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-ordinary-governor-feasibility-counterexample]]
records the shared V193 continuation: T0 33/22263, CLI116, HTTP835 and
image-reuse76 pass, then the plain governor gate stops on eight failures.
The startup planner's ordinary-context floor advisory was being accepted by
the live governor without a separate complete-ledger physical-fit check.
The source correction preserves startup defaults, deadbands and the original
refusal assertions, adding pure plain/MTP floor/recovery coverage. It remains
unbuilt; both native variants and the existing governor suite must pass before
C05 is closed. The failure is not waived by the earlier MTP-only result.
P5 and release/installed acceptance remain open.

### Governor fixture classification after physical-fit correction (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-governor-advisory-fixture-counterexample]]
records shared V198: compilation passes; T0 returns32of33groups and22295
assertions with20failed expectations in four old ordinary-context fixtures.
All32new exhaustion/recovery assertions pass. Both reviewers independently
verify through the frozen public doctor that the old10GB whole-availability
rows require7.919289600/7.921999104GB while only7.425GB remains after safety
slack. A successful legacy startup advisory cannot imply live feasibility.

The diagnostic-only correction retains every original10/18/44GB input, checks
refusal/floor/still-unavailable behavior for the exact four invalid advisories,
and adds12GB feasible near-floor rows. Existing settlement/cooldown/recovery
assertions remain for feasible plans. The production guard, deadbands,
allocation goldens and numerical tolerances are unchanged. The revised fixture
is unbuilt at this capture, and both native governors still must pass; C05
remains open. No CLI or model case followed the failed T0 run.

The verified transport release and exact evidence packet are integrated while
preserving context documentation, shipped Hermes history and restored original
historical sources. This prepares a common successor source closure; it is
not context capacity qualification. P5 and P6 remain open, with default32768
and public ceiling65536 unchanged.

### Shared regression and excluded full-resource interval (2026-09-06)

[[sources/runs/2026/09/2026-09-06-optimization-merged-correctness-v202]]
records the exact shared V202 candidate: all 19 correctness groups pass,
including T0 33/22346, both native governor variants at 80 assertions,
the 832-assertion combined scope lifecycle, and the 835-assertion HTTP suite.
The production feasibility correction and revised advisory classification
now pass their relevant pure and native checks. Experimental defaults remain
off. This correctness result does not qualify a larger context window.

[[sources/runs/2026/09/2026-09-06-configurable-context-full-resource-swap-exclusion]]
preserves the full 13 GB elastic drill's incomplete first attempt: the first
answer is exact and the sampled footprint remains below the ceiling, but
eight global swap-ins stop the diagnostic before a second completed answer.
Two controls without an owned model also observe swap-ins. No process is
attributed from global counters, no resource criterion is relaxed, and the
full MTP/image diagnostic does not start after the failure.

[[sources/runs/2026/09/2026-09-06-configurable-context-vision-target-counterexample]]
records independent tower/reference parity and the subsequent 10 GB image
suite's four failures. Full-size fruit and two-image requests receive the
correct typed memory refusal. The raw 21-pass report also exposes one fixture
false positive: an empty error answer counted as different image content.
Its prospective correction requires successful, nonempty responses. The
full-image successor reserves its actual attention workspace at 14.5 GB with
a 3072-token prefill reservation and a 20.5 GB real preflight; the ordinary
quality probe remains at 10 GB. Its outcome is not yet recorded here.

P5 has not run. Full resource, retained/minimum/pass-transition capacity and
remaining client/release/installed acceptance remain open. The public ceiling
is still 65536 and the default is 32768. Neither a plan nor an excluded
interval is a 262144-token qualification.

### Full images and actual clients complete (2026-09-06)

[[sources/runs/2026/09/2026-09-06-configurable-context-full-image-and-actual-client-pass]]
records all 25 full-image assertions passing on V202, including successful
nonempty responses for the different-picture predicate. The original images
use an explicit 14.5 GB total target and a 3072-token prefill reservation,
after a 20.5 GB real preflight. That override is local to the full-image
server. The original 10 GB refusal remains preserved. A separate ordinary
10 GB server passes all 15 quality assertions. Actual pinned Hermes at 65K
passes its tool/follow-up/title/image flow with exactly one fixture tool
execution, and actual Ollama returns the dog image's correct subject.

`Tools/verify.sh`, the helper and testing instructions now carry that exact
full-image profile. The helper's semantic correction was exercised; its
subsequent header edit is documentation only. No runtime source, model
ceiling, default option, image fixture or memory criterion changed. Timing
and capacity conclusions remain excluded because global swap-ins continue.
Full resource, P5 and release/installed acceptance remain open.

### Structured overflow refusals and verification completeness (2026-09-07)

[[sources/runs/2026/09/2026-09-07-configurable-context-ollama-overflow-wire-counterexample]]
preserves the original HTTP suite's 73/74 result and four independently
recorded live HTTP 400 responses. Length enforcement works, but the old
shell assertion matches obsolete prose and both Ollama overflow branches
omit the structured code/details used by other refusals. A focused source
successor keeps the legacy string and status, adds the existing typed
refusal wrapper, and exercises 21 new pre-header/code/no-success assertions
across all seven dialect variants. The shell suite now checks status and
code directly. Its shared build and affected acceptance are pending.

The full verification script also now treats missing vision-reference
dependencies and insufficient full-image headroom as failed acceptance,
while preserving the no-launch behavior and rerun guidance. The reviewed
provider-free shell/predicate checks live in the shared V209 evidence;
no mandatory vision skip can silently yield a passing full battery.

The successful V202 client source is marked discarded for resource/timing
measurement because its recorded global swap-ins are nonzero. Its exact
raw body and passing correctness assertions are preserved unchanged. The
flag is measurement disposition, not a claim that those client assertions
failed. The independent runtime correction leaves default 32768, public
65536, all optional defaults and the memory ledger unchanged.

### Additional OpenAI code field found by the unchanged matrix (2026-09-07)

[[sources/runs/2026/09/2026-09-07-optimization-typed-context-wire-counterexample]]
preserves the shared V211 successor. Build, T0 22346 and CLI116 pass; the
extended context matrix passes 854 of 856 assertions and stops before the
two actual HTTP suites. All four Ollama structured-code cases pass. Only
the OpenAI JSON/SSE structured-code checks fail: their local helper puts
the code in the existing type field but omits error.code.

The one-line V214 correction adds error.code while retaining type, message
and HTTP status. The same 856 assertions and actual suites are being rebuilt
and rerun on V215. No fixture, criterion, default, context ceiling or memory
constant changes. The V211 result remains preserved, not replaced.

A byte-exact local rollback copy of installed public v0.2.11 is prepared
with both binary and Metal hashes; its doctor accepts configured65536. This
is preservation and a model-free configuration check only: rollback itself
and installation of the new context work have not run.

### Current integrated gate map (2026-09-07)

[[sources/runs/2026/09/2026-09-07-optimization-typed-context-correctness]]
records V215 passing T0 (33 groups, 22346 assertions), CLI116, all856 native
context-serving assertions, all74 actual API robustness assertions and the
four actual overflow requests. The two Ollama error codes and the additive
OpenAI error.code are present. Existing error text/type/status and successful
delivery are preserved. V215 changes no inference arithmetic, memory credit,
optimizer default or public context ceiling from the preceding integration.
Its native context-serving interval observes eight global swap-ins, and API
robustness observes28. The source is marked discarded:true for resource and
timing use; these predicate results support correctness only. V211's matching
metadata correction also preserves its complete original failed body/raw bytes.

| Acceptance | Current evidence and remaining boundary |
| --- | --- |
| C01–C03 | Frozen ordinary allocation goldens, additive CLI schema and discrete feasibility pass on the integrated source. Additional actual minimum-target accepted/refused neighbors are now recorded. No golden was refreshed. |
| C04–C05 | Owned-allocation and interleaved retention contracts pass; the corrected governor passes both80-assertion native variants in V202. Full-window retention capacity remains C19. |
| C06–C07 | All ten bounded numerical shape/prefix witnesses pass their1605 unchanged assertions and greedy-ID criteria; the full schedule satisfies actual query/key bounds. Normal long-window activation still requires P5 execution. |
| C08–C15 | Checked diagnostics, entrypoints, exact limits, deadline/queue clocks, ownership, pressure interruption and cleanup/recovery pass the identified component and live matrices. Their contaminated intervals carry no resource/timing claim. |
| C16–C17 | V215 passes all seven dialect variants and real HTTP overflow/robustness checks. The strict consumer fixture is engine-side evidence; actual Sevra H2 receiving-side action authority remains outside this claim. |
| C18–C19 | Eight exact prospective protocols are prepared, covering sixteen main rungs: memory/pass transitions, cold and four-conversation retained ladders through262144, then exact minimum-target full-window variants. None has executed or performed its mandatory full payload verification. |
| C20 | Live injected deadline/guard interruption and recovery are exercised without a memory hog or overlapping model. Existing full resource acceptance still remains open under C21. |
| C21 | Identified native, static, planner, sampler, numerical, image, cache and HTTP checks pass within their stated scopes. The full governor resource battery stopped on swap-ins; the remaining whole-process resource/recall battery and combined MTP/image resource completion remain open. |
| C22 | Public Swift compatibility, actual32K Ollama/gateway and65K Hermes/Ollama/gateway checks pass. Full images and actual Hermes CLI/compaction/image behavior have separate evidence. Context release, installed e2e and actual rollback remain unrun. |

The ordinary-target protocols are preserved at
[[sources/runs/2026/09/2026-09-07-configurable-context-v215-capacity-protocols]];
their exact minimum and pass-transition additions and campaign order are at
[[sources/runs/2026/09/2026-09-07-configurable-context-v215-minimum-and-transition-protocols]].
The campaign stops at the first failed profile/rung. A further83.21061301231384s
idle control observes16 global swap-ins with no owned model, preserved at
[[sources/runs/2026/09/2026-09-07-configurable-context-v215-idle-swap-control]].
No clean resource retry or capacity run followed that control.

[[sources/runs/2026/09/2026-09-07-configurable-context-installed-gate-discovery-preparation]]
records correcting the installed gate's discovery nesting before execution.
A subsequent review identified that its previously added expected doctor
refusal also needs explicit exit-status handling under pipefail; that bounded
harness correction is in review, with no installed run attempted.

P5 and P6 remain open. Public context stays65536, default32768, and the
separately qualified MTP/vision limits65536. The saved public0.2.11 binary
and Metal library prepare rollback but do not prove it. No context release,
installation, whole-plan completion or262144 support is claimed.

### Final harness review and resource hold (2026-09-07)

The bounded release-shell review is complete. Its exact20case-tested
correction now requires the expected doctor exit2 under pipefail as well as
typed insufficient_memory, validates the nested discovery cap before any POST,
and requires context_length_exceeded alongside HTTP400. This supersedes the
preceding pending harness status; the installed model suite itself is unrun.
The original failed proposal and passing successor are preserved at
[[sources/runs/2026/09/2026-09-07-optimization-default-and-release-acceptance-review]].
All143runtime source files still match the exactV215archive in both worktrees,
recorded at
[[sources/runs/2026/09/2026-09-07-configurable-context-v215-prepared-state-audit]].

The final readiness snapshot has21,662,023,680reclaimable bytes, below the
ordinary capacity profile's25GB prerequisite, with further background swap
activity. There is no owned model/compiler and the model lock is free. The
raw snapshot is at
[[sources/runs/2026/09/2026-09-07-configurable-context-v215-capacity-preflight-hold]].
No capacity or full resource run was attempted. The eight frozen profiles
remain unconsumed; P5/P6, remaining resource acceptance, installation and
rollback remain open. Default32768 and public65536 remain in force.

### Selected-candidate acceptance correction (2026-09-07)

[[sources/runs/2026/09/2026-09-07-configurable-context-candidate-selection-acceptance]] preserves the full five-file acceptance correction and its
first counterexamples. Static, planner and installer gates consistently use
the explicitly selected candidate, including paths with spaces/apostrophes.
The installer packages that executable and its colocated Metal library, then
verifies exact installed bytes through fresh/repeated activation, checksum
refusal and legacy upgrade. The static entrypoint retains the mandatory
qualification-driver checks. All64 planner assertions remain unchanged.

Six static-selection fixtures, ten real-install.sh selection/fault fixtures,
and the unchanged nine qualification-driver groups pass. The old installer
fails seven of the same ten cases. A separate private fixture installation of
V215 also passes exact binary/Metal identity; the real user installation and
rollback remain unexercised. This does not close release/installed C22.

On the loaded machine, the old quoted planner path produces18pass/46fail.
The corrected quoted and normal paths both produce58pass/6fail, with the
same six malformed-checkpoint diagnostics blocked by the real startup memory
guard. A captured direct fixture shows9.2GB reclaimable and an8.1GB target,
then insufficient allocation headroom before checkpoint parsing. These failures
are preserved; no expected error or guard is weakened. A complete64/64 planner
and full static pass remain required on a quiet machine. All143runtime source
files still match frozenV215 in both worktrees; no frozen P5 driver changes.

P5/P6 remain open. Eight prospective profiles/sixteen main rungs remain unrun;
no model payload verification or capacity run starts during this work. Public
65536 and default32768 remain unchanged. Loaded-machine results carry no
resource or timing qualification.

### Final acceptance-script integration (2026-09-07)

[[sources/runs/2026/09/2026-09-07-optimization-verification-selected-paths]]
records the full verification entrypoint preserving the selected executable
through its evaluated call sites; its nine focused fixtures pass. Mandatory
vision, context-qualification and installer checks remain enforced.
[[sources/runs/2026/09/2026-09-07-configurable-context-sampler-exit-and-binary-selection]]
preserves the five original sampler false passes and the corrected eight-case
full-shell fixture, plus all17real V215 sampler/NumPy/governor checks passing.
API version checks now require both the selected quoted executable and a
successful process exit. No new full74-case API server run is claimed.

[[sources/runs/2026/09/2026-09-07-optimization-mandatory-sampler-and-final-handoff]]
records the final mandatory static wiring: sampler8/static9/syntax3pass,
exact integrated files,95public-number checks and zero validation errors.
All143runtime sources and the frozen capacity drivers remain unchanged.
The shared context plan, source closure, public docs and client fixtures are
now integrated. P5/P6 remain open: all eight capacity profiles/sixteen main
rungs are unrun; the current six headroom-blocked planner checks, remaining
full resource tests, actual final clients, release/installation and rollback
still require their own passing evidence. Public65536/default32768 remain.
### Final release-response acceptance review (2026-09-07)

[[sources/runs/2026/09/2026-09-07-optimization-serial-and-installed-gate-integration]]
records16original installed-gate false passes and one quoted-binary-path
failure, followed by19/19passing complete-shell fixtures and mandatory static
wiring. The separate successor at
[[sources/runs/2026/09/2026-09-07-configurable-context-openai-release-completion]]
preserves those19cases and exposes nine OpenAI false passes. All28complete-shell
fixtures pass after requiring successful curl, one successful text completion,
nonempty assistant content and a positive integer completion-token count.
These are local process/response fixtures; no real installed model, socket,
release or rollback ran. The original failed scripts and raw results remain
preserved. The existing mandatory static entrypoint runs the expanded suite.

[[sources/runs/2026/09/2026-09-07-optimization-cached-planner-build-and-typed-parity]]
separately records the successful temporary cached build and441exact typed
planner comparisons, with shared V215runtime/release/build state restored.
It grants no new context capacity or performance claim. All eight V215P5
profiles/sixteen main rungs remain unrun. Full resource acceptance, P5/P6,
actual installed/release/rollback gates remain open; the original ordinary
capacity preflight remains25GB, public context65536 and default32768.


The source/protocol dependency split is preserved at
[[sources/runs/2026/09/2026-09-07-optimization-planner-metadata-and-context-boundary]].
The full262Kexpansion campaign remains necessary for this context-expansion
goal, while unchanged32768/65536optimization retains its own applicable gates.
No context profile is waived. That source also records the corrected e2e_release.sh
executable bit; verify.sh remains at its tracked0644mode.

### Software delivery with native testing explicitly deferred (2026-09-07)

Carlos instructed: finish implementation, use proxies to test what can be tested, and prepare the full work for testing later without running model/hardware qualification on this Mac. This changes execution, not the meaning of capacity or release evidence. No memory wait loop remains. The current software delivery gate is the runnable source/proxy suite and a portable native qualification handoff; the original applicable native C01–C22 cases remain deferred, never relabeled as passes.

[[sources/runs/2026/09/2026-09-07-configurable-context-portable-software-acceptance]] captures 79 passing Python unittest cases across nine suites and 964167 assertions in the isolated Swift policy executable. Most assertions exercise schedule products across many dispatches, not independent model tasks. Production Plan, ContextGeometry, feasibility, GovernorPolicy and RequestController execute with inert device readings, fake clocks, synthetic availability and exact source-derived declarations. No SwiftPM/MLX build, tensor allocation, model, server or real pressure test ran. CI wiring is delivered but has not been executed remotely in this task.

Planner's three public device-reading functions now live in PlannerDevice.swift with byte-identical bodies; planning arithmetic, public signatures and availabilityOverride are unchanged. Context evidence validation rejects malformed/duplicate-terminal tool streams, invalid token IDs/types and missing MLX observations. Native readiness also refuses unknown/non-normal OS pressure. Counterexamples and before/after hashes are preserved in the source. Optional PlannerCostModel source joins the proxy compile/hash closure when present; previous frozen binaries and driver snapshots remain historical and immutable.

`make context-test` is the software command. Its fresh JSON report binds the actual source/driver closure, lists proxy coverage for every C01–C22 case, and keeps hardware/release qualification false. `Tools/context_acceptance.py prepare` exports source and exact acceptance profiles without a model. On the intended target, `bind` verifies a freshly built candidate's source archive and resolves only metadata/plan-only requests; retained profiles explicitly price their original warm-up lengths and retention mode. An old candidate is rejected before model access. `run-capacity --execute-on-target` runs required governor/MTP+vision resource gates and the original eight capacity profiles/sixteen main rungs sequentially, preserving minimum dependencies, full model verification, readiness and first-failure stop. This code was exercised with inert children, including successful binding and rejection of forged source and omitted/reordered profiles. Portable source export/extraction identity passed.

Public/default context stays65536/32768; MTP/vision limits stay65536. The full model-limit text path exists behind explicit diagnostic qualification. A larger public serving limit, real numerical/capacity/API/client acceptance, complete native build and release/install/rollback still need the intended target's evidence. They are deliberately deferred under Carlos's instruction and do not block delivering or reviewing the software. No full-feature release or measured262K support is claimed.

### Explicit window CLI testing (2026-09-07)

[[sources/runs/2026/09/2026-09-07-configurable-context-window-cli-matrix]] records the final model-free matrix against V215: 93 commands pass across 1024, 4096, 8192, 16384, 32768, 65536, 128256, 131072 and 262144 tokens. Public configuration accepts windows through65536 and explicitly refuses larger ones. Scheduling passes at every listed window for cold, middle-prefix and last-token starts, with requested chunks256/4095/4096 and actual padded query/key products bounded. Immediate public overflow and cold/continued model overflow are refused. Three adversarial validator tests pass; the original test-only ledger multiplier error is retained in the source.

`Tools/context_window_matrix.py` and the testing guide make this check repeatable without compilation or a model. Candidate artifacts and source snapshots are checked at both ends. The final restored-checkout receipt identifies Plan.swift and PlannerDevice.swift as the only V215 differences, so it remains identified-candidate CLI evidence rather than a new full build of current source. No runtime source changed for this matrix.

The software implementation is delivered, but the whole plan is not complete: full native capacity, final applicable build/numerical/resource/API/client gates, public-limit activation and release/install/rollback still require their own evidence. This latest request allowed feasible context testing; available resources did not support a fresh model launch. No native test, memory waiter or expanded-capacity claim was introduced. Existing proxy and historical model receipts retain their original scope.

### Automatic context window by memory tier, planned (2026-09-13)

Carlos asked the public docs to say that auto mode will choose the context window as it already chooses the memory target. It does not today: every plan starts at 32,768 tokens unless `--max-context` names another window. The tier tables in README.md and docs/HARDWARE.md recommend 32,768 below 36 GB and 65,536 from 36 GB, from `doctor --sim-ram` plans on the 0.2.16 candidate ([[records/measurements/decode-lookahead-default-2026-09-13]]): at 65,536 tokens a 16 GB Mac's cache shrinks by a third and a 32 GB Mac's cache falls below the draft head's floor. Automatic selection would apply that rule when no window is given, keep an explicit `--max-context` authoritative, and move no public limit without this plan's qualification gates. Not implemented.

### Automatic context window and the full public limit, implemented (2026-09-13)

Carlos asked to enable the full window, finish the remaining work with best guesses from what the development Mac can measure, have auto choose the best default for every memory tier, and update the tables. This supersedes the planned addendum above. It opens the public limit before the frozen native campaign could run, at his direction, and records exactly what evidence exists.

**Implemented.** `ContextPolicy.maxTokens`, the implementation limit and the draft-head limit equal the pinned model's 262,144 tokens; images stay within 65,536. `--max-context` accepts `auto`, now the default for `serve`, `run` and `doctor`, or any window from 1 to 262,144. Auto follows [[records/decisions/automatic-context-window-per-machine]]. A window above 32,768 retains one complete conversation when its plan can, and otherwise the plan says how much a follow-up reuses. `doctor` reports every automatic candidate and names why a memory target is unavailable. The diagnostic recovery ladder stops at 65,536.

**Frozen baselines.** C01's default allocation is pinned at an explicit 32,768 tokens in `Tools/fixtures/context-default-v2.json`; v1 stays unchanged. `Tools/fixtures/context-automatic-v1.json` freezes the automatic windows of the four fixture tiers. Plans by memory and the software checks are in [[records/measurements/automatic-context-window-plans-2026-09-13]].

**Native evidence.** The 131,072-token cold rung completed inside its 16 GB plan ([[records/measurements/automatic-context-window-131072-read-2026-09-13]]). It is not the frozen campaign: that campaign's 22 GB and minimum-target 262,144-token profiles could not run on the development Mac, where every target from 18 to 24 GB was refused. With the draft head on, the same prompt and window stayed inside an 18 GB plan and matched the draft-off reply token for token ([[records/measurements/automatic-context-window-draft-head-131072-2026-09-13]]), which keeps the draft-head limit at the model limit.

**Still open.** The frozen native campaign, including cold and retained 262,144-token capacity; native vision at the new cap; prefill anchors that depend on position and cover passes under 256 tokens; and the installed-client battery at larger windows. The public limit and the automatic windows above 65,536 rest on the ledger arithmetic plus the rungs above, at Carlos's direction.

**Released.** Slotstream 0.2.17 shipped this on 2026-09-13. The exact CI artifact passed 25 of 25 model gates, and the installed release passed 31 of 31 checks with auto choosing 32,768 tokens at a 10 GB target ([[records/measurements/release-0-2-17-published-2026-09-13]]). The open items above are unchanged.

## Expert Lookahead: local training and exact prefetch experiment
**Status: ready for execution. Five numbered review passes, two depth-two alignment reviews and a closing consistency review are complete; the fifth pass was a separate code-grounding review that corrected seven items. No predictor has been trained, no performance gain has been demonstrated, and this planning task launches no model or paid job.** This is the local successor experiment requested on September 10. Work is divided into an initial proof and conditional extensions so that a useful negative result is also a complete outcome.

### 1. Objective, scope and fixed decisions

Build **Expert Lookahead**, a learned expert-ranking and prefetch system for the existing Slotstream checkpoint. It must improve measured native decode throughput while preserving the chosen baseline's actual routing, expert weight bytes, numerical execution and output behavior.

The first experiment runs entirely on the existing M5 Pro with 48 GB unified memory and internal SSD. Rental budget is zero. Start with a 10,000-output-token pilot, then expand to approximately 50,000 outputs only after the pilot establishes valid features and a plausible performance opportunity. Train a roughly 5M-parameter whole-pass predictor and compare it with cheap routing baselines and a short-lookahead predictor. The initial win criterion is at least 10% repeatable throughput improvement at equal total memory, with exact correctness gates passing.

The teacher remains frozen: Qwen3.8-Flash-Next, approximately 125B parameters plus 51B ngram tables, in the existing roughly 104 GB quantized checkpoint. Only the prediction network is trained. Expert caches contain weights, not reusable token outputs. Predicting or substituting expert activations, changing selected experts, skipping the authoritative router, and changing MTP depth are outside the first experiment.

The primary execution mode is text, MTP enabled at the adopted depth two (pending token plus two draft positions), fixed prefill chunk 256, prefix retention disabled, elastic resizing disabled, and the current resolved deployment optimizations. Greedy generation is the primary performance workload; seeded sampling is also a correctness gate. Qualification applies to this exact mode. Other modes retain the current path until separately qualified. An explicitly requested missing, malformed or incompatible prediction pack fails validation before allocation; this is distinct from a valid pack being bypassed for an unsupported execution mode. In particular, do not load the MTP head merely to obtain predictor features when MTP is off.

P0 must explicitly freeze `SLOTSTREAM_DRAFT_DEPTH=2` for collection and every B0/B1/B2 arm, with adaptive speculation and experimental draft-tail shortening disabled. Do not rely on an executable's default: the installed public 0.2.14 artifact still defaults to one. Bind the actual candidate/source identity, three-position feature schema and pack compatibility before data collection; preserve normal context-bound shortening.

The completed OPT00–OPT36 program stays complete. This is a new experiment, not a reopening of its rejected accelerators. When this plan is later executed, continue through its local work packages and qualifying 10k-to-50k expansion without asking for a new approval at every step; the current task authorizes preparation of the plan, not starting those runs. The old M1 proxy/cloud proposal is historical; this plan uses the actual native teacher locally. No installed executable replacement, default activation, publication or release is part of the initial proof.

### 2. Evidence and what it permits us to infer

Code review baseline: repository commit `4ab794a3bc5afce9092db7db5a0e3e0a12258bd2`; the code map was re-verified unchanged at `2726a8658b233a0a160524d7f8a745f6a89ee53f`, whose intervening source changes are the two-draft default and lifetime memory-observation recording, with no mapped seam altered. Freeze the actual implementation commit, build archive, binary, Metal library, checkpoint manifests, tokenizer/template, resolved optimization flags, MLX versions and protocol again before execution. A later documentation commit is not proof that compiled sources match a previous measurement.

| Evidence | Relevant finding | Consequence |
| --- | --- | --- |
| [[records/measurements/decode-wall-time-attribution-2026-09-10]] | At a 24 GB target and 5,702 slots, one clean 512-output pair gives 43.9435 seconds unprofiled, about 11.65 output tokens/sec. The profiled partition is 33.6654% file reads, 1.6095% staging, 31.9764% GPU envelopes, and 32.7487% remaining CPU work/coordination. | Reads are a substantial opportunity, but hiding them does not remove the comparable CPU contribution. These categories are exclusive observed wall time, not independently removable costs. |
| Same attribution study | 46,902 records and 129.675 GB requested file bytes for 512 outputs; 81.6340% expert hit rate. MTP performs 321 main forwards and evaluates 642 positions. | Use native counters and pass/position labels. Emitted tokens, target positions, expert uses and distinct reads are different denominators. |
| Same study, small-cache control | Reads plus staging occupy 42.8458% at the 10 GB profile. | Memory size changes the opportunity. Do not pool large and small profiles or use one fraction universally. |
| [[records/measurements/mtp-depth-fixed-total-inconclusive-2026-09-11]] | Neither 24 GB nor 20 GB obtained enough clean matched pairs to qualify a new depth. Swap activity, thermal state and headroom caused exclusions. | These historical attempts did not qualify a new default. The later explicit adoption fixes new arms at depth two; see [[records/decisions/draft-depth-defaults-to-two]] and [[records/measurements/mtp-depth-auto40-multitasking-2026-09-11]]. Readiness, finite attempts and clean paired eligibility must be specified before testing. A failed resource gate is not a negative predictor result. |
| [[records/measurements/optimization-final-composition-2026-09-09]] | Accepted whole-engine work includes memory/performance tradeoffs, not a general sustained speedup. | Compare against the actual current deployment family, not a slower historical or all-default reference. |
| [[sources/runs/2026/09/2026-09-05-optimization-ngram-lookahead]] | A large improvement in the exposed prefetch stage did not improve total request time. | Prefetch timing and classifier accuracy cannot substitute for an end-to-end gain. |
| [[records/measurements/release-qualification-0-2-14]] | All original v0.2.14 public-artifact gates subsequently qualified through unchanged clean reruns; the original exclusions remain preserved. | Release acceptance is separate evidence from predictor qualification; this experiment cannot mark a release gate passed. The newer two-draft candidate still has one paging-sensitive gate open, which P5 requires closed on the actual experiment baseline; see [[records/measurements/release-0-2-15-prepublication-2026-09-11]]. |

Requested file bytes are not measured physical NAND traffic. The Mac uses SSD to raw RAM staging, then GPU scatter within shared RAM, then expert computation. There is no separate host-to-VRAM PCIe transfer. Prior discrete-GPU papers provide architectural ideas, not transferable throughput numbers.

For a conditional estimate only, let `f = 0.336654`, `q` be the fraction of the exposed read time actually hidden, and `o` be new exposed overhead as a fraction of baseline decode. Then `speedup = 1 / (1 - f*q + o)`. With `o = 0.02`, hiding 50% or 70% of that read time gives approximately 1.17x or 1.27x throughput. Timely record coverage is not equal to `q`; measure native stalls and total time. Predictive retention or different overlap can change this model. No gain is guaranteed.

### 3. Code map and implementation boundaries

Paths below are repository-relative and symbols control if line numbers move. New files and interfaces in this plan are proposed work, not existing commands.

| Existing seam | Current behavior | Planned change |
| --- | --- | --- |
| `Sources/Slotstream/Checkpoint.swift`, `ModelConfig` | 48 layers, 512 experts/layer, top 10, hidden width 2,560, four hyperconnection streams. PLE IDs are one-based; configured `[2]` means zero-based layer 1. | Validate predictor geometry against the actual config. Never infer layers from prose labels. |
| `Sources/Slotstream/Generate.swift`, `speculativeDecode` | `state.lastMulti`, pending input `p`, draft embeddings and the MTP head exist before `[p] + drafts` is verified. Reconciliation retains only the accepted main state. | Emit causal pass/position events. Compute start forecasts before the main verify call. Preserve sampling order, draft depth, verify shapes and rollback. |
| `Sources/Slotstream/MTP.swift`, `MTPHead.callAsFunction`, `consumeChecked` | A single fully resident head produces draft features; `consumeChecked` returns the last main-model multi row. Head experts are distinct from main-layer experts. | Reuse existing head features for the draft position. Do not map head expert IDs onto the 48 main layers or add head steps. |
| `Sources/Slotstream/Model.swift`, `runHiddenMulti` | Base embeddings become a 10,240-wide residual; `x2` is the 2,560-wide MoE input. `eval(h)` ends each ordinary layer's pool-reading lifetime. | Consume the existing package-level per-layer numerics observer, which already receives `x2`, and the public router observer, which already receives each layer's ordered expert IDs on the model thread; add only a scheduler callback at this existing completed-layer boundary. Defer any host copy of `x2` until after the layer's `eval(h)`. Preserve that evaluation and lifetime rule in the first implementation. |
| `Sources/Slotstream/Layers.swift`, `MoELayer.callAsFunction`, `cached` | Exact FP32 router projection/top-k; IDs materialize on CPU; current hits are pinned; gathered expert calculations preserve original ranks. | Observe exact labels; adopt prefetched raw bytes through the demand path. No gate replacement, expert reorder, padding change or resident/missing split of arithmetic. |
| `Sources/Slotstream/ExpertStore.swift`, `readBatchChecked`, `stagingArrays` | Checked reads allocate nine raw buffers; staging creation wraps them in MLX and calls `eval`. | Split raw byte acquisition from MLX ownership. Background workers receive immutable addresses and write only their own buffers. |
| Same file, `SlotPool.ensureCore`, `resize`, `unpinAll`, sweep admission | Global CLOCK cache, deduplicated demand, pin-before-victim selection, bounded reads/scatters; sweep has its own admission rules. | Keep the demand reservation order. Adopt complete raw tickets only for actual misses. Add lifecycle invalidation and budget accounting; retain normal CLOCK and sweep admission in v0. |
| `Sources/Slotstream/NgramPrefetch.swift` | CPU-only tickets with cancellation and join, no background MLX/cache mutation. | Reuse its ownership pattern, not its disabled performance policy or unbounded cross-layer prefetch assumptions. |
| `Sources/Slotstream/RouterTrace.swift` | Per-call layer/row/K/IDs, buffered until generation ends. | Preserve its format. Add a separate bounded, versioned dataset collector with pass IDs, features and cache events. |
| `Sources/Slotstream/Plan.swift`, `PlannerCostModel.swift`, `ContextMemory.swift`, allocation checks | Existing target, context and MTP allowances determine cache capacity. | Charge incremental prediction/staging memory before solving expert slots. Do not lower current fixed/margin constants or grant speculative memory credits. |
| `Sources/Slotstream/Optimizations.swift` | Explicit deployment family and validated controls. | Add disabled experimental controls and an explicit local model-pack path. Default-off must take no predictor allocation or feature-copy path. |
| `Sources/Slotstream/StateRecovery.swift`, `StateCheckpoint` | Recovery accepts completed token boundaries with matching KV/indexer offsets and ownership. | Use normal request recovery for v0. Existing checkpoints cannot support speculative mid-layer rollback. |
| `Sources/SlotstreamDiagnostics`, `SlotstreamTestKit`, `slotstream-cli`; `Tools/serve_bench.py`, `prefill_bench.py` | Shared check catalogue, bounded child execution, provenance and paired serving infrastructure. | Extend these patterns for collector checks, scheduler checks, pack parity and native A/B; use no nonexistent XCTest target. |

Do not attach `perLayerHook` blindly: its presence affects terminal pruning conditions, and it observes the wide post-layer state rather than `x2`. The existing numerics and router observers are the observation interface; add a separate hook only if their extra MoE-internal emissions measurably cost time, with the same shape and no different mathematical branch. Any collector-induced graph/lifetime change still needs a capture-on/off parity check.

### 4. Candidate architecture and causal feature contract

#### Whole-pass model G128

For main verification position zero, use the previous committed main context `state.lastMulti` (10,240 values) and the known pending token's base embedding (2,560 values). For position one, use the already-computed MTP multi after consuming the pending token together with the first draft token's embedding. For position two, use the MTP multi after consuming the first draft together with the second draft token's embedding. The latter two contexts are draft proxies, not uncomputed main-model hidden states. Retain these existing head outputs without an additional head step, and add a three-entry hidden bias for position/feature kind. Charge retained features, any missing embedding read, feature copies and all three predictor rows. Near a context bound, emit features only for positions the native pass actually evaluates.

Every position's inputs must be available before `allLogitsWithMultiChecked(verifyIds, state:)`. Never train either draft position using a main-model state from that same future verification pass. Never drop a rejected draft position from the labels: its main-model experts were still evaluated and loaded. If features are absent or the mode is unsupported, use ordinary demand loading.

Normalize each input block with fixed RMS normalization, epsilon `1e-6`. Trainable computation is `12,800 -> width -> 24,576`, with SiLU and biases. Interpret the output as 48 independent 512-expert ranking vectors. For the three positions, form a demand ranking over the union of `(layer, expert)` keys; calibrate this union on validation rather than assuming independent position probabilities. Each distinct record has one shared read ticket even when multiple positions predict or demand it; retain the per-position labels separately.

| Width | Base MLP parameters, including biases | FP16 base weights | Role |
| --- | ---: | ---: | --- |
| 64 | 2,416,704 | 4.83 MB | Capacity/overhead lower bound |
| 128 | 4,808,832 | 9.62 MB | Initial candidate |
| 256 | 9,593,088 | 19.19 MB | Capacity check only if justified |

The three position biases add `3*width` parameters (384 for G128). Fixed normalization adds none. These counts exclude implementation workspaces and the optional refiner. G128 is therefore approximately 4.81M parameters, not a 5M language model. Use native MLX as the first runtime implementation. At the pilot, benchmark CPU-device versus GPU-device prediction with the same exported pack, including feature materialization and score transfer; select by net exposed overhead while preserving the target stream. No background task may invoke MLX unless separately qualified for its ownership/stream semantics. Start with FP32 weights for Python/Swift comparison, then test FP16 export; lower-precision prediction may change rankings but must never change target arithmetic. Retain the faster validated representation.

#### Short-lookahead and refinement

Train a shared conditional model `L128`: normalized `x2` from completed layer L, learned source-layer and horizon embeddings of width 16 each, hidden width 128, and 512 output scores for target layer `L+d`, where `d` is one of 1, 2, 4, 8. At this geometry it is 398,784 parameters. Mask nonexistent target layers. Apply L128 to each actually evaluated position of the completed layer, with its own `x2` row and matching future-layer target row; union the predicted record keys before admission. Batch these rows where useful and charge all feature, model and transfer work. Layer/expert identities are not interchangeable: condition on layer and retain target-specific reporting.

Use full-layer pilot captures to compare horizons and layers. Initially deploy either G128 or a short-lookahead schedule selected on validation. Test combined G128 + L128 only if both add timely coverage: initial refinement anchors are after zero-based layers `3, 7, 11, ..., 43`, forecasting future valid horizons. Do not execute a refinement for every future layer just because it can be evaluated offline. Measure batching, feature extraction, model execution and score transfer together.

The initial size range is thus about 2.4M to 9.6M for the global model, optionally plus the small refiner. There is no evidence that 48 separately trained large predictors are optimal. A per-layer model can be a later capacity diagnostic if shared conditioning underfits; it is not the first architecture grid.

#### Required baselines and ablations

Compare ordinary CLOCK/demand loading; per-layer training-set frequency; previous observed pass routes with causal updates; G64/G128; L128 at fixed horizons; and an offline perfect-future oracle under the same bounded scheduler. Test G256 only if G128 underfits or its validation curve supports capacity. Test G128+L128 only if coverage is complementary. Frequency and recent routes must use the same byte budget and deadline rules. Filter resident experts for every predictor. Every policy in this comparison, including a non-learned baseline, is an eligible final candidate: the experiment tests bounded prefetch, and the learned-versus-cheap coverage margin decides only whether training continues.

At the pilot stage, run one seed per candidate. Repeat the leading learned candidate with three fixed seeds before selecting the final pack. A model with better offline accuracy but slower measured prediction can lose selection. The oracle is a diagnostic upper bound with forbidden hindsight, never a deployable performance result.

### 5. Dataset, collector and evidence contracts

#### Corpus and split

Create a public, reproducible prompt manifest before collecting. Use authored fixtures or sources with recorded licenses and provenance; do not read private chats, the private command-center brain or production prompts. Include actual code completion/debugging, arithmetic/reasoning, prose/document tasks, multilingual text, structured/tool-shaped text and dialogue. Require code prompts to begin producing code so the corpus does not repeat the recent code-benchmark problem of measuring introductory prose.

Target roughly 300–400 requests and 50,000 emitted outputs. Prompts should span short contexts, ordinary contexts around 512–2,048 tokens, and a smaller long-document stratum up to 8,192 tokens. Use per-request output caps of 128 or 256. Keep normal EOS behavior. Do not pad a short answer with forced tokens or selectively discard difficult prompts to reach a token total. Freeze reserve prompt IDs in each split before collection.

Assign complete source documents/conversation families to train, validation or test before generation. Related templates, paraphrases and document excerpts stay together. Freeze the corpus shuffle and family split with seed 1729. Record the resulting IDs and hashes; the seed alone is not the split. Targets are about 40k train, 5k validation and 5k test outputs; whole completed requests may overshoot a split target by at most one request cap. Counts are targets, not a claim of exact independent examples.

The 10k pilot comprises approximately 8k train and 2k validation outputs from those already assigned splits. It is included in the 50k total, not added to it. Test prompts and their labels stay sealed until candidate selection is frozen. The completed learning-curve comparison uses nested 8k, 20k and 40k training prefixes against the same final validation set. This replaces the conversational shorthand of 10k/20k/40k training, which did not account for pilot validation.

#### Observation schema

Implement proposed `ExpertLookaheadTrace.swift` and a versioned writer under `SlotstreamDiagnostics`, with package-level events where needed. Dense hidden features are collected for main decode/verification positions only. Prefill contributes token/route/cache-admission events and the final context required by the next pass, not full per-token hidden tensors. The 45 GB storage allowance below excludes dense prefill features. Every completed request has:

- Run/request/family/split IDs; immutable prompt/template/tokenizer hashes; sampling seed/settings; context lengths; generation cap; emitted token IDs and finish reason.
- Actual engine/model/Metal identities, execution mode, MTP depth, resolved flags, total budget, actual slot count, prefill policy and cache reset/warmup policy.
- Monotonic event sequence, forward/pass ID, phase (`prefill`, `main-verify`, `main-plain`, `mtp-head`), input IDs, physical row/position, source feature kind, source layer and target layer. Do not infer phases merely from row count.
- Start features for each actually evaluated main position; per-layer `x2` features; exact ordered top-10 router IDs; which positions were later kept or rejected. MTP-head router IDs have a separate namespace and are not main labels.
- Demand keys after deduplication; hits/misses before this layer's reservation; CLOCK hand/reference state at replay boundaries; insertion/victim events; pool capacity; file-record/byte counts; raw-read issue/completion, demand arrival, staging adoption, and completed-layer times when instrumented.
- Complete/incomplete/error state, shard lengths and checksums, resource observations and capture timing. A cancelled or partial forward cannot masquerade as a full all-layer training example.

Store BF16 feature bits losslessly as little-endian uint16 or native BF16 safetensors with an explicit schema. Do not silently cast BF16 to FP16. The reader must round-trip known BF16 bit fixtures. Store exact IDs and variable position counts separately; derive dense selection targets from top-k IDs. The runtime target remains frozen and gradients never flow into it.

Capture `x2` after its existing layer evaluation has materialized it: retain only the current layer's small feature reference, copy to an owned CPU buffer at the completed-layer boundary, and release it before subsequent pool reuse. Verify that this does not add a GPU evaluation or retain the expert arena. Never retain full request tensor graphs in the writer. The writer owns CPU buffers only, with a 256 MiB queue ceiling and bounded shards (at most 256 MiB payload each). Backpressure is allowed during collection and must be reported; dropped rows are not allowed. Disk-full errors close the incomplete shard and stop collection.

Prefer full 48-layer `x2` capture for the pilot, since it lets us compare horizons without rerunning the teacher. Continue full capture for the larger run if overhead and resource gates pass. If capture adds more than 25% wall time on the capture qualification, first fix materialization/serialization. If it remains too costly, freeze an explicit selected-anchor capture schema after the pilot, train only models supported by it, and label comparisons with unequal feature coverage. Do not silently combine missing features as zeros. Freeze capture schema/anchors per dataset version and compare candidates on a common eligible subset when claiming an architectural advantage.

#### Storage, replay and resumption

Use ignored `.build/expert-lookahead/<run-id>/` for active shards, packs and logs. Preserve the frozen protocol, source/binary identity, corpus manifest, selected model, result summaries and reproducible drivers in the public brain when the experiment ends. Large lossless data archives need an explicit artifact manifest with hashes and a retained location; a missing ignored directory is not a reproducible dataset. Use distinct basenames for a source wrapper and its payload directory. Never modify an existing source wrapper or failed run in place.

The historical `642/512` ratio measured depth one and must not size the new depth-two collection. Budget at most three evaluated main positions per emitted output: full 48-layer BF16 `x2` capture is at most 36.864 GB per 50k outputs (`50,000 * 3 * 48 * 2,560 * 2` bytes), and global feature pairs add at most 3.84 GB. Allow roughly 45 GB including labels and metadata, excluding dense prefill features. Require at least 100 GB free incremental storage for active shards, a preserved copy and working space. These are planning bounds, not measured depth-two capture volumes; reprice actual bytes and predictor work after the pilot. Monitor actual growth and stop before exhausting the reserve. Eight- or twelve-anchor figures from earlier discussion do not apply to full capture.

Resume only at completed request boundaries with exact identities. In v0, each dataset request starts from an empty expert/ngram/prefix state in the same long-lived teacher process using a qualified reset path, or a fresh process if a safe reset is unavailable. `SlotPool.diagnosticDiscardResidency` already clears mappings/reference bits/hand while retaining storage, but requires completed readers and zero pins; qualify its use together with fresh model state and cleared ngram/prefix state rather than assuming it is a complete engine reset. Record which strategy is used and its timing. Replay the actual prefill admission and decode cache evolution. Do not let a lost cache state change training labels on a silent mid-request resume. Extra timing from fresh launches belongs in the updated estimate.

Numeric examples from a swapping run may remain usable training data if complete and capture-correct; their timing is ineligible for latency claims. Keep this distinction explicit. Generate held-out timing requests separately from feature collection, with tensor logging disabled.

### 6. Training, offline evaluation and model selection

Use local MLX Python in a dedicated reproducible environment. Existing `.venv31` has MLX 0.31.1 and `.venv` has 0.32.2 at review time; do not alter the reference parity environment. Record the interpreter and exact packages. A trainer environment may use the available newer version, but export is qualified against the native Swift/MLX build. Do not assume a Python version match proves identical inference.

Initial trainer recipe: G128, batch 512 (fall back to 256 if needed), AdamW, learning rate `3e-4`, weight decay `1e-4`, gradient norm cap 1.0, at most 20 epochs, validation every epoch, patience three, training seeds 17/29/43. These are explicit starting hyperparameters, not measured optima. Keep one configuration initially; use learning rates `1e-4` and `1e-3` only if the pilot shows nonconvergence. Use greedy teacher collection with seed 42 and thinking disabled in v0; sampled correctness fixtures separately freeze their settings and seeds. The trainer must log total examples, target positions, steps, seed, loss, validation metrics, elapsed time and peak footprint, and must resume optimizer/RNG/shuffle state exactly.

Use balanced multilabel BCE over each layer's selected ten versus the other 502 experts, so predicting all negatives cannot win. Do not weight away a selected expert merely because its router contribution is small: its bytes are still required. Balanced loss outputs are not calibrated probabilities. Fit any calibration/thresholds on validation only; evaluate ranking and budget-constrained utility separately. Add miss-focused sample weighting only as a named ablation after the general predictor works.

The trainer consumes bounded batches from shards; it does not load the teacher or all features into GPU memory. Start with an 8 GB process working-set ceiling, at least 12 GB reclaimable before launch, and no teacher, benchmark or large build running concurrently. The small network normally needs much less, but the ceiling includes loader, optimizer, runtime cache and activations. A measured breach stops the run and leads to a smaller batch or buffer before retrying under a new recorded attempt.

#### Metrics and scheduler simulation

Implement proposed `Tools/expert_lookahead.py` commands for `validate-data`, `train`, `evaluate`, `export` and `report`, with strict JSON protocols. Use the existing `Tools/cache_policy_probe.py` as the batch-pinning reference, not the older hindsight-hot-set simulator. The no-prefetch replay must reproduce native miss counts and requested bytes at recorded request boundaries before its predictive results are trusted. Record initial slot mapping, CLOCK hand and reference bits, and all prefill admissions needed for this match. Validate scheduler replay on the actual warmed cache state used by B1, captured separately without using its instrumented timing as a performance result. Cold-request training traces alone do not establish warmed-cache utility.

Report ordinary recall@10/16/24/32 by layer/horizon/feature kind, but select using:

1. **Timely missing-byte coverage:** baseline-demand-miss bytes available in staged buffers by their demand deadline, divided by baseline-demand-miss bytes. Each record is counted under an explicit deduplication/reload rule.
2. **Useful-prefetch precision:** consumed prefetched bytes divided by all prefetched bytes, including cancelled/expired reads. Report partially completed wasted bytes separately.
3. **Traffic amplification:** all candidate requested file bytes divided by baseline requested file bytes, including demand reads, speculative reads, failures and reloads.
4. Exposed demand waits, predictor/feature-transfer overhead, staging high-water, extra cache misses caused by reduced capacity or retention changes, and per-layer arrival slack.
5. Native emitted-token throughput, time per output token, total request duration, TTFT, tail latency, resource exclusions and exact correctness. These decide adoption.

Baseline timestamps alone overestimate future lead time when earlier stalls are removed. Run a causal event simulation that advances deadlines as the candidate becomes faster, and vary service times under shared CPU/SSD/RAM contention. Calibrate using unprofiled native observations and the isolated attribution evidence; do not turn simulated milliseconds into measured gains. The perfect oracle is subject to the same staging capacity, release lifetime, queue lanes and cache budget as learned candidates.

Freeze candidate ranking rules and thresholds before unsealing test data. Proposed useful targets are 50–70% timely missing-byte coverage, no more than 1.20x total file bytes, predictor plus scheduler exposed overhead under 2% of baseline decode, and at least 10% actual throughput gain. These are engineering targets, not four mathematically equivalent quantities. Use native demand-wait events to distinguish a ready raw buffer from a fully installed cache slot; v0 does not promise that GPU scatter time is hidden. A target miss triggers diagnosis; only correctness/resource gates are immediate hard stops. The final throughput criterion cannot be replaced by hitting the classifier targets.

### 7. Bounded prefetch runtime, version zero

The first runtime performs **raw staging prefetch only**. It does not speculatively insert experts into the slot pool, change CLOCK retention, or reserve future pool victims. The normal router and demand path decide which bytes enter which slots. This avoids turning an incorrect forecast into a speculative cache eviction while still testing whether reads can overlap useful work.

Implement proposed `ExpertPrefetch.swift` with an immutable ticket identity `(request, pass, model/store revision, feature/predictor version, key set)`. A ticket moves through queued, reading, ready, adopted or discarded/error. Every ownership transfer is single-use. Workers have immutable checked file descriptors/offsets and their own raw buffers; only the model owner touches MLX, pool maps, pins and model state.

The scheduler starts forecasts before main verification. At each existing completed-layer boundary it updates eligibility, drops expired candidates, and starts a bounded set of near-deadline reads. Filter resident/loading/ready keys at admission; retain soft forecasts for resident keys so a later eviction can make them eligible again; avoid duplicate reads; rerank soft queued work after a refinement. Distant layer scores stay metadata until their deadline window approaches. Start candidate ranking caps at top 16 and 24 per position/layer, and choose admission thresholds on validation under the global byte cap. These are candidates to rank, not instructions to load that many experts immediately.

Raw payload capacity is at most **32 expert records**, or **88,473,600 bytes**, across reading, ready and adopted-but-not-yet-released speculative buffers. This is one shared cap across all positions, layers and predictors, not one cap per draft; additional positions do not multiply the I/O lanes or memory reservation. Initially reserve **128 MiB incremental runtime memory** for predictor weights/workspace, metadata and these buffers, inside the existing total process target. Ordinary demand staging already belongs to the baseline ledger; both must fit simultaneously. Count each physical allocation once and keep it charged until the consuming GPU graph releases it. If the measured predictor representation/workspace exceeds the reserve, shrink staging or revise the declared reserve before testing; never conceal the excess in a larger process target.

Deduct the incremental reservation before solving expert capacity. A 128 MiB reservation corresponds to at least 49 current expert records if paid entirely from the pool. That is arithmetic, not a claimed final planner slot count. Benchmark actual plans. Do not assume model weights are the only incremental memory or retain aliases that cause a full expert arena copy.

Share a bounded I/O lane budget with demand reads. Start at the existing default checked-read queue depth, not an additional full set of lanes per speculative ticket. Demand has highest priority; stop launching speculative work when demand needs bandwidth. A prediction must not set CLOCK reference bits or pins; only real demand does so in v0. Cancellation is checked between checked read operations, including EINTR handling; an already blocked `pread` cannot be preempted instantly. Never claim zero cancellation latency. Avoid head-of-line waits on an unrelated multi-layer batch: completion/adoption must be available per record or per same-deadline mini-batch, while retaining the nine-piece record's all-or-nothing validity. A single owner-side scheduler enforces lane and byte limits across speculative and ordinary demand reads; replacing `concurrentPerform` with a shared executor must preserve checked-read joining and error propagation.

When `ensureCore` sees actual misses, preserve its current key order, hit pins and victim reservation. For a complete matching raw ticket, adopt the bytes into the usual MLX staging/scatter dependencies. For a matching in-flight record, promote it to demand and join it without launching a duplicate. For absent, stale or unusable data, use the ordinary checked read. Adoption is zero-copy by default: a ready ticket's nine buffers become their own MLX staging arrays and scatter per record or per same-deadline mini-batch, while the demand batch keeps its existing contiguous layout, so mixed ready/demand records are assembled by ordering scatters in the logical demand order, not by copying bytes. A host copy into the contiguous batch layout is allowed only as a measured fallback: copying every adopted record would repeat a 2,764,800-byte copy per miss, so its bytes and time are reported in C09/B2 and count against the 2% overhead budget. Publish slot mappings only after all nine pieces are complete and the required scatter dependency is installed. A partial read must not become a cache hit. Completed-reader cancellation and adopted-buffer lifetime tests must prove that freeing a ticket cannot free bytes still referenced by a scatter graph.

A failure on a purely speculative unused read records the failure and disables/discards the hint as appropriate; it must not expose invalid bytes or fail an otherwise valid unrelated demand. A requested record still goes through the authoritative checked-read error policy. Model-file mutation, identity mismatch or checkpoint corruption is not silently treated as a harmless predictor miss.

On request completion, error or cancellation, stop submissions, join owned readers and release buffers. Clear future-pass predictions after MTP reconciliation. On model/store replacement, pool resize, prefix restoration or request reuse, invalidate old ticket identities before new work; join before closing descriptors or releasing ownership. The next request must not consume a prior request's speculative ticket. Close and drain the scheduler before a between-request governor resize. Byte-compatible cache entries already installed by real demand remain governed by normal cache rules.

### 8. Correctness and resource acceptance

Implement proposed `ExpertLookaheadChecks.swift` in diagnostics and catalogue cases in `SlotstreamTestKit`. Model-free tests exercise the scheduling state machine and trace reader. MLX checks exercise staging ownership and exported predictors. Real-model checks qualify only after these pass.

| Case | Required proof |
| --- | --- |
| C01: collector and disabled path | No feature work/allocation when disabled; capture-on/off has exact routes, logits and tokens on a bounded native fixture with identical math settings. All 48 layers and actual MTP rows join to the correct pass. |
| C02: causal features | Position zero uses previous committed main state; positions one and two use their corresponding existing MTP proxy states and known draft embeddings. Verify all three feature/label joins, shorter context-bound passes and rejected positions. No current-pass target state appears in input; L128 forecasts only from a completed source layer. Kept/rejected labels join correctly after reconciliation. |
| C03: file/schema integrity | Detect truncation, bad length, wrong geometry/ID, missing feature, NaN, wrong byte order, corrupt checksum, duplicate row, partial pass and wrong pack/checkpoint identity. Resume cannot silently append to an incompatible run. |
| C04: bounded demand priority | Fake readers cover all-hit/all-miss, duplicate keys, late/expired hints, overfull queue, slow unrelated reads and mixed completion. No deadlock or extra lane budget; demanded hits pinned before victims. |
| C05: read failure and lifecycle | Partial piece, EINTR, cancellation before/during/after read, ticket double adoption, stale pass/model, close, reset and resize. All owned tasks join; buffer release is exactly once; demanded errors preserve current behavior. |
| C06: native byte and math parity | Ready, in-flight and fallback paths deliver identical nine-piece bytes; arbitrary correct or wrong forecast lists cannot alter exact router IDs/ranks, expert kernels/reductions, logits, output IDs or text. |
| C07: MTP and sampling | Depth-two accepted/rejected paths, including zero, one and two accepted drafts, EOS, context tail, seeded sampling and continuation keep state, main/MTP offsets, RNG order and outputs exact against the same baseline mode. No cross-depth identity assumption. |
| C08: serving interactions | Request cancellation, subsequent recovery, cold and repeated requests, prefix reuse on/off, between-request resize and model shutdown. Unsupported modes explicitly use the original path; those checks prove bypass behavior, not acceleration in unqualified prefix/elastic/vision/plain configurations. No public API schema break. |
| C09: actual memory | Sample RSS/physical footprint, raw-buffer lifetime, allocator cache and staging high-water. No whole-pool copy, retained feature graph, undeclared capacity, growing request leak, or clean-timing claim from a swap-contaminated interval; global paging is recorded as a diagnostic, not a functional failure. |
| C10: export/inference | Python and Swift agree on shape, finite scores and bounded score error, with exact ranking on non-tie fixtures. Near-tie differences are recorded; the exported pack itself is the object used for native selection and timing. |

If arithmetic diverges, fix dispatch/lifetime/dependencies before proceeding. Do not relax the gate to 'the answer looks similar.' Exact diagnostics are separate from final timing so retaining state arrays or collecting logits cannot distort performance/memory measurements. Release diagnostics snapshots before measuring liveness.

Before every heavy step: inspect the current model lock and process inventory; do not stop another task's process; check actual reclaimable memory and normal pressure. Ordinary equality runs use 8.1–10 GB targets; existing MTP/vision and governor gates retain their own documented exceptions. Training, native inference, large builds and storage-heavy jobs run sequentially on this Mac.

Primary performance and collection target: 24 GB, with at least 29 GB reclaimable at launch and the incremental predictor/collector memory charged inside the configured target. Require nominal sampled thermal state, low-power mode off, stable swap counters and a sampled 120-second readiness interval before timing arms, with at most 600 seconds waiting per launch. Preserve all refusals and exclusions. If this profile cannot run cleanly, stop that cohort; a separately frozen 20 GB profile with a 25 GB preflight is allowed as a new experiment, never a pooled replacement. No cache purge, memory hog or simulated availability override. Never reduce the existing minimum slot floor to fit predictor memory. If an explicit low-budget configuration cannot admit the reservation, exercise the documented refusal/bypass behavior; run enabled equality checks at a budget that fits.

The 24 GB target is a conditional fixed-budget experiment, not the automatic-40%-RAM multitasking recommendation. The recent 24/20 GB depth attempts and full MTP checks encountered resource exclusions; neither a clean depth-two cohort at those sizes nor full MTP qualification follows from adopting two drafts. Complete the outstanding native MTP gate on the actual experiment baseline before P5 held-out qualification; the P5 dependency carries this. Under [[records/decisions/global-paging-is-diagnostic]], that functional gate is decided by process-footprint ceilings, real headroom, pressure handling and completed numerical checks, with global paging recorded separately; timing eligibility in Section 9 keeps its clean-interval rule. If the declared profile cannot obtain clean readiness, record a stop or freeze a separate lower-budget cohort; do not relabel contaminated timings or pool different cache policies.

### 9. Native benchmark and decision protocol

Use one frozen candidate binary with experimental control off/on where possible, and verify its off path against the pristine baseline. Use no hidden-feature trace, tensor snapshot or intrusive GPU profiler in headline timing. Keep lightweight counter overhead common and check a counters-off control on the pilot. Compute a rolling main-router digest and count from IDs already materialized on the host in both arms; full route/logit tensor comparisons belong to separate correctness runs. Bind corpus, pack, driver and binary hashes before collecting final results.

Three comparisons distinguish the mechanisms:

- **B0, headline:** current deployment with its full ordinary cache at the fixed total budget, versus predictor plus charged memory at that same total budget. This is the user-relevant gain.
- **B1, mechanism:** identical reduced slot count/reservation in both arms, predictor/prefetch off versus on. This separates overlap benefit from lost cache capacity; do not advertise it as B0.
- **B2, overhead:** same candidate pack/features executing in shadow mode, without reads, against its matching no-predictor capacity control. This reveals feature, model and host overhead.

Run B1/B2 on pilot validation fixtures, select a candidate, then freeze it. Do not run a full matrix of every width, horizon and cache size. The final held-out B0 cohort has six workload families, two frozen prompts per family, and three preplanned paired rounds per prompt: 36 pairs, 72 measured arms. Its twelve prompts are drawn from the sealed test families only; no B0 prompt, paraphrase, template or source document may belong to a train or validation family, and the frozen corpus manifest records that check. Each arm starts a fresh server, performs the same-prompt 128-output warmup, then measures up to 512 outputs. Greedy sampling, seed 42, thinking disabled, maximum context 32,768, prefill chunk 256 and all other controls remain fixed. Arm order alternates by prompt/round and family order rotates. Keep EOS and all work counters; an earlier clean EOS is valid if both arms match exactly, but report its shorter length and do not call it a 512-output observation.

**Any target-route, logit or output mismatch is a correctness failure of the candidate, including a mismatch in a resource-contaminated run; preserve it and investigate before proceeding. It cannot be hidden by excluding a timing pair. An actual candidate budget overrun, unsafe lifetime or unexplained runtime failure likewise blocks qualification until resolved.** A pair is timing-eligible only if both arms complete, match exact token outputs and required settings, stay within their budgets, and have no observed swap changes, non-nominal thermal state or declared contention during the measured request. Preserve excluded rows. Require at least two clean pairs per prompt out of its three frozen rounds. There are no replacement pairs inside this cohort. If eligibility is insufficient, report inconclusive and design a new named cohort after the environmental issue is resolved.

For each prompt compute the median paired throughput ratio `TPS_on/TPS_off`; aggregate with equal family weight using geometric means. Report all prompt/family ratios, absolute TPS, time per output, request duration, TTFT, p95 native per-token callback interval (separately from HTTP chunk arrival), read traffic, hit rate, MTP accepted/evaluated counts, memory and exclusion rate. Avoid pooled-token weighting that lets a long easy task dominate. Use 10,000 bootstrap draws with seed 1729, resampling prompts within each family and eligible paired rounds within each selected prompt; keep family weights equal. Report the 95% interval and explicitly limit inference to this small frozen workload set.

**Initial proof passes only when:** all relevant C01–C10 checks pass; B0 aggregate throughput ratio is at least 1.10; the lower bootstrap bound is above 1.00; no family median throughput ratio is below 0.95; and no family median total-request duration regresses by more than 5%. Investigate material TTFT/tail regressions before calling the candidate usable. A positive point estimate with insufficient clean evidence is inconclusive, not success. Lower than 10% can be recorded as a small measured benefit but does not close this experiment's success gate.

The 50k training-data budget excludes benchmark outputs. This full B0 design can add up to 36,864 measured outputs and 9,216 warmup outputs. Existing release-wide checks are additional work and are not included in the first model-training estimate.

### 10. Ordered work packages and exit conditions

All items start pending. Execute dependencies in order; implementation can proceed without model runs while the Mac is occupied. Proposed tool names below must be implemented and their help/protocol validated before use.

| Package | Work and outputs | Dependency and exit |
| --- | --- | --- |
| P0: freeze and fixtures | Record baseline identities, exact flags, mode/budget, local environment, public corpus/splits, and JSON protocol schema. Define target resource and artifact locations. Freeze B0/B1/B2 arithmetic and a small correctness corpus. | First. Existing model provenance is usable only if files still match. No data run until preflight and schema checks work. |
| P1: causal capture | Add separate collector events, bounded shards, BF16 round-trip, request/pass/MTP labels and cache replay events. Add proposed `expert-lookahead-capture` diagnostic CLI and `validate-data`. Qualify short capture-on/off and then about 1k outputs. | P0. C01–C03 pass, no dropped rows, replay counts match, actual capture overhead/storage measured. The 1k is included in the pilot only when its schema and teacher identity match the frozen pilot; otherwise preserve it as separate qualification data. |
| P2: prefetch foundation | Split checked raw reads from MLX wrapping; implement tickets, shared lane budget, demand adoption, reserve accounting and disabled controls. Use deterministic wrong/late/empty hints and an oracle only in diagnostics. | P0 and P3a's offline continuation rule; P1 trace events aid comparison. Model-free groundwork may start earlier while the Mac is busy, but P2's native qualification does not start before P3a passes. C04–C09 pass on small fixtures; no heavyweight trace in speed tests. A trained pack is not required to test safety. |
| P3a: offline pilot | Collect about 8k train/2k validation, train G64/G128 and L128, evaluate cheap baselines and the bounded oracle in the calibrated replay, export the leading candidates, pass C10 and measure native pack inference cost in isolation. | P1 only. Apply the offline continuation rule below: proceed to P2 and P3b, select a cheap policy, or record a justified stop before any prefetch runtime is built. No sealed test access. |
| P3b: native pilot screen | Run B1/B2 and the small B0 validation screen on the preferred candidate, then on the runner-up if needed. | P3a passed and safe P2. Apply the native continuation rule below; select viable feature/horizon/scheduler settings, or record a justified stop. No sealed test access. |
| P4: 50k campaign | Complete roughly 40k/5k/5k splits. Compare nested learning curves, repeat selected candidates with three seeds, calibrate/export and freeze one pack/policy. Use G256 or combined refinement only under the stated criteria. | P3b shows useful visibility and a plausible native opportunity for a learned candidate; skipped when the selected candidate is a non-learned policy, which P5 then qualifies directly. Data, trainer and exported pack manifests complete. Stop expansion if scaling evidence does not support it. |
| P5: held-out qualification | Run frozen B0 cohort and remaining C01–C10 native checks, then a bounded sequential-request leak/cancellation check on the final candidate. Compute predeclared metrics without tuning on test outcomes. | P4 candidate frozen, or the non-learned policy selected at P3b; the frozen experiment baseline has completed the existing native MTP gate under the standing paging-diagnostic memory policy. Result is success, small benefit, no benefit, correctness failure, or resource-inconclusive, with all exclusions preserved. |
| P6: closure | Record result, source/pack/corpus hashes, exact reproduce commands, artifact locations, failed attempts, limits and next decision. Regenerate projections, validate claims/store, stop owned processes and verify installed artifacts unchanged. | P5 or an earlier justified stop. Initial experiment complete without requiring a win or default activation. |

**Offline continuation rule (P3a):** first require correct complete capture. In the calibrated validation replay, the bounded oracle must project at least a 1.10x ratio after charging the measured predictor overhead under the nominal calibrated service model, and at least one candidate policy, learned or cheap, must project at least 1.05x under the same model; report the service-time sensitivity alongside both. Training continues past the pilot only if a learned candidate has at least five percentage points more timely missing-byte coverage than the strongest cheap baseline or lower file traffic at comparable coverage; otherwise the strongest cheap policy is the candidate and no further training is scheduled. Stop this setup before building P2 if even the bounded oracle cannot plausibly meet the target and no specific capture or replay defect explains it. A high-accuracy classifier alone does not meet this rule.

**Native continuation rule (P3b):** require safe demand adoption. Run a validation screen with two frozen prompts from each of code, reasoning and prose, two paired rounds per prompt, at most 128 measured outputs after the same warmup: 12 pairs. Require at least one clean pair per prompt and an aggregate median-pair B0 gain of at least 3%. This is a permissive screen, not a qualified speed claim. If it fails, inspect correctness, visibility, overhead and scheduler timing using the existing pilot before generating more data. Record any revised pilot as a new protocol and keep the original outcome.

Shortlist at most two native candidates, packs or non-learned policies, using the calibrated validation simulator. Prefer the smallest candidate whose estimated throughput ratio is within 0.01 absolute of the best ratio under the same schedule and budget, counting a non-learned policy as size zero; break ties by lower measured predictor overhead, then lower traffic. Run the native pilot screen on the preferred pack, and on the runner-up if the preferred pack fails. Do not substitute recall percentages for throughput ratios in this selection rule. Never select using final test outcomes. The final three-seed comparison assesses stability; select its exported pack by the same frozen validation criterion, not the best test score.

Implementation command contracts: the proposed driver reads one frozen protocol and exposes `prepare`, `capture`, `validate-data`, `train`, `evaluate`, `export`, `bench`, `report`. Mutating/executing stages require explicit subcommands, never run as a side effect of `prepare` or `report`. `prepare` resolves paths and refuses unresolved identities; it does not claim a nonexistent future binary is qualified. `capture` resumes completed requests only. `bench` verifies pack/binary/protocol identity and finite attempts. Reports return nonzero for incomplete required evidence rather than a misleading success banner.

Use existing supported checks (`make checks`, `make checks-all`, scoped diagnostic commands) as appropriate after code changes. Complete the affected existing `Tools/verify.sh` acceptance gates before any production activation. Do not run the entire heavyweight battery for this planning-only change. Default-on adoption, docs/claims, distribution, independent API compatibility and installed upgrade/rollback are a separate follow-up after a positive qualified result.

### 11. Scaling rules and conditional extensions

Stop adding data when the fixed validation learning curve shows less than approximately 2–3 percentage points of extra timely missing-byte coverage and less than 1–2% projected incremental throughput from the latest near-doubling, within its uncertainty. These are stopping heuristics, not measured universal sample-complexity laws. Use actual native screens to check the projection. Do not claim 50k is the mathematical optimum.

If both training and validation utility are low, compare G256 and shorter horizons or better causal features before collecting more. If training is strong but held-out utility is weak, prioritize new source families and undercovered contexts. If predictions are accurate but late, change deadlines, batching or feature availability. If on-time coverage rises but TPS does not, investigate read contention, staging copies, CPU work and cache capacity. Repeated epochs cannot fix missing information or an already saturated critical path.

The initial collection budget in this plan is scoped by its frozen local run protocol to roughly 50k outputs, not an automatic unbounded search. A larger 100k/200k successor needs a new recorded decision and resource/time estimate based on the measured curve. Preserve the original held-out test result; after using it for a design decision, obtain a fresh untouched test cohort for the successor. No cloud expenditure is implied by any scaling result.

| Extension | Entry condition | Required additional work |
| --- | --- | --- |
| Forecast-guided retention/admission | Raw prefetch is useful but traffic or lost residency still limits speed. | Separate policy ablation versus CLOCK at equal total memory, causal next-use estimates, bounded victim search, no permanent speculative pins. Predict retention horizons such as 8/32 future outputs only with exact emitted/physical-position labels. |
| Final-prefill cache seeding | Cold decode misses dominate and a forecast exists before each relevant layer's final sweep admission ends. | Hook `admitOnSweep`/commit timing correctly; charge preload reads and TTFT. Final-model hidden state is too late to retroactively retain already-released early-layer weights. |
| Earlier start forecast | Waiting for the draft head wastes significant available read overlap. | Predict pending-position demand before drafting, then revise with existing draft features. Measure the second invocation and cancellation/waste. No additional MTP step. |
| GPU slot lookup and groups of layers | A working predictor covers enough required bytes and repeated host handoffs remain costly. | A 48x512 Int32 expert-to-slot map is about 98 KB, but exact actual routing, slot lifetime and map epochs remain mandatory. Pin a bounded group, check actual residency on GPU and design private group state transactions. |
| Provisional multi-layer execution | Group coverage and saved host time can outweigh replay. | Freeze pool mappings/weights while readers run. Missing slots use safe provisional storage and validity flags; never publish logits, RNG advances or state before success. Restore the input residual, all GDN/PLE/conv state, KV/indexer offsets and caches, ngram context, token/recording flags and relevant MTP/last-multi metadata before replay from a valid boundary. Existing token checkpoints are insufficient; do not bypass `StateRecovery` validation. Qualify unchanged kernels/math or reject the approach. |
| Larger or per-layer predictors | A smaller network demonstrably underfits useful causal labels. | Compare utility per added inference microsecond and memory byte, not parameter count or training loss alone. |

Predicting all layers does not make their dependent hidden-state computation parallel. Predicting expert outputs would change answers unless verified by the original computation, which removes the obvious saving. Keep those distinctions explicit. Research such as [ProMoE](https://arxiv.org/html/2410.22134v3), [pre-attention prediction](https://arxiv.org/abs/2511.10676), and [SP-MoE](https://arxiv.org/abs/2510.10302) motivates feature lookahead and scheduling; their architecture, memory placement and reported gains are not this experiment's baseline.

### 12. Time, cost and completion definition

Rental cost is zero. Electricity and local storage usage are not claimed to be literally free. No new machine is needed for the proposed bounded experiment.

| Work once implementation exists | Planning allowance | What changes the estimate |
| --- | --- | --- |
| 1k collector qualification | 15–30 minutes | Capture overhead, prefill and process startup; reusable pilot data only if final schema and identity match. |
| 10k pilot data, training and initial inspection | 1–3 hours | Included in the larger run; not a separate extra 10k. Native scheduler qualification is separate implementation work. |
| Full 50k data collection | 2–4 hours total | About 72 minutes decode at the historical depth-one 11.65 TPS observation, not a measured depth-two projection; prompt prefill, capture and process/reset strategy add the rest. Reprice from the depth-two 1k qualification before extending collection. Context mix or reduced slots can make it longer. |
| One small model fit | 15–60 minutes | Unmeasured estimate, replace with pilot step throughput. Full architecture/seed search is extra. |
| Initial offline evaluation and native screen | 1–2 hours | Bounded validation fixtures only; not complete production qualification. |
| Initial 50k experiment to first screened candidate | About 4–8 hours machine time | Assumes tooling is built, modest capture overhead and a quiet Mac. Its components are depth-one derived; reprice from the depth-two 1k qualification. The ranges overlap; do not sum pilot time twice. |
| Limited ablations and final seed repeats beyond the first candidate | About 1–3 additional hours | Bounded shortlist only; reuse completed fits and measure this in the pilot. |
| Full held-out cohort and final scoped checks | About 4–8 additional hours | Includes up to 72 fresh arms, warmups/readiness, correctness and lifecycle checks. Resource failures may prevent a conclusion. |

Allow approximately **9–19 hours of machine time for the complete planned campaign**, including the initial 4–8 hour screen, limited repeat fits and full held-out qualification. This is conditional on clean resource availability and on repricing its depth-one components after the depth-two 1k qualification; it is not a guaranteed elapsed-time deadline. The pilot and data-collection rows are components, not additive campaigns. Engineering is separate from those machine-time estimates and is not sized in human time: it is sequenced by dependency, P0, P1, P3a, then P2 and P3b, and each package ends on its exit criterion. Its components are the collector/protocol, raw-read ownership and memory accounting, trainer/export, diagnostics and benchmark driver, built on existing infrastructure. Multi-layer GPU transactions are a separate project and are not included. The earlier '4–8 hours' discussion described an initial run after tooling exists, not delivery from today's code through release qualification.

**Plan completion** means the work packages, causal features, memory ownership, data split, benchmark arms, stop rules and evidence requirements are concrete and mechanically consistent. **Experiment completion** means P6 records an honest outcome with reproducible evidence. **Successful acceleration** additionally requires the P5 gate. **Production readiness** additionally requires affected existing acceptance, mode coverage and explicit adoption work. None implies the next automatically.

### 13. Review record and execution checklist

**Review pass 1, architecture and code:** checked configuration, MTP feature production/consumption, router synchronization, `x2`, layer completion, raw staging, CLOCK reservation and checkpoint validation against the named sources. Corrected the PLE index, distinguished draft-proxy from main hidden features, separated the existing ID trace from the proposed collector, and named the existing residency-reset seam without claiming a full reset already exists.

**Review pass 2, ownership and correctness:** narrowed v0 to raw staging with demand-time installation; charged adopted buffers until graph release; required one shared I/O lane budget and record-level completion; retained soft forecasts for later-evicted keys; made mismatched outputs a hard candidate failure even in excluded timing runs. No current runtime has been certified by this review.

**Review pass 3, experiment and cost:** included pilot validation within the 50k dataset, sealed test selection, excluded dense prefill features from the collector, distinguished cold-data replay from warmed serving state, and specified B0/B1/B2 plus finite clean-pair rules. Separated first-candidate timing from seed repeats, confirmation outputs, readiness waits and engineering. Added a concrete pilot continuation rule and data-scaling stop rule.

**Review pass 4, mechanical checks:** independently recomputed parameter counts, raw staging bytes, reservation slots, feature-volume estimate and final benchmark arm/output counts; checked existing source paths and all wiki links; regenerated the plan projection and ran store/claims/whitespace checks. Final results are recorded with the plan's closure log. Proposed files/commands remain clearly labeled as future implementation. Existing historical log warnings are preserved rather than rewritten.

**Review pass 5, separate code-grounding review (2026-09-11):** verified every code-map claim against the repository at the review commit, recomputed all counts, resolved all links and confirmed the three cited papers. Corrected seven items: split the pilot into an offline P3a that depends only on P1 and a native P3b, so no prefetch runtime is qualified before the oracle and candidate projections justify it; made non-learned policies eligible final candidates, with P4 skipped when one is selected; required C10 export parity at P3a; restricted B0 prompts to sealed test families; made ticket adoption zero-copy by default with any copy charged to the overhead budget; removed the human-time engineering estimate in favor of dependency order; and pointed the collector at the existing numerics and router observers.

Execution begins with P0, not with training. The implementation agent must be able to answer all of the following from the frozen protocol: which exact code/model runs; what features exist at each forecast moment; how MTP positions join to labels; which bytes and lifetimes the memory cap counts; who owns every buffer; how a wrong/late/failed forecast falls back; how dataset leakage is prevented; which control receives memory back; what makes a pair eligible; and what stops work without changing the success rule.

**Alignment review, September 11:** The user-adopted two-draft default now controls both arms. The unrun design covers three causal forecast positions, three position biases and a conservative three-position storage bound. Preserve the old attribution ratios only as depth-one evidence. Re-freeze the actual implementation, three-position schema, resource estimates and all test protocols at P0; no collection or training has run under this update.

**Depth-two follow-up review, September 11:** Rechecked the native draft/verify loop and completed the remaining causal-test, per-position refiner, deduplicated admission and shared-budget details. P0 explicitly pins both arms to two because installed binaries may still default to one. Time estimates require a depth-two pilot, and the larger fixed-memory cohort remains conditional on fresh resource and baseline qualification. No model, training or prefetch experiment was started.

**Consistency review, September 11, after the depth-two edits:** re-verified the pinned depth, the disabled adaptive and tail controls, context-bound shortening, the three-position feature contract, the shared ticket cap, C02 and the repriced collection row against the native draft/verify loop at the recheck commit and against the cited records; recomputed every count and resolved every link. Carried the baseline MTP-gate precondition into P5, stated that release acceptance is separate evidence, marked the machine-time totals as depth-one derived, removed a duplicated phrase and logged the earlier unlogged evidence-row update. Aligned the functional MTP-gate and C09 wording with the same-day decision that global macOS paging is diagnostic, while timing eligibility keeps its clean-interval rule. No model, training or prefetch experiment was started.

### Addendum: execution outcome (2026-09-11)

Executed P0 through P3a and the P2 runtime on the 48 GB M5 Pro under protocol `xla-pilot-20260911` (20 GB profile with the 25 GB preflight, because the 24 GB profile's 29 GB reclaimable requirement was not met at freeze). C01, C02, C03 and the CLOCK replay passed on the correctness pair and on all 69 pilot requests (12,899 outputs, 4.40 GB). The offline continuation rule stopped the experiment: the bounded oracle projects 1.59x, but the best policy projects 1.0055x under the 1.20x traffic bound (G128 with L128 refinement, 1.48% timely coverage at 7.7% precision) and 1.064x without the bound at 4.9x traffic. Whole-pass models G64, G128 and G256 converged to the per-layer frequency prior; L128 learned (recall@16 0.19 against the prior's 0.12) but its nested learning curve adds about three points per data doubling. The P2 runtime was built and proven exact natively (C04 through C10 coverage as recorded), so the stop is about forecast precision, not the mechanism. P3b, P4 and P5 were not run and no speed gain is claimed. Evidence and the next decision: [[records/measurements/expert-lookahead-pilot-offline-stop-2026-09-11]].

## Expert Lookahead 2: replacement policy, router-reuse prefetch and early-layer memo
**Status: proposed, awaiting Carlos's approval. Nothing in this plan has run natively; no code path described as planned exists unless the code map says it does. It is the successor to [[records/plan/expert-lookahead-local-experiment-2026-09-10]], which closed at its offline continuation rule, and it stands on that experiment's exact capture, calibrated replay and proven prefetch runtime plus the three probes in [[records/measurements/expert-lookahead-router-reuse-and-replacement-probes-2026-09-11]]. No predictor is trained on the main path.**

### 1. Objective, scope and fixed decisions

Reduce the exposed expert-read time of MTP text decode on the existing Slotstream checkpoint by three exact mechanisms, in dependency order: a better slot replacement policy, prefetch driven by the model's own routers evaluated one and two layers early, and an early-layer routing memo keyed by token identity. Exactness is not negotiable: the chosen baseline's routing, expert bytes, numerical execution and outputs stay identical, so every mechanism only changes which bytes are in memory when. Levers that change arithmetic (dropping low-weight missing experts, lower-precision records, changed draft depth) are outside this plan; they are product decisions, not engineering steps.

The experiment runs on the existing 48 GB M5 Pro with its internal SSD. Rental budget is zero. The primary profile is the one the evidence was measured in: 20 GB target, 25 GB reclaimable preflight, 4,255 global slots (about 89 experts per layer), where demand reads take 38.7% of decode time. A 24 GB cohort (29 GB preflight, 5,702 slots, reads about 34% of wall time per [[records/measurements/decode-wall-time-attribution-2026-09-10]]) is a separately frozen experiment with a smaller ceiling, run only when the machine offers the headroom; results are never pooled across profiles.

Fixed decisions carried from the previous protocol: text mode, MTP at the adopted depth two ([[records/decisions/draft-depth-defaults-to-two]]), `SLOTSTREAM_DRAFT_DEPTH=2` pinned in every arm with adaptive speculation and draft-tail shortening off, prefill chunk 256, prefix retention off, elastic resizing off, greedy seed 42, thinking off, maximum context 32,768, the current resolved deployment optimizations, and the public 307-request corpus with its seed-1729 family split (train 242, validation 32, test 33 requests; test families sealed). The offline dataset is the pilot capture `xla-pilot-20260911` (69 requests, 12,899 outputs, 5,205 verification passes, 4.40 GB of shards): no new capture is required for any offline package, and the 50k campaign of the previous plan is retired.

The learned whole-pass predictor, its trainer and its packs are retired from the main path. The only trained component this plan admits is a small reuse-distance scorer for eviction, conditional, offline-only, and entered only if implementable policies stall (Section 5). "No training" is a property of the primary path, not a rule that forbids the conditional package.

Win criterion, unchanged from the previous plan: at least 10% repeatable native throughput improvement at equal total memory with exact correctness gates passing, on the sealed held-out B0 cohort. Each mechanism also has its own smaller gate so that a partial result is a complete outcome. No installed executable replacement, default activation, publication or release is part of this proof.

### 2. Evidence and what it permits us to infer

Code review baseline: repository commit `f60e9a83037fea576bf210a3650d6f29e164f183` plus the uncommitted Expert Lookahead sources and tools of the working tree (the `Sources/Slotstream/Expert*.swift`, `ExpertLookaheadTrace.swift`, diagnostics, CLI and `Tools/expert_lookahead*.py` files); every seam in Section 4 was read at that state on 2026-09-11, and the probe tools' hashes are in the linked run source. Freeze the actual implementation commit, binary, Metal library, checkpoint identity, resolved flags and protocol again at W0.

| Evidence | Relevant finding | Consequence |
| --- | --- | --- |
| [[records/measurements/expert-lookahead-pilot-offline-stop-2026-09-11]] | Capture is exact (collector on/off identical), the CLOCK replay reproduces 249,840 of 249,840 native demand events, the raw-staging prefetch runtime is exact under adoption, promotion and cancellation, and shadow forecasting costs 1.3 ms per pass. A perfect forecast projects 1.59x; the best learned policy projects 1.0055x. | The instruments and the mechanism are qualified. What failed was the forecast, so this plan changes the forecast and the cache policy, not the runtime. |
| Same record | Whole-pass models G64/G128/G256 converged to the per-layer frequency prior; the completed-layer refiner reached recall@16 0.19 against the prior's 0.12; recent-routes covered effectively no misses. | Pass-start features carry the marginal, which the cache already holds. Misses are the tail. Any predictor must be scored on misses, and any learned component must beat a free baseline first. |
| [[records/measurements/expert-lookahead-router-reuse-and-replacement-probes-2026-09-11]] (router reuse) | Applying layer T's real router to layer T-1's captured MoE input reproduces T's exact top-10 in 59.5% of positions (recall@16 72%), 50.0% two layers early, 45.3% and 43.2% at three and four; per-layer-group agreement is 0.55 to 0.66 except layers 1 to 3 at 0.43; the self-check at stride zero is 99.9996%. Under the plan's bounded twin with exact replayed residency, stride 1 top-10 covers 48.8% of misses in time at 40.1% precision and 1.73x traffic (1.17x by the read-cost model, 1.23x by the previous plan's formula), stride 2 top-10 39.8% at 30.7% and 1.90x (1.14x and 1.18x). | The model's own routers are a zero-parameter forecast 3.7 times more accurate than the trained refiner (recall@16 0.722 against 0.193). This is a lower bound: the probe uses the source layer's mixing and lacks that layer's MoE output; the native forecast uses layer T's own hyper-connection mixing on the live streams and omits, at stride 1, only T's attention sublayer, and at stride 2 the whole of layer T-1 (its attention and MoE updates) plus T's attention. The twin's traffic figures are upper bounds: it counts a correct but late ticket as wasted and the record as a demand read, whereas the native runtime promotes and joins it without a second read. |
| Same record (replacement) | On the 1,025 validation passes at 4,255 slots, native CLOCK hits 69.3%; exact LRU 70.5%, segmented LRU 71.7%, decayed LFU 67.3%; Belady's optimal policy 84.8% (167,620 misses against 338,147, verified against brute force). 66.6% of misses are re-references of experts evicted earlier in the same request. At 1.2x slots LRU removes 22% of misses, optimal 57%. | Replacement has a 15.5-point ceiling at this size, more than three times the 4.6-point static bound of M1 (a different size and workload). Off-the-shelf recency and frequency rules capture little of it (segmented LRU already exceeds CLOCK by 2.4 points offline), so the gap needs scan resistance, ghost history or reuse-distance prediction, all of which can be evaluated offline exactly. |
| Same record (structure) | A layer's demand read costs 0.61 ms for one missing record and grows by about 0.21 ms per additional record (seven records: 1.83 ms; fitted line 0.38 ms plus 0.21 ms per record); 97.3% of layer events have at least one miss; the lead from a completed layer to the next layer's demand is 1.16 ms median (that layer's attention sublayer), 5.5 ms two layers ahead. Layers 0 to 7 carry 22.2% of misses at 9.2 per event with the weakest temporal locality (previous-position overlap 0.21 at layers 0 to 3 and 0.27 at 4 to 7, against 0.34 over all layers); a repeated token routes to the same layer 0 to 3 experts 60% of the time, and 54% of positions repeat a token already seen in the request. Positions share experts: 22.4 unique per layer per three-position pass. | The objective is the read-cost model, not the miss count: clearing a layer's last miss is worth the fixed cost plus the marginal one. Stride 2 has the lead to hide a whole layer's misses; stride 1 has time for about four records. The early layers need a different signal; the token memo has one whose lead runs from pass begin, about one attention sublayer at layer 0 and one more layer for each memo layer after it. |
| [[records/measurements/m1-expert-locality-on-a-real-trace-2026-09-03]] and [[records/decisions/clock-stays-the-eviction-policy]] | On a 220-step single-token trace at 30 experts per layer, CLOCK 0.557, LRU 0.568, LFU-decay 0.480 and a static hot-set-plus-LRU bound 0.603; the decision keeps CLOCK and is reversible if a second workload shows a policy beating CLOCK by more than the roughly one point LRU managed. | The new bound is Belady's dynamic optimum on a second workload of 13 requests and six kinds, and it is 15.5 points, not 4.6. LRU is again within about one point of CLOCK, consistent with M1, but segmented LRU beats CLOCK by 2.4 points in the exact replay, which already meets the decision's literal condition offline. This plan deliberately raises the bar for reversing it to an exact native port with a measured gain (Section 5); the agentic many-short-turns-over-one-prefix workload the decision names stays uncovered. |
| [[records/measurements/decode-wall-time-attribution-2026-09-10]] | At 24 GB the profiled partition is 33.7% file reads, 1.6% staging, 32.0% GPU envelopes and 32.7% remaining CPU coordination. | Hiding reads does not remove the CPU share. Ceilings below are read-share ceilings, and the CPU cost of forecasting and policy bookkeeping counts against them. |
| [[records/decisions/global-paging-is-diagnostic]] | Host-wide swap counters do not attribute pressure to a process; functional acceptance is decided by process footprint, headroom, pressure handling and completed checks; timing keeps a clean-interval rule. | The previous readiness rule (swap-ins stable for 120 s) was not satisfied during the pilot's native pairs. Section 8 freezes a process-attributed eligibility rule for this protocol; it needs Carlos's explicit approval before any timing arm. |

**Ceiling arithmetic.** Use the measured read-cost model, not a proportional one. Over the 1,025 validation passes decode took 233.6 s, of which demand reads 90.4 s; the fitted cost splits those reads into a fixed part `F` (0.38 ms for each of the 49,200 layer events with a miss, 18.9 s) and a marginal part `M` (0.21 ms for each of the 338,147 missing records, 71.0 s). A replacement policy multiplies the record count by `m`; timely prefetch hides a fraction `c` of the marginal part and hides a layer's fixed part only when it covers every miss of that layer, which is ignored below as a conservative simplification; `o` is new exposed overhead as a fraction of decode. Then `decode' = 143.2 + 18.9 + m * (1 - c) * 71.0 + o * 233.6` seconds and `ratio = 233.6 / decode'`. Hiding every read gives 1.63x. Replacement alone at the optimal bound (`m = 0.50`) gives about 1.18x; at an illustrative `m = 0.75` about 1.08x (the best implementable policy measured so far is `m = 0.92`, about 1.03x). Stride-1 top-10 prefetch alone (`c = 0.49` from the twin) projects 1.17x (the previous plan's proportional formula, which lets prefetch hide the fixed part too, says 1.23x). Combined with 2% overhead, `m = 0.75, c = 0.5` projects about 1.21x and `m = 0.5, c = 0.5` about 1.27x. These are conditional projections under an idealized service model that ignores contention from wasted reads; native measurement decides. No gain is guaranteed, every number above is smaller at 24 GB, and a proportional formula overstates replacement because halving misses does not halve the fixed cost.

### 3. Principles the pilot established

1. **Counts offline, time native.** The twin reproduces native cache decisions exactly, so any replacement policy or admission rule is scored offline with no fidelity loss in seconds. The twin's timing projection is optimistic in four known ways: it ignores bandwidth contention from wasted reads; it serves speculative reads on eight lanes at the measured 0.27 ms each, about 34 records inside a 1.16 ms lead, whereas the demand path's marginal rate is about 4.8 records per ms; it keeps the recorded baseline deadlines instead of advancing them as stalls disappear (the previous plan's causal event simulation requirement, carried into W3 as a sensitivity report); and it releases cap bytes at the demand check rather than at scatter release. It was contradicted natively once (a 2.6% precision policy at roughly 4x traffic decoded 2x slower). Throughput is therefore only ever claimed from paired native arms, and W4's calibration gate reads native counts against these optimisms.
2. **The free predictor comes first.** Routing at layer T is a linear readout of T's residual, and the residual changes by one bounded sublayer per step; the router applied one sublayer early is the strongest zero-cost forecast available. A learned correction on the same input could add to it, which is why learned components enter only as a measured improvement over that baseline, never instead of it.
3. **Misses are the target.** Every policy is scored on timely miss coverage, precision among issued non-resident candidates, and traffic amplification, never on routing recall alone.
4. **The read-cost model is the objective.** A layer with any miss pays about 0.4 ms plus about 0.2 ms per record. Replacement and prefetch are both scored by the read time they remove under this model, and zero-miss layer events are reported separately.
5. **Retire, do not delete.** The trainer, pack export and predictor-pack policies stay in the repository as historical instruments; this protocol does not run them. The collector, the ticket runtime, the twin and the paired bench are the instruments it does run.

### 4. Code map and implementation boundaries

Paths are repository-relative and symbols control if line numbers move. Existing behavior was verified by reading the code at the review commit; planned changes are proposed work.

| Existing seam | Current behavior | Planned change |
| --- | --- | --- |
| `Sources/Slotstream/ExpertStore.swift`, `SlotPool.victim(preferLayer:)` | Global CLOCK: the hand sweeps `refBit`, skips pinned slots, clears a set reference bit and takes the first clear unpinned slot; a precondition fires if all slots are pinned. `LayerLocalVictim.choose` replaces it only when the `layerLocalFloorEviction` flag is set (default off), the pool is at the floor slot count and a `preferLayer` hint is given. | Introduce a `package` replacement-policy seam consulted here and notified on hit (`ensureCore` sets `refBit` and pins), install (`ensureCore`, `admit`, ticket adoption) and eviction. CLOCK stays the default with no indirection cost. Policies must respect pins, the batch order (all victims chosen before reads), determinism (reference counters, never wall time), and the `preferLayer` hint. |
| Same file, `ensureCore` | Hits are pinned first and their reference bits set; misses are deduplicated in demand order and each gets a victim before any read; complete matching tickets are adopted into the chosen slots; the rest are read in batches of `defaultLoadBatch` (32) across `poolQueueDepth` (32) lanes, nine pieces per record; the demand event records unique, hit, miss, victim and adopted lists with start, read, adopt and end times; the schema's promoted list is never populated natively (promotion is counted only in the scheduler observation, and a promoted record lands in the adopted list). | Unchanged order and semantics. The policy seam only changes which slot `victim` returns and what bookkeeping runs on hits and installs. The event schema gains nothing for replacement; victims are already recorded, which is what makes native-versus-twin exactness checkable. |
| Same file, `admit`, `commitAdmissions` | Prefill sweep admission calls `victim()` per admitted expert and sets reference bits. | The policy seam serves sweep admission too. The twin does not replay prefill and collector kind 6 records only the admitted expert IDs, not victims, so the exactness reference for sweep admission under a new policy is the post-prefill residency snapshot (kind 7) compared against a replay of the recorded admissions (kind 6) under that policy. |
| Same file, `resize`, `diagnosticDiscardResidency` | Grow preserves occupied slots in a prefix and resets the hand; shrink starts cold; the diagnostic reset clears maps, bits and hand. | Policy state is rebuilt on grow, cleared on shrink and on the diagnostic reset. Elastic resizing is off in this protocol; the C08 between-request resize check covers the state transitions. |
| `Sources/Slotstream/ExpertPrefetch.swift`, `ExpertPrefetchScheduler` | Candidates are computed once per pass at `beginPass` from start features (pack), recent routes, or a diagnostic override; `layerCompleted(_:)` expires tickets through that layer and issues targets up to `layer + windowLayers`; `claim` adopts or promotes; `IOLaneBudget` gives demand priority and bounds speculative lanes (8); the cap is 32 records (88,473,600 bytes) charged until scatter release. | Add `Policy.router` and a `forecast(target:candidates:)` entry that merges per-target candidates at each completed-layer tick before `issue`; window equals the largest stride. `issue` today walks a monotone `issuedThrough` cursor and would never revisit a layer it already issued, so a merged target inside the window must be marked dirty and re-scanned (the existing `tickets`/`isResident` filters already make a re-scan idempotent), and a cap refusal must not stop later dirty targets permanently. Add a per-target issue cap and a margin threshold as admission controls. `forecastOverride` stays diagnostics-only. |
| Same file, `ExpertPrefetchConfiguration.environment` | Parses `SLOTSTREAM_EXPERT_PREFETCH_{CAP,LANES,WINDOW,TOP,RECENT,THRESHOLD,POLICY,DEVICE}` and the reserve. | Add `router` to the policy set, `SLOTSTREAM_EXPERT_PREFETCH_STRIDES` (default `2,1`), `SLOTSTREAM_EXPERT_PREFETCH_ISSUE_CAP` (per target layer per pass), reinterpret `TOP` as per-position candidates for the router policy, and `SLOTSTREAM_EXPERT_PREFETCH_MEMO_LAYERS` (0 = off). Every new control is default-off and validated. |
| `Sources/Slotstream/Engine.swift`, engine init | A non-recent policy loads and validates a predictor pack before the model; the scheduler is installed after the generator when MTP is present; pack plus cap must fit the charged reserve. | The router policy loads no pack, so both the pack requirement in `ExpertPrefetchConfiguration.environment` and the predictor construction here exempt it; its resident bytes are the staging cap alone, so the reserve can shrink to the cap (a 16-record cap is 44,236,800 bytes) and return slots. The MTP-absent bypass stays. |
| `Sources/Slotstream/Model.swift`, `runHiddenMulti`, ordinary layer path | Per layer: optional PLE, `x1 = attnHC[l](h)`, attention (GDN or full every fourth layer), `h += attn * inject`, `(x2, inj2) = mlpHC[l](h)`, `moe[l](x2)`, `h += moe * inj2`, `eval(h)`, then `lookahead.layerCompleted(layer:x2:)`. | After `eval(h)`, when the router policy is active, compute for each configured stride `s` and target `T = l + s < runLayers`: `mixed = mlpHC[T](h).0`, `logits = moe[T].routerProjection(mixed)`, top-`c` per row with margins relative to each row's tenth logit, one host materialization for all targets, then hand `(T, ids, margins)` to the session before the tick. This reuses the exact modules the real router uses. At stride 1 the only omitted term is target T's own attention update (and PLE when T is zero-based layer 1); at stride 2 the forecast also lacks all of layer T-1 (its attention and MoE residual updates), which is why stride 2 agrees less. No change to the real routing path. |
| `Sources/Slotstream/Layers.swift`, `GatedResidual`, `MoELayer`, `RouterProjection` | The hyper-connection read normalizes the four streams per stream, mixes them with dynamic sigmoid weights from two small quantized linears, and returns the mixed input plus the inject weights; the router is an FP32 matmul against the BF16 gate (optionally a promoted copy) followed by `RouterSelection.indices`. | Called, not modified. The forecast pays one extra hyper-connection read and one router matmul per target per layer (three rows), plus the inject projection it does not need; a mix-only path is an optimization to measure, not a first requirement. |
| `Sources/Slotstream/ExpertLookaheadTrace.swift`, `ExpertLookaheadSession` | Routes pass/route/layer/demand/residency/reconciliation events to the collector and ticks the scheduler; `wantsStartFeatures` is true for the collector, a predictor or a diagnostic forecast override. | Add a forecast event (source layer, target layer, ids, margins) to the observer protocol and a `forecast` hand-off to the scheduler. The router policy sets no predictor, so no start features and no extra draft-embedding read are computed for it. |
| `Sources/SlotstreamDiagnostics/ExpertLookaheadCollector.swift`, shard format `XLA1` v1 | Nine record kinds; per-request shards; BF16 lossless; demand events with victims. | Add record kind 10 (forecast: source layer, target layer, ids, margins, and optionally the target's mixed input rows for C12) under shard header version 2 (the header carries a single version integer); the reader accepts versions 1 and 2. Native forecasts in the shards are what the twin projects from and what the native agreement audit scores. |
| `Sources/Slotstream/Generate.swift` | Prefill, plain and verification passes are wrapped in session passes; the residency snapshot follows prefill; start features are built only when wanted. | The memo, when enabled, is seeded at `beginPass(.mainVerify, tokens:)` from the verify IDs (pending plus drafts) and updated from observed routes at layers below the memo depth for the row's token. |
| `Sources/Slotstream/Governor.swift` | Invalidates the scheduler before a between-request resize. | Also resets replacement-policy state through the pool's resize path. |
| `Tools/expert_lookahead_train.py`, `ClockPool`, `BoundedScheduler`, `cmd_evaluate` | Exact CLOCK replay from the post-prefill snapshot; a lane/service twin for prefetch timeliness; policy sweeps with an amplification bound and a proportional projection. | Add the replacement policies as replay classes with the same batch-pin semantics, native forecast candidates as a policy source, the read-cost projection alongside the proportional one, exact-residency accounting, and the per-target issue cap. |
| `Tools/expert_lookahead_probes.py` | New with this plan: `router-reuse`, `replacement`, `structure` over captured shards and the checkpoint's router weights; the source of Section 2's numbers. | Extend with `forecast-audit` (native forecasts against true routes, misses and residency) and `memo` (offline token-memo coverage). |
| `Tools/expert_lookahead_bench.py`, `Tools/expert_lookahead_plans.py` | Paired fresh-process arms with readiness (thermal, low power, host swap counters stable for 120 s, at most 600 s waiting), eligibility (no host swap delta), bootstrap; B0/B1/B2 plan generation. | Add process-attributed page-in sampling (the engine reads `task_info` today and the bench records host-wide VM activity; a per-process page-in counter through `proc_pid_rusage` is a new seam in the engine's process-memory reporting or the bench sampler) for eligibility, plan generators for the replacement and router arms, and the revised readiness rule of Section 8 once approved. |
| `Sources/slotstream-cli/ExpertLookaheadCommands.swift` | `expert-lookahead-capture`, `-bench`, `-predict`, `-check`. | `capture` gains the forecast record when the router policy runs in shadow mode; `check` gains the new C11 to C15 catalogue entries; `predict` is unused by this protocol. |
| `Sources/SlotstreamTestKit/T0Checks.swift`, `Diagnostics+ExpertLookahead.swift` | Weights-free checks for the lane budget, tickets and scheduler at T0, adoption at T1. | Add replacement-policy state checks and forecast-merge scheduler checks at T0; policy exactness and forecast parity at T1. |

Do not compute the forecast inside the MoE call or before `eval(h)`: the streams after the MoE add are the input of the next layer, and the existing boundary is the only place with both the completed state and the scheduler tick. Do not let the forecast set reference bits or pins; only real demand does. Do not model the forecast as a change to routing: the real router runs unchanged at every layer, and the parity checks compare its IDs.

### 5. Replacement policy: offline lab, then native port

**Why first.** It costs no read bandwidth, cannot change outputs, and is evaluated exactly offline in seconds per policy, so it is the cheapest lever to prove or kill. Its ceiling at 4,255 slots is a hit rate of 84.8% against 69.3%.

**Candidates.** CLOCK (reference, ships). Exact LRU (implementable, but within a point of CLOCK; kept as a reference). Segmented LRU with a 20% probation segment. S3-FIFO (small FIFO, main FIFO with lazy promotion, ghost queue). W-TinyLFU (1% window LRU, segmented main, count-min sketch admission with periodic halving). ARC (adaptive recency/frequency lists with ghost lists). CLOCK-Pro (the CLOCK approximation of LIRS, the natural upgrade for a CLOCK engine). Each policy is implemented once in the twin with `ensureCore`'s batch semantics: all keys of a layer event are pinned before victims are chosen, victims are chosen in demand order, and no key of the current batch is evicted. Constants (segment sizes, sketch width, ghost length) are fit on the train split's requests and selected on validation; report the train-prefix curve to expose overfitting. Recency state is seeded from the recorded prefill admission order (collector kind 6) and the snapshot's reference bits rather than from slot order, so recency policies are not handicapped at the start of each request as they were in the probe; report both seedings.

**Scoring.** For each policy at the native slot count and at 1.2x: misses versus native, hit rate, projected read time under the read-cost model, zero-miss layer events, and the per-layer miss profile. Report Belady's optimal beside every table. Also score each policy at the 24 GB slot count as a projection only.

**Layer-aware objective.** Because a layer with any miss pays the fixed cost, test two variants of the best policy: a per-layer quota that protects layers whose miss rate is already low, and victim preference for layers that already have a miss in the current pass. These are scored by read time, not miss count.

**Conditional learned evictor (W1c).** Only if the best implementable policy stays above 0.85x native misses on validation: a reuse-distance scorer with per-key features (passes since last use, count in the last 8, 32 and 128 passes, layer, expert frequency rank) and a logistic or small tree model trained on train-split traces to predict reuse within the next 16 passes, used to choose among a bounded sample of unpinned slots near the hand. It is trained in minutes on the existing shards, exported as a small table or weight vector, and ported only if it reaches the same 0.85x port threshold on validation and beats the best implementable policy by at least three hit-rate points. This is the only trained component in the plan.

**Native port.** Implement the winner behind the policy seam, default CLOCK, selectable by `SLOTSTREAM_EXPERT_REPLACEMENT=<policy>` registered as a validated optimization control. Requirements: victim selection amortized O(1) with bounded scans, per-reference bookkeeping on the model thread, which is the pool's only serialization, ghost state bounded to at most two keys per slot, no wall-clock inputs, and identical behavior in `ensureCore`, ticket adoption and sweep admission. Exactness proof: a native capture with the policy on must replay 100% in the twin (victims and miss lists), as CLOCK did; any mismatch blocks the port. Outputs are compared exactly against the CLOCK arm (C06 on the six correctness requests).

**Interplay with [[records/decisions/clock-stays-the-eviction-policy]].** The decision's reversal condition is an implementable policy beating CLOCK by more than about one point on a second workload. Segmented LRU already does so by 2.4 points in the exact offline replay of the pilot corpus, so the literal condition is met; this plan sets a stricter bar before the shipped policy changes: the 0.85x port threshold, the 100% replay proof and a measured native gain. The reversal is recorded as a new decision linking this plan's measurement, and it notes that the agentic many-short-turns workload the original decision named remains unmeasured.

### 6. Router-reuse forecast and prefetch

**Forecast.** At the completed-layer boundary of layer `l`, for strides `s` in the configured list (default 2 then 1) and targets `T = l + s`, run target T's own hyper-connection read on the live streams and T's router, take the top-`c` per position (default `c = 10`), and record each candidate's margin (its logit minus the row's tenth logit). Candidates whose margin is at or above the threshold, unioned across the three positions and deduplicated against resident keys and live tickets, are issued up to the per-target cap. Stride 2 carries the bulk (5.5 ms of lead, enough for a layer's misses by the demand-batch cost model); stride 1, whose 1.16 ms lead fits about four records by that same model (`(1.16 - 0.38) / 0.21`), refreshes target `l + 1` with the better forecast and can add candidates the stride-2 pass missed; it never cancels tickets already issued. Because the scheduler's issue cursor is monotone today, the refresh requires the dirty-target re-scan named in Section 4; without it the stride-1 candidates would silently never be read.

**Admission and traffic.** The read-cost model and the SSD's idle share bound how much waste is affordable: demand reads occupy about 39% of decode, so speculative reads that land inside compute windows are free up to roughly the same volume again, and the ticket runtime already defers speculative lane acquisition while demand reads run. Offline, choose `c`, the margin threshold, the per-target cap and the strides on validation to maximize the read-cost projection under an amplification allowance of 2.0x, reporting the whole frontier. The allowance is untested: the pilot's only native traffic evidence is the 2x slowdown at roughly 4x traffic, so W3 must report the amplification of the default stride union (which the twin has not run yet) and W4's calibration must relate measured traffic to measured throughput before any setting above 1.5x is trusted. Natively, the shadow arm measures forecast cost and the prefetch arm measures the truth. If the native gain is absent while the twin projects it, the diagnosis order is: issued and adopted counts against the projection, lane deferrals, promotion joins, adoption seconds, then read contention.

**Memory.** The router policy needs no pack. The reserve is the staging cap: keep the 32-record cap and 128 MiB reserve for the first native screen so F2 stays comparable with the pilot's B1 arms, then test a 16-record cap with a 64 MiB reserve, which returns about 24 slots to the pool at equal total memory.

**Exactness.** The forecast is an observer of the completed state and a producer of candidate lists; the real router, expert kernels and outputs are untouched. C06 (arbitrary correct or wrong forecast lists cannot change outputs) already exists and is rerun with the router policy. A new self-check (C12) runs the forecast function on the true post-attention streams of layer T and requires exact agreement with T's real router IDs on the correctness requests; it proves the mixing and router path is the model's own.

**Overhead.** The forecast adds, per layer, two hyper-connection reads and two router matmuls on three rows and one host materialization. Budget: at most 2% of decode in the shadow arm (F1), measured against the reserve-only control, the same budget the previous plan set for predictor plus scheduler overhead. If it exceeds the budget, first batch the strides into one graph and one sync, then drop stride 1.

**Early layers.** Layer 0 has no source and layer 1 only stride 1; layers 1 to 3 forecast at 0.43. This is where the memo of Section 7 applies, and until it does, those layers use ordinary demand loading.

### 7. Early-layer token memo

Per request, keep a bounded table from token ID to the observed top-10 experts at each of the first `M` layers (default `M = 8`, zero-based 0 to 7), updated from the real router IDs at those layers for each evaluated position's token, and used at the start of every verification pass to seed candidates for those layers from the pending token and the two drafts. The keys exist only once the drafts do, a few milliseconds before the pass begins, so the lead runs from pass begin: about one attention sublayer (1.2 ms) at layer 0 and about 4.6 ms more for each later layer, roughly 35 ms at layer 7. That is enough for every memo layer but the first, provided the memo has its own pass-start issue path: `beginPass` today issues only the first window of layers, so memo candidates for layers beyond the window would otherwise wait for their tick and lose most of the lead. The memo issues in layer order at pass start under its own share of the record cap (at most half), so it cannot starve the router candidates of the first window, and a cap refusal on a memo candidate does not block the router path. The table is exact observed labels, not a trained object; it is cleared at request end and bounded to 4,096 tokens (about 1.3 MB at ten IDs per layer), evicting least recently used entries.

Offline first: `Tools/expert_lookahead_probes.py memo` replays the memo over the validation shards and reports, per layer, miss coverage, precision among non-resident candidates and amplification, then the twin projects it with candidates handed over at pass begin against the recorded per-layer deadlines. Gate for native work: at least 4% of all misses covered in time at precision 0.30 or better, which is about 1.01x alone by the read-cost model; the memo enters B0 only if the combined twin projection improves by at least 0.01 with it. Natively, the memo is a candidate source of the same scheduler, on only with `SLOTSTREAM_EXPERT_PREFETCH_MEMO_LAYERS > 0`, exact under C06, and measured as a third arm on the screen. If the offline gate fails, record the numbers and drop the package; a bigram-keyed variant is an extension, not a fallback in this protocol.

### 8. Correctness, resource and eligibility rules

Reuse C01 to C10 of the previous plan where they apply, and add:

| Case | Required proof |
| --- | --- |
| C11: replacement policy state | Weights-free: pins are never evicted, batch keys are never evicted by each other, the exhausted-pool precondition still fires, resize grow/shrink and the diagnostic reset rebuild state, ghost bounds hold, and the policy is deterministic under replay. Native: a capture with the policy on replays 100% in the twin (victims and misses), and C06 outputs match the CLOCK arm exactly. |
| C12: forecast parity | On the six correctness requests in shadow mode, the forecast function applied to the true post-attention streams of layer T reproduces T's real router IDs exactly; with the optional forecast-input capture on (record kind 10 carries each target's mixed 2,560-wide input rows for those requests), the recorded stride forecasts match an offline recomputation from those rows within BF16 rounding; arbitrary or wrong forecast lists cannot change outputs (C06 rerun). |
| C13: forecast overhead | Shadow arm against the reserve-only control on the screen prompts: forecast plus schedule seconds from the observation, and paired wall time, within the 2% budget. |
| C14: forecast merge and issue caps | Weights-free scheduler checks: per-target merge deduplicates against live tickets and resident keys, stride-1 refresh never cancels issued tickets, the per-target cap and margin threshold hold, expiry at the target layer is unchanged, and the cap refusal path is exercised. |
| C15: memo lifecycle | The memo cannot alter routes, is keyed per request, is cleared on completion, error and cancellation, is bounded, and produces no candidates when disabled. |
| C16: combined lifecycle | Policy plus prefetch plus memo on: cancellation mid-decode with tickets in flight, exact recovery request, between-request resize, cold and repeated requests, sequential-request leak check on physical footprint. |

Memory rules are those of `AGENTS.md` and the previous plan: inspect the model lock and process inventory before every heavy step, never stop another task's process, check actual reclaimable memory, one model process at a time, kill test processes when done and verify with `pgrep`, no cache purge, memory hog or simulated availability, never lower the minimum slot floor. Offline packages need no model process and run under an 8 GB physical-footprint ceiling. Native arms need the 25 GB preflight at 20 GB (29 GB at 24 GB) and at most 600 s of waiting per launch.

**Eligibility rule for timing (needs approval).** The previous rule required host swap-in and swap-out counters stable for 120 s before an arm and unchanged during it; during the pilot's native pairs swap-ins moved continuously with other applications and no pair qualified, although earlier clean pairs on this Mac (the wall-time attribution and the 0.2.15 reruns) did satisfy it. Under [[records/decisions/global-paging-is-diagnostic]] this protocol freezes, before any timing arm and only with Carlos's explicit approval: readiness is thermal nominal, low power off, reclaimable memory at or above the preflight, and host swap-outs stable over a 30 s sample; a pair is timing-eligible when both arms complete with exact outputs, no host swap-outs occur during either arm, the engine process's own page-ins (a new per-process counter, see the bench row of Section 4) grow by at most 4,096 pages during the measured request, and thermal state is nominal afterwards. Swap-ins are dropped from readiness because they are other processes reading back their own pages and cannot be attributed; swap-outs are kept because they mean active pressure that can evict the engine's pages. The page-in counter is process-attributed (`proc_pid_rusage`, which also counts file-backed faults, at this machine's 16 KiB page size, so 4,096 pages is 64 MiB), and because MLX maps the resident weights, W0 measures the engine's own page-ins over a clean 512-output request before the threshold is frozen; the 4,096 figure is a placeholder until then. Both the old and the new verdict are recorded per pair; the headline uses the new rule; excluded rows are preserved. If Carlos does not approve the change, the old rule stands and a cohort that cannot qualify is recorded as inconclusive after its finite attempts.

### 9. Native benchmark and decision protocol

Arms use one frozen candidate binary with controls off and on, the off path verified against the pristine baseline. Comparisons, reusing the pilot's generators and drivers:

- **R1 (replacement):** CLOCK versus the ported policy at the same slot count and total budget; no prefetch in either arm.
- **F1 (forecast overhead):** reserve-only control versus router policy in shadow mode (C13).
- **F2 (prefetch mechanism):** both arms charge the reserve; prefetch off versus router prefetch on, CLOCK in both.
- **B0 (headline):** current deployment versus the frozen combination (policy, prefetch, memo where admitted) at the same total budget.

The validation screen is the previous plan's: two frozen prompts each from code, reasoning and prose, two paired rounds, 128 measured outputs after the 128-output warmup, 12 pairs, at least one clean pair per prompt, aggregate median-pair gain of at least 3% to proceed. The held-out B0 cohort is unchanged: six sealed test families, two frozen prompts per family, three preplanned paired rounds, 36 pairs and 72 arms, 512 measured outputs, alternating arm order, equal family weights, geometric-mean aggregation, 10,000 bootstrap draws with seed 1729, at least two clean pairs per prompt, no replacement pairs. Any output mismatch is a correctness failure of the candidate regardless of timing eligibility.

**Initial proof passes only when** all relevant checks pass, the B0 aggregate ratio is at least 1.10, the lower bootstrap bound is above 1.00, no family median ratio is below 0.95 and no family median request duration regresses by more than 5%. A 1.03 to 1.10 aggregate is recorded as a small measured benefit that does not close the gate; a per-mechanism arm may pass its own gate while the combination does not.

### 10. Ordered work packages and exit conditions

Execute in dependency order; offline packages may proceed while the Mac is occupied. Every package ends on its exit criterion, never on a time.

| Package | Work and outputs | Dependency and exit |
| --- | --- | --- |
| W0: freeze | New protocol under `Tools/expert_lookahead.py prepare` with a fresh run ID: identities, pinned flags, profile and preflight, corpus split unchanged, the pilot shards' sizes and hashes re-verified from `requests.jsonl` (`capture.shards[]`) and the identity header from `run.json` (recapture with the existing collector if any shard is missing or altered), the probes tool identity, and the eligibility rule chosen in Section 8. | First. Exit: protocol JSON written, shard identity verified, no model run. |
| W1: replacement lab | Policies in the twin, constants fit on train and selected on validation, read-cost scoring, layer-aware variants, Belady beside every table, 24 GB projection; W1c only under its entry condition. | W0. Exit rule: port only if the best implementable policy reaches at most 0.85x native misses on validation with train-selected constants; otherwise record "replacement is not the lever at this size" and continue to W2. |
| W2: forecast diagnostic | Router policy, forecast event, shadow capture on the six correctness requests and the 13 validation requests (16 distinct, three overlap); C12 parity, C13 overhead, `forecast-audit` agreement, coverage, precision and lead per stride and layer group. | W0; independent of W1. Exit: C12 exact and overhead within budget. Native agreement is recorded beside the probe's (0.595 at stride 1, 0.500 at stride 2, expected to be exceeded but not required); if it is lower while C12 passes, that is the true forecast quality and W3 proceeds on the native forecasts it uses anyway. |
| W3: prefetch calibration | Twin sweep over `c`, margin, per-target cap and strides using the native forecasts of W2, exact residency, read-cost projection, amplification allowance 2.0x with the default stride union reported explicitly, late-correct tickets counted separately from wasted ones, and sensitivity to service time (1.5x and 2.0x) and to advanced deadlines; plus the memo offline gate (`memo` probe). | W2. Exit rule: proceed natively only if a setting projects at least 1.10x by the read-cost model under the allowance; record the frontier and the sensitivities either way. Memo enters W5 only if its gate passes. |
| W4: native prefetch screen | Router prefetch on with the W3 setting: C06, C14, C16 subsets; twin calibration (native issued and adopted counts, where native adopted includes promoted in-flight tickets, against the twin's issued and timely-plus-late-correct counts, within 10%; W3 extends the twin to count late-correct tickets separately from wasted ones); F2 screen with measured traffic reported against measured throughput; then a 16-record cap variant. | W3 passed. Exit: exact, calibrated, screen gain at least 3%; otherwise diagnose in the order of Section 6 and record a stop or a revised protocol. |
| W5: policy port and combined screen | Native port of the W1 winner behind the seam, C11 exactness (100% replay), R1 screen; then the combination with W4's prefetch and the memo if admitted, screen again. | W1 passed and W4 reached (or W4 stopped, in which case W5 screens the policy alone). Exit: each admitted mechanism exact and screen-positive; freeze the combination. |
| W6: held-out qualification | Frozen B0 cohort, remaining checks, sequential-request leak and cancellation check on the final candidate, predeclared metrics. | W5 frozen; the baseline's outstanding native MTP gate closed as the previous plan required. Exit: success, small benefit, no benefit, correctness failure or resource-inconclusive, with all exclusions preserved. |
| W7: 24 GB cohort | The same combination at the 24 GB profile as a separately frozen experiment. | Only if 29 GB reclaimable is available and W6 reached; never pooled with 20 GB. |
| W8: closure | Records, hashes, exact reproduce commands, artifact locations, failed attempts, limits, next decision; a decision record reversing or confirming the CLOCK decision; projections regenerated, store validated, processes stopped, installed artifacts verified unchanged. | W6 or an earlier justified stop. |

Selection rules: among router settings choose the smallest configuration whose projected ratio is within 0.01 of the best under the same budget, where smaller means lower `c`, then lower cap, then fewer strides; among replacement policies the one with the least state; break remaining ties by lower measured overhead, then lower traffic. Never select on test outcomes. The memo and the policy are admitted to B0 only if each passed its own screen; a mechanism that fails is dropped, not tuned on the cohort.

### 11. Conditional extensions

| Extension | Entry condition | Required additional work |
| --- | --- | --- |
| Forecast-protected eviction | Prefetch works but a measurable share of misses are keys the forecast named for the next two layers and CLOCK evicted meanwhile. | Let the policy treat active forecast targets as referenced; ablate against the plain policy at equal memory; no permanent pins. |
| Union of strides and shorter refresh | Stride 1 adds coverage but its 1.16 ms lead wastes reads. | Issue stride-1 candidates only when the lane budget is idle; measure separately. |
| GPU-resident forecast | The per-layer host materialization is the dominant forecast cost. | Keep candidates on the GPU and read them at the next boundary with a one-layer delay; requires the GPU slot lookup of the previous plan's extension table. |
| Bigram or n-gram memo | The token memo passes its gate and later-layer misses remain token-driven. | Key the memo by the last two tokens, reusing the n-gram hashing precedent of `NgramStore`; measure repeat rate against precision. |
| Persisted hot set across processes | M1's remaining lever: 10% of records serve 71% of accesses. | A warm-start artifact and its cold-start cost; separate protocol. |
| Learned reuse-distance evictor beyond W1c | W1c ports and the remaining gap to Belady is still large. | Richer features, per-layer models, cost measured per eviction. |

### 12. Machine time and completion definition

Rental cost is zero. Engineering is sequenced by dependency and not sized in human time. Machine-time allowances below cover runs only and change with the listed factors.

| Run | Allowance | What changes it |
| --- | --- | --- |
| W1 offline lab per policy | Seconds to minutes on the existing shards | Policy complexity; W1c training is minutes. |
| W2 shadow capture (16 distinct requests, 3,836 outputs) | About 6 minutes of decode at the pilot's 11 tok/s, about 15 minutes with prefill and process starts | Prefill, process starts, readiness waits. |
| W3 twin sweeps | Minutes | Sweep size. |
| W4, W5 screens (12 pairs each, up to four screens) | About 60 to 90 minutes per screen | Readiness waits dominate; fresh process per arm. |
| W6 held-out cohort (72 arms) | About 4 to 8 hours | Readiness, exclusions, reruns are not allowed inside the cohort. |
| W7 24 GB cohort | Same as W6 | Only when the headroom exists. |

**Plan completion** means the seams, policies, forecast contract, admission controls, checks, arms, stop rules and evidence requirements are concrete and mechanically consistent. **Experiment completion** means W8 records an honest outcome with reproducible evidence. **Successful acceleration** additionally requires the W6 gate. **Production readiness** additionally requires the affected existing acceptance gates, mode coverage and explicit adoption work, none of which this plan performs.

### 13. Review record and execution checklist

**Pass 1, code grounding (2026-09-11):** read `SlotPool` (state, `victim`, `ensureCore`, `admit`, `resize`, diagnostic reset, read batching and lanes), `LayerLocalVictim`, `ExpertPrefetchScheduler` (configuration, lane budget, `beginPass`, `observeRoutes`, `layerCompleted`, `issue`, `expire`, `claim`), `ExpertLookaheadSession`, the ordinary layer path of `runHiddenMulti`, `GatedResidual`, `MoELayer`, `RouterProjection`, the engine's session wiring, the generation-side pass boundaries and start-feature cost, the governor resize hook, the collector's record kinds, the twin's replay and scheduler, the bench's readiness and eligibility, the plan generators and the CLI commands. Every "current behavior" cell in Section 4 comes from that reading.

**Pass 2, evidence and arithmetic:** the three probes were rerun from the consolidated `Tools/expert_lookahead_probes.py` and their outputs hashed in the linked run; Belady was verified against brute force; the read-cost model was fit on the recorded demand events; the ceiling and combination projections were recomputed from the read-cost formula stated in Section 2 after the review found the first draft's proportional numbers inconsistent; the M1 comparison was checked against its record, including that its bound was a static hot set and not Belady.

**Pass 3, independent adversarial reviews (2026-09-11):** two separate reviews, one against the code (18 findings) and one against the plan's logic, arithmetic, evidence use and store conventions (17 findings). Corrections applied: the per-stride list of what the forecast omits; the always-empty promoted list in demand events; the scheduler's monotone issue cursor, which requires a dirty-target re-scan for stride-1 refreshes and a pass-start issue path for the memo; the twin's late-ticket accounting and its other optimisms; the memo's lead, which runs from pass begin rather than a full pass; the ceiling arithmetic rewritten on the read-cost model; the W2 gate no longer treats a lower native agreement as a defect; C12 made evaluable by an optional forecast-input capture; the CLOCK decision's condition already met offline by segmented LRU; W1c held to the same port threshold; the eligibility rule's counter semantics, justification and W0 pre-measurement; the selection rule defined for router settings; the traffic allowance labeled untested; and the transcription slips (layer-group overlap, request counts, tier of the adoption check, the missing lock, kind 6 without victims, hashes in `requests.jsonl`, the W2 allowance).

**Pass 4, consistency:** every wiki-link resolved, projections regenerated, store validated, no human-time sizing, no em dashes, proposed files and controls labeled as proposed.

Execution begins with W0, not with a native run. Before W1 to W3 the implementation agent must be able to answer from the frozen protocol: which exact code and shards are used; how each policy's batch-pin semantics match `ensureCore`; what the forecast function computes and omits; which bytes the reserve counts; how a wrong, late or failed forecast falls back; what makes a pair eligible; and what stops work without changing the success rule.

### Addendum: execution outcome (2026-09-12)

Executed W0 through W4 on the 48 GB M5 Pro under protocol `xla2-20260911` (20 GB profile, 25 GB preflight) after Carlos approved the plan and its Section 8 eligibility rule on 2026-09-11. W0 froze the successor protocol with the pilot's 69 shards re-verified. W1 closed on its exit rule: segmented LRU is the best implementable policy at 0.924x native misses on validation (Belady 0.496x), the learned evictor of W1c reaches 0.966x, and nothing reached the 0.85x port threshold, so replacement is not the lever at this size and W5's port never entered. The memo failed its offline gate (1.49% coverage) and never ran natively. W2 built the router-reuse forecast: C12 exact (189,936 of 189,936 self-check rows; offline recomputation exact within BF16 ties), native stride-1 top-10 agreement 0.763 against the probe's 0.595, C06 exact with prefetch on at both profiles; the shadow capture ran at a 12 GB diagnostic profile because another application's virtual machine held reclaimable memory below the 25 GB preflight, and forecast coverage numbers carry that label. C13 measured 2.8% of decode against the 2% budget after the listed remedies; the deviation is recorded and the screens went ahead. W3's twin on the native forecasts projected 1.247x for strides [1], 10 per row, issue cap 16 (gate passed). W4 then contradicted it: 0.713x on the first screen (12 of 12 pairs eligible), 0.895x after the adoption path was revised inside W4 on Section 6's diagnosis order (batched scatter, promoted-ticket lane priority; 11 of 12 eligible), and 0.935x and 0.956x on the two thresholded lower-traffic settings the twin projected at 1.175x and 1.153x (12 and 12 of 12 pairs eligible). The twin calibrates on counts within 4% and reproduces every arm's demand-IO time within 3 s, and is wrong on cost: the non-IO part of decode grew 38% to 111% with prefetch on (joins on in-flight tickets, adoption work, the forecast and an unprofiled remainder), which the read-cost model does not charge. No screen reached 1.03x, so the W4 stop rule fired; B0 (prepared as `w6/plan-b0.json`), W5, W6 and W7 did not run, and no speed gain is claimed. The CLOCK decision is confirmed with a tightened reversal condition. Evidence and the next decision: [[records/measurements/expert-lookahead-2-router-reuse-prefetch-native-screens-2026-09-12]]; raw runs: [[sources/runs/2026/09/2026-09-11-expert-lookahead-2-offline-lab]] and [[sources/runs/2026/09/2026-09-12-expert-lookahead-2-native-screens]].

### Addendum: reopened for a first-principles retry (2026-09-12)

Reopened the same day on Carlos's instruction to try the optimization again from first principles. The closure addendum above stands as the record of the staging-mode outcome; this addendum records what the retry changed. The runtime gained a second adoption mode (`SLOTSTREAM_EXPERT_PREFETCH_ADOPT=slot`): the victim slot is reserved when a ticket is issued, the worker reads the record into that slot, adoption is a map insert, unused reservations recycle their slots, and a stale pool pointer refuses adoption; the byte cap is replaced by a reservation cap. C06 held (six correctness requests exact against the shadow and pilot captures). On the same validation screen as the closure, the stride-2 thresholded setting measured 1.138x (11 of 12 pairs eligible, bootstrap 1.114 to 1.134), clearing the W4 exit (3%) and the B0 entry rule (1.03x); one-round pilots of nearby settings measured 1.085x to 1.129x. Forecast-protected eviction was evaluated offline and closed (0.9985x misses). Before the B0 cohort could run, an engine process stalled inside a kernel file read and a second one blocked behind it; both remain stuck exiting and hold the model lock, so the host must restart before any model process runs. The stall's cause is unproven; the uncached read into GPU-shared pool memory was replaced by a host scratch copy (candidate 7) as a precaution, and the bench gained an arm timeout. W6 (B0, plan `w9/plan-b0-slot-s2thr-l16/plan-b0.json`) is the next package; W5 stays closed (no policy port); W7 remains conditional. Evidence: [[records/measurements/expert-lookahead-2-slot-adoption-screen-2026-09-12]] and [[sources/runs/2026/09/2026-09-12-expert-lookahead-2-slot-adoption-screen]].

### Addendum: held-out cohort passed (2026-09-12)

After the host restart, `w9/run-after-reboot.sh` ran end to end on candidate 7 (file reads into host scratch, then a copy into the reserved slot): 138 T0 checks, exactness parity on the six correctness requests, a one-round pilot at 1.124x, then B0. B0 passes every gate: aggregate 1.120x (required 1.10), lower bootstrap bound 1.090 (above 1.00), lowest family median 1.093 (floor 0.95), every family's median request duration shorter (0.890 to 0.976), and 36 of 36 pairs eligible under `process-pageins-v1` with none excluded. Median decode throughput rose from 12.24 to 13.59 tok/s, demand records fell 47.0% and precision was 0.557. The host I/O stall that had blocked the cohort did not recur across the parity run, the pilot and 72 cohort arms. The initial proof therefore passes for the slot-adoption router-reuse prefetch at the screen setting: [[records/measurements/expert-lookahead-2-b0-cohort-2026-09-12]].

The plan stays in progress. The stride-union and stride-1 pilots under slot adoption are folded into the decode-path coverage sweep, and production adoption (acceptance gates, mode coverage, defaults) remains separate engineering. The same session opened a decode-path serialization line from this result: deferring the per-layer drain measured 1.038x to 1.051x with prefetch off, and its composition with prefetch is the next measurement: [[records/measurements/decode-path-serialization-round-1-2026-09-12]].

### Addendum: B0 cohort rescored with true medians (2026-09-13)

The held-out B0 cohort passes at 1.105x, not 1.120x: the cohort report took the upper middle value of even-count medians. Rescored from the recorded pairs ([[sources/runs/2026/09/2026-09-13-cohort-rescoring-true-medians]]), the lower bound stays 1.090, the lowest family is 1.065 and median decode throughput is 12.20 to 13.50 tok/s. The verdict is unchanged.

## Decode path serialization: overlap expert I/O, GPU work and host work instead of alternating
Opened on 2026-09-12 after the held-out B0 cohort passed ([[records/measurements/expert-lookahead-2-b0-cohort-2026-09-12]]), to take decode further from first principles. Round 1 is recorded in [[records/measurements/decode-path-serialization-round-1-2026-09-12]].

### Problem

[[records/measurements/decode-wall-time-attribution-2026-09-10]] partitions decode into expert I/O 37.2%, GPU work 30.1% and host work 32.7%, summing to 100%: the three alternate instead of overlapping. Each MoE layer materializes its routing on the host and each layer ends in a full evaluation, 96 host-blocking synchronizations per forward pass. Profiles show the host share is mostly waiting ([[records/decisions/decode-host-time-is-waiting-not-graph-construction]]), so the work is overlap and fewer real reads.

### Closed by evidence

Whole-record speculative reads (paired 1.002), nine implemented but disabled host accelerators (paired mean 1.0012), graph compilation of layer spans, and residency speculation ([[records/decisions/residency-speculation-waits-for-layer-completeness]]).

### Rounds, in dependency order

Every configuration is compared with a reference inside the same sweep by paired per-(request, round) ratios, and its output ids must equal the reference's. Exploration uses r0005, r0206 and r0096 at 256 outputs; round 5 adds r0074.

1. Round 1, done: barrier period with multi-generation pins, prefetch off, 1.025 to 1.051 with exact outputs.
2. Round 2, prefetch coverage depth: c10-s2 (the B0 setting, reference), c16-s2, c24-s2, c32-s2, c24-s1, c16 over strides 1 and 2, and c24-s2 without the margin threshold. Exit: each configuration's paired ratio, demand-record ratio and layer completeness recorded.
3. Round 3, composition: deferred router-forecast consumption (forecasts queued with asyncEval and consumed at the next barrier) built, T0 checks and exactness parity with prefetch at K=3, then k1-s2 (reference), k3-s2, k3-s3, k3-s4, k4-s4, k8-s4 and a k1-s3 control, all under prefetch. Exit: exact parity, and a paired answer to whether the barrier gain survives with prefetch on.
4. Round 4, draft depth: depths 1, 2, 3, 4 and 6 under prefetch, each under a protocol variant pinning that depth. Exit: paired ratios with tokens per verify pass and demand records per depth.
5. Round 5, router weight cache: prefetch alone, with the cache, and with the cache plus router top-k, four prompts by four rounds. Exit: whether round 1's 1.010 hint survives at 16 pairs.
6. Combination: the best setting of every lever that gained in rounds 2 to 5 built into one candidate, re-checked for exactness and screened once against the shipped path on the exploration prompts. Exit: an exact combined candidate with its screen ratio.
7. Confirmation: one held-out cohort of the combined candidate against the shipped path on B1, three rounds at 512 outputs, 36 pairs, under `process-pageins-v1` and the B0 gate. Exit: the gate verdict recorded, pass or fail.

### Held-out set B1

Registered before any round 3, 4 or 5 result existed, as `.build/decode-cpu/b1-prompts.json` (sha256 `cf519d2aee0a7470b8e8a62d9cbb7d93b60f54695ccbf20505716ae6848dfbc5`). Test split only, excluding every exploration prompt and every B0 prompt sharing a source document or family with one: code r0062 and r0033, dialogue r0296 and r0295, multilingual r0244 and r0245, prose r0171 and r0173, reasoning r0124 and r0125, structured r0256 and r0257. The exploration prompts are B0 test prompts, so no selection made on them can be confirmed on B0; the B0 result itself predates exploration and stands.

### Limits

One machine at the 20 GB profile. A lever that changes the pinned draft depth needs per-arm protocols in the bench before step 7 can compare it with the shipped path. Nothing is installed, published or committed by this plan.

### Addendum: round 2 recorded (2026-09-12)

Round 2 is done: [[records/measurements/decode-path-serialization-round-2-2026-09-12]]. The B0 coverage setting stays. Top 16, 24 and 32 at stride 2 are inert under the margin threshold, so they form an A/A comparison of the exploration sweep (paired 0.991 to 1.022): exploration differences inside about 2.5% are treated as noise, and no per-lever gain below that is claimed. Under the pre-registered rule the coverage lever carries top 24 with issue cap 32 into step 6, an inert override. Stride 1, the stride union and no threshold cut demand records by 21% to 55% but ran at 0.969, 0.991 and 0.555.

The limit on per-arm protocols is lifted for step 7: `Tools/decode_cohort.py` runs the bench-equivalent cohort with a protocol per arm, and a variant arm may differ from the base only in draft depth.

### Addendum: round 3 recorded, round 3b registered (2026-09-12)

Round 3 is done: [[records/measurements/decode-path-serialization-round-3-2026-09-12]]. Holding router forecasts until the next barrier is exact but loses 7% to 26% under prefetch, with no pair above 1, so its barrier lever has no winner.

Registered before round 3b ran and before any B1 result exists. Round 3b rebuilds with forecasts and completed-layer ticks riding the next layer's routing readback (a `RoutingReadbackQueue` drained inside `MoELayer` before the routes reach any observer), runs the weights-free checks including a new one for that queue, captures exactness with prefetch at K = 8, stride 2, then sweeps k1-s2 (reference), k2-s2, k3-s2, k4-s2, k8-s2 and k16-s2 on the exploration prompts over two rounds. Exit: exact parity and a paired answer to whether routing-readback consumption keeps the barrier gain under prefetch.

Step 6 takes its barrier lever from round 3b (`sweep-routing`) instead of round 3, whose design the rebuilt binary no longer contains; the other levers, the selection rule and step 7 are unchanged. At barrier period 1 the rebuilt binary runs the same path as the one rounds 4 and 5 use, so their results carry over.

### Addendum: round 4 recorded (2026-09-12)

Round 4 is done: [[records/measurements/decode-path-serialization-round-4-2026-09-12]]. No draft depth qualifies: depth 3 ran at 1.022 with four of six pairs above 1, depths 4 and 6 lost because each verified position loads its own experts, and depth 1 changed the greedy output under the registered shape-change interpretation. Step 6 keeps depth 2 and the protocol pin, so the combined candidate needs no protocol variant.
### Addendum: round 5 recorded, cohort start gated on quiet swap (2026-09-12)

Round 5 is done: [[records/measurements/decode-path-serialization-round-5-2026-09-12]]. The router weight cache qualifies at 1.017 over twelve pairs, ten above 1, and carries `SLOTSTREAM_OPT_ROUTER_WEIGHTS=1` into step 6; router top-k on top of it does not.

Eight of round 5's 48 cells were excluded for host swap-outs, all between about 22:55 and 23:29, after a virtualization process belonging to another application started on the host at about 22:48; swap-outs then stopped while it kept running. Registered before round 3b's results and any B1 result: steps 6c and 7 start only after the host swap-out counter has held still for five minutes, on top of the bench's per-arm readiness check. The eligibility rule and the gate are unchanged.

### Addendum: round 3b recorded (2026-09-13)

Round 3b is done: [[records/measurements/decode-path-serialization-round-3b-2026-09-13]]. Consuming forecasts at the next routing readback is exact and removes round 3's loss. The registered rule selected barrier period 4 (1.087 over six pairs, all above 1), and step 6 carries it together with the router weight cache and the inert coverage override. Two slow r0206 reference cells inflate every period's ratio; without them period 4 reads 1.014 over four pairs. The screen and the held-out cohort decide what the combination adds, and no exploration ratio is a claim.

### Addendum: steps 6 and 7 recorded (2026-09-13)

Step 6: [[records/measurements/decode-path-serialization-combination-screen-2026-09-13]]. The rule selected barrier period 4 with forecasts on the routing readback, the router weight cache and the inert coverage override; draft depth stayed at 2. The combined candidate was exact and screened at 1.104 against the shipped path, 1.018 over B0 prefetch.

Step 7: [[records/measurements/decode-path-serialization-b1-cohort-2026-09-13]]. On B1 the candidate ran at 1.116 against the shipped path, with a bootstrap lower bound of 1.073, every family at 1.037 or above, every duration shorter and identical outputs. Evidence was insufficient: r0245 kept one clean pair after host swap-outs from another application's virtual machine, so the registered verdict is not a pass and the status stays in_progress. Remaining exit: a complete B1 rerun of the same candidate on a quiet host, reported alongside this run.
### Addendum: step 7 replication and attribution sweep registered (2026-09-13)

Registered at 03:05, before either ran.

1. Step 7 replication. The same candidate and cohort plan (`plan-b1.json`, sha256 `4c41885fe40e0c6cc15333514ceada7a5a6ed1c81ec866edb2625a0d20475b3b`), the same binary (`ed139334a9f7d0b46c5f4894da6c1de8c8fe789e257aa3fcb6eddae3f45ca3db`), gates and eligibility rule. All 36 pairs run fresh into their own directory after ten minutes without host swap-outs; the virtualization process from another application, holding 8.4 GB at registration, is not waited out. The replication's verdict stands on its own, the first run is reported alongside it, and no pair from one run fills the other.
2. Attribution sweep. On the exploration prompts r0005, r0206, r0096 and r0074, four rounds at 256 outputs, reference B0 prefetch: the shipped path, B0 prefetch, B0 plus the router weight cache, B0 plus barrier period 4, and the combined candidate (`configs-attribution.json`, sha256 `5ecf111096f09fac9ce44a25866c941947273ea6cd4e3abce0952409d6109237`). It prices each part on one binary with sixteen pairs, selects nothing and changes no candidate.

### Addendum: step 7 replication passed (2026-09-13)

The registered replication passed every gate: [[records/measurements/decode-path-serialization-b1-cohort-replication-2026-09-13]]. On B1 the combined candidate ran at an aggregate 1.124 against the shipped path, with a bootstrap lower bound of 1.105, every family at 1.072 or above, every duration shorter, 34 of 36 pairs eligible and identical outputs; the first run's insufficient evidence is reported alongside it. Step 7's exit is met. The status stays in_progress until the attribution sweep and the closing profiles are recorded. Changing defaults, committing the code and any public claim remain separate decisions.

### Addendum: attribution and closing profiles recorded (2026-09-13)

The registered attribution sweep and the closing profiles are recorded, so every step of this plan has met its exit.

- Confirmation: the combined candidate passed the held-out B1 replication at 1.124 against the shipped path ([[records/measurements/decode-path-serialization-b1-cohort-replication-2026-09-13]]).
- Attribution on one binary ([[records/measurements/decode-path-serialization-attribution-2026-09-13]]): B0 prefetch is 1.090 over the shipped path; over B0 prefetch, the router weight cache adds 1.021, barrier period 4 with forecasts on the routing readback 1.022, and both together 1.046.
- Profiles ([[records/measurements/decode-path-serialization-closing-profiles-2026-09-13]]): decode waiting now splits between file reads and GPU waits, and round 1's profile split is corrected.

Defaults are unchanged and the engine changes are uncommitted. Changing defaults, committing and any public claim are separate decisions.

### Addendum: held-out cohorts rescored with true medians (2026-09-13)

The cohort report took the upper of the two middle values whenever it formed a median over an even count, so each two-prompt family counted its faster prompt. Rescored from the recorded pairs with true medians ([[sources/runs/2026/09/2026-09-13-cohort-rescoring-true-medians]]), the B1 replication is 1.114 (bootstrap 1.104 to 1.121), the first B1 run 1.106 and the B0 cohort 1.105. Every verdict is unchanged, and both passing cohorts still clear the 1.10 gate. The figures 1.124, 1.116 and 1.120 in the addenda above are the uncorrected ones.
### Shipped as the 0.2.16 default (2026-09-13)

The combined candidate that passed the B1 replication is the default in 0.2.16 wherever the draft head runs ([[records/decisions/decode-lookahead-default-with-the-draft-head]]), with the head's floor lowered to 76 experts per layer so 32 GB Macs and up get it ([[records/decisions/draft-head-auto-floor-76-per-layer]]). Implementation checks and plans by Mac size: [[records/measurements/decode-lookahead-default-2026-09-13]].

## Decode forecast taps: a more accurate forecast at the same lead time
Opened on 2026-09-14 from the closing profiles of the decode serialization plan ([[records/measurements/decode-path-serialization-closing-profiles-2026-09-13]]), which leave file reads as the largest cost in decode and name forecast accuracy at the same lead time as the lever, after round 2 showed that more speculative reads do not reduce demand reads ([[records/measurements/decode-path-serialization-round-2-2026-09-12]]).

### Problem

The shipped forecast for target layer T applies T's own hyper-connection read and router to the streams after layer T-2's MoE add. Between deferred barriers it is evaluated with layer T-1's routing readback and reaches the scheduler there ([[records/measurements/decode-path-serialization-round-3b-2026-09-13]]). At that readback the streams already hold T-1's attention output, so a forecast built on them arrives just as early and misses only T-1's routed experts and T's attention sublayer, instead of two whole layers. T-1's shared expert is resident and needs no routing, so it can be added too.

### Taps

`RouterForecastTap`: `boundary` (shipped: after a source layer's MoE add, at a stride), `attention` (the streams of T-1 after its attention add, riding T-1's routing readback) and `attention-shared` (the same plus T-1's shared expert, whose matmuls T-1's MoE call then reuses). A router policy on an attention tap issues its candidates on arrival. The capture command records any tap without a scheduler, and shard format 3 carries the tap in the forecast record.

### Steps, in dependency order

1. Offline gate (registered before the capture): capture the 13 pilot validation requests with every tap, audit top-10 agreement, replay in the twin on the pilot's 20 GB residency. Exit: agreement at least 0.03 above stride 2, matched-traffic timely coverage at least 0.03 above the shipped setting with no more wasted reads.
2. Native screen: the qualified configuration against the same configuration with each tap, on the exploration prompts r0005, r0206, r0096 and r0074 at 256 outputs, three interleaved rounds, identical outputs required; at 20 GB when 25 GB is reclaimable, otherwise a mechanism screen at the largest smaller profile that fits. Exit: exact outputs and, for the chosen tap, a paired ratio of at least 1.01 with at least 8 of 12 pairs above 1, before B1 is run. Outcome, 2026-09-15: passed at 18 GB after the contention rule added a fourth round. All 48 cells had identical outputs; for attention the first 12 counted pairs gave 1.054 with 11 above 1 (all 14 pairs 1.047), and attention-shared gave 1.078 (all 14 pairs 1.077). Records read in decode fell 13 to 14% and reads expiring unused about 45%.
3. B1 confirmation at 20 GB, three rounds at 512 outputs, 36 pairs, against the qualified configuration. Exit: identical outputs, aggregate at least 1.02, lower bootstrap bound above 1.00, no family median below 0.97, no family duration regression above 2%, at least two clean pairs per prompt. This is the second lever confirmed on B1, which no tap decision has seen. Outcome, 2026-09-15: not passed as registered. Outputs were identical in all 36 pairs, and every effect condition passed on the 30 counted pairs: aggregate 1.058 (bootstrap 1.031 to 1.088), families 1.019 to 1.105, family durations at most 1.005. Host swap-outs during an arm excluded six pairs and left r0033 with one counted pair, which fails the pairs condition. Records read in decode fell 12% and wasted read bytes 43%. The default is unchanged; a further confirmation needs its own registration.
4. Changing the default, docs and claims are a separate decision after step 3.
5. Co-routing prior, offline. When T-1's routing readback returns, the host also holds T-1's routed experts. A per-layer pointwise mutual information table counted on the pilot's training routes rescores the tap's 24 recorded candidates (margin plus beta times the summed PMI), with beta chosen by leave-one-request-out over the validation requests. Exit: leave-one-request-out top-10 agreement at least 0.03 above the tap, and twin coverage at the shipped traffic at least 0.03 above the tap with no more wasted reads, before a native diagnostic. Outcome, 2026-09-15: negative. Leave-one-request-out agreement rose 0.0032 (beta 0.02 in every fold) and matched coverage 0.0094, so no native diagnostic.
6. Learned correction, offline. A training capture records the tap's router input and the true router input for the pilot's 56 training and 13 validation requests; per target layer, a ridge regression on training rows corrects the tap's logits, with lambda chosen by five-fold cross-validation grouped by request. Exit: validation top-10 agreement at least 0.05 above the tap, twin coverage at the shipped traffic at least 0.05 above the tap with no more wasted reads, and at most 64 MiB of FP16 weights (the rank-128 form), before a native implementation. Outcome, 2026-09-15: passed for the rank-128 form. Validation top-10 agreement rose from 0.7292 to 0.7980, twin coverage at the shipped traffic from 0.5397 to 0.6209 with 27,467 fewer wasted reads, and its FP16 weights take 35.2 MiB; the full form missed only the memory bound. Steps 5 and 6 need no native timing and run whenever no timed run is active; neither waits for step 3.
7. Native learned correction, correctness without timing. The engine applies the rank-128 factors as a fourth tap, `attention-corrected`, loads them at engine start and charges them to the lookahead reserve (164 MiB with this file). A capture of the 13 validation requests records the plain and corrected taps side by side. Exit, over targets 2 to 47: the native corrected top-10 set equals the offline one in at least 99% of rows, native agreement within 0.005 of 0.7980 and the plain tap within 0.002 of 0.7292, before any timing. Outcome, 2026-09-15: passed. Over all 141,450 validation rows the sets matched in 99.90%, native corrected agreement was 0.79799 and the plain tap 0.72918.
8. Correction screen: the attention tap against the corrected tap on the exploration prompts at 256 outputs, at 20 GB when 25 GB is reclaimable and otherwise at 18 GB, with the contention rule, adding rounds until 12 counted pairs or six rounds. Exit: identical outputs and a paired ratio of at least 1.01 with at least 8 of the first 12 counted pairs above 1, before step 9. Outcome, 2026-09-15: passed at 20 GB. A smoke of one 32-output request per arm first showed both arms starting with identical outputs and the corrected arm loading the file. Host swap-outs made three r0074 cells unclean, so the rule added a fourth round; outputs were identical in all 32 cells, and the first 12 counted pairs gave 1.036 with 9 above 1 (all 13 pairs 1.036). Median records read in decode fell 10% and wasted read bytes 57%.
9. Held-out confirmation: ten prompts from the 36 training-split families that no capture, fit, screen or cohort has used, two per kind (code, reasoning, prose, dialogue and structured; no multilingual family remains), drawn with a fixed seed: r0026, r0050, r0304, r0292, r0186, r0195, r0156, r0128, r0268 and r0271. Three rounds at 512 outputs at 20 GB against the attention tap. Exit: identical outputs, aggregate at least 1.02 with equal kind weight, lower bootstrap bound above 1.00, no kind below 0.97, no kind duration ratio above 1.02 and at least two counted pairs per prompt, under the contention rule as written and the Sevra sensitivity. A default change, docs and claims for the correction are a separate decision after this step, reported apart from step 3. Outcome, 2026-09-15: passed under both readings. All 30 pairs counted with identical outputs: aggregate 1.031 (bootstrap 1.013 to 1.041), kinds 1.025 to 1.055, kind durations at most 1.012, three counted pairs per prompt. Records read in decode fell 9% and wasted read bytes 48%, with forecast time unchanged.
10. Issue threshold for the corrected tap. Its issued reads are adopted at 0.83 against the attention tap's 0.70, and the 0.062 margin cutoff was set for the boundary forecast. Screen: t062 (reference), t031 and t000 on the exploration prompts at 256 outputs, contention rule, rounds until 12 counted pairs per candidate or six; a candidate passes at a paired ratio of at least 1.01 with at least 8 of the first 12 pairs above 1, and goes to confirmation only at 1.02 or more. Confirmation: eight prompts from the 26 unused training-split families (two per kind where two exist; dialogue and structured have one), three rounds at 512 outputs at 20 GB, the correction's gate with equal kind weight. Outcome, 2026-09-15: no confirmation. At 16 GB (another session's virtual machine held 9 GB at launch), four rounds gave t031 1.015 on the first 12 counted pairs (10 above 1) and 1.013 over 16, below the 1.02 bar, and t000 0.994: every top-10 read issued arrives late and wastes 2.4 times the bytes. The threshold stays at 0.062.
11. Attention readout tap, correctness and accuracy without timing. The stride-1 boundary forecast, which knows T-1's experts exactly, reaches only 0.762, so most of the remaining error is T's own attention over the context; that state is resident, so T's attention sublayer is run early on the approximate input, reading its caches without writing them. A 10 GB capture of the 13 validation requests records the attention, corrected and readout taps and a readout self-check on T's exact input. Exit: the self-check at least 0.995 on requests within the indexer budget and the attention tap at 0.7292 within 0.002; then the readout's agreement decides: at least 0.90 leads to a registered timing design, 0.85 to 0.90 to a correction fitted on top first, below 0.85 stops the lever. Outcome, 2026-09-15: the readout path is exact (self-check 0.9997 pooled, 0.99998 within the indexer budget; attention tap 0.7292) and the readout reads 0.8614 against the corrected tap's 0.7980, with the twin at 0.687 coverage against 0.620 at the shipped traffic, so the correction on top comes first. A smoke of one 32-output request per placement, readback and after-demand, exited cleanly with identical outputs in both.
12. Readout correction, offline: the learned correction fitted on the readout tap's input from a training capture of the pilot's 56 training and 13 validation requests. Exit: validation top-10 agreement of the rank-128 corrected readout at least 0.90, twin coverage at least 0.03 above the plain readout with no more wasted reads, at most 64 MiB, before a timing design that states where the readout's evaluation sits relative to the source layer's demand reads. Outcome, 2026-09-15: the gate fails. The rank-128 correction lifts the readout from 0.8614 to 0.8790 (bar 0.90) and twin coverage from 0.688 to 0.716 (bar +0.03) with 9,266 fewer wasted reads; as registered, the readout lever stops at the offline result. The engine keeps the readout tap, a placement that evaluates it while the source layer's demand reads are in flight, and a corrected readout tap, none of them timed; a timing run would be a new decision.
13. Readout timing, reopened on 2026-09-15 at Carlos's request under its own registration (`readout-timing-preregistration.md`, sha256 5358e3ea65c2e660..., written before any configuration existed). Screen: the corrected tap as reference against the readout in both placements, with and without its correction, on the exploration prompts at 20 GB, rounds until 12 counted pairs per arm or six, with two long-prompt observation cells per round; a candidate needs the screen reading at a first-12 ratio of at least 1.02. Confirmation: eight prompts from the 26 unused training-split families at 512 outputs, the candidate against the corrected tap as the gate (the correction's gate) and both against the qualified configuration as default evidence, with a long-context guard. Outcome of the screen, 2026-09-15: negative. Over four rounds and 77 counted cells with identical outputs, no readout arm had a pair above 1: readback 0.952, corrected readback 0.952, after-demand 0.737, corrected after-demand 0.742, and 0.85 to 0.89 (readback) or 0.70 (after-demand) on the long prompts. The readout cut records read in decode by 8% to 13% but its GPU work costs more than the reads it saves, and the after-demand placement synchronizes the GPU at every layer. The lever closes; the confirmation runs with the qualified and corrected arms only, for the default evidence. Outcome of the confirmation, 2026-09-15: the corrected tap against the qualified configuration on the eight registered held-out prompts at 512 outputs and 20 GB decoded at 1.111 (bootstrap 1.102 to 1.123), all 23 counted pairs above 1, kinds 1.079 to 1.136, kind durations at most 0.956, outputs identical in 60 of 60 cells; records read in decode fell 21% and wasted bytes 73%. The registered recommendation is the corrected attention tap as the default; the change itself, the file's home, docs and any public number are a separate decision.

### Registration

`.build/expert-lookahead/xla3-taps-20260915/taps-preregistration.md` holds the question, gates and screen design written before the capture, an addendum written after the offline gate and before any screen, and a correction to the B1 gate written 36 seconds after the 18 GB screen launched and before its first cell finished. Its first time labels were estimates, corrected against file times, logs and the session transcript. Three later sections were added before the analyses they govern: the contention rule (23:41:24, before the screen resumed in a new directory), a sampler scope note for the other session's Sevra app processes (23:46:09, after two resumed cells had written results and before any was read; its heading first said otherwise and was corrected in place) and the B1 hygiene rule (00:03:04). Steps 5 and 6 have their own registrations, `.build/expert-lookahead/xla3-coroute-20260915/coroute-preregistration.md` (23:57:17) and the learned-correction registration (00:01:07), each written before any of its data existed. Steps 7 to 9 are registered in `.build/expert-lookahead/xla3-learned-20260915/learned-native-preregistration.md` (02:08:16), written after the offline gate passed and before any engine code for the correction existed; the confirmation prompts were drawn by its rule afterwards, and an audit of every run artifact found none of their families in any earlier capture, fit, screen or cohort. Steps 10 and 11 are registered in `threshold-preregistration.md` (12:59:38, with a profile amendment at launch, before any cell ran) and `readout-preregistration.md` (13:09:02, before the readout code was built), both under the scratchpad and copied into their run directories.

### Limits

One machine. The twin counts reads, not in-flight joins or evictions by wasted reservations, so its projections are not claims. The screen's reference arm is the explicit qualified configuration: with the draft head forced on below its activation floor, automatic mode runs without the lookahead, so the default itself cannot be the reference at a forced profile. The correction is derived data for one model checkpoint, trained on the pilot's training requests; shipping it needs its own decision about where the file lives and how it is reproduced. The confirmation set has no multilingual prompt.

### Closed (2026-09-16)

The corrected attention tap ships as the default in 0.2.19 ([[records/decisions/corrected-decode-forecast-default-with-the-sidecar]]): the correction file is an optional pinned sidecar of the model mirror that `pull` fetches next to the weights, the engine locates it there and charges 409 MiB, and `SLOTSTREAM_EXPERT_PREFETCH_TAP=boundary` keeps the previous forecast. The measurements are recorded under [[records/measurements/decode-forecast-taps-2026-09-15]] and the shipping build's own default-path benchmark under [[records/measurements/corrected-forecast-release-benchmark-2026-09-16]]. Levers closed on their registered readings: the shared-expert variant (by rule), the co-routing prior, the lower issue threshold and the attention readout in both placements. What the program leaves open is outside the forecast: about 64% of decode time is GPU compute of the verify passes at about 2.1 accepted tokens per pass, so the next levers are acceptance per pass, kernel time per pass and the attention cost at long contexts.

### Deprioritized (decision 2026-08-29)

Not abandoned, but explicitly behind N1–N5. None of these changes whether a person keeps
using slotstream; they are credibility artifacts and completeness for the milestone map:

- **M8 in full** — the `slotstream bench` subcommand, the W1–W7 workloads, the §5
  Measured column, the presets freeze. Days of building a measurement rig whose output is
  a table. Do targeted measurements ad hoc instead.
- **`lite16` on a real 16 GB Mac** — also blocked on someone else's hardware.
- **M1 locality traces**, **LaunchAgent**. (M9 MTP left this list 2026-09-01 — built; see the M9 design note addendum.)
- *Exception on the soak:* a leak that only appears after 30 minutes is a real bug, not a
  credibility item. Ordinary daily use covers it in the meantime; W5 does not have to be
  built to catch it.

---

### M9 design note — MTP self-speculative decode: when it pays, and when experts win

Not built in v0, and the pinned community conversion drops the MTP tensors
(`sanitize` removes them; the measured 103.8 GB inventory has none), so the block
must first be converted and quantized from Qwen's official release. This note
records the first-principles analysis of *whether* to spend memory on it, because
the answer is strongly tier-dependent and the earlier plan text had it backwards.

**Two walls, and memory decides which one you are against.**

- **Small cache → fetch-bound.** Most tokens miss; decode waits on SSD reads
  layer by layer. Compute idles.
- **Large cache → launch-bound.** Nearly everything hits; decode is limited by
  kernel-launch count, not bytes (MEASUREMENTS §M0.6: batch-1 4-bit matmul reaches 20% of
  memory bandwidth; measured decode is flat above ~181 experts/layer).

Extra experts attack the fetch wall. MTP attacks the launch wall: verifying k
drafted tokens costs roughly the launches of one. So MTP's ~1.4–1.6× is a
*constant multiplier that only applies in the launch-bound regime*, while the
value of the ~17 experts/layer its 2.25 GB displaces follows the measured
decode curve (≈ experts^0.7 between the 30/layer = 5.6 and 181/layer = 20.0
anchors, flat above).

*[Measured 2026-09-02, and false as stated: with every expert resident a
five-token verify pass costs 1.65 single passes, not one (`mtp-passcost`), so
the fetch-free ceiling is ×1.4 at depth 4 and ×1.5 at depth 2 rather than a
multiplier on top of a free verify. The M9 addendum below has the ladder.]*

**Why tight memory keeps the experts** — three compounding reasons, not one:

1. **It sells cache at its highest price.** Those 17 experts/layer are worth
   ≈ +40% near the floor, ≈ +12% at 100/layer, and 0% past the plateau.
2. **It spends the scarce resource on discarded work.** Every rejected draft
   (30–40% at plausible accept rates) still fetched that token's experts from
   SSD. Wasted verification is nearly free when compute-bound and maximally
   expensive when fetch-bound.
3. **Its one genuine small-tier upside is already harvested.** Batching two
   tokens' expert fetches works because consecutive tokens reuse experts — which
   is exactly the locality the cache already exploits, and which the MTP tax
   shrinks the cache's ability to hold.

Real small Macs also have slower SSDs than this dev machine, which deepens the
fetch wall and widens the verdict.

**Enable policy (to be confirmed by measurement, not shipped on this reasoning
alone):** auto turns MTP on only when the target still affords ~120+ experts per
layer *after* paying its 2.25 GB — roughly targets above ~22 GB, so 32 GB
machines always, 24 GB when quiet, 48 GB unconditionally (there the displaced
experts are past the plateau and worth nothing, making MTP close to free). At
16 GB and at the floor, MTP stays off. **Governor ordering: shed MTP before
shrinking the pool** — dropping a constant-multiplier feature always beats
starving the cache below its knee.

**Correctness:** with exact-match acceptance under greedy sampling, speculative
decode is byte-identical to plain greedy, so MTP on/off (including a live
governor toggle) falls under the existing golden-equivalence invariant (§6.1)
and `elastic-check`-style gating rather than needing a new correctness story.

**Measure before freezing any threshold:** (a) this model's real accept rate on
representative text — nobody has published one; (b) whether draft-batched expert
fetches actually raise queue depth enough to matter on fetch-bound tiers; (c)
the true resident cost of the converted 4-bit MTP block (2.25 GB is an estimate).

### M9 addendum — built and measured (2026-09-01)
**Current operating choice, September 11:** Two drafts are now adopted in [[records/decisions/draft-depth-defaults-to-two]]. The dated measurements and former choices below remain historical evidence; the automatic activation floor is unchanged.

The analysis above survived contact with implementation almost intact; the
corrections are recorded here and in MEASUREMENTS.md (M9 section).

- **Weights**: converted from the official release via ranged downloads of
  exactly the 31 `mtp.*` tensors (4.9 GB, 92 s) — `Tools/mtp_convert.py`,
  provenance in `mtp.provenance.json`. Real resident cost **1.47 GB**, not
  the 2.25 estimated. The +1 norm-centering convention was verified
  tensor-by-tensor against the official raw checkpoint before conversion.
- **Port**: the Swift head is **bit-exact** against the MLX Python reference
  (`mtp-parity`, real captured inputs, reference run under mlx 0.31.x — the
  kernel family mlx-swift pins). Semantics follow vLLM's scheme A: the
  chained draft feeds the PRE-mixer multi stream forward.
- **Accept curve (measured, previously unpublished)**: 85.8% at depth 1,
  71.0/53.8/41.3% for 2/3/4-chains; E[tokens/round] 3.52 at depth 4.
- **Enable policy shipped as designed**: `--mtp auto|on|off`; auto turns the
  head on only when the cache still reaches **≥120 experts/layer after its
  1.6 GB** (`mtpAutoFloorPerLayer`), and raises the auto ceiling to 34.6 GB
  so the pool still reaches the knee. A/B ladder on the released 0.2.0 (four
  drafts): **×0.55 / 0.69 / 0.88 / 0.96** at 20 / 29 / 42 / 57 experts/layer
  (round cost 5.2 → 3.0 plain tokens against a 2.87 break-even), and ×0.65
  at a fixed 14 GB target — the "tight memory keeps the experts" argument
  with numbers. The plateau A/B (`slotstream mtp-bench --memory-gb 28
  --pairs 5`, 122/layer with the head on, 20 GB real footprint) measured
  **×0.88 at depth 4, ×1.13 at depth 2, ×1.17 at depth 1**; `mtp-passcost`
  measured the ceiling: a verify pass costs about a sixth of a pass per
  extra token with every expert resident, so the multiplier is at most
  ×1.4 (depth 1) or ×1.5 (depth 2). The ×1.5–1.9 estimate is withdrawn
  (MEASUREMENTS M9). The default draft depth is now 1 (was 4): depth 4
  loses at every size measured, depths 1 and 2 pay 13–17% at auto's size,
  and the shortest chain wastes the least on a rejection.
- **Rebuild eliminated (2026-09-02)**: the verify pass records the GDN /
  conv state after every position (the recurrence stepped per token,
  bit-identical to the fused kernel), so a rejection rolls back with no
  model compute. At 57/layer depth 1 goes ×1.12 → ×1.20, depth 2 → ×1.20;
  at 122/layer depth 1 reads ×1.24 (code ×1.33, list ×1.19, sampled
  ×1.18), depth 2 ×1.27. Gated in `mtp-check` (recording pass exact vs
  batched; rollback state inside 3x the plain re-chunk band; next step inside
  the prefill-rechunk band).
- **One claim corrected**: "byte-identical to plain greedy" was wrong for the
  same reason prefix reuse isn't byte-identical — the verify pass re-batches
  tokens and re-association moves near-tie logits. Gates (`mtp-check`):
  determinism across runs, prefix-cache continuation of speculative
  conversations (next-turn logits inside the prefill-rechunk band, the
  prefix-check method), non-degenerate accept, divergence reported. Sampling
  semantics are exact by construction (sequential draws off verified logits).
- **Governor**: MTP is a startup decision; the elastic governor sizes the
  pool around it (the head's 1.6 GB shows up in availability, so the pool
  self-corrects). "Shed MTP before the pool" from the note is NOT built —
  a live toggle would need golden-equivalence gating of the switch itself;
  revisit only if pressure shedding proves insufficient in practice.

## Vision: how a picture becomes tokens, and the four decisions that shape it
The checkpoint is a VLM. `vision_tower.*` is 333 tensors beside the language
model, and the chat template already rendered an image part to
`<|vision_start|><|image_pad|><|vision_end|>`. Turning that into an answer is
four steps and four decisions.

**The shape of it.** The template emits exactly one `<|image_pad|>` per
picture. The tower turns that picture into `mergedTokens` rows — patches after
the 2x2 spatial merge, from the image's dimensions alone. So the tokenizer's
output is rewritten before the model sees it: each single pad becomes a run of
`mergedTokens` pads, and the tower's rows replace the embeddings under that
run. Everything after the first image shifts, ids and offsets together, in one
sweep.

**1. A run, not a list of positions.** The splice takes `[VisionRun]` — a
start offset and the rows that fill it — rather than an array of rows plus a
scan of the ids for placeholders. The scan version had to reconcile two counts
that could disagree, and when they did it logged a line and continued with
unspliced placeholder embeddings: a silently wrong answer, in a feature whose
whole failure mode is fluent wrongness. A run carries its own rows, so there
is nothing to reconcile. Prefill clips runs to each chunk; the MTP draft head
clips the same runs by its own start offset, so its cache is built on the
embeddings the main model actually consumed.

**2. Contiguous spans on the GPU, not a scalar loop on the CPU.** A run is a
contiguous span, so the splice is a concatenation of alternating slices —
text, rows, text. The first implementation copied the whole hidden state to
the CPU as float32, wrote `S x hidden` scalars in a Swift loop, and uploaded
it again, on every prefill pass of every vision request.

**3. The fused attention kernel, for the reason `Layers.attend` gives.**
The tower is 27 blocks of full bidirectional attention over every patch.
Written as `softmax(q·kᵀ)·v` that materializes an `[heads, N, N]` score matrix
and a second one for the probabilities — at the largest image slotstream
accepts, 5.4 GB each, twice, per block, on a machine whose entire promise is a
memory plan. `MLXFast.scaledDotProductAttention` never forms it, and an `eval`
per block bounds the graph the same way the language model's attention does.

**4. Inline bytes only.** A `data:` URL or bare base64 is accepted; anything
with a scheme is refused with a 400 that says so. slotstream does not
dereference a URL a request hands it. The alternative — falling back to
`Data(contentsOf:)` when a base64 decode fails — is an unbounded, untimed
fetch of an attacker-chosen host and, through `file://`, a read of any image
on the disk, reachable from anything that can POST to the loopback port. It
would also make `SECURITY.md`'s "loopback only, no outbound traffic" false of
the serving path. Ollama takes base64 only for the same reason.

**What the engine caps, beyond the processor.** The reference processor would
allow 16.7M pixels, which is 16,384 tokens of a 32,768-token context and a
tower pass to match. slotstream caps one image at 1536² pixels — 2,304 tokens
— and refuses an aspect ratio past 200:1 or more than 24 MiB of decoded bytes.
The 24 MiB follows from the 32 MiB body cap: base64 costs 4/3.

**Memory is a stated cost, not a paid one.** See the V1 measurement for why
the tower's 0.9 GB is announced by the plan and charged at load time against
what is actually reclaimable, rather than folded into the fixed footprint.

### M9+ — Later (each gated on v0.1 done)
Vision input (mlx-vlm parity). Compact 3-bit-expert build with eval gate.
fp8 KV. Memory-pressure dynamic resharding. JSON-schema constrained output. Upstreaming
reusable pieces (GDN Swift kernel) to mlx-swift-lm. Multi-model residency. Qwen4 when it
lands (the whole point of targeting the preview architecture now).

---

## 9. Risk register

| Risk | Likelihood | Mitigation |
|---|---|---|
| Expert locality worse than assumed (flat router → low h) | medium | **downgraded**: at the measured 17.3 GB/s even h=0 sustains ~13 tok/s, so low locality costs speed, not viability. M1 still sizes the tiers |
| Model is far slower than hoped for *compute* reasons (launch-bound decode) | **medium-high, newly elevated** | batch-1 4-bit matmul measured at 47 GB/s vs 235 GB/s bandwidth → decode is kernel-launch-bound. Mitigation: MLX compiled graphs (`Qwen3NextCompiledDecodeTests` shows upstream does this), fewer/larger kernels, MTP self-speculation. Measure on the real model before optimising |
| Community 4-bit conversion broken/requantized badly | medium | spot-check vs FP8 endpoint at M0; pin revision; fallback own conversion on rented box (approval-gated) |
| mlx-lm PR churn / reference impl bugs | medium | vendor the exact reference revision into repo; layerwise parity catches divergence |
| GDN port numerics (fp32 state, chunked scan) | medium | §6.2 layerwise harness from day one; keep pure-MLX-op version as oracle for any later Metal kernel |
| QSA indexer complexity (core path — dense is only exact ≤ 2048 tokens) | medium | port from reference impl; dense path as ≤2k test oracle; +1–2 d already in the M3 estimate |
| ~~`gatherQMM`/slice-write perf in mlx-swift~~ | **RESOLVED** ✅ | `MLX.gatherQuantizedMM` exists and is bit-identical to `quantizedMatmul`; batched slot scatter measured 49.8 GB/s (Swift) / 74.9 GB/s (Python, 27 GB pool), in place. ~12× faster than the SSD can feed it |
| **mlx-swift Metal shaders cannot be built by SwiftPM CLI — needs Xcode** | **CONFIRMED, resolved by vendoring** | mlx-swift's own README says so; this machine has CLT only. Workaround shipped: colocate the prebuilt `mlx.metallib` next to the binary. **Scope correction 2026-08-30 — this does not block writing new kernels.** It is about building mlx-swift's *bundled* library. A custom kernel goes through `MLXFast.metalKernel`, which JIT-compiles Metal source at runtime with no offline toolchain; `GatedDelta.swift` already ships one, and a fresh kernel was verified compiling and running on this CLT-only machine. The two were conflated and the grouped-GEMM work was wrongly called blocked |
| F_NOCACHE semantics/perf on APFS | low-med | DiskBench A/Bs it at M0; pagecache mode as fallback |
| Wired-limit ceilings on ≤16 GB Macs | high (known) | doctor measures + documents `iogpu.wired_limit_mb`; budgets sized under recommendedMaxWorkingSetSize; never auto-sudo |
| Thermal throttling on fanless Airs | high (known) | W5 soak captures sustained numbers; publish sustained not burst |
| Model is 2 days old — ecosystem/weights churn | high | pin everything; manifest checksums; §2 re-verify step in M0 is repeatable |
| Scope creep (vision, MTP, batching, other models) | high | §8 gates; v0 = text, single-flight, this model only |

## 10. Dev-Mac reference (measured 2026-08-28 — full data in MEASUREMENTS.md)

MacBook Pro, Apple M5 Pro, 48 GB unified, 18 cores, 2 TB SSD (APPLE SSD AP2048Z),
macOS 26 (Darwin 25.6.0), Swift 6.3.3, page size 16 KiB.

| Measured | Value |
|---|---|
| Metal working set | 40,200,896,512 B (**37.4 GiB**) |
| Metal max single buffer | 30,150,672,384 B (**28.1 GiB**) |
| `iogpu.wired_limit_mb` | 0 (default) |
| Unified-memory bandwidth | **235.1 GB/s** |
| Batch-1 4-bit matmul | 47.2 GB/s (launch-bound) |
| SSD random 2.7648 MB, cold never-repeat | 9.46 (QD1) → **17.3 GB/s** (QD8+) |
| SSD random 4 KiB / 16 KiB, QD1 | 0.08 / 0.27 GB/s (53.6 / 60.1 µs) |
| Slot scatter, 204-experts/layer (27 GB) pool | **74.9 GB/s**, in place |
| Xcode | **not installed** (CLT only) — see risk register |

## 11. Definition of Done — v0.1 (updated with 2026-08-28 results)

- [x] Golden: tiny-cache ≡ big-cache greedy outputs, exact — **passed on the full
      model** (30 vs 181 experts/layer cached, identical text).
- [x] Parity vs Python reference: layers 0–1 (GDN, MoE-over-pool, PLE,
      hyper-connections, embeddings) **bit-exact**; QSA layer ≤2.4% RMS with the
      few-ulp vendored-kernel origin documented in MEASUREMENTS.md.
- [ ] This Mac: decode ≥ 20 tok/s warm chat — **was marked ✅ (20.01) and is now
      unconfirmed**: re-measured on 0.1.6 the curve reads 6.0 / 8.2 / 11.2 / 11.6
      tok/s at 30 / 60 / 120 / 150 experts per layer and is already flat by 120.
      Not a regression (0.1.5 and 0.1.6 A/B identical) and not under-warming
      (flat over 14 consecutive generations). The 181/layer config that produced
      20.01 peaks at 27.4 GB and has not fit in reclaimable memory since.
      · cold→first-token ≤ 15 s ✅
      (~12 s) · prefill ≥ 150 tok/s @8k **not yet** (needs the dense sweep; chunked
      prefill sized from the memory plan measures 92 tok/s, up from 40) · 30-min
      soak not yet run.
- [ ] `lite16` on a **real** 16 GB Mac (emulated already: 5.6 tok/s in 7.3 GB peak)
      — **deliberately deferred**, see §8.1.
- [ ] bench matrix ≥ 4 tiers; presets frozen — **deliberately deferred**, see §8.1.
- [~] Ollama surface: full curl-level battery passes (`Tools/api_test.sh`:
      version/tags/chat±stream/generate/v1±SSE/embed-reject); real GUI clients
      not yet exercised (sandbox intercepts local HTTP clients; nc transport used).
- [~] Clean-machine install: one-line curl installer + v0.1.0 release binary +
      consent-gated auto-pull shipped and tested end to end here; LaunchAgent
      not built, and no clean *machine* has run it yet.
- [x] Docs from measured numbers only (README/MEASUREMENTS).

## 12. Open questions (answer at the milestone noted)

1. ✅**ANSWERED (M0).** N-gram indexing: `ngram_heads = (ngram_size−1) × heads_per_ngram
   = 16`; per head, id = XOR of `splitmix64`-derived multipliers over the token n-gram,
   mod that head's distinct prime near 20 M (`_nth_prime_after(19_999_999, g+1)`), plus
   the head's offset; the concatenation is split into 128 shards of 2,500,012 rows.
   Rows are 160-dim, 100 B packed, quant group 32. PLE sits at layer index 1.
2. ✅**ANSWERED (M0).** **16 rows per token = 1,600 B** of n-gram data (≈20× less than
   the 23 KB assumed). Unrepacked that is 48 scattered preads/token; repacked, 16.
3. ✅**ANSWERED (M0), and better than hoped.** `mlx-swift-lm` ships
   `Qwen3NextGatedDeltaNet` + `gatedDeltaUpdate` (with `conv1d`, `dt_bias`, `A_log`,
   a `decodeConv` fast path, and compiled-decode tests), plus `SwitchGLU` /
   `QuantizedSwitchLinear` over `MLX.gatherQuantizedMM`. Novel Swift work reduces to
   the QSA indexer, hyper-connections, and the PLE path.
4. MTP block internals (own experts?) and self-spec accept rates on this model (M9)
   — now the load-bearing unknown for §8.1's enable thresholds, together with
   whether draft-batched expert fetches help fetch-bound tiers at all.
5. h-curves per tier (M1) — **no longer the load-bearing unknown**: at 17.3 GB/s even
   h=0 sustains ~13 tok/s. It now sizes tiers rather than deciding viability.
6. Whether `mixed-4-8` measurably beats all-4-bit on agentic evals worth +disk (M8).
7. External-USB4-NVMe tier viability for 256 GB-disk Macs (M8, one bench row).
8. **NEW — the actual binding constraint.** How much of decode is kernel-launch
   overhead, and how far do MLX compiled graphs close it? Batch-1 matmul hits only
   20% of memory bandwidth. This displaced IO as the top performance risk (M4/M5).
9. ✅**ANSWERED (M7).** Metal shader build: **vendored metallib**. CI builds the
   release with Xcode on its runner and ships `mlx.metallib` beside the binary;
   a CLT-only machine builds and runs fine against it. Separately — and this was
   conflated for two releases — *writing a new kernel* needs neither, because
   `MLXFast.metalKernel` JIT-compiles at runtime, which is how the gated-DeltaNet
   kernel already ships.
10. **NEW (2026-08-29), and the reason N1 exists.** How much of real-world latency is
   re-prefill rather than prefill? Found by inspection, not measurement: state is rebuilt
   per request, so for a conversation it is *all* of it after turn 1. Open sub-question
   for N1: how often does a real client actually send an exactly-extending prefix
   (tool loops and edited history both break it), i.e. what is the true hit rate of an
   extend-only cache?

## 13. References

- Model: [Qwen/Qwen3.8-Flash-Next](https://huggingface.co/Qwen/Qwen3.8-Flash-Next) ·
  [config.json](https://huggingface.co/Qwen/Qwen3.8-Flash-Next/resolve/main/config.json) ·
  [FP8](https://huggingface.co/Qwen/Qwen3.8-Flash-Next-FP8) ·
  [QwenLM repo](https://github.com/QwenLM/Qwen3.8-Flash-Next) ·
  [vLLM recipe](https://recipes.vllm.ai/Qwen/Qwen3.8-Flash-Next)
- Guides: [Unsloth run-locally + PLE/SSD offload notes](https://unsloth.ai/docs/models/qwen3.8-next) ·
  [Unsloth GGUFs](https://huggingface.co/unsloth/Qwen3.8-Flash-Next-GGUF)
- MLX ecosystem: mlx-lm PR #1788 (qwen4_exp, open) ·
  [pipenetwork MLX-4bit](https://huggingface.co/pipenetwork/Qwen3.8-Flash-Next-MLX-4bit) ·
  [MLX-mixed-4_8bit](https://huggingface.co/pipenetwork/Qwen3.8-Flash-Next-MLX-mixed-4_8bit) ·
  [ml-explore/mlx-swift-lm](https://github.com/ml-explore/mlx-swift-lm)
- llama.cpp support (competitive reference): [PR #27739](https://github.com/ggml-org/llama.cpp/pull/27739) ·
  [PR #27742](https://github.com/ggml-org/llama.cpp/pull/27742)
- Prior art on expert offloading: Eliseev & Mazur, "Fast Inference of Mixture-of-Experts
  Language Models with Offloading" (arXiv:2312.17238 — LRU expert cache + speculative
  loading); MoE-Infinity; PowerInfer; AirLLM (layer streaming); llama.cpp
  `--n-cpu-moe`/mmap; KTransformers.
